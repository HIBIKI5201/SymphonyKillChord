# 八幡さんの決定（2026-09-22、第 3 弾: R1〜R30）

番号はチャットで尋ねた R 番号。R18 は第 5 弾で回答済み。R31 以降は第 4・5 弾で回答済み（[93](93_決定_第4弾_R31-R60.md)・[94](94_決定_第5弾_R61-R111.md)）。実装修正が要る項目はすべて GitHub Issue 化済み（→ #番号）。

| R | 論点 | 決定 | 原稿への反映 |
| --- | --- | --- | --- |
| R1 | 被弾直後の入力と All ノーツ（PR #1893 で保留） | **All ノーツは廃止する。リズムタイムアウトと同じ扱いにする** | リズム判定のページから All ノーツを外し（廃止と明記）、該当する入力はリズムタイムアウトと同じ扱いと書く → 実装（→ #2041）|
| R2 | リズムゲージのジャスト帯の太さ | **判定範囲に影響されない、統一の太さ。太さは今と同じ** | リズム UI に「ジャスト帯は判定幅に関係なく一定の太さで描く（現在の見た目の太さ）」と書く → 実装（今は判定幅の 1/3 で描いている、`ACLikeRhythmGuideView.cs:448`）（→ #2042）|
| R3 | ターゲットがいないときのリズムゲージの半透明 | **追認（半透明にしない）** | 仕様を実装に合わせる |
| R4 | Just 成立時のスローモーション・ヒットマーカーの色 | **スローモーションは、プレイヤーと敵のアニメーションだけにかける。ヒットマーカーは未実装なだけ（仕様として残す）** | Just判定について の演出の節を直す。どちらも「未実装」と明記 → 実装（→ #2043）|
| R5 | 拍の色 | **統一: オレンジ = SMG、黄色 = ハンドガン、緑 = アサルト、水色 = リボルバー、青 = ショットガン、紫 = スナイパー** | 武器と拍子の対応（1=M82 スナイパー / 2=M1892 ショットガン / 3=OTs-20 リボルバー / 4=AK-74 アサルト / 6=M9 ハンドガン / 8=MP5 SMG）と合わせて、色の表をリズム UI・スキル入力表示のページに書く → 実装（`InGameCanvas.prefab:1821-1827` と `SkillInputProgressUIConfig.asset` を揃える）（→ #2044）|
| R6 | 攻撃硬直 | **拍にだけ影響されるようにする（固定の秒数をやめる）。押せない範囲はゲージを灰色にする** | 攻撃アクションに書く → 実装（今は 0.2 秒 + 0.2 拍）（→ #2045）|
| R7 | スキルモーション中の操作ロック | 未実装として残す | 「未実装」と明記 |
| R8 | 攻撃が障害物に遮られない | **遮るように直す** | 攻撃アクションに「障害物で遮られる」と書く → 実装（`TargetAreaQuery.cs` に Raycast が無い）（→ #2046）|
| R9 | 確定ダメージ（下限 50） | **実装する** | ダメージ計算に確定ダメージの手順を足す（未実装と明記。計算順のどこに入るかは【要確認】）→ 実装（→ #2047）|
| R10 | HP 条件・命中数による強化、同時装備制約 | 未実装として残す | 「未実装」と明記 |
| R11 | スキル効果の実値一覧 | 値はマスターデータに置き、仕様には効果の説明だけを書く | 仕様ページから数値を外し、マスターデータのページ（A3b `new-master-data-current-values.md`）に現在値を書く |
| R12 | ロコモーションの BlendTree 化・音楽の位相への同期 | **続ける** | 計画として残す（未実装と明記）→ 実装（→ #2048）|
| R13 | 入力履歴 `InputBufferingQueue` | **先行入力用に残す** | 「先行入力のために残している（現状は読む処理が無い）」と書く |
| R14 | 敵の攻撃ダメージの固定値 10 | **マスターデータに移し、敵の種類・攻撃ごとに設定できるようにする** | 敵の仕様に「値はマスターデータ」と書く → 実装（既存 Issue #2004） |
| R15 | マーク付き砲兵のプレハブ | **専用のプレハブを作る** | → 実装（→ #2049）|
| R16 | 歩兵の攻撃判定の太さ・7m 未満での後退 | **見直す** | 現状を書いたうえで【要確認: 見直し後の値】 → 実装（Issue で値を決める）（→ #2050）|
| R17 | 被弾・味方撃破で発見状態にならない | **発見状態のシステムを無効にし、常に発見状態に固定する。モジュールは残し、再利用できる形にする** | 敵の仕様を「常に発見状態（発見システムは無効化して残す）」にする → 実装（→ #2051）|
| R19 | キル数の計上と Wave 全滅判定の時間差 | **揃える** | 同じタイミングで行うと書く（どちらに揃えるかは【要確認】）→ 実装（→ #2052）|
| R20 | ステージ 05〜09・ボスの Wave とミッションの仮データ | **Issue にする** | → Issue（→ #2053）|
| R21 | ミッションデータの不整合 | 直す | → 既存 Issue #1984 |
| R22 | サブミッションの数 | **現状のまま（八幡さんの認識では各 3 個）** | 実データの数を確かめて書く。2 件のステージがあれば【要確認】 |
| R23 | 環境音が開始演出の間しか鳴らない | 追認する | 仕様を実装に合わせる |
| R24 | 空のページ・本文の無いページ | 削除する | 原稿の処置を「削除推奨」にする |
| R25 | オートロックオン中に対象が消えたとき・入力 | 対象が消えたら解除し、ロックオン入力で次の敵へ移る | 仕様に書く。入力で次の敵へ移る動きが実装に無ければ → 実装（→ #2058）|
| R26 | カメラ感度・反転・オートロックオン ON/OFF の設定 | **製品版で実装する** | 設定画面に「未実装（製品版で実装）」と書く → 実装（→ #2054）|
| R27 | ロックオン表示の 3 段階 | 追認する | 仕様を実装に合わせ、フォーラムの却下とは経緯に書く |
| R28 | 敵方向マーカーの仮モデル | **仮のままでよい** | 台帳の状態はそのまま |
| R29 | スマホの視点操作 | **スマホは画面右半分のスワイプが基本。コントローラーを繋げばスティックでも操作できる** | 両ページをこの内容に揃える。実装が右半分スワイプになっているか確かめ、違えば → 実装（→ #2071）|
| R30 | ロックオン対象がいないとき弾が真っすぐ飛ばない | **飛ぶように直す** | 仕様に「対象がいなければ正面へ直線で撃つ」と書く → 実装（`PlayerAttackController.cs:134-139`）（→ #2055）|

## 追加の不具合報告（八幡さん）
- ターゲットがいないと SMG が 3 点バーストしない（→ #2056）
- ホーム画面で、既定のフォーカスが設定ボタンになっていることがある（チュートリアルの後と思われる）（→ #2057）
