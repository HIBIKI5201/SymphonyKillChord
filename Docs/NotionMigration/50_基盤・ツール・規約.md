# 基盤・開発ツール・ビルド・開発規約 — Notion 反映項目

- 調査日: 2026-09-22 / 対象: origin/develop 73fefeb45 / Notion スナップショット 2026-09-22
- 対象 Notion ページ (パス / id):
  - システム概要 (27d7c2c6-cc02-818d-95ec-c1dc9a3c6761)
  - システム概要 / コード規定 (2a87c2c6-cc02-80f9-a10a-f0b72b95fbb3)
  - システム概要 / GitHub 運用規定 (2a87c2c6-cc02-804b-9b05-d384f4d76dbc)
  - システム概要 / 設計思想 (31c7c2c6-cc02-80f3-92c0-e6929d962476)
  - システム概要 / 初期化ライフサイクル (3bf7c2c6-cc02-81fc-afe1-dbe0d028cda1)
  - システム概要 / シーンマネージャー (39e7c2c6-cc02-8009-8530-cc10a4396fa5)
  - システム概要 / EventBus (3d67c2c6-cc02-816d-bc9d-d0a44b549c00)
  - システム概要 / SourceDataProvider (3d67c2c6-cc02-8169-9132-fa007a0af707) と子 ③ GameDataVariant切替… (3d67c2c6-cc02-81c5-bdd1-fad0d552e2d2)
  - システム概要 / マスターデータ運用ガイド（プランナー向け） (39e7c2c6-cc02-809f-a6df-ee406c242e70)
  - システム概要 / AutoBuilder (3d67c2c6-cc02-8130-9cb7-ec47e253a290)
  - システム概要 / Unity Loop MCP (uloop-*) (3d67c2c6-cc02-81af-adb7-de390511bffe)
  - システム概要 / SinfoniaOperator (31b7c2c6-cc02-80ab-b731-fd020e05bf1c) と子 アーキテクチャ (3d47c2c6-cc02-8122-b5c1-e06b79d59012) / 仕様検索Bot (3d47c2c6-cc02-8186-b646-c785716fedde) / 設定・環境変数リファレンス (3d47c2c6-cc02-81e6-93d5-fdca6adc65f9) / Oracle Cloudデプロイ手順 (3d47c2c6-cc02-81df-a3fd-ebaaff186a65)
  - システム概要 / SinfoniaAssetManager (32d7c2c6-cc02-8026-8e09-f37a558a73bd)
  - システム概要 / シーンのチケットシステム (3587c2c6-cc02-803e-ad97-c1df9f4985b9)
  - システム概要 / ホームページ仕様 (3a47c2c6-cc02-8078-ac23-dba265af3b93)
  - システム概要 / シナリオCSV～ドライブ同期～ (3b57c2c6-cc02-803d-96c9-de406ce46dec)
  - システム概要 / シナリオコマンド使い方 / ScenarioCommandSpec (3af7c2c6-cc02-8079-a505-e997a12a0f7e) ※参照リンクのみ
  - システム概要 / StageNodeAssetの使い方 (3797c2c6-cc02-8083-8469-f92524ecff07) ※構造指摘のみ
  - システム概要 / 7月β大改修リファクタリング / 疑問点 (3937c2c6-cc02-80a4-b6a7-fb0ab57d29f6) ほかリファクタリング資料群
  - シーン構成 (30b7c2c6-cc02-8082-b67b-c1b4aa90bbd4) / シーンの概要と意図 (3167c2c6-cc02-80a5-996d-f7693b7c6cbd)
  - ワークフロー (2ef7c2c6-cc02-8001-b4f0-d69b499c64f0) / はじめに (3c87c2c6-cc02-8189-af78-c0b1497b00d0) / Unityセットアップ (3c87c2c6-cc02-81cc-9260-c80a87e89399) / 社内ツール (3c87c2c6-cc02-815e-b882-f8e5182ea23f) / ビルドとリリース (3c87c2c6-cc02-81c6-9a6b-f1d5a06e4b58)
  - ワークフロー / プログラマー / パッケージ導入手順 (3367c2c6-cc02-8028-86f0-e7b1743c3582) / クライアント部門 Unityプロジェクト開発規定 (34a7c2c6-cc02-8007-a640-cc8a457ddd69)
  - プログラマーワークフロー パッケージ導入について (3277c2c6-cc02-8076-bc6d-f325365871cd)
  - DB「使用素材一覧表」 (27d7c2c6-cc02-814a-aae1-c011961e1d46) のサードパーティー行
  - 仕様書 ガイド / システム概要・マスターデータ・ワークフロー (2ee7c2c6-cc02-81e2-8b06-c9e4aeb357ac / 3347c2c6-cc02-80d0-8bbd-eceeb71b75b5 / 3127c2c6-cc02-8011-8813-db68091ef6c4)
- 件数: 変更反映 20 / 未定義仕様 16 / 仕様のみ 1 / リポ文書から移植 7 / 構造 10 (計 54)

## 反映先ページ別サマリ
| Notion ページ | id | 項目ID | 優先度 |
|---|---|---|---|
| (リポ内 AI スキル・設定の参照先。Notion 側は受け皿ページ新設) | 新規 | RL-01, RL-02, RL-03, RL-17 | 最高, 最高, 高, 中 |
| マスターデータ運用ガイド（プランナー向け） | 39e7c2c6-cc02-809f-a6df-ee406c242e70 | TL-01, TL-02, IF-11 | 最高, 高, 高 |
| SourceDataProvider (+③) | 3d67c2c6-cc02-8169-9132-fa007a0af707 | IF-07, IF-08, IF-09, IF-10, IF-11, IF-13 | 最高, 高, 低, 中, 高, 中 |
| AutoBuilder | 3d67c2c6-cc02-8130-9cb7-ec47e253a290 | TL-03, TL-04, TL-08 | 高, 高, 高 |
| ビルドとリリース | 3c87c2c6-cc02-81c6-9a6b-f1d5a06e4b58 | TL-04, TL-05, TL-06, TL-07, TL-09, TL-10 | 高, 高, 中, 中, 高, 中 |
| 初期化ライフサイクル | 3bf7c2c6-cc02-81fc-afe1-dbe0d028cda1 | IF-01, IF-02, IF-03 | 高, 高, 中 |
| シーンマネージャー | 39e7c2c6-cc02-8009-8530-cc10a4396fa5 | IF-04, IF-03 | 中, 中 |
| シーン構成 / シーンの概要と意図 (または新規) | 30b7c2c6-cc02-8082-b67b-c1b4aa90bbd4 | IF-05 | 高 |
| EventBus | 3d67c2c6-cc02-816d-bc9d-d0a44b549c00 | IF-06 | 中 |
| 新規: Addressables 運用規定 | 新規 | IF-12 | 高 |
| SinfoniaOperator 配下 | 31b7c2c6-cc02-80ab-b731-fd020e05bf1c ほか | TL-11, TL-12, TL-13, TL-14 | 高, 中, 中, 中 |
| Unity Loop MCP (uloop-*) | 3d67c2c6-cc02-81af-adb7-de390511bffe | TL-15 | 低 |
| 新規: AI デバッグ QA ツール / QA | 新規 | TL-16, TL-22 | 中, 中 |
| 社内ツール / シーンのチケットシステム | 3c87c2c6-cc02-815e-b882-f8e5182ea23f / 3587c2c6-cc02-803e-ad97-c1df9f4985b9 | TL-17, TL-18 | 中, 低 |
| SinfoniaAssetManager | 32d7c2c6-cc02-8026-8e09-f37a558a73bd | TL-19 | 低 |
| ホームページ仕様 | 3a47c2c6-cc02-8078-ac23-dba265af3b93 | TL-20 | 中 |
| シーンのチケットシステム / シナリオCSV～ドライブ同期～ | 3587c2c6-… / 3b57c2c6-cc02-803d-96c9-de406ce46dec | TL-21 | 高 |
| 新規: Runtime 監査アーカイブ | 新規 | TL-23 | 低 |
| コード規定 | 2a87c2c6-cc02-80f9-a10a-f0b72b95fbb3 | RL-04, RL-07 | 高, 高 |
| 設計思想 | 31c7c2c6-cc02-80f3-92c0-e6929d962476 | RL-05, RL-06 | 高, 中 |
| GitHub 運用規定 | 2a87c2c6-cc02-804b-9b05-d384f4d76dbc | RL-08, RL-09 | 高, 低 |
| ScenarioCommandSpec (またはシナリオ系新規) | 3af7c2c6-cc02-8079-a505-e997a12a0f7e | RL-10 | 高 |
| 新規: UI 実装用語 / DevelopProducts | 新規 | RL-11 | 低 |
| 使用素材一覧表 | 27d7c2c6-cc02-814a-aae1-c011961e1d46 | RL-12 | 中 |
| パッケージ導入手順 系 | 3367c2c6-cc02-8028-86f0-e7b1743c3582 | RL-13 | 低 |
| システム概要 (DB整理) | 27d7c2c6-cc02-818d-95ec-c1dc9a3c6761 | RL-14, RL-15 | 中, 高 |
| ワークフロー | 2ef7c2c6-cc02-8001-b4f0-d69b499c64f0 | RL-16 | 低 |
| scripts/ 系 (新規) | 新規 | TL-24 | 低 |

## 項目

### RL-01 AI スキル・エージェント設定がリポ内文書を直接読んでいる（全削除で参照が壊れる）
- 種別: 構造
- 優先度: 最高(誤実装・誤発注につながる)
- 反映先: 新規: `システム概要` / 「AI エージェント向け参照先一覧」（あわせて各スキルの参照先を Notion ページ ID かエクスポートミラーのパスへ張り替える作業を別途行う）
- 仕様書の現記述: 記述なし
- 実装の実態: 次のファイルがリポ内の文書をパス指定で読む。削除すると、コード規約チェック・Codex 委譲・コードレビュー・QA 自動化の各スキルが根拠文書なしで動く。
  | 参照元 | 参照先 (削除候補) |
  |---|---|
  | `AGENTS.md:2-3` / `.gemini/GEMINI.md`（AGENTS.md 経由） | `Assets/Scripts/DesignPhilosophy.md`, `Assets/Scripts/CodeGuidelines.md` |
  | `.claude/skills/code-guideline-check/SKILL.md:3,10-11,19,36` | 同上（チェック項目の根拠そのもの） |
  | `.claude/skills/codex-implement/SKILL.md:30-31` | 同上（Codex へ渡す規約） |
  | `.claude/skills/notion-spec-diff-check/SKILL.md:76` | `Assets/Scripts/DesignPhilosophy.md` |
  | `.codex/skills/symphony-kill-chord-code-review/SKILL.md:14-15`, `references/source-routing.md:5-7,21-22` | `DesignPhilosophy.md`, `CodeGuidelines.md`, `Assets/Docs/ScriptsDocs/Architecture.txt`, `CodingConventions.txt`, `Assets/Docs/ScriptsDocs/Modules/NotionModuleDocs/`, `Assets/Docs/*.md` |
  | `.claude/skills/ai-debug-qa/SKILL.md:7`, `.codex/skills/ai-debug-qa/SKILL.md:7`, `.agents/skills/ai-debug-attack-queue/references/qa-coverage.md:3` | `Docs/QA/AI_QAツール対応表.md` |
  | `.claude/skills/notion-spec-write/references/module-docs.md:10,12,32` | `Assets/Docs/ScriptsDocs/Modules/NotionModuleDocs/<Module>.md`, `NotionDocsRules.txt`（RL-02） |
  | `.claude/skills/notion-spec-diff-check/SKILL.md:3,11,93,123` | `Docs/仕様書と実装の差分分析_*.md`（出力先の命名規則も `Docs/` 前提） |
  | Notion `ScenarioCommandSpec` 関連ドキュメント節 (0683 行174-175) | `Assets/Scripts/ScenarioCsvGuide.md`, `NotionModuleDocs/Scenario.md`（RL-10） |
  | `.coderabbit.yaml:7` | `Assets/Docs/AGENTS.md`（既に存在しない。RL-03） |
  - Editor ツール (C#) と CI (`.github/workflows/*`) からのリポ内 `.md/.txt` 参照は無い（`git grep` で `Assets/Editor`・`Assets/Scripts`・`.github` を確認。CI が読むのは `ProjectSettings/ProjectVersion.txt` と `ProjectSettings/KillChord/AutoBuilderSettings.asset` のみ: `BuildAndRelease.yml:390-420,501`）。
  - `SinfoniaOperator` の仕様検索は `Docs/NotionSpecifications` の存在を前提にリポジトリルートを探す（`SinfoniaOperator/SinfoniaOperator/SinfoniaOperator.cs:500-514`, `SpecSearch/MarkdownChunker.cs:69`）。このフォルダは `.gitignore:96` で追跡外のため、全削除の対象外として明示しておく必要がある。
- 反映内容案:
  - Notion に「AI エージェント向け参照先一覧」ページを作り、上表の参照先ごとに移行先 Notion ページと ID を載せる。
  - 移行順を決める: 先に RL-04/RL-05/RL-10/TL-02 の中身を Notion へ移し、次にスキル側の参照を「`Docs/NotionSpecifications/.../コード規定.md`（エクスポートミラー）」へ張り替え、最後にリポ内文書を削除する。
  - 「`Docs/NotionSpecifications/` と `Docs/DiscordLog/` は生成物のため削除対象外」と明記する。
- 確認: 事実のみ (そのまま反映可)。移行順は要企画確認

### RL-02 モジュール文書の「正本はリポジトリ側」運用が Notion 一元化方針と逆向き
- 種別: 構造
- 優先度: 最高(誤実装・誤発注につながる)
- 反映先: `システム概要 / 7月β大改修リファクタリング / モジュールドキュメントテンプレ` (3957c2c6-cc02-80c0-a09f-ea75f74f92cc) または新規: `システム概要` / 「モジュール文書の書き方」
- 仕様書の現記述: テンプレートのみ。運用ルール（どちらが正本か）の記述なし
- 実装の実態: `.claude/skills/notion-spec-write/references/module-docs.md:8-14` が「正本はリポジトリ側」「Notionを直接編集せず、正本を直してから反映する」と定め、`scripts/notion/sync_module.py` で `NotionModuleDocs/<Module>.md` を Notion へ `push --whole` する。NotionModuleDocs は 36 モジュール分と `NotionDocsRules.txt` がある。リポ側のほうが新しいものもある（例: `SourceDataProvider.md` は 2026-09-13 の 131f5c573 で更新済みだが、Notion は 2026-09-09 のまま）。
- 反映内容案:
  - 「正本は Notion。リポ内の `NotionModuleDocs` は廃止する」と明記する。
  - 削除の前に、リポ側だけが新しいモジュール文書を Notion へ反映する（少なくとも SourceDataProvider。IF-07/IF-08 参照）。
  - `NotionDocsRules.txt` の節構成ルールを「モジュールドキュメントテンプレ」ページへ移す。
  - スキル `notion-spec-write` の module-docs.md は「Notion を pull → 編集 → push」の手順に書き換える（リポ側作業）。
- 確認: 要企画確認

### RL-03 すでに壊れている参照とハブの残骸
- 種別: 構造
- 優先度: 高
- 反映先: 新規: 「AI エージェント向け参照先一覧」(RL-01) の注意事項節
- 仕様書の現記述: 記述なし
- 実装の実態:
  - `.claude/skills/notion-spec-diff-check/SKILL.md:3,11` は `Docs/仕様書と実装の差分分析_2026-07-27.md` を前例として参照するが、存在しない（実在するのは `_2026-08-23.md`）。同 `:27` の `Docs/Missionステップ用バフ・デバフ付与機構_計画書.md` と、`:39` の `SinfoniaOperator/sinfonia-operator.settings.json` も存在しない。
  - `Assets/Docs/ScriptsDocs/CodingConventions.txt:112` は `ArchitectureValidationReport.txt` を参照するが、存在しない。
  - `.coderabbit.yaml:5-7` は CodeRabbit のレビュー規約に `Assets/Docs/AGENTS.md` を指定しているが、存在しない。自動レビューは規約を読まないまま動いている。
  - `Assets/Docs/README.md` は 0 バイト。
- 反映内容案: 移行時に上記を正しい参照先へ直すか削除する。CodeRabbit の規約参照先は RL-04/RL-05 の Notion ページ（またはそのエクスポート）にする。
- 確認: 事実のみ (そのまま反映可)

### RL-04 コード規定: リポ版にだけある規則が 4 件ある
- 種別: 変更反映
- 優先度: 高
- 反映先: `システム概要 / コード規定` (2a87c2c6-cc02-80f9-a10a-f0b72b95fbb3)
- 仕様書の現記述: 設定・順序・サマリー・命名規則・その他・UnityAPI の 6 節（最終更新 2026-07-28）
- 実装の実態: `Assets/Scripts/CodeGuidelines.md` には、Notion に無い次の規則がある。
  - 命名規則: 「名前空間はレイヤーフォルダの番号を除いたフォルダ構成をそのまま反映する（例 `1.Domain/InGame/Skill` → `KillChord.Runtime.Domain.InGame.Skill`）」
  - その他: プロパティ公開時に変更を伴う場合は `public set` を使わず、`SetXxx`/`RecordXxx` のように意図が分かるメソッドを介する
  - その他: 1 ファイルに公開型は 1 つとし、ファイル名を型名と一致させる（ネストした private 型は例外）
  - ログ出力: `Debug.LogError`/`LogWarning` は `[{nameof(ClassName)}] メッセージ` 形式で書き、MonoBehaviour では第 2 引数に `this` を渡す
  - `Assets/Docs/ScriptsDocs/CodingConventions.txt` には、既存コードから抽出した例（`private static readonly` 定数もアッパースネーク、Tooltip は日本語）と「古いログ形式が `5.InfraStructure`・`4.View` に残っている」という注記がある。
- 反映内容案: コード規定の「命名規則」「その他」に上記 3 件、「ログ出力」節を追加する。CodingConventions.txt のコード例も補足として併記する。
- 確認: 事実のみ (そのまま反映可)

### RL-05 設計思想: リポ版にだけある節と、実際のレイヤーフォルダ構成
- 種別: 変更反映
- 優先度: 高
- 反映先: `システム概要 / 設計思想` (31c7c2c6-cc02-80f3-92c0-e6929d962476)
- 仕様書の現記述: レイヤーは Domain/Application/Adaptor/View/InfraStructure/Composition の 6 層。クラス設計・参考資料まで（最終更新 2026-07-09）
- 実装の実態: `Assets/Scripts/DesignPhilosophy.md` には、Notion に無い次の記述がある。
  - Entity: 公開プロパティは読み取り専用とし、変更は `ChangeValue` のようなメソッドで行う。`public set` は使わない
  - Asset: 種類が増えるデータは `XxxAssetBase` 抽象基底と `[SerializeReference, SubclassSelector]` で多態選択させ、基底に `abstract Create()` を持たせて Domain 型へ変換する
  - Initializer: 3 種の `~InitializationModuleBase` を継承し、`Init → ResourceLoadAsync(CancellationToken) → Build → Ready` を実装する。どれかのフェーズが失敗したら以降は実行しない。`Shutdown()` は登録順の逆順に呼ぶ
  - Container: `ServiceLocator.RegisterInstance` で登録し、`TryGetInstance<T>()` で取得するのが標準
  - 非同期の型: Composition のライフサイクルは `Awaitable`、Application/Adaptor のユースケースは `Task<T>`/`ValueTask<T>`。橋渡しが要る箇所に限り Application でも `Awaitable` を使ってよい
  - 実フォルダは `Assets/Scripts/Runtime/0.Utility, 1.Domain, 2.Application, 3.Adaptor, 4.View, 5.InfraStructure, 6.Composition` の 7 つ。`0.Utility`（EventBus・DataID・定数など横断基盤）が Notion に無い。
- 反映内容案:
  - 上記 5 節を設計思想へ追加する。
  - 「レイヤー」節にフォルダ番号の対応表と `0.Utility` の位置付け（どの層からも参照できる横断基盤）を追加する。
- 確認: 事実のみ (そのまま反映可)。0.Utility の参照可否ルールは要企画確認

### RL-06 Architecture.txt / CodingConventions.txt / RefactoringMemo.txt の移植と、Notion「疑問点」の古い内容
- 種別: リポ文書から移植
- 優先度: 中
- 反映先: `システム概要 / 設計思想` と `システム概要 / 7月β大改修リファクタリング / 疑問点` (3937c2c6-cc02-80a4-b6a7-fb0ab57d29f6)
- 仕様書の現記述: 疑問点ページは ParticleController の意義、カメラの Application 層の位置計算、CameraLockOnState などを未解決のまま載せている（最終更新 2026-07-10）
- 実装の実態:
  - `Assets/Docs/ScriptsDocs/Architecture.txt` は設計思想の写しに、「アーキテクチャ監査を AI に依頼するプロンプト」（調査内容 1〜7、出力形式）を付けたもの（`Architecture.txt:1-25,287-425`）。
  - `RefactoringMemo.txt:1` は「2026-07-16 にコードと照合し、実装済みの項目（ParticleController・カメラのApplication層依存・CameraLockOnState・…・EventBusの静的リスナークリア・PriorityQueueの自作理由）は削除した」と書き、未対応の項目だけが残っている（ServiceLocator 依存のブラックボックス化、`RingBuffer<T>.AsReadonlySpan()` の stackalloc、HealthEntity/CharacterEntity の不変量、ScenarioCsvUtility の GC Alloc、Adaptor 層の UnityEngine 依存など）。
- 反映内容案:
  - 疑問点ページで解決済みの項目に「解決済み（2026-07-16 照合）」と付け、RefactoringMemo の未対応項目を「未解決の設計課題」として移す（または Issue 化）。
  - Architecture.txt の監査プロンプトは、AI 用手順として RL-01 のページ配下へ移すか廃止するかを決める。
- 確認: 要企画確認

### RL-07 コード規定「AI によるファイル変更は…に留める」と実際の AI 実装運用が食い違う
- 種別: 変更反映
- 優先度: 高
- 反映先: `システム概要 / コード規定` (2a87c2c6-cc02-80f9-a10a-f0b72b95fbb3) の「設定」節
- 仕様書の現記述: 「AIによるファイル変更の使用はサマリー追加、コメント追加、タイポ修正の編集に留める」
- 実装の実態: 2026-08-15〜09-22 にマージされた PR 506 件のうち `agent/` ブランチが 40 件（`prs-list.tsv`）。機能実装・CI 改修も含まれる（例 #1534 Build Profile 16構成、#1575 Planner Window、#1827 Release 再試行）。`AGENTS.md:6-15` は AI エージェント用のブランチ・PR 運用を定め、`.claude/skills/codex-implement` は実装を Codex へ任せる前提で書かれている。
- 反映内容案: 現行ルールを実態に合わせて書き直すか、AI 実装の許可範囲と条件（ブランチ命名、Draft PR、必須チェックの扱い）を明記する。
- 確認: 要企画確認

### RL-08 GitHub 運用規定: ブランチ命名・自動 PR・AI エージェント運用・保護ルールが実態と合っていない
- 種別: 変更反映
- 優先度: 高
- 反映先: `システム概要 / GitHub 運用規定` (2a87c2c6-cc02-804b-9b05-d384f4d76dbc)
- 仕様書の現記述: 「featureはfeature/レイヤー/作業名/作業者」「hotfixはhotfix/作業対象」。自動化は AutoCreateMasterBranch と ValidateFeaturePRTarget の 2 つだけ
- 実装の実態:
  - 実際のブランチ先頭語（マージ済み 506 件）: feature 263 / hotfix 161 / agent 40 / develop 29 / fix 3 / designer 3 / spec 2 / chore 2（`prs-list.tsv`）。feature の 2 階層目はレイヤーではなく段階（demo/research/release/docs/tools）。`クライアント部門 Unityプロジェクト開発規定` (34a7c2c6-…) は `feature/[段階]/[プロダクト名]/[個人名]` としており、GitHub 運用規定と食い違う。
  - `AGENTS.md:6-15`: AI は `feature/○○/○○/agent`（指定が無ければ `agent/○○`）で作業し、同階層 master へセルフマージしてよい。`feature/` 以外の作業ブランチの PR 先は develop。
  - `.github/workflows/AutoCreateDevelopPullRequest.yml`: `feature/**/master` への PR がマージされると、master→develop の Draft PR を PR テンプレート構成の本文付きで自動作成する（#1539）。既存の PR があれば作らず、差分が無い（422 No commits between）場合も作らない。
  - `AGENTS.md:13` の例外: AutoCreateMasterBranch はブランチ作成時の内容で master を作るため、作業ブランチと master が同じコミットになり、上記の自動 Draft PR が発火しないことがある。
  - develop の ruleset（PR 必須、承認 1 件、最終 push 後の別承認必須）が PR #1528 本文に書かれている。
  - `.coderabbit.yaml` で CodeRabbit の自動レビューが有効（`auto_review.enabled: true`, `drafts: false`, 日本語、`profile: chill`）。
  - main への反映は develop から直接 PR する運用がある（#1789 `release: 体験版データの組み込み対策をmainへ反映`）。
- 反映内容案:
  - 命名規則を実態（`feature/<段階>/<機能>/<作業者|agent>`, `hotfix/<対象>`, `agent/<作業>`）に揃え、クライアント部門規定とどちらかへ統一する。
  - 自動化節に AutoCreateDevelopPullRequest と、発火しないケースを追加する。
  - AI エージェント運用節（AGENTS.md の内容）を追加する。
  - develop の保護ルールと CodeRabbit の自動レビューを追記する。
- 確認: ruleset は要実機確認（GitHub 設定）。それ以外は事実のみ

### RL-09 PR 作成ガイドラインの節番号の誤りと、必須チェックの適用範囲
- 種別: 構造
- 優先度: 低
- 反映先: `システム概要 / GitHub 運用規定` (2a87c2c6-cc02-804b-9b05-d384f4d76dbc)
- 仕様書の現記述: 見出しが「0. PR提出前」「1. 本文構成」「1. PR作成後」「3. PRテンプレート」の順で、1 が重複し 2 が無い
- 実装の実態: `.github/PULL_REQUEST_TEMPLATE.md:1-10` は「元本はNotion」と明記し、必須チェック 2 項目を持つ。CI だけを変える PR では「PlayMode を通した」にチェックできず、本文で適用範囲を説明している（#1827「必須チェックの適用範囲・未確認」節）。
- 反映内容案: 節番号を直す。C#/シーンの変更が無い PR（CI・ドキュメント・ツールのみ）での必須チェックの扱いを明記する。
- 確認: 要企画確認

### RL-10 シナリオ CSV ガイド（リポ版）が Notion から参照されている
- 種別: リポ文書から移植
- 優先度: 高
- 反映先: `システム概要 / シナリオコマンド使い方 / ScenarioCommandSpec` (3af7c2c6-cc02-8079-a505-e997a12a0f7e) の子ページとして新規「シナリオCSVの書き方（非プログラマー向け）」
- 仕様書の現記述: 関連ドキュメント節で「`Assets/Scripts/ScenarioCsvGuide.md`: 非プログラマー向けの最小ガイド」「`NotionModuleDocs/Scenario.md`」へリンクしている
- 実装の実態: `Assets/Scripts/ScenarioCsvGuide.md`（5.7KB、最終更新 2026-09-14 の 301581881）。保存場所、5 つの命令（背景/立ち絵の重なり順/立ち絵/会話/フェード）、省略記法、ルール、よくあるミス、コピペ用テンプレート、表示拡張の確認用 CSV を含む。
- 反映内容案: 本文をそのまま Notion の子ページへ移し、ScenarioCommandSpec の関連ドキュメント節のリンクを Notion ページへ張り替える。シナリオ仕様の中身の照合はシナリオ担当に任せる。
- 確認: 事実のみ (そのまま反映可)

### RL-11 Runtime フォルダ内の用語集と DevelopProducts の README
- 種別: リポ文書から移植
- 優先度: 低
- 反映先: 新規: `システム概要` / 「UI 実装の用語（ボタン）」、`ワークフロー / プログラマー / 開発部門 Unityプロジェクト開発規定` (3bd17837)
- 仕様書の現記述: 記述なし
- 実装の実態: `Assets/Scripts/Runtime/4.View/OutGame/Common/ButtonTerminology.md` は「ボタンの見た目」「アニメーション」などの用語と、対象ファイル・USS クラス（例 `Button.uss` の `.btn-skin`、通常 `UI_Button_off.png`・hover/focus/active `UI_Button_on.png`、既定 200×100px）の対応表。AI への実装指示で使う想定。`Assets/DevelopProducts/Design/Architecture/Scripts/README.md` も存在する。
- 反映内容案: ButtonTerminology を Notion の用語ページへ移す（UI 担当と重複しないか確認）。README は中身を確認し、必要なら開発部門規定へ移す。
- 確認: 事実のみ。README の中身は要確認

### RL-12 使用素材一覧表: パッケージ・ライブラリの行が不足している
- 種別: 未定義仕様
- 優先度: 中
- 反映先: DB「使用素材一覧表」 (27d7c2c6-cc02-814a-aae1-c011961e1d46)
- 仕様書の現記述: サードパーティー行は LitMotion, Bitonic Pixel Sorter, CRIWARE（アセットサポートアドオンのみ）, Google.Apis×3, Microsoft.Bcl×2, R3, System.CodeDom, System.ComponentModel.Annotations, System.Management, System.Threading.Channels, VRM Importer（行名は「Inporter」と誤記）, ZLinq
- 実装の実態:
  - `Packages/manifest.json` にあって表に無いもの: UniTask 2.5.10, ObservableCollections 1.1.3, ZString 2.6.0, NuGetForUnity (git), UnityChanSpringBone (git `#release/1.2`), SymphonyFramework (git), unity-cli-loop / uloopmcp (git), graph-visualizer (git), CopyComponentsByRegex (git), Cursor IDE 連携 (git)。
  - `Assets/Settings/Nuget/packages.config` にあって表に無いもの: System.Runtime.CompilerServices.Unsafe 6.0.0。
  - CRIWARE 本体（ADX 等のランタイム。Persistent シーンに `CriWareLibraryInitializer` がある: `Assets/Level/Scenes/Master/Persistent.unity:970`、#1452）の行が無い。
  - `third-party-notices.md` に Material Symbols / Material Icons（Apache 2.0）があるが、表に無い。
  - リポに含めない配布物（AssetStoreTools、Google Drive から CI が取得する `.unitypackage` 群: `BuildAndRelease.yml:829-1020`）の扱いが表に無い。
- 反映内容案: 上記を「サードパーティー」行として追加し、ライセンス・導入経路（UPM git / OpenUPM / NuGet / Drive 配布）を列にする。フォント（KosugiMaru・Anton・IBM Plex Serif・Inria Serif、`Assets/Arts/Fonts/`）はアセット担当と分担を決める。
- 確認: 事実のみ。ライセンス種別は要確認

### RL-13 パッケージ導入手順: リンク切れと重複ページ
- 種別: 構造
- 優先度: 低
- 反映先: `ワークフロー / プログラマー / パッケージ導入手順` (3367c2c6-cc02-8028-86f0-e7b1743c3582)、`プログラマーワークフロー パッケージ導入について` (3277c2c6-cc02-8076-bc6d-f325365871cd)
- 仕様書の現記述: 手順 3「プランナーが[[]] に追記」のリンク先が空。ルート直下に同趣旨の「プログラマーワークフロー パッケージ導入について」がある。`Unityセットアップ` のプログラマー節は申請フローのリンク先がワークフロー最上位になっている
- 実装の実態: 追記先は DB「使用素材一覧表」と推定される（RL-12）
- 反映内容案: `[[]]` を使用素材一覧表へのリンクに直し、重複ページを統合する。Unityセットアップのリンクをパッケージ導入手順へ向ける。
- 確認: 要企画確認

### RL-14 リファクタリング資料・開発計画・旧モックのアーカイブが未実施
- 種別: 構造
- 優先度: 中
- 反映先: `システム概要` (27d7c2c6-cc02-818d-95ec-c1dc9a3c6761) の DB「システムリスト」
- 仕様書の現記述: システムリスト 75 行に、カテゴリ「リファクタリング資料」の 8 ページ（音楽同期/敵関連/プレイヤー/カメラ/スキル/攻撃パイプライン 2026-05、7月 リファクタリング、7月β大改修リファクタリング）、「〜開発」ページ（セーブシステム・設定システム開発・ポーズシステム開発・セーブデータの暗号化開発など）、2025-11 の「音楽同期戦闘モック」（子 40 ページ超）が現行モジュール文書と並んでいる。`仕様書 ガイド` 配下の「システム概要」「マスターデータ」「ワークフロー」は本文が `:未定義:` のみ
- 実装の実態: 前回調査 `Docs/仕様書全体_改修追加項目一覧_2026-08-24.md` B-1・B-2（アーカイブ表示・区分けの新設）はスナップショットに反映されていない（0671〜0679・0686・0689・0690・0692・0626 のいずれにも「アーカイブ」「現行ページ」の記載なし）。7月 リファクタリング/計画は本文 1 行だけ。
- 反映内容案: 前回案どおり、冒頭にアーカイブのコールアウトと現行ページへのリンクを置くか、`システム概要/アーカイブ` へ移す。仕様書ガイドの空ページには、各ツリーの説明を書くか削除する。
- 確認: 要企画確認

### RL-15 StageNodeAsset の使い方: 対象のクラスが存在しない
- 種別: 構造
- 優先度: 高
- 反映先: `システム概要 / StageNodeAssetの使い方` (3797c2c6-cc02-8083-8469-f92524ecff07)
- 仕様書の現記述: StageNodeAsset の作り方（最終更新 2026-06-10）
- 実装の実態: `class StageNodeAsset` はリポに無い（`git grep` 0 件）。ステージは `BattleStageAsset` / `ScenarioStageAsset` / `StageBindAsset` / `StageTreeAsset`（`Assets/Scripts/Runtime/5.InfraStructure/OutGame/StageSelect/`）へ分割済み（`Assets/Docs/StageAssetSeparationAndBindingPlan.md`）。
- 反映内容案: アーカイブ表示を付け、マスターデータ運用ガイド①（TL-01）へ誘導する。ステージ仕様の詳細はステージ担当と調整する。
- 確認: 事実のみ (そのまま反映可)

### RL-16 ワークフロー: QA/バグ報告と配信準備のページが無い
- 種別: 構造
- 優先度: 低
- 反映先: `ワークフロー` (2ef7c2c6-cc02-8001-b4f0-d69b499c64f0)
- 仕様書の現記述: 環境と道具・進め方・役職の手引きのみ
- 実装の実態: `Docs/ワークフローページ改修計画_2026-08-24.md` 2-4「バグ報告とQA」は未反映。QA の実資料は `Docs/QA/`（TL-22）にだけある。ストア登録の手順・判断材料は `Docs/ストア仮登録_手順と事前決定事項_2026-08-14.md` にだけある（Steam Direct $100、Google Play 個人アカウント $25、12人×連続14日のテスト条件など）。
- 反映内容案: ワークフローに「QA・バグ報告」「配信・ストア登録」ページを作り、上記資料を移す（管理系の担当と分担を決める）。
- 確認: 要企画確認

### RL-17 リポ内の生成物・AI 分析出力フォルダの扱いが決まっていない
- 種別: 構造
- 優先度: 中
- 反映先: 新規: 「AI エージェント向け参照先一覧」(RL-01) の「削除対象外」節
- 仕様書の現記述: `社内ツール` に「`Docs/NotionSpecifications/` は読み取り専用コピー」とだけある
- 実装の実態: `spec/`（Anatomia/Augur の分析結果・`spec/plan/problem_logs/`）と `Docs/G-Lab/`（レビュー HTML）は追跡対象。problem_logs はワークスペース共通スキル record-problem-log の書き込み先。`Docs/NotionSpecifications/` と `Docs/DiscordLog/` は `.gitignore:96-97` で追跡外。
- 反映内容案: 「全削除」の対象範囲（`Assets/Docs`, `Docs/*.md`, `Assets/Scripts/*.md`, `spec/`, `Docs/G-Lab/`）を明示し、生成物・AI 作業ログのフォルダを残すかどうかを決める。
- 確認: 要企画確認

### IF-01 初期化ライフサイクル: 失敗時の挙動・エントリポイント・収集条件が未定義
- 種別: 未定義仕様
- 優先度: 高
- 反映先: `システム概要 / 初期化ライフサイクル` (3bf7c2c6-cc02-81fc-afe1-dbe0d028cda1)
- 仕様書の現記述: 「Coordinatorが`Order`の昇順で呼ぶ」「Initializerクラス: `IngameComposition`（インゲーム）と、各シーンのCoordinator」。失敗時の扱いは記述なし
- 実装の実態:
  - 各フェーズは成功/失敗を `bool` で返し、1 モジュールでも失敗したらその場で以降のフェーズを止める（`Assets/Scripts/Runtime/6.Composition/Bootstrap/InitializationCoordinator.cs` の `RunInitPhase`〜`RunReadyPhase`）。例外は `LogPhaseFailure` でモジュール名とフェーズ名を記録してから再送出し、キャンセルはそのまま通す。
  - 進捗は「モジュール数×4 フェーズ」のステップ数で `IProgress<float>` へ報告し、ロード画面の進捗に使う（同ファイル `InitializeAsync`）。
  - シーンごとのエントリポイントは `IngameComposition`（InGame）、`OutGameSceneInitializer`（OutGame。自身も Order 0 のモジュール）、`PersistentEntryPoint`（Persistent）。`PersistentEntryPoint` は Notion のクラス表に無い。
  - 収集対象は `FindObjectsByType` で見つかったモジュールのうち `isActiveAndEnabled` のもの。Persistent はさらに同じシーンのものだけ（`IngameComposition.cs:213-219`, `PersistentEntryPoint.cs:160-169`, `OutGameSceneInitializer.cs:160-174`）。
  - 結果は `ISceneInitializationReadiness.Complete(sceneName, isSuccess)` で通知する。失敗時は `Shutdown()` を呼んで後始末し、1 モジュールの Shutdown 例外で残りを止めない（#1529、`OutGameSceneInitializer.cs:107-120`）。
- 反映内容案: 「フェーズの戻り値と失敗時の挙動」「進捗報告」「シーン別エントリポイント（PersistentEntryPoint を含む）」「収集条件（非アクティブなモジュールは実行されない）」の 4 節を追加する。
- 確認: 事実のみ (そのまま反映可)

### IF-02 初期化 Order の全一覧と、同じ Order 同士の順序
- 種別: 未定義仕様
- 優先度: 高
- 反映先: `システム概要 / 初期化ライフサイクル` (3bf7c2c6-cc02-81fc-afe1-dbe0d028cda1) の「🔁 初期化フェーズ」節
- 仕様書の現記述: 実例は 9 件のみ（Target 100 / Result 400 / Skill 450 / Player 500 / Camera 600 / Mission 600 / Enemy 700 / Stage 800 / Sequence 1000）
- 実装の実態（`override int Order` 51 件、`Assets/Scripts/Runtime/6.Composition/**`）:
  - Persistent: SceneTransition 0 / SavedataSystem 10 / MusicPlayer 20 / InitialSkillLoadout 20 / SoundEffect 30 / EnvironmentAppliers 32 / AudioSettings 35 / UISoundEffect 36 / EnvironmentSettings 37 / Camera 40 / EventNotification 40（Input 系は `Persistent/Input/InputComposition.cs`）
  - OutGame: OutGameSceneInitializer 0 / OutGameScenarioScene 0 / OutGameScenario 10 / Title 20 / OutGameSortie 20 / OutGameBgm 30 / Screen 100 / StageSelect 110 / HomeCharacterPreview 110 / SkillTree 120 / OutGameUISoundEffect 125 / SkillBuild 130 / OutGameTutorial 150
  - InGame: SceneDependency 0 / Target 100 / MusicSync 200 / EquipmentBgm 250 / Result 400 / SkillCrosshairProgressUI 440 / SkillListUI 441 / SkillInputProgressUI 442 / Skill 450 / PlayerStatusBonus 490 / InGameHud 495 / Player 500 / Camera 600 / Mission 600 / HUDEnemyHealth 650 / ReticleHud 660 / EnemyDirectionIndicator 660 / Enemy 700 / StageEffect 800 / ACLikeRhythmGuide 800 / Sequence 1000 / PauseWindow 1010（SkillEffect は定数 `MODULE_ORDER`）
  - 同じ Order の組がある（Persistent 20・40、OutGame 0・20・110、InGame 600・660・800）。収集は `FindObjectsSortMode.None` で、InGame/Persistent は `OrderBy`、OutGame は `List.Sort` を使うため、同値同士の実行順は保証されない。
- 反映内容案: 全一覧をシーン別の表で載せ、「同じ Order のモジュール同士は互いを Ready で取得してはならない（順序が保証されない）」と明記する。
- 確認: 一覧は事実のみ。同値ルールは要企画確認

### IF-03 OutGame 初期化失敗時の復帰画面（#1529）
- 種別: 未定義仕様
- 優先度: 中
- 反映先: `システム概要 / 初期化ライフサイクル` (3bf7c2c6-…) または `シーンマネージャー` (39e7c2c6-cc02-8009-8530-cc10a4396fa5)
- 仕様書の現記述: 記述なし
- 実装の実態: OutGame の初期化に失敗すると、シーン内の UIDocument を無効にし、通常 UI と Addressables に依存しない IMGUI の復帰画面（`Assets/Scripts/Runtime/4.View/OutGame/Screen/OutGameInitializationFailureView.cs`、560×180px、文字 20pt）を出す。文言は「画面の読み込みに失敗しました。」「タイトルへ戻る」「画面を読み込めませんでした。もう一度お試しください。」「読み込み中…」をコードに直書きしており、ローカライズ対象外（同ファイル 54-57 行）。マウスまたは Enter でタイトルへ戻る。Title 自身の初期化失敗時は再読み込みする。シナリオ戦闘出撃中や常駐の寿命トークンがキャンセル済みの場合は復帰を始めない（`OutGameSceneInitializer.cs:242-295`）。
- 反映内容案: 失敗時の画面・操作・遷移先・連打防止を仕様として書く。英語表示の要否を決める。
- 確認: 挙動は事実。ローカライズ要否は要企画確認。実機での表示は未確認（#1529 本文に未実施と記載）→ 要実機確認

### IF-04 シーンマネージャー: SceneTransitionController と Tips 設定が大きく変わった
- 種別: 変更反映
- 優先度: 中
- 反映先: `システム概要 / シーンマネージャー` (39e7c2c6-cc02-8009-8530-cc10a4396fa5)
- 仕様書の現記述: 「`SceneTransitionController`: Viewからのシーン遷移要求を受け取り`SceneTransitionUsecase`へ委譲する薄いパススルー」「`LoadingTipsConfig`: View の ScriptableObject」。Persistent の SceneManagement 配下は `SceneTransitionInitializer` のみ
- 実装の実態:
  - `Assets/Scripts/Runtime/3.Adaptor/Persistent/SceneManagement/SceneTransitionController.cs` は `PersistentLifetimeToken`（常駐の寿命トークン）、`~WithPersistentLifetimeAsync` 系の API（Change/Reload/Unload/UnloadAndSetActive/KeepingLoading）、シナリオ戦闘出撃専用の受付・初期化停止・決着処理（`HasScenarioBattleSortie`, `SettleScenarioBattleInitializationAsync` など、30-240 行）を持つ。多重操作とアンロード中断の防止（b17f82dca, 2026-09-05）、初期化失敗からの復帰（dfa98cce9 / #1529）、シナリオ後のチュートリアル戦闘出撃の修正（180d02f2c, 2026-09-16）で追加された。
  - `Assets/Scripts/Runtime/6.Composition/Persistent/SceneManagement/EventNotificationInitializer.cs`（Order 40）が常駐の通知 View を入力初期化の前に公開する。
  - `LoadingTipsConfig` は Addressables（`GameData.Shared`）に登録され、SourceDataProvider の Collection `LoadingTip` として Planner Master Data の UI ページから編集する（#1575）。
- 反映内容案: クラス表の SceneTransitionController の説明を直し、常駐寿命トークンとシナリオ戦闘出撃の専用経路を処理フローに追加する。EventNotificationInitializer を表へ追加する。Tips の編集方法を「Planner Master Data > UI > LoadingTip」と書く。
- 確認: 事実のみ (そのまま反映可)

### IF-05 Unity 上のシーン構成（常駐シーン・追加ロード）が仕様書に無い
- 種別: 未定義仕様
- 優先度: 高
- 反映先: `シーン構成` (30b7c2c6-cc02-8082-b67b-c1b4aa90bbd4) の改訂、または新規: `システム概要` / 「シーン構成（実装）」
- 仕様書の現記述: 2026-02 の企画段階の構成（アウトゲーム=タイトル/ミッションセレクト/収集リスト/編成、グローバル=設定、インゲーム=チュートリアル/シナリオ/バトル）と遷移図の画像のみ
- 実装の実態:
  - Build Settings のシーン（`ProjectSettings/EditorBuildSettings.asset`）: `Master/Persistent`（index 0・常駐）、`Master/Title`、`Master/OutGame`、`Master/InGame`、`Master/Scenario`、`Master/Stages/Stage_01`、`Master/Stages/Stage_02`（バトル背景。InGame へ追加ロード）、`Develop/Dodge/InGameDodgeTest`（無効）、`Demo/DemoEnd`（体験版 Build Profile だけに同期される: `GameDataVariantProfiles.cs:19,53-100`）。
  - 「収集リスト」画面は存在しない。OutGame は Home/StageSelect/SkillTree/SkillBuild/Setting の 5 コンテナで構成される（`Assets/Editor/Scripts/Build/UiDocumentBuildValidator.cs:50-55`）。
  - 製品版が使う UXML が `Assets/Level/Scenes/Develop/OutGameTest/…`（Title: `Title/UIToolkit/TitleScreen.uxml`、OutGame: `ScreenTransitionTest/UI Toolkit/OutGame.uxml`）にある（同 37-40 行）。
  - Persistent シーンに CRIWARE の `CriWareLibraryInitializer` がある（#1452、`Persistent.unity:970`）。
- 反映内容案: 実装のシーン一覧と役割（常駐・追加ロード・体験版専用）、遷移の主経路、Develop フォルダにある製品用 UXML の注意書きを載せる。旧シーン構成ページは「企画時点の構成」と明記する。
- 確認: 事実のみ。UXML の配置を移すかどうかは要企画確認

### IF-06 EventBus: イベント 1 種と利用箇所 2 件が漏れている
- 種別: 変更反映
- 優先度: 中
- 反映先: `システム概要 / EventBus` (3d67c2c6-cc02-816d-bc9d-d0a44b549c00)
- 仕様書の現記述: イベント定義は EOnTakeDamage / EOnPlayerTakeDamage / EOnEnemyDefeated / EOnPlayerAttackExecuted / EOnSkillExecuted の 5 種。DB のカテゴリーは「インゲーム」、本文は「Utility / Persistent（横断基盤）」
- 実装の実態: `Assets/Scripts/Runtime/0.Utility/Persistent/EOnLockOnAcquired.cs`（2026-09-14 の 79dee834c で追加）。`TargetingSystem.cs:283` で発火し、`PlayerView.cs:181,231` が購読する（ロックオン成立時の SE）。`TutorialSkillFeedbackPresenter` も EventBus を購読するが、Notion の利用箇所一覧に無い。
- 反映内容案: クラス表に EOnLockOnAcquired（中身なし、ロックオン成立通知）を追加し、利用箇所へ TargetingSystem と TutorialSkillFeedbackPresenter を追加する。DB カテゴリーを本文に合わせる（開発用 など）。
- 確認: 事実のみ (そのまま反映可)

### IF-07 GameDataVariant: 種別の正本が Build Profile へ移り、ビルド時の処理も変わった
- 種別: 変更反映
- 優先度: 最高(誤実装・誤発注につながる)
- 反映先: `システム概要 / SourceDataProvider` (3d67c2c6-cc02-8169-9132-fa007a0af707) と子 `③ GameDataVariant切替でRelease/Demoのデータを出し分ける仕組み` (3d67c2c6-cc02-81c5-bdd1-fad0d552e2d2)
- 仕様書の現記述: 「`GameDataVariantEditorState`: 選択中の`GameDataVariant`を`EditorPrefs`で保持し、変更時に`Apply`を呼ぶ」「`KILLCHORD_DEMO`シンボルを追加/削除」「DemoEnd を EditorBuildSettings へ登録」「`IPreprocessBuildWithReport`(GameDataVariantBuildPreprocessor)がビルド直前に再適用」「KillChord/Game Data Variant メニューで選択」「Planner Master Data Window はどちらの Variant でも両方の Group を表示する」
- 実装の実態:
  - 種別の正本はアクティブな Build Profile。Scripting Defines に `KILLCHORD_DEMO` があれば Demo、無ければ Release（`Assets/Editor/Scripts/SourceDataProvider/Core/GameDataVariantProfiles.cs:26-44`、#1534）。Demo の 8 Profile だけがシンボルを持ち、Windows 共通設定からは削除済み（#1788）。
  - 体験版 Profile のシーン一覧は「グローバルのシーン一覧＋`Demo/DemoEnd.unity`」へ自動で揃える（同 53-100 行）。
  - ビルド時の適用は `GameDataVariantBuildProcessor`（`BuildPlayerProcessor.PrepareForBuild`、callbackOrder -1000。Addressables のビルドより前）へ移った（`GameDataVariantBuildProcessor.cs`、#1788）。Profile とコンパイル済みコードの種別が食い違う、対象 Group がアドレス付きでビルドされない、必須エントリ（`StageTreeAsset` / `EnemyMissionKeyRepository` / `MissionDefinitionRepository`、Demo ではさらに `DemoRuntime` / `DemoExperienceConfig`）が無い、のいずれかで `BuildFailedException` になる。
  - 「KillChord/Game Data Variant」メニューは無い（`MenuItem` の全検索で 0 件）。`EditorPrefs`（キー `KillChord.GameDataVariant`）は Planner Master Data Window のツールバー「Variant」の表示・生成先の切替にだけ使う（`GameDataVariantEditorState.cs`, `PlannerMasterDataWindow.cs:177-185`、#1588）。
  - `SourceDataProviderRepositoryResolver.TryResolveAsset` は選択中 Variant の Group から SourceAsset を解決する（`SourceDataProviderRepositoryResolver.cs:23-31`）ため、Window は Variant ごとに別のアセットを表示する（「両方表示」は誤り）。
  - Build Profile の切替 UI がメインツールバーにある（`Assets/Editor/Scripts/Build/BuildProfileToolbar.cs`、要素名「KillChord/Build Profile」）。
  - リポ側の `NotionModuleDocs/SourceDataProvider.md` は 2026-09-13 に Build Profile 正本へ更新済みだが、Preprocessor の記述は #1788 前のまま。
- 反映内容案:
  - クラス表の GameDataVariantEditorState・GameDataVariantBuildSettings・Preprocessor の行を直し、GameDataVariantProfiles・GameDataVariantBuildProcessor・BuildProfileToolbar を追加する。
  - ③ のシーケンス図を「ツールバーで Build Profile を選ぶ → Profile のシンボルで種別判定 → Group の IncludeInBuild 切替 → ビルド時 PrepareForBuild(-1000) で再適用と検証」に描き直す。
  - 「Window の Variant 切替は表示と生成先だけで、ビルド成果物には影響しない」と明記する。
- 確認: 事実のみ (そのまま反映可)

### IF-08 SourceDataProvider: クラス構成と Planner Window の役割が変わった
- 種別: 変更反映
- 優先度: 高
- 反映先: `システム概要 / SourceDataProvider` (3d67c2c6-cc02-8169-9132-fa007a0af707)
- 仕様書の現記述: `PlannerEnemyStatusPreview` / `PlannerEnemyWavePreview` / `PlannerStageTreeGraphRenderer` / `PlannerBattleSceneMapRenderer` が Window 内のプレビュー。「Window は選択要素の Inspector 描画を行う」。拡張ポイント「専用プレビューは `PlannerMasterDataWindow.DrawSourceAssetPreview` 等の分岐へ追記」。`PlannerMasterDataEditorSettings` は `ProjectSettings/PlannerMasterDataEditorSettings.asset`
- 実装の実態:
  - `SourceDataProvider/` は `Core/`（登録・解決・DataID）と `Wiki/`（Window と Collection の追加・削除）に分割された（#1564）。
  - プレビューは `[CustomEditor]` として `Assets/Editor/Scripts/Inspectors/SourceData/` へ移った（CharacterDefinitionAssetEditor + EnemyStatusRadarChart、EnemyWaveDefinitionAssetEditor + EnemyWaveSummaryDrawer + BattleSceneMapRenderer、StageTreeAssetEditor / StageAssetEditor / StageBindAssetEditor + StageTreeGraphView、StageWavePreview、SourceCollectionItemInspector）。Notion の Planner*Preview クラスはどれも存在しない。
  - Window は値を編集しない。行を選ぶと実アセットを Project/Inspector で選択・Ping する（#1564、`Assets/Docs/PlannerGuide/MasterDataGuide.md:282-300`）。
  - `PlannerNavigationHub`（Core→Wiki の依存を逆転させる窓口）、`SourceCollectionElementDisplay`（Collection 要素の表示名と検索キーの抽出）、`BattleSceneDataReader` は Core にある（#1588, 5f3f065f1）。
  - `PlannerMasterDataEditorSettings.asset` の実パスは `ProjectSettings/KillChord/`。
- 反映内容案: クラス表と構成要素節を Core / Wiki / Inspectors / ProjectWindow（IF-09）の 4 区分に書き直す。「専用プレビューの追加 = 対象型の CustomEditor を Inspectors/SourceData に追加する」へ拡張ポイントを直す。設定ファイルのパスを直す。
- 確認: 事実のみ (そのまま反映可)

### IF-09 Project ウィンドウの Addressable / Collection バッジ（#1565）
- 種別: 未定義仕様
- 優先度: 低
- 反映先: `システム概要 / SourceDataProvider` (3d67c2c6-cc02-8169-9132-fa007a0af707)、`ワークフロー / 社内ツール` (3c87c2c6-cc02-815e-b882-f8e5182ea23f)
- 仕様書の現記述: 記述なし
- 実装の実態: `Assets/Editor/Scripts/ProjectWindow/SourceDataAssetIndex.cs`（GUID→フラグの逆引き。Addressables の変更イベントと AssetPostprocessor でキャッシュを無効化）と `SourceDataProjectWindowIcons.cs`（`projectWindowItemOnGUI` で描画）。Addressables 登録済みには組込みアイコン「AddressableAssetsWindow」（無ければ色付き四角）、SourceDataProvider の Collection 登録済みには別色の四角を出し、両方同時に出ることもある。Window を開かなくても動く。Variant 切替時はインデックスを作り直す（#1588）。
- 反映内容案: 「Project ウィンドウのバッジの意味」を SourceDataProvider と社内ツールへ追記する。
- 確認: 事実のみ。色の具体値は要実機確認

### IF-10 SourceDataProvider に登録済みの Collection 一覧（#1575 で 4 件追加）
- 種別: 未定義仕様
- 優先度: 中
- 反映先: `システム概要 / SourceDataProvider` (3d67c2c6-cc02-8169-9132-fa007a0af707) に新節「登録済み CollectionKey 一覧」
- 仕様書の現記述: 一覧なし（Planner Window のページ構成も記述なし）
- 実装の実態:
  - コードの `[SourceDataCollection]` キー: Character, EnemyData, EnemyMissionKey, Mission, ScenarioAnimation, ScenarioBackground, ScenarioPortrait, Skill, SkillNode, StageAsset, StageEffect, Wave、シーン内完結の SpawnPoint（`SpawnPositionPair.SPAWN_POINT_COLLECTION_KEY`, `isSceneScoped: true`）。
  - `ProjectSettings/SourceDataProviderSettings.asset` の Collection にはほかに BossAttackEntry, BgmSelectorLabel, LoadingTip, SkillGenreIcon, StatusBonusEffectIcon, SkillNodeBind, SkillNodePhaseBind, StageBind などがある。#1575 で LoadingTip（UI ページ）、BgmSelectorLabel（Enemy ページ）、StatusBonusEffectIcon（Skill Tree ページ）、SkillGenreIcon（Player ページ）を追加し、`LoadingTipsConfig` と `BgmSelectorLabelTableAsset` を `GameData.Shared` へ登録した。
  - Window のページは Player / UI / Enemy / Stage Select / Skill Tree / Scenario（`MasterDataGuide.md:284`）。登録済み SourceAsset 約 45 件のうち約 14 件はどのページにも割り当てられておらず、検索からだけ開ける（#1588 本文）。
  - `StageEffect` はコード側で使っているが、設定アセットの Collection 一覧には見当たらない。
- 反映内容案: CollectionKey・所属ページ・データ型の表を追加する。`StageEffect` が未登録で警告が出ていないかを確かめる。
- 確認: 一覧は事実。StageEffect の登録状態は要実機確認

### IF-11 Release / Demo のデータ配置と Addressables Group の規約
- 種別: 未定義仕様
- 優先度: 高
- 反映先: `システム概要 / マスターデータ運用ガイド（プランナー向け）` (39e7c2c6-cc02-809f-a6df-ee406c242e70) の「前提知識」と `SourceDataProvider` (3d67c2c6-…)
- 仕様書の現記述: 「マスターデータの実体は `Assets/Level/Data/Master/` 以下にあります」
- 実装の実態:
  - 製品版のデータは `Assets/Level/Data/Master/`、体験版は `Assets/Level/Data/Demo/`（`PlannerCollectionItemCreator.cs:381-385`）。Asset Creation Directory に `{Variant}` を書くと、Window の Variant に応じて `Master`/`Demo` に置き換わる。
  - Addressables の Group は `GameData.Release` / `GameData.Demo` / `GameData.Shared` の 3 つ（`GameDataVariantEditorState.cs` 定数）。同じアドレス（例 `StageTreeAsset`, `MissionDefinitionRepository`, `EnemyMissionKeyRepository`）を Release と Demo の両 Group に置き、ビルドには Shared と片方だけが入る（#1508, #1510）。Shared に同じアドレスを残すと Release 側と重複してビルドが失敗する（#1508）。
  - 旧「配信版」用の `Stream_StageTreeAsset.asset` と `StageBinds/StreamingBind/` 配下（計 6 ファイル）は Addressables から外れたまま残っている。削除は判断待ち（#1508 補足）。
- 反映内容案: 「体験版用データの置き場所と Group の選び方（両版共通は Shared、版ごとに違うものは同名アドレスで Release と Demo の両方）」を追加する。Stream_* の扱いを決めて書く。
- 確認: 規約は事実。Stream_* の扱いは要企画確認

### IF-12 Addressables の運用規定（ビルド前検証の内容）
- 種別: 未定義仕様
- 優先度: 高
- 反映先: 新規: `システム概要` / 「Addressables 運用規定」（`Android向けのAddressablesとPAD関連記事集` は記事リンク集のため別扱い）
- 仕様書の現記述: 記述なし
- 実装の実態:
  - `Assets/Editor/Scripts/Addressables/AddressableKeyValidator.cs`（ビルド前 callbackOrder 0、手動は `Tools/Addressables/Validate Keys`）: ビルド対象 Group の全エントリについて、アドレス未設定・パス区切り文字（`/` `\`）を含む・拡張子を除いたファイル名に含まれない文字列・重複、をエラーにする（111-150 行）。
  - `AddressableStoreBuildValidator.cs`（callbackOrder -100、`Tools/Addressables/Validate Store Build Settings`）: Google Play と Steam にローカル同梱するため、Build Addressables on Player Build を「Build with Player」にする、Remote Catalog を無効にする、各 Group の Build/Load Path を Local、圧縮を LZ4、Bundle Mode を Pack Together にする、Google Play 向けリリースビルドは AAB にする、を検証する（76-164 行）。
- 反映内容案: アドレスの命名規則とストア同梱方式の設定値を規定として書き、違反するとビルドが止まることを明記する。
- 確認: 事実のみ (そのまま反映可)

### IF-13 ID 統一移行計画書の移植（移行済み ID とセーブデータへの影響）
- 種別: リポ文書から移植
- 優先度: 中
- 反映先: `システム概要 / SourceDataProvider` (3d67c2c6-cc02-8169-9132-fa007a0af707) の子として新規「ID 統一の経緯と対象一覧」
- 仕様書の現記述: DataID の仕組み・採番・衝突検出はある。移行対象・除外対象とセーブデータへの影響は記述なし
- 実装の実態: `Assets/Docs/ID統一移行計画書.md`（最終改訂 2026-07-29）。
  - 移行対象: Stage / EnemyMissionKey / Skill / SkillNode / ScenarioAnimation / ScenarioPortrait / ScenarioBackground / StageEffect（Infra と View で同じキーを使い対応付け）。2026-07-28 追記で MissionId も移行済み（`BattleStageAsset._missionDefinitionId` を DataID 化し、MissionDefinition の生成を InGame 側へ遅らせた）。
  - 除外と理由: MissionEvaluationId（重複チェック専用）、SkillNodeBindData.NodeName（UI 要素名）、BossAttackEntryAsset.AttackIndex（配列位置）、CharacterAnimationPlaybackMap._oneShotIndices（View の再生インデックス）、ScreenId（enum）。
  - セーブデータ: 保存値が「手書き連番」から「ハッシュ値」に変わるだけで型は int のまま。旧セーブとの互換は取らない。初期所持/装備スキル ID もハッシュへ更新が必要。
  - 計画書の用語 `[DataCategory]` は、実装では `SourceDataCollectionAttribute` になっている。
- 反映内容案: 上記の「対象・除外・セーブへの影響」を Notion へ移し、計画書本体は削除する（用語は実装名に合わせる）。
- 確認: 事実のみ (そのまま反映可)

### TL-01 マスターデータ運用ガイド（Notion 版）が旧データ構造のまま
- 種別: 変更反映
- 優先度: 最高(誤実装・誤発注につながる)
- 反映先: `システム概要 / マスターデータ運用ガイド（プランナー向け）` (39e7c2c6-cc02-809f-a6df-ee406c242e70) の ①②⑤
- 仕様書の現記述（最終更新 2026-08-24）: ①「メニュー `KillChord/StageSelect/StageNodeAsset`」「ステージの種類 Battle/Scenario」「バトルパートで遷移するステージシーン名」「敵Wave定義のAddressablesキー（Addressableアドレス文字列）」「ミッション定義アセットを割り当てる」「ステージツリーの接続情報に接続元→接続先のIDを追加」。②「敵種類 Infantry/Artillery の2種類のみ」「Wave定義アセットにAddressableアドレスを設定」
- 実装の実態:
  - ステージは `BattleStageAsset` / `ScenarioStageAsset`（`CreateAssetMenu` は `PathConst.CREATE_ASSET_MENU_PATH + "StageSelect/"`、`BattleStageAsset.cs:15-17`）。StageNodeAsset は存在しない。
  - 接続は `StageBindAsset`（From/To と Advance Mode = Manual Selection / Auto Advance）を作って StageTreeAsset の接続一覧に入れる。作戦画面のノードと線は StageTreeAsset から自動生成し、起点が複数なら警告、循環はエラー、同じ接続元から Auto Advance が複数でもエラー（`Assets/Docs/PlannerGuide/MasterDataGuide.md:42-82`）。
  - Battle ステージは Wave 定義 ID（Collection `Wave`）とミッション ID（Collection `Mission`）を DataID で選ぶ。ステージシーン名は Wave 定義側が持つ（`BattleStageAsset.cs:27-31` のツールチップ）。
  - Wave 定義は `EnemyWaveDefinitionRepository` に登録すれば ID で引ける。個々の Wave 定義を Addressable にする必要は無い（`MasterDataGuide.md:102-103`）。
  - Wave の中身は「個別の敵データ（Collection `EnemyData` の `EnemyDefinitionAsset`）× 生成数 0〜20」と候補スポーンポイント（空なら全地点）で、Infantry/Artillery の敵種類選択ではない（`EnemyWaveDefinitionAsset.cs:116-157`、`EnemyDefinitionAsset.cs:50-74`）。リポ版ガイドもこの点は古い。
  - ミッションは `MissionDefinitionRepository`（`KillChord/Mission/MissionDefinitionRepository`）への登録が要る（`MissionDefinitionRepository.cs:12-14,72`）。
  - 報酬は「初回クリア報酬」「毎回の成功報酬」（リソースと数量の組）に変わり、旧形式の改造/研究ポイントは初回クリア報酬が空のときだけ使う（`StageAssetBase.cs:77-91`）。
- 反映内容案:
  - ① を「Battle/Scenario ステージ作成 → StageBind 作成 → StageTree 登録」の手順と注意点へ書き換える（リポ版 `MasterDataGuide.md:36-87` を基にし、ミッション ID・報酬の新形式を足す）。
  - ② を「EnemyDefinitionAsset（個別の敵データ）→ Wave 定義（対象シーン・敵データ×数・スポーン候補）→ EnemyWaveDefinitionRepository へ登録」に書き換える。
  - ⑤ にミッション定義を MissionDefinitionRepository へ登録する手順を足す。
  - 早見表の「新しい敵の種類（歩兵・砲兵以外）」を、EnemyDefinitionAsset の「敵の処理種別」の範囲で言い直す。
- 確認: 構造は事実。敵の処理種別の選択肢と、プランナーが新しい敵データを作れる範囲は要企画確認

### TL-02 マスターデータ運用ガイド: Planner Master Data の操作節がリポ版にしか無い
- 種別: リポ文書から移植
- 優先度: 高
- 反映先: `システム概要 / マスターデータ運用ガイド（プランナー向け）` (39e7c2c6-cc02-809f-a6df-ee406c242e70) に新節
- 仕様書の現記述: Window の操作説明なし（`Unityセットアップ` と `社内ツール` にメニューパスだけ）
- 実装の実態: `Assets/Docs/PlannerGuide/MasterDataGuide.md:282-300`「Planner Master Dataからデータを探してInspectorで編集する」（2026-09-16 の 5f3f065f1 で更新）。最初にツールバーの Variant で Release/Demo を確認する。Source Assets / Collections / Search（アセット名・DataID・Tips 本文・BGM ラベル・アイコン種別を横断）/ Sort（表示順だけ）/ データを追加 / Source Assetを開く / Collectionから外す（アセットファイルは消さない）の各操作。Inspector をロックしていると選択が表示されない。Stage/Bind/StageTree の Inspector グラフ（緑=自動遷移、灰=ホーム帰還）、Wave の Inspector のマップと Refresh Map、Character のレーダーグラフ、複数選択時の一括編集の制限。
- 反映内容案: この節を Notion ガイドへ移す（IF-07 の Variant の意味、IF-09 のバッジも併記）。
- 確認: 事実のみ (そのまま反映可)

### TL-03 AutoBuilder: 設定・ウィンドウ・CLI が Release/Demo × Master/Develop の 4 枠になった
- 種別: 変更反映
- 優先度: 高
- 反映先: `システム概要 / AutoBuilder` (3d67c2c6-cc02-8130-9cb7-ec47e253a290)、`ワークフロー / ビルドとリリース` (3c87c2c6-cc02-81c6-9a6b-f1d5a06e4b58) のローカルビルド節
- 仕様書の現記述: 「`ProjectSettings/AutoBuilderSettings.asset`」「`MasterPath`/`MasterBuildProfiles` と `DevelopPath`/`DevelopBuildProfiles` の 2 系統」「『Master Build』『Develop Build』ボタン」「`-buildMode`（省略時は両方）と `-selectedProfiles`」「CI は `-buildMode` と `-selectedProfiles` で 1 回起動」
- 実装の実態:
  - 設定ファイルは `ProjectSettings/KillChord/AutoBuilderSettings.asset`。枠は ReleaseMaster / ReleaseDevelop / DemoMaster / DemoDevelop の 4 つで、それぞれ Path と BuildProfiles を持つ（出力先は `Builds/Release/Master/` など）。
  - `AutoBuildWindow` は枠ごとに「<Variant> <Mode> Build」ボタンを並べ、パスや Profile が不正な枠はエラー表示にする（`AutoBuildWindow.cs:33-67`, `AutoBuildSlot.cs`）。
  - CLI は `-gameDataVariant release|demo` が必須（無いか不正なら失敗: `AutoBuilder.cs:19,189-198`）。枠と Profile の種別が食い違うとビルド前に失敗する（`ValidateProfileVariants`, 302 行）。
  - `ExportProfileNamesFromCli`（`AutoBuilder.cs:38`）が対象 Profile 名を書き出し、CI は Profile ごとに別の Unity プロセスで `-selectedProfiles <Profile名>` を付けて起動する（`BuildAndRelease.yml:1155-1390`）。
  - ZIP は `<出力先>/<Profile名>.zip` に一時ファイル `.partial` 経由で作り、Windows の MAX_PATH を超えるパスも拡張長パスで読む（`AutoBuildExecuter.cs:736-820`、#1562）。EditorReady 待ちの延長タイムアウト 900 秒（同 134 行）。
- 反映内容案: 設定パス・4 枠・ボタン・CLI 引数（`-gameDataVariant` 必須）・Profile 検出と Profile ごとのプロセス分割を反映し、処理フロー図を直す。
- 確認: 事実のみ (そのまま反映可)

### TL-04 Build Profile の 16 構成と、実際に自動ビルドされる構成
- 種別: 変更反映
- 優先度: 高
- 反映先: `ワークフロー / ビルドとリリース` (3c87c2c6-cc02-81c6-9a6b-f1d5a06e4b58) の「BuildProfile」節、`システム概要 / AutoBuilder` (3d67c2c6-…)
- 仕様書の現記述: 「BuildProfileは`Assets/Settings/Build Profiles/`に置かれている。プラットフォームやシーン構成の違いはここで分かれる」のみ
- 実装の実態:
  - `Assets/Settings/Build Profiles/` に `<Variant>_<Mode>_<Platform>` 形式の 16 個（Release/Demo × Master/Dev × Android/Windows/iOS/macOS）がある（#1534）。Demo の 8 個だけが `KILLCHORD_DEMO` を持つ。全 Profile にシーン一覧を明示設定している（#1674, #1680）。
  - `AutoBuilderSettings.asset` では、ReleaseMaster と ReleaseDevelop に各 4 Profile、DemoMaster と DemoDevelop には Windows の 1 Profile だけを登録している（GUID が `Demo_Master_Windows` / `Demo_Dev_Windows` の .meta と一致。#1555「体験版のビルドをTGSに向けてPCのみに」）。
  - CI の自己ホスト Windows ランナーでは、ホスト OS で作れない macOS/iOS の Profile を除外する（`BuildAndRelease.yml:1213-1229`）。このため自動リリースの実際の成果物は、Release が Android と Windows、Demo が Windows になる。
- 反映内容案: 16 構成の命名規則とシンボルの有無を表にし、「現在の自動ビルド対象（Demo は PC のみ、TGS 対応）」「Windows ランナーでは Apple 系を作らない」を明記する。TGS 後に Demo の Android を戻すかを決める。
- 確認: 構成は事実。TGS 後の方針は要企画確認

### TL-05 BuildAndRelease の起動条件・入力・実行環境が仕様書より大幅に増えた
- 種別: 変更反映
- 優先度: 高
- 反映先: `ワークフロー / ビルドとリリース` (3c87c2c6-cc02-81c6-9a6b-f1d5a06e4b58)、`システム概要 / AutoBuilder` の「BuildAndRelease.yml との対応」節
- 仕様書の現記述: 「手動実行ではビルドモードを Development/Master から選ぶ」「最終リリースからの経過時間の閾値を超えないとスキップ」（閾値の値は記述なし）。Android/Windows/MacOS/iOS のチェックボックス
- 実装の実態（`.github/workflows/BuildAndRelease.yml`）:
  - 手動入力: build_mode（既定 Development）、build_variant（demo/release、既定 demo）、各プラットフォーム（既定すべてオン）、clean_build（既定オフ）、cpu_usage_percent（25/50/75/100%、既定 25%）（10-57 行）。
  - PR マージ起動時の種別は GitHub Environment `AutoBuildAndRelease` の変数 `BUILD_VARIANT` で決まる（現在 demo: #1528 本文）。demo/release 以外なら失敗する（357-367 行）。
  - 経過時間の閾値はリポジトリ変数 `RELEASE_THRESHOLD_HOURS`、未設定なら 72 時間。手動実行はこの判定を飛ばす（71-146 行）。
  - 実行は自己ホスト Windows ランナー（`runs-on: [self-hosted, windows]`）。ビルドは同時に 1 つだけで、後続はキューで待つ（61-64, 156-159 行）。ジョブのタイムアウト 1440 分、Profile ごとのタイムアウト 7200 秒（160, 172 行）。
  - Unity の場所はリポジトリ変数 `UNITY_EXE_PATH`、無ければ `.github/unity-paths.json` と `ProjectVersion.txt` から探す（455-560 行）。実行メソッドは変数 `UNITY_BUILD_EXECUTE_METHOD`、既定 `KillChord.Editor.AutoBuilder.AutoBuilder.RunFromCli`。
  - CPU: 自動実行は論理 CPU の半分かつ Windows では最大 4、手動実行は指定割合（最大 63 コア）を `-job-worker-count` とプロセッサアフィニティで制限する（1147-1275 行、#1691, #1693）。
  - Unity ライセンスの事前確認、AAPT2 の場所を Android SDK 配下から探して gradle.properties へ設定（562-660, 781-828 行、#1517）。
- 反映内容案: ビルドとリリースの「起動する条件」表に入力項目と既定値、変数、閾値 72 時間、実行ランナーと同時実行制御、タイムアウトを追加する。
- 確認: 事実のみ。変数の現在値は要実機確認

### TL-06 CI の容量枯渇対策と Library の扱い
- 種別: 未定義仕様
- 優先度: 中
- 反映先: `ワークフロー / ビルドとリリース` (3c87c2c6-cc02-81c6-9a6b-f1d5a06e4b58) の「ビルドが失敗したとき」節
- 仕様書の現記述: 記述なし
- 実装の実態: ビルド前に `git clean -ffdx -e Library/` と `reset --hard` で作業ツリーを戻し、Library はビルド高速化のため残す。空きが 40GB 未満、または clean_build 指定時だけ Library も消す。消しても 40GB 無ければ失敗（181-245 行、#1653）。RUNNER_TEMP の過去の残り、Gradle キャッシュ全体、Android SDK のキャッシュ（cache / build-cache）、1 日より古いランナーの `_diag` ログを消す。NuGet キャッシュは残す（246-316 行）。出力先を空にしてから再度 40GB を確認する（663-740 行）。ビルド後にもディスクを片付ける（2022 行〜、#1344）。
- 反映内容案: 「CI が毎回消すもの・残すもの」と「クリーンビルドを使う場面」を追記する。
- 確認: 事実のみ (そのまま反映可)

### TL-07 CI の外部パッケージ展開・UI 復元・NuGet 復元・終了コード取得
- 種別: 未定義仕様
- 優先度: 中
- 反映先: `ワークフロー / ビルドとリリース` (3c87c2c6-…)、`システム概要 / AutoBuilder` (3d67c2c6-…)
- 仕様書の現記述: 「Google Driveから`.unitypackage`依存関係をダウンロードして展開」「`NuGetForUnity.Cli`でNuGetパッケージを復元」のみ
- 実装の実態:
  - Drive のフォルダ（Secrets `GOOGLE_DRIVE_API_KEY` / `GOOGLE_DRIVE_FOLDER_ID`、変数 `GDRIVE_ASSET_SEARCH_QUERY`）から更新日時が最新の ZIP を 1 つ取り、中の `.unitypackage` を tar 展開して直接配置する（829-1020 行）。
  - 展開後、Git 管理の UXML・USS と .meta を HEAD から復元し、全ファイルの存在を確かめる（`.github/scripts/RestoreTrackedUiAssets.ps1`、1023-1025 行、#1786）。外部パッケージが UI を上書きして Title の UXML テンプレートが欠落した事故への対策。
  - NuGetForUnity.Cli は導入済みならそのまま使い、無ければ dotnet tool で入れる。stderr のリダイレクトを避け、再実行できるようにした（1027-1073 行、#1912）。
  - Windows PowerShell 5.1 で Start-Process のハンドルを先に取らないと終了コードが null になる問題に対処した（1087-1128 行、#1537, #1549）。run 本文は UTF-8 で読み込んでから実行する（150-154 行、#1784）。
  - Unity ログと、警告・エラー行をまとめた `workflow_issues.log` を Artifact として残す（1494 行、1995 行〜）。
- 反映内容案: 「CI が自前で補っている環境差」として上記を追記し、ローカルでは通るのに CI で落ちる場合の確認先にする。
- 確認: 事実のみ (そのまま反映可)

### TL-08 ビルド前検証の一覧（フォント文字抜け・UI テンプレート・種別・Addressables）
- 種別: 未定義仕様
- 優先度: 高
- 反映先: `システム概要 / AutoBuilder` (3d67c2c6-cc02-8130-9cb7-ec47e253a290) に新節「ビルド前検証」
- 仕様書の現記述: 記述なし
- 実装の実態（実行順）:
  1. `GameDataVariantBuildProcessor`（PrepareForBuild -1000）: IF-07 の種別・Group・必須エントリの検証
  2. `UiDocumentBuildValidator`（-300）: Title / OutGame の UXML から依存をたどって依存先から同期再インポートし、GUID 不整合・インポートエラー・必須コンテナ欠落（Title: TitleContainer/MenuContainer/CreditContainer、OutGame: Home/StageSelect/SkillTree/SkillBuild/SettingContainer）で失敗させる（`Assets/Editor/Scripts/Build/UiDocumentBuildValidator.cs:35-55`、#1786）
  3. `FontBuildValidator`（-200、手動は `Tools/Build/Validate Fonts`）: UI Toolkit / TextMesh Pro の既定フォント、Dynamic Atlas、Multi Atlas、Clear Dynamic Data On Build の無効化、必須日本語グリフ（「日本語表示テスト：設定・研究・改造・作戦・戻る・保存・出撃・効果音・攻撃力・クリティカル率」）、元 TTF までの依存、各 PanelSettings が共通 Text Settings を参照しているか、を検証する（`FontBuildValidator.cs:20-32,55`、#1454）。UI Toolkit の既定は KosugiMaru-Regular 由来の Dynamic FontAsset
  4. `AddressableStoreBuildValidator`（-100）と `AddressableKeyValidator`（0）: IF-12
  - いずれも失敗時は `BuildFailedException` でビルドを止める。
- 反映内容案: 上記を表にし、「フォントや UI を追加したら Validate Fonts を手元で実行する」「必須グリフの一覧の変更手順」を明記する。
- 確認: 事実のみ (そのまま反映可)

### TL-09 リリースの公開規則（タグ・バージョン・latest・再試行）
- 種別: 変更反映
- 優先度: 高
- 反映先: `ワークフロー / ビルドとリリース` (3c87c2c6-cc02-81c6-9a6b-f1d5a06e4b58) の「成果物の受け取り」節
- 仕様書の現記述: 「ビルドが成功するとリリースとして公開される」「Developmentはprerelease扱い」（AutoBuilder ページ）
- 実装の実態:
  - バージョンはリポジトリ変数 `VERSION_BASE_<BuildMode>`（例 `v1.0.X`、未設定なら `v1.0.X`）の X を `BUILD_VERSION_<BuildMode>` のパッチ番号で置き換える。タグは `dev-<版>`（develop）/ `release-<版>`（main・手動 Master）（1560-1710 行）。パッチ番号はビルドモード単位で、demo と release で共有する。タグに種別は入らない。
  - prerelease になるのは Development または demo。「Latest」は Master かつ release のときだけ付く（1776-1778 行、#1884）。
  - アップロードは最大 3 回（間隔 20 秒・40 秒）。その後 Release API で名前・uploaded 状態・サイズを照合し、失敗なら ZIP を Artifact `unreleased-build-<run>-<attempt>` に 7 日間退避する。公開を確認できたときだけパッチ番号を進める（1772-1947 行、#1827）。
  - 一部の Profile が失敗・タイムアウトしても、成功した分はリリースし、最後にジョブを失敗にする（1948 行〜）。本文に失敗・タイムアウトした Profile を書く。
- 反映内容案: 上記を追記する。「体験版と製品版のリリースがタグ名で区別できない」ことを明記し、タグへ種別を入れるかを決める。
- 確認: 規則は事実。タグ命名の見直しは要企画確認

### TL-10 GitHub Actions 一覧に 2 本が無い
- 種別: 変更反映
- 優先度: 中
- 反映先: `ワークフロー / ビルドとリリース` (3c87c2c6-cc02-81c6-9a6b-f1d5a06e4b58) の「その他の自動処理」表
- 仕様書の現記述: AutoCreateMasterBranch / ValidateFeaturePRTarget / Sinfonia Operator / Deploy Astro to GitHub Pages の 4 本
- 実装の実態: `.github/workflows/` には他に `AutoCreateDevelopPullRequest.yml`（RL-08）と `DeploySinfoniaOperator.yml`（TL-13）がある。
- 反映内容案: 2 本を表に追加する（契機: `feature/**/master` への PR マージ / develop への push で SinfoniaOperator のソース変更時と手動）。
- 確認: 事実のみ (そのまま反映可)

### TL-11 仕様検索 Bot: 「LLM で要約しない」と書いてあるが Gemini 要約が実装済み
- 種別: 変更反映
- 優先度: 高
- 反映先: `システム概要 / SinfoniaOperator / 仕様検索Bot` (3d47c2c6-cc02-8186-b646-c785716fedde)、`設定・環境変数リファレンス` (3d47c2c6-cc02-81e6-93d5-fdca6adc65f9)、`SinfoniaOperator` (31b7c2c6-…)
- 仕様書の現記述: 「なぜLLMで要約しないか…生成AIによる要約は行わない。これは意図的な設計判断」「外部AI APIへの課金は発生しない」
- 実装の実態: `SinfoniaOperator/SinfoniaOperator/GeminiSummarizer.cs` があり、`DiscordBotManager.cs:243-258` は要約器が設定されていれば検索結果のチャンクを Gemini で要約し、失敗時は「AI要約の生成に失敗しました。」と抜粋を返す。設定キー `GEMINI_API_KEY`（秘匿。未設定なら要約無効）と `GEMINI_MODEL`（未設定時 `gemini-3.6-flash`: `SinfoniaOperator/GeminiSummarizer実装レポート.md:12-13`）が `OperatorConfigKeys.cs:29-33` にある。PR #1502 のタイトルは「SinfoniaOperatorを仕様検索Bot化(Oracle Cloud + Gemini要約)」。
- 反映内容案: 「要約しない」節を「GEMINI_API_KEY がある場合だけチャンクを根拠に要約し、出典として抜粋とリンクも返す」に書き換え、課金の有無を直す。設定リファレンスに 2 キーを追加する。
- 確認: 事実のみ。本番 VM で有効かは要実機確認

### TL-12 SinfoniaOperator: branches コマンド・検索の重み付け・追加設定キー
- 種別: 未定義仕様
- 優先度: 中
- 反映先: `システム概要 / SinfoniaOperator / アーキテクチャ` (3d47c2c6-cc02-8122-b5c1-e06b79d59012)、`設定・環境変数リファレンス` (3d47c2c6-cc02-81e6-93d5-fdca6adc65f9)、`仕様検索Bot` (3d47c2c6-cc02-8186-b646-c785716fedde)
- 仕様書の現記述: CLI サブコマンドは（なし）/ send / index / serve の 4 つ
- 実装の実態:
  - CLI に `branches`（`--remote` `--base` `--keep` `--no-fetch` `--github`）があり、Discord の `/branches` スラッシュコマンドで削除可能なリモートブランチを最大 25 件表示する（`SinfoniaOperator.cs:37,371-396`, `DiscordBotManager.BranchCleanup.cs:11-37`、35d7a1406 / #1525）。GitHub API を参照するため、Bot のホストにクローンは要らない。
  - 検索結果にパスごとの重みを掛ける `SinfoniaOperator/spec-search-priority.json`（仕様概要/システム概要 1.5、ワークフロー等 1.2、議事録 0.7、仕様検討フォーラム 0.4 など）と `SpecPriorityTable`。
  - 設定キーに `SPEC_SEARCH_PRIORITY_PATH`, `GITHUB_TOKEN`, `GITHUB_REPOSITORY`, `BRANCH_CLEANUP_DISCORD_GUILD_ID` が増えた（`OperatorConfigKeys.cs`）。
- 反映内容案: サブコマンド表に branches を追加し、`/branches` の仕様、検索の重み付け（どのツリーを優先するか）、追加キーを書く。
- 確認: 事実のみ (そのまま反映可)

### TL-13 Oracle VM への自動デプロイと、手順書の食い違い
- 種別: 変更反映
- 優先度: 中
- 反映先: `システム概要 / SinfoniaOperator / Oracle Cloudデプロイ手順` (3d47c2c6-cc02-81df-a3fd-ebaaff186a65)、`仕様検索Bot` の「仕様書を更新したときの運用」
- 仕様書の現記述: 「開発機で self-contained publish し、scp で VM へ転送する」「手順3・4（index 再生成と serve 再起動）は手動運用」
- 実装の実態:
  - `.github/workflows/DeploySinfoniaOperator.yml`（#1538）: develop への push（SinfoniaOperator の本体・Core・SpecSearch・`deploy/deploy-release.sh` の変更時）または手動で、`linux-x64`（変数 `ORACLE_RUNTIME` で変更可）の self-contained publish を作り、SSH（Secrets `ORACLE_HOST` / `ORACLE_SSH_USER` / `ORACLE_SSH_PRIVATE_KEY` / `ORACLE_KNOWN_HOSTS`）で `/opt/sinfonia-specsearch` へ送る。`deploy-release.sh` がリリースを切り替えてサービスを再起動し、`is-active` を確かめ、失敗したら前のリリースへ戻す。古いリリースは上限数を超えた分を消す（`deploy-release.sh:95-184`）。
  - サービスは `User=sinfonia`、`EnvironmentFile=/etc/sinfonia-specsearch.env`、`ExecStart=/opt/sinfonia-specsearch/publish/SinfoniaOperator serve`、`Restart=on-failure`（10 秒）（`deploy/sinfonia-specsearch.service`）。
  - `deploy/oracle-vm-setup.md:30-38` は `Docs/NotionSpecifications` を sparse-checkout で取得するとしているが、このフォルダは `.gitignore:96` で追跡外のため取得できない。VM 上でインデックスの元になる Markdown をどう用意しているかがコードからは分からない。
- 反映内容案: デプロイ手順を「通常は自動（develop へのマージで配備、失敗時は自動で戻す）」に改め、手動手順は障害時用として残す。インデックス再生成の実運用（どこで Exporter を回すか）を確認して書く。
- 確認: 要実機確認

### TL-14 NotionMarkdownWriter / Exporter の記述が古く、ページ間でも食い違う
- 種別: 変更反映
- 優先度: 中
- 反映先: `システム概要 / SinfoniaOperator` (31b7c2c6-cc02-80ab-b731-fd020e05bf1c)、`ワークフロー / 社内ツール` (3c87c2c6-cc02-815e-b882-f8e5182ea23f)
- 仕様書の現記述: SinfoniaOperator「Writer の更新は部分置換（`update_content`）のみ。全文置換や子ページ削除は実装されていない」「Exporter は更新・新規ページのみを再取得する差分エクスポートに対応」。社内ツール「NotionMarkdownExporterは全ページの再取得を行う（1,800ページ以上）」
- 実装の実態: Writer のコマンドは pull / push / create / set-properties / rename の 5 つで、`push --whole` は本文全体を置換 1 件として送る（`SinfoniaOperator/NotionMarkdownWriter/Program.cs:37-41,80-89`）。子ページ作成時にタイトルが設定されない不具合は #1507 で修正済み。Exporter は差分エクスポートに対応している（`NotionMarkdownExporter/ExportManifest.cs`、#1308）。`SinfoniaOperator/NotionMarkdownWriter/README.md:12` も「部分置換だけ」のまま。
- 反映内容案: Writer のコマンド一覧（`--whole`・set-properties・rename）を反映し、社内ツールの「全ページ再取得」を「初回は全件、2 回目以降は差分」に直す。
- 確認: 事実のみ (そのまま反映可)

### TL-15 Unity Loop MCP: ツール一覧が半分しか載っていない
- 種別: 変更反映
- 優先度: 低
- 反映先: `システム概要 / Unity Loop MCP (uloop-*)` (3d67c2c6-cc02-81af-adb7-de390511bffe)
- 仕様書の現記述: launch / compile / execute-dynamic-code / find-game-objects / get-hierarchy / get-logs / clear-console / screenshot と、V3 で削除された record-input。「場所」欄は空
- 実装の実態: `.claude/skills/` の uloop スキルは 18 個。Notion に無いのは control-play-mode, hot-reload, pause-point, record-video, replay-input, run-tests, set-game-view-size, simulate-keyboard, simulate-mouse-input, simulate-mouse-ui。Codex 用に同じ内容が `.agents/skills/uloop-*` にある。パッケージは `Packages/manifest.json:50` の `io.github.hatayama.uloopmcp`（unity-cli-loop の git）。V2→V3 の構文移行は #1505。
- 反映内容案: 残り 10 ツールの用途を 1 行ずつ追加し、「場所」欄にパッケージとスキルの置き場所（`.claude/skills` と `.agents/skills`）を書く。
- 確認: 事実のみ (そのまま反映可)

### TL-16 AI デバッグ QA ツール（JSON 状態取得・戦闘記録・拍指定攻撃）
- 種別: 未定義仕様
- 優先度: 中
- 反映先: 新規: `システム概要` / 「AI デバッグ QA ツール」
- 仕様書の現記述: 記述なし
- 実装の実態: Editor 専用アセンブリ `Assets/Editor/AIDebugPlay/`（KillChord.Editor.AIDebugPlay: AIDebugQaApi / AIDebugQaMonitor / AIDebugCombatRecorder / AIDebugCombatSnapshot / AIDebugSaveSnapshot / AIDebugAttackQueue / AIDebugUi / AIDebugJson）と、共有 JS（`.agents/skills/ai-debug-attack-queue/scripts/ai-debug-qa.mjs` / `ai-debug-attacks.mjs` / `qa-aging.mjs` / `qa-options.mjs` / `qa-transport.mjs`）。スキル `ai-debug-qa`（`.claude`・`.codex`）は「QA シートに沿って JSON 状態取得、戦闘イベント記録、有限時間の観測、拍指定攻撃を行う」「実行成功と QA 合格を区別し、実機確認を Editor の結果だけで合格にしない」と定める。PR #1585（LLM QA 向け JSON 状態取得とエージングツール）。QA 項目との対応表は `Docs/QA/AI_QAツール対応表.md` にある（冒頭に作業状態のログが混ざっている）。
- 反映内容案: ツールの目的・構成・API・QA シート項目との対応・判定上の注意を Notion にまとめ、対応表の本体（作業ログ部分を除く）を移す。
- 確認: 事実のみ。API の詳細は `.agents/skills/ai-debug-attack-queue/references/json-api.md` を要確認

### TL-17 社内ツール一覧に無いツール・メニュー
- 種別: 変更反映
- 優先度: 中
- 反映先: `ワークフロー / 社内ツール` (3c87c2c6-cc02-815e-b882-f8e5182ea23f)
- 仕様書の現記述: PlannerMasterDataWindow / KillChordDebugger / TicketSystem / SinfoniaOperator / AutoBuilder / Import CSV / DriveImportSync / Addressables 検証 / Source Data Provider の 9 件
- 実装の実態（`MenuItem` 全検索と Editor フォルダ）:
  - `Tools/Build/Validate Fonts`（TL-08）
  - `KillChord/Localization/チュートリアル用テーブルをセットアップ`、`KillChord/Localization/UI共通テキスト用テーブルをセットアップ`（`Assets/Editor/Scripts/Localization/`）
  - メインツールバーの Build Profile 切替（IF-07）
  - Inspector のプレビュー群（IF-08）と Project ウィンドウのバッジ（IF-09）
  - `RhythmJudgmentDefinitionAssetEditor`（`Assets/Editor/Scripts/Music/`）、`SkillEffectParameterGrowthStepSettingDrawer`（`Assets/Editor/Scripts/Skill/`）
  - DriveImportSync は Service Account の JSON 鍵（`DriveImportSecrets`）で認証し、配置先を `Assets/` 配下に限る（`Assets/Editor/Scripts/AssetManagement/DriveImportSync.cs:238-256`）。設定は `ProjectSettings/KillChord/DriveImportSettings`。Drive への `.unitypackage` アップロード手順 `AssetStorePackagePipeline/DriveUploadPackageStrategy.cs` もある。
  - AI デバッグ QA（TL-16）
- 反映内容案: 表に上記を追加し、DriveImportSync の認証方法と設定場所を書く。
- 確認: 事実のみ。各 Inspector 拡張の担当は要確認

### TL-18 シーンのチケットシステム: メニューの場所が違う
- 種別: 変更反映
- 優先度: 低
- 反映先: `システム概要 / シーンのチケットシステム` (3587c2c6-cc02-803e-ad97-c1df9f4985b9)
- 仕様書の現記述: 「Window > Master Ticket Window から開く」「Edit > Project Settings > KillChord > Ticket System > Editor / Management」
- 実装の実態: ウィンドウは `Window/KillChord/TicketSystem`（`EditorWindowPathConst.cs` の `TICKET_SYSTEM_PATH`、`MasterTicketWindow.cs:18`）。設定は Project Settings の `KillChord/TicketSystem/` 配下（`TicketSystemConst.cs`）。
- 反映内容案: メニューパスを直す。
- 確認: 事実のみ。設定画面の下位名（Editor / Management）は要実機確認

### TL-19 SinfoniaAssetManager: 計画した機能名で実装されていない
- 種別: 仕様のみ
- 優先度: 低
- 反映先: `システム概要 / SinfoniaAssetManager` (32d7c2c6-cc02-8026-8e09-f37a558a73bd)
- 仕様書の現記述: 「1. Drive からデザイナーのアセットを AssetStoreTools へインポート 2. AssetStoreTools のパッケージを Drive 経由で同期 3. Drive の unitypackage で自動ビルド」（最終更新 2026-04-05）
- 実装の実態: `SinfoniaAssetManager` という型は無い（`git grep` 0 件）。同等の機能は DriveImportSync（Drive→Assets 同期）、`DriveUploadPackageStrategy`（出力したパッケージを Drive へアップロード）、CI の Drive ZIP 取得（TL-07）に分かれて実装されている。
- 反映内容案: 冒頭に「上記 3 機能は DriveImportSync / DriveUploadPackageStrategy / BuildAndRelease で実装済み。本ページは設計時の資料」と書き、各ページへリンクする。
- 確認: 要企画確認

### TL-20 ホームページ仕様: 構成・データ・公開 URL が書かれていない
- 種別: 未定義仕様
- 優先度: 中
- 反映先: `システム概要 / ホームページ仕様` (3a47c2c6-cc02-8078-ac23-dba265af3b93)
- 仕様書の現記述: Astro 製で `HomePage/` 配下、main への push で GitHub Pages へデプロイ。キャラクター紹介（NieR:Automata 参考）と参考ページの構想のみ
- 実装の実態:
  - 公開 URL `https://hibiki5201.github.io/SymphonyKillChord/`（`HomePage/astro.config.mjs:4-11`、ルートは `home` へリダイレクト。リポの `README.md` もこの URL だけ）。
  - ページ: home / character / gameplay / skill / news / credit / policy（`HomePage/src/pages/`）。表示データは `src/data/characters.json`・`credits.json`・`gameplayFeatures.json`・`skills.json`・`news.ts`（お知らせは追加すると新しい順にホームと一覧へ出る。現在 0 件）。Steam / Google Play へのリンク部品（`PlatformLinks.astro`）がある。
  - 開発: Node.js 22.12 以上、Windows は `dev.bat` でバックグラウンド起動（`HomePage/README.md:3-7`）。README の残りは Astro テンプレートのまま。
  - #1905（news/SNS）など `feature/release/home-page/*` の PR が多数ある。
- 反映内容案: ページ構成・各ページのデータの置き場所と更新方法（お知らせの追加手順）・公開 URL・ストアリンクの有無を追記する。参考ページの構想が未実装なら「未実装」と書く。
- 確認: 構成は事実。構想の扱いは要企画確認

### TL-21 公開 Notion ページに API キー類がそのまま載っている
- 種別: 構造
- 優先度: 高
- 反映先: `システム概要 / シーンのチケットシステム` (3587c2c6-cc02-803e-ad97-c1df9f4985b9)、`システム概要 / シナリオCSV～ドライブ同期～` (3b57c2c6-cc02-803d-96c9-de406ce46dec)
- 仕様書の現記述: チケットシステムに GAS の URL と apiKey、シナリオ CSV 同期に Google API キーを本文で掲載している（本レポートでは値を転記しない）
- 実装の実態: 今回のスナップショットは公開ページ全体を取得したもの（共通ブリーフ）。SinfoniaOperator 側は秘匿情報をリポ・ログへ出さない運用を定めている（`設定・環境変数リファレンス` の「秘匿情報の扱い」）。
- 反映内容案: キーを本文から外し、配布方法（Discord の限定チャンネル、プロジェクト設定の秘匿ファイル等）だけを書く。掲載済みのキーは無効化・再発行を検討する。
- 確認: 要企画確認

### TL-22 QA 資料（CBT / PC 版 QA シート・フォーム生成スクリプト）の移植
- 種別: リポ文書から移植
- 優先度: 中
- 反映先: 新規: `ワークフロー` / 「QA・バグ報告」（RL-16）
- 仕様書の現記述: 記述なし（QA シート・アンケートは Notion に無い。「感情曲線」は議事録 2 件で言及のみ）
- 実装の実態: `Docs/QA/` に CBT_QAシート_2026-09-06.md、CBT_QAシート_ドメイン解析版、CBT_QA回答方式、CBT_感情曲線アンケート、PC版_QAシート_2026-09-07.md、AI_QAツール対応表.md と、Google フォームを自動生成する Apps Script（`Docs/QA/AppsScript/*.gs`: Android 用 5 フォーム `createQaForms`、PC 用 4 フォーム。`FormDefinitions.gs:6` と `PcFormDefinitions.gs:7` が QA シート md を原本としている）。
- 反映内容案: QA シートとアンケートの本文を Notion へ移す。Apps Script はコードなので削除対象から外すか、別の置き場所を決め、原本参照先を Notion へ直す。
- 確認: 要企画確認

### TL-23 Runtime 実装監査（2026-07-27）の扱い
- 種別: リポ文書から移植
- 優先度: 低
- 反映先: 新規: `システム概要` / 「Runtime 実装監査 2026-07（アーカイブ）」、または Issue
- 仕様書の現記述: 記述なし
- 実装の実態: `Docs/RuntimeAudit/00〜18`（842 ファイル・約 6 万行を対象、確定/要検証/設計指摘の 3 段階で `file:line` 付き）。観点はイベント購読解除漏れ、Null 参照、ライフサイクル、GC Alloc、レイヤー違反、DRY/KISS/SOLID/GRASP など。調査時のブランチは `for-stream`。
- 反映内容案: サマリ（00）と優先度表だけを Notion のアーカイブへ移し、未対応の確定指摘は Issue 化する。個別ファイルの行番号は古くなっている前提で扱う。
- 確認: 要企画確認

### TL-24 Notion 同期スクリプトと Codex 実行スクリプトの説明が無い
- 種別: 未定義仕様
- 優先度: 低
- 反映先: 新規: 「AI エージェント向け参照先一覧」(RL-01) の配下
- 仕様書の現記述: 記述なし
- 実装の実態: `scripts/notion/sync_module.py`（pull → 処理フローの切り出し → 子ページ作成・更新 → 本文 `push --whole`。子ページはタイトルで突き合わせるため、見出しを変えると別ページができる: `module-docs.md:28-40`）、`split_module_doc.py`、`apply_writing_rules.py`、`scripts/codex_runner.py`（codex-implement スキルが使う）。
- 反映内容案: RL-02 の運用変更に合わせ、残すスクリプトの用途と使い方を書く。
- 確認: 事実のみ。RL-02 の結論次第で要否が変わる

## 未調査・調査しきれなかった範囲
- Unity エディタ・CI の実行による確認はしていない（読み取り専用の静的調査のみ）。IF-03 の表示、IF-09 の色、TL-18 の設定画面名、各 GitHub 変数・Environment・ruleset の現在値（`RELEASE_THRESHOLD_HOURS`, `BUILD_VARIANT`, `VERSION_BASE_*`, develop 保護）は未確認。
- `BuildAndRelease.yml`（2109 行）はステップ単位の要点のみ確認した。「ビルド成果物のチェック」（1503 行〜）と「ZIP へのタグ付与」（1711 行〜）の細かい条件は読み切っていない。
- NotionModuleDocs 36 本のうち、本領域の Bootstrap / SceneManagement / EventBus / SourceDataProvider 以外と Notion との全文差分は取っていない（他領域の担当）。
- `Assets/Docs/` の計画書のうち、カメラ・チュートリアル・アニメーション・リズム UI・Enemy-Stage 結合の各計画書は他領域として中身を照合していない（ID 統一移行計画書・PlannerMasterDataEditorWindowDesignPlan・StageAssetSeparationAndBindingPlan は要点のみ）。PlannerMasterDataEditorWindowDesignPlan-2026-07-16.md の未実装の構想と現行 Window の差は照合していない。
- `Docs/` 直下のうち、SymphonyKillChord_PlannerDocs.html、用語集_追加候補.md、アセット名リスト_命名規則提案.md、チュートリアルフロー詳細_ボイス収録用、逆算スケジュール案は他領域（企画・アセット・管理）として扱っていない。`spec/`、`Docs/G-Lab/` の中身は照合していない（扱い方針のみ RL-17）。
- GCスパイク対策・TBDR・Adaptive Performance・UIToolkit概要・SpringBone・リアルトゥーンシェーダー・Behavior Graph など技術調査ページの内容と実装の照合はしていない（構造指摘 RL-14 のみ）。
- SinfoniaOperator の日次通知（NotionTaskListReader 等）と Unity 側の SinfoniaOperatorWindow（作業ログ送信）の挙動は Notion の記述と照合していない。DiscordLogExporter も未照合。
- KillChordDebugger・TicketSystem の機能詳細、HomePage 各ページの文言、`third-party-notices.md` 以外のライセンス表記（フォント、Asset Store 素材）は照合していない。
- `.agents/skills/ai-debug-attack-queue/references/json-api.md` と AIDebugPlay の API 仕様の詳細は読んでいない。
