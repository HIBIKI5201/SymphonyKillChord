# 判定調整機能

- page_id: 3b27c2c6-cc02-8024-8e69-e5a5b1f69532
- Notion パス: Symphony Kill Chord / **仕様概要** / コンフィグ / 判定調整機能
- スナップショットの最終更新: 2026-08-04T15:24:36.632Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 陳腐化 — 実装（PR #1675、Issue #1662、2026-09-16 hotfix）は ±0.30 秒・0.05 秒刻み・ホームの設定画面だけ（`Assets/Scripts/Runtime/1.Domain/Persistent/Savedata/EnvironmentSettingsData.cs:69-85`）。オフセットは判定用の時刻にも足される（`Assets/Scripts/Runtime/3.Adaptor/InGame/Music/MusicSyncController.cs:37-38`）ため「表示専用」ではない。メトロノームプレビュー・ポーズメニューでの調整は無い。最終確認 2026-09-22 / 73fefeb45
- 整合性:
  - 本ページの「技術的前提（判定は間隔で見るので遅延の影響を受けない）」は正しいが、「対象外: MusicSyncServiceの拍子判定」は実装と矛盾する
  - セーブデータ (3457c2c6-cc02-801d-91fc-f042482d733b) の「オプションの内容」に保存するとあるが、実装は環境設定（`EnvironmentSettingsData`）に保存する（他フォーク担当ページ）
- 変更点:
  - 決定 R59（2026-09-22 八幡）: 親ページのコンフィグを設定画面 3437c2c6-cc02-807b-a886-c75657af8359 に統合するため、このページは設定画面の子へ移す（Notion パスが「仕様概要 / 設定画面 / 判定調整機能」に変わる）。本文は変えない
  - 仕様概要・仕様意図: 変更なし（既存のまま）
  - 技術的前提: 旧: 「表示専用の補正である」 → 新: 現状の実装は判定用の時刻と表示の両方を同じだけずらすこと、自分の入力の判定結果はほぼ変わらず、影響するのは曲頭オフセットとの比較・タイムアウトの周期・予約した処理（敵の攻撃予約・ステージ演出）の発火タイミングであること【要確認】
  - 設定項目: 旧: 「表示オフセット：-300ms〜+300ms（10ms刻み）」 → 新: 「リズム判定タイミング：-0.30〜+0.30秒（0.05秒刻み、13段階）、既定0秒」と表示形式
  - 対象となる表示・対象外: 実装に合わせ、表示と判定の両方が対象である事実を書いた。敵の実ダメージ判定の扱いは【要確認】
  - プレビュー機能・UI配置: 未実装であることを明記した（ホームの設定画面だけ）
  - データ永続化: 旧: 「セーブデータの『オプションの内容』に追加」 → 新: 環境設定に保存、「設定を保存」を押したときだけ保存
  - 残タスク: 実装済み・未実装に合わせて更新した
  - 仕様意図: 節をページの末尾へ移し、経緯と却下・保留した案を追記した（決定 2026-09-22 八幡、W-18）
- 決定（2026-09-22 八幡）: No.11 リズム判定オフセットは判定と表示の両方をずらす。刻み幅・範囲は実装値（±0.30 秒・0.05 秒刻み）。「技術的前提」「設定項目」「対象となる表示」「対象外」節と「残タスク」の表示専用に関する記述を直し、判定にも効く挙動を正とするかと 10ms 刻みの【要確認】を外した。自分の入力に効きにくい可能性は【要実機確認】に残した（根拠 `Assets/Scripts/Runtime/3.Adaptor/InGame/Music/MusicSyncController.cs:35-41`）
- 決定（2026-09-22 八幡）: No.34 メトロノームプレビューとポーズメニューでの調整は仕様に残し、未実装と明記したまま（「プレビュー機能」「UI配置」節）。実装するかの【要確認】を外した
- 織り込んだ反映項目: BT-08, OG-201, OG-203（判定調整機能の部分）。BT-14 の振動設定は、判定のタイミングとは別の項目のため親ページ「コンフィグ」(3337c2c6-cc02-8090-b06c-d9617104076d) の原稿に書いた
- Discord 照合: 該当する検討なし（「オフセット」「遅延」「判定タイミング」などで探した）。【要確認】はそのまま。2026-09-22 の決定との食い違いは無い
- 出典:
  - 設定値: `EnvironmentSettingsData.cs:69-85,214-228`、`EnvironmentSettingsPresenter.cs:59,119`、`UIT/Setting.uxml`（`RhythmOffsetSlider`）
  - 判定への注入: `MusicSyncController.cs:31-41`、`MusicSyncService.cs:78-83`
  - 保存のタイミング: `EnvironmentSettingsController.cs:186-215`（30_アウトゲーム OG-202）
  - PR #1675（Issue #1662）、フォーカス枠の修正 PR #1718
  - 仕様意図は既存本文のまま（議事録・フォーラムに本機能の記録は見つからなかった）
- 要確認:
  - 音声の遅延による体感のズレが実際に補正できているか（判定が前回入力との間隔で行われるため自分の入力には効きにくい可能性）、符号の向き（＋で早まるか遅れるか）（実機確認、BT-08）
  - 敵の実ダメージ判定のタイミングがオフセットでずれるか（実機確認）

## 適用する本文

## 仕様概要
音声出力デバイス（Bluetoothイヤホン等）の遅延により、プレイヤーが聴くBGMと見る視覚ガイド（リズムUI、敵攻撃予兆表示）の体感タイミングがズレる問題を、プレイヤー自身が設定値で補正できるようにする機能。

## 技術的前提（判定ロジックへの影響について）
現行の[[リズム判定]]は「前回入力と今回入力の**間隔**」を見て拍子を判定する方式であり、太鼓の達人やバンドリのような「譜面上の絶対時刻に対するズレ」を見る方式とは異なる。
このため、入力・音声デバイスに一定の遅延が乗っても、前回・今回の両方に同じだけ遅延が乗るなら判定結果（間隔）は変化しない。
設定値は曲の再生時刻に足してから、表示と判定の両方に渡す。判定と表示の両方をずらす補正であり、表示だけの補正ではない。
- 自分の入力の拍子・Just判定は間隔で決まるため、設定値を変えても結果はほぼ変わらない。
- 設定値の影響を受けるのは、曲頭オフセットとの比較、2小節周期のリズムタイムアウト、音楽に合わせて予約した処理（敵の攻撃の予約・ステージ演出）が発火するタイミングと考えられる。

【要実機確認: 判定が前回入力との間隔で行われるため、自分の入力には効きにくい可能性がある。音声遅延の補正として機能しているか、符号の向き（＋で早まるか遅れるか）を実機で】

## 仕様詳細

### 設定項目
- リズム判定タイミング：-0.30秒〜+0.30秒（0.05秒刻み、13段階）、デフォルト0秒
  - 範囲外の値は範囲内に収め、0.05秒刻みに丸める。不正な値は0秒に戻す。
  - 表示は「+0.15秒」の形式である。
- 旧仕様は -300ms〜+300ms（10ms刻み）だった。

### 対象となる表示
- リズムUIの表示（インジケーター等）
- [[リズム判定]] の判定用の時刻も同じだけずらす（上記「技術的前提」参照）。

### 対象外（変更しない処理）
- 旧仕様では「MusicSyncServiceの拍子判定」と「敵の実ダメージ判定タイミング」を対象外としていた。
- 拍子判定も対象とする。敵の実ダメージ判定のタイミングへの影響は未確認である。【要確認: 実機で】

### プレビュー機能
設定画面に専用メトロノームプレビューを新規実装する。
- 一定BPMのクリック音を再生
- リズムUIの縮小版インジケーターを同時に表示
- 試聴しながらオフセット値をリアルタイム調整可能

未実装である（体験版の時点）。

### UI配置
アウトゲームの設定画面での初期調整＋インゲームのポーズメニューでの微調整の二段構成
- 現状はホームの設定画面（環境設定）の「リズム判定タイミング」スライダーだけである。タイトルのメニューとインゲームのポーズメニューには無い（ポーズメニューでの微調整は未実装である）。

### データ永続化
環境設定（画面・言語・振動などと同じ保存先）に保存する。
- 「設定を保存」を押したときだけ保存し、保存せずに設定を閉じた場合は破棄する。

## 残タスク
- メトロノームプレビュー用のBPM・クリック音アセットの選定（サウンド班）
- メトロノームプレビューとポーズメニューでの調整の実装（未着手）

## 仕様意図

リズムUIの既存の仕様意図（「聴覚だけでなく視覚的な情報からリズムの判定タイミングを確認・予測できるようにする」）を、デバイス間の音声遅延差にも対応できるよう拡張する。
音声出力デバイス（Bluetooth イヤホンなど）の遅延で、聴く BGM と見る視覚ガイドの体感タイミングがずれる問題を、プレイヤー自身が設定値で補正できるようにする（本ページの仕様概要）。

### 経緯

日付は、議事録は開催日、PR はマージ日、フォーラムは作成日の記録が無いため最終更新日を書いた。

| 日付 | 種別 | 内容 | 判断・理由 | 出典 |
| --- | --- | --- | --- | --- |
| 2026-03-31 | 決定 | 時間のかかる重要な設定はアウトゲームにまとめ、短時間で変えられる設定（操作感・音量）はインゲームでも変えられるようにする | インゲームで設定したときにすぐ反映されないものがあると混乱を招くため。インゲームでの設定時間を短くするため | フォーラム「コンフィグについて」(3137c2c6-cc02-8092-a2fe-ca60eabec568、適用済) |
| 2026-08-04 | 決定 | 本ページを作った: 表示オフセット（-300〜+300ms、10ms 刻み）、表示専用の補正、メトロノームのプレビュー、設定画面での初期調整とポーズメニューでの微調整の二段構成 | 判定は前回入力との間隔で見るので遅延の影響を受けない、という技術的前提から表示専用とした | Notion スナップショット（本ページの最終更新 2026-08-04） |
| 2026-08-08 | 検討 | UI の一覧に「判定調整用UI」を入れた | UI 発注の洗い出し | 議事録「プランナー定例会 8/8」(3b67c2c6-cc02-8071-9db9-f1e32cfea879) |
| 2026-09-16 | 変更 | ホームの設定画面に「リズム判定タイミング」を追加した（±0.30 秒、0.05 秒刻み、13 段階）。値は環境設定に保存する | インゲームのリズム判定が体感でずれるプレイヤー向けの新規要望。開発用の固定値以外に調整手段が無かったため（Issue #1662） | PR #1675 |
| 2026-09-22 | 決定 | リズム判定オフセットは、判定と表示の両方をずらす。刻み幅・範囲は実装値（±0.30 秒・0.05 秒刻み） | 理由の記録なし（決定の注記に「実装は再生時刻そのものにオフセットを足して表示と判定の両方に渡しているので、構造上は一致する」とある） | [議事録 2026-09-22 仕様整理](https://app.notion.com/p/3e37c2c6cc0281079e4bec7dd81be5de) 決定（八幡）No.11 |
| 2026-09-22 | 決定 | メトロノームのプレビューとポーズメニューでの調整は、仕様に残して「未実装」と明記する | 未実装の仕様は消さずに残す方針 | [議事録 2026-09-22 仕様整理](https://app.notion.com/p/3e37c2c6cc0281079e4bec7dd81be5de) 決定（八幡）No.34 |
| 2026-09-22 | 決定 | セーブをリセットしても、言語と判定オフセットは残す | 理由の記録なし | [議事録 2026-09-22 仕様整理](https://app.notion.com/p/3e37c2c6cc0281079e4bec7dd81be5de) 決定（八幡）No.25 |

Discord での検討: 照合済み（該当する検討なし）。音声の遅延の補正、判定のオフセット、判定調整の UI について、Discord のチャンネル・進捗共有に検討の記録は見つからなかった

### 却下・保留した案

- 表示専用の補正（判定は変えない）: 2026-09-22 に撤回し、判定と表示の両方をずらすことにした（決定 No.11）
- -300〜+300ms・10ms 刻み: 2026-09-22 に実装値（±0.30 秒・0.05 秒刻み）へ改めた（決定 No.11）
- メトロノームのプレビュー、ポーズメニューでの微調整: 未実装。仕様に残す（決定 No.34）
- セーブデータの「オプションの内容」に保存する: 実装は環境設定に保存している
- 補正が実際に効いているか（判定が間隔で行われるため自分の入力には効きにくい可能性）、符号の向き、敵の実ダメージのタイミングがずれるか: 【要実機確認】のまま保留（同上）
