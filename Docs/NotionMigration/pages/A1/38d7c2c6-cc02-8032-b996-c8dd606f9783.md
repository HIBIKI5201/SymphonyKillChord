# Just判定について

- page_id: 38d7c2c6-cc02-8032-b996-c8dd606f9783
- Notion パス: Symphony Kill Chord / **仕様概要** / リズム判定 / Just判定について
- スナップショットの最終更新: 2026-07-28T11:13:43.705Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 陳腐化（判定ウィンドウなど主要な数値が実装と異なる） — Just ダメージ倍率はデータの誤り（`_justDamageMultiplier` を 2 に直す必要あり。`Assets/Level/Data/Master/InGame/Battle/PlayerAttack_Beat*.asset:18`、6 武器とも該当）。判定ウィンドウは小節比・非対称・約 89ms 幅（`Assets/Level/Data/Master/InGame/Music/RhythmJudgmentDefinitionAsset.asset:16-45`、PR #1513 / #1692）、スローモーション無し。最終確認 2026-09-22 / 73fefeb45
- 整合性:
  - 計算式が ダメージ計算 (3767c2c6-cc02-80f5-b2e7-f2de9f55413f) と食い違う（本ページは防御を掛け算、ダメージ計算は割り算）。用語「ダメージ計算式」も掛け算のまま（他領域）
  - 用語「Just判定／Justダメージ倍率」(3a37c2c6-cc02-80b6-9990-eb1b9ec9de0e) の 200% は本ページと一致する（誤りはデータ側）
  - 決定（2026-09-22 八幡）: Just ダメージ倍率は仕様（200%）が正しく、全 6 武器の `_justDamageMultiplier: 1` はデータの誤り（`_justDamageMultiplier` を 2 に直す必要あり）。判定幅（±0.1 秒と小節比、TGS での 1.25 倍）はこの決定の対象外
  - 実装: Just の判定は「直前の入力＋拍子の理論間隔」基準であり、曲の拍基準ではない
- 変更点:
  - 関連ページ・関連用語: リンクを足し、空の関連用語を埋めた
  - 仕様意図: 既存の文は残し、固定値方式・±0.1 秒の意図の各項目に、現状の実装が異なることを【要確認】で添えた。200% の意図は既存のまま（決定 2026-09-22 により【要確認】を外した）
  - 成立条件: 旧: 「固定値方式」「±0.1秒以内」「対称」 → 新: 小節比方式（BPM 連動）、拍子ごとの範囲の表、非対称であること、範囲の重なり。「理論上のタイミング」を「直前の入力＋拍子の理論間隔」と定義し直した
  - ダメージへの反映: 旧: 計算式の引用と「倍率は200%」 → 新: 式は ダメージ計算 へのリンクにし、「倍率は200%」は残して設定場所を書き足した
  - キルコード（スキル）発動条件・ノーツ種別への影響: 既存の「Justダメージ倍率（200%）」を残した
  - 演出（フィードバック）: 旧: ヒットマーカー色変化・スローモーション → 新: 実装済みの演出（ゲージのフラッシュと伸び、Vignette、ゲームパッド振動）を書き、スローモーションとヒットマーカーの色変化は未実装と明記した
  - 背景（新しいトグル、末尾）: 判定経路の統一（PR #1513）と TGS での 1.25 倍緩和（PR #1692）の経緯、実装上の要件、回帰確認項目を移した
- 織り込んだ反映項目: BT-01, BT-02, BT-03, BT-10, BT-13, BT-14（演出節）, BT-27
- 出典:
  - 倍率: `PlayerAttack_Beat*.asset:18`、`Assets/Scripts/Runtime/2.Application/InGame/Battle/WeaponDamageStep.cs:28-31`
  - 範囲: `RhythmJudgmentDefinitionAsset.asset`、`MusicSyncService.cs:114-133`、PR #1513、#1692（Issue #1664）
  - 演出: `Assets/Level/Data/Master/InGame/Music/ACLikeRhythmGuideEffectConfig.asset`、`Assets/Level/Data/Master/InGame/Haptics/GamepadHapticsConfig.asset`、`TutorialAttackFeedbackView.cs:20-22`、PR #1441 / #1573 / #1872
  - 背景: `spec/plan/problem_logs/2026-09-09-just-judgement.md`
  - 仕様意図は既存本文のまま（議事録・フォーラムに Just の数値の意図を記した記録は見つからなかった）
- 要確認:
  - 判定ウィンドウの小節比方式・約 89ms 幅（TGS で 1.25 倍に緩和）を製品版でも追認するか、±0.1 秒・対称に戻すか（企画、BT-02、D-07）
  - 6・3 拍子の中心が早い側にずれていること、8 拍と 6 拍の範囲の重なりが意図どおりか（企画）
  - スローモーション・ヒットマーカーの色変化を実装するか（企画、BT-13）
  - Discord 未照合（1.25 倍緩和の判断者と理由）

## 適用する本文

# **仕様概要** {color="gray_bg"}
より正確なタイミングで攻撃アクションを行った際の判定について。

## **関連ページ**
[[リズム判定]]  [[ダメージ計算]]  [[リズムUI]]  [[判定調整機能]]

## **関連用語**
[[Just判定／Justダメージ倍率]]  [[拍子（ノード／ノーツ）]]  [[クリティカル判定／クリティカル率／クリティカルダメージ]]

# **仕様意図** {color="gray_bg"}
リズムゲームとしての精度を追求したいプレイヤーに対して、
大まかなリズムに乗るだけでもプレイ可能な間口の広さを保ちつつ、
より正確なタイミングを取れる上級者には明確な追加リターン（ダメージ倍率）を
与えることで、コアメカニクスをやり込む動機付けを行う。
※このゲームは初心者でもクリアできる設計方針（[[レベルデザイン方針]] 参照）だが、
　Just判定はその上に積む上級者向けの精度報酬として位置づける。
各数値・仕様判断の意図は以下の通り。
- **判定ウィンドウを固定値方式にした意図**
  Just判定自体は上級者向けの高難易度要素として位置づけたいため、
どの拍子でも同じ絶対的なシビアさを保てる固定値方式（拍子によらず常に同じ秒数）
を採用する。
  - 【要確認: 現状の実装は小節比方式（BPM連動）である。固定値方式に戻すか、小節比方式を追認して意図を改めるかを企画に】
- **判定ウィンドウを±0.1秒にした意図**
  リズムアクションとしてはシビアな部類の設定であり、
Just成立の希少性・上級者向け報酬としての位置づけを重視した数値。
  プレイテストの結果次第で調整の余地は残す。
  - 【要確認: 現状の実装は幅約89ms（BPM84）で、遅れ側に偏っている。値を追認するかを企画に】
- **Justダメージ倍率を200%にした意図**
  ±0.1秒というシビアな判定をプレイヤーに視認できる目標で与え、
そのシビアさにあった報酬を強化することでプレイヤーの攻撃の操作に対し
簡潔かつ強力な達成感やくやしさを押し付けることができると考えたため。
- **演出のチャンネルを分けた意図**
  クリティカル判定とJust判定は独立した別軸の判定であり、同一攻撃で両方が成立するケースもある。
  専用の合体演出を別途用意するコストをかけずに両方の成立をプレイヤーへ伝えるため、判定の種類ごとにフィードバックのチャンネル（SE／エフェクト）を分けて割り当て、それぞれが同時に再生されることで自然に重畳表現させる。

# **詳細説明** {color="gray_bg"}

## **成立条件**
[[リズム判定]] における通常の拍子判定に加えて、
そのタイミングが理論値にどれだけ近かったかを追加で評価する。
理論値は「直前の入力の時刻＋判定した拍子の理論上の間隔」である。曲の拍の格子は基準にしない。

### 判定方式は小節比方式（BPM連動）である。
- 直前の入力からの経過時間を1小節の長さで割った値（小節比）が、拍子ごとのJust範囲に入っていればJust判定成立とする。
- 範囲は開始を含み、終了を含まない。秒数はBPMに比例して変わる。
- 範囲の正本は `Assets/Level/Data/Master/InGame/Music/RhythmJudgmentDefinitionAsset.asset` の `JustStartNormalized` / `JustEndNormalized` である。

| 拍子 | 理論上の間隔（BPM84、秒） | Just範囲（小節比） | Just範囲（BPM84、秒） | 理論値からのズレ |
| --- | --- | --- | --- | --- |
| 8拍子 | 0.357 | 0.121875〜0.153125 | 0.348〜0.438 | -8.9ms〜+80.4ms |
| 6拍子 | 0.476 | 0.146875〜0.178125 | 0.420〜0.509 | -56.5ms〜+32.7ms |
| 4拍子 | 0.714 | 0.246875〜0.278125 | 0.705〜0.795 | -8.9ms〜+80.4ms |
| 3拍子 | 0.952 | 0.321875〜0.353125 | 0.920〜1.009 | -32.7ms〜+56.5ms |
| 2拍子 | 1.429 | 0.496875〜0.528125 | 1.420〜1.509 | -8.9ms〜+80.4ms |
| 1拍子 | 2.857 | 0.996875〜1.028125 | 2.848〜2.938 | -8.9ms〜+80.4ms |

- 範囲の幅はどの拍子も0.03125小節（BPM84で約89ms）である。この幅はTGS対応（2026-09-16）で1.25倍に緩和した値であり、Master データのため製品版と体験版で共通である。【要確認: 製品版でもこの幅を追認するかを企画に】
- 早入力と遅入力は対称ではない。8・4・2・1拍子はほぼ遅れ側だけに幅があり、6・3拍子は早い側に寄っている。【要確認: 意図どおりかを企画に】
- 8拍子の範囲の終わり（0.153125）と6拍子の範囲の始まり（0.146875）は重なっており、重なった区間では8拍子のJustになる。
- 前回の入力がない場合（最初の入力、曲頭オフセットより前）はJustにならない。
- 1小節を過ぎた入力は、通常の拍子判定では1拍子に丸めるが、Just判定には丸める前の経過を使う（1拍子のJustが1小節を過ぎても続かないようにするため）。

## **ダメージへの反映**
[[ダメージ計算]] の計算式に「Justダメージ倍率」として組み込まれる。計算式と計算の順序は [[ダメージ計算]] を正とする。
- Just判定成立時の倍率は**200%**とする（＝基礎ダメージ2倍）。
- Justダメージ倍率は武器（拍子ごとの攻撃定義）ごとに設定する。設定場所は `Assets/Level/Data/Master/InGame/Battle/PlayerAttack_Beat*.asset` の `_justDamageMultiplier` である。
- クリティカル判定とJust判定は独立した別軸の判定であり、
両方成立した場合は式の通り両方の倍率が乗算される。
- キルコード発動時のダメージにも同じ式がそのまま適用される。
（詳細は次項「キルコード（スキル）発動条件・ノーツ種別への影響」参照）

## **キルコード（スキル）発動条件・ノーツ種別への影響**
Just判定は、リズム判定で記録されるノーツの種類やキルコードの発動条件
（コマンドパターン判定）そのものには**影響しない**。
- リズム判定で記録される「ノーツ」の種類（1〜8拍子に対応するノーツ）は、
Just成立の有無にかかわらず通常通り記録される。
- キルコードのコマンド判定ロジック（どの順番でどの拍子・アクションが入力されたか）
自体は、Just成立の有無に左右されない。
一方で、
**キルコードの発動条件を満たした最後の入力がJust判定に成功していた場合、
そのキルコードのダメージにもJustダメージ倍率（200%）が乗算される。**
- キルコードは全てのジャンル（攻撃／バフ／デバフ）で敵にダメージを与える処理を
持つため、このJustボーナスはジャンルを問わず全てのキルコードに適用される。
- キルコードが発動するたびに最後の入力のJust成否がそのまま判定対象になる。
- ダメージへの反映は通常攻撃時と同じ計算式（[[ダメージ計算]] ）に、
キルコードの基礎ダメージ量を当てはめる形で行う。
  - キルコードは最後の入力の拍子の攻撃定義のJustダメージ倍率を使う。

## **演出（フィードバック）**
- **クリティカル成立** → **SE**で表現する（クリティカル専用の効果音を再生）。
- **Just成立** → **エフェクト**で表現する。現状の実装は以下の組み合わせである。
  - **リズムUIの演出**
    - [[リズムUI]] のゲージ上のビートを0.2秒フラッシュさせ、伸ばしてから戻す（伸び64を0.12秒、戻り0.45秒）。
  - **画面演出**
    - 画面全体に拍子の色のVignetteを出す（強さ1.0、0.4秒）。Justでない攻撃では強さ0.3、0.2秒である。
  - **ゲームパッドの振動**
    - Justの攻撃（スキルを含む）のときだけゲームパッドを短く振動させる（低周波0.35、高周波0.6、0.12秒）。振動の強さは設定画面で変えられる（[[コンフィグ]] 参照）。ゲームパッドが無いときは振動しない。
  - 演出値の正本は `Assets/Level/Data/Master/InGame/Music/ACLikeRhythmGuideEffectConfig.asset` と `Assets/Level/Data/Master/InGame/Haptics/GamepadHapticsConfig.asset` である。
  - 着弾時のヒットマーカーの色変化とスローモーションは未実装である。【要確認: 実装するかを企画に】
- 両方成立時は、クリティカルのSEとJustのエフェクトが同時に再生されることで
「両方成立した」ことがプレイヤーに伝わる。
専用の複合演出アセットを別途用意する必要はない。
- 色を指定するチュートリアルの課題中は、指定した色の拍子でJustしたときだけ「Perfect」と表示し、振動する（[[チュートリアル]] 参照）。

- ▶ 背景
  - 2026-09-12（PR #1513）: Justの判定がロジック・リズムUI・画面演出で別々に行われ結果が一致しない問題を直し、判定を1か所に統一した。このとき旧リズムUIの1ブロック（0.025小節）をJust範囲として移した。
  - 統一時に決めた実装上の要件
    - Just範囲はマスターデータ（`RhythmJudgmentDefinitionAsset`）に集約する
    - 範囲は開始を含み、終了を含まない
    - Just範囲と通常の拍子範囲が重なる場合はJust範囲の拍子を優先し、リズムUIの表示と攻撃の拍子を一致させる
    - 判定を読んでも状態は変わらない。入力の履歴を更新する前に1回だけ判定し、その結果を攻撃・スキル・多段ヒット・演出に渡す
    - 履歴が無いとき・曲頭オフセット前はJustにしない。1小節を過ぎてもJustが続かないようにする
  - 回帰確認の観点: 6拍子それぞれの範囲の始まりと終わり、繰り返しの読み取り、最初の入力、1小節の超過、巻き戻し、空振り、スキルの発動、多段ヒット、リズムUIの更新順とレイアウト変更
  - 2026-09-16（PR #1692、TGS対応）: 範囲の中心を固定したまま、幅を1.25倍（0.025→0.03125小節）に広げた。
  - 仕様の±0.1秒・対称は2026-07時点の記述であり、上記の変更は仕様書に反映されていなかった。
