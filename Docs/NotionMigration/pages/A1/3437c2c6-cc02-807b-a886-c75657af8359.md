# 設定画面

- page_id: 3437c2c6-cc02-807b-a886-c75657af8359
- Notion パス: Symphony Kill Chord / **仕様概要** / 設定画面
- スナップショットの最終更新: 2026-05-29T06:38:49.000Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 一部古い — 「画面は遷移せずウィンドウのみ表示」は実装と合う（Overlay）。使用するアセット（決定・キャンセル・ミュート・チェックボックス）は実装の構成と違う。メニュー構成・各設定の範囲と既定値・保存のタイミング・開閉の操作が無い。`EnvironmentSettings` 系の仕様はどの Notion ページにも無い
- 整合性:
  - 実装との矛盾: ミュートボタンとチェックボックスは使っていない。選択肢は「< 値 >」形式、数値はスライダー（`Setting.uxml`）
  - 実装との差: 保存のタイミングが設定ごとに違う。環境設定は「設定を保存」を押したときだけ保存し、オーディオ設定は動かした時点で保存する（`EnvironmentSettingsView.cs:79-80`、`EnvironmentSettingsController.cs:9-12,186-215`、`AudioSettingsController.cs:43-80,123-125`）
  - 他ページとの重複: コンフィグ (3337c2c6-cc02-8090-b06c-d9617104076d) と役割が分かれていない（前回調査 `Docs/仕様書全体_改修追加項目一覧_2026-08-24.md:71,79` で指摘済み）。本原稿では「設定画面 = 画面の仕様（構成・操作・値の範囲）、コンフィグ = 項目の方針」として書いた
  - 他ページとの矛盾: 判定調整機能 (3b27c2c6-cc02-8024-8e69-e5a5b1f69532) の「10ms 刻み・表示専用」は実装（0.05 秒刻み・判定にも効く）と違う（判定調整機能ページ側の担当。ここでは実装の値と参照だけを書いた）
  - 入力の割り当て: アウトゲームの決定はゲームパッドの右ボタン（Xbox 表記 B）、キャンセルは下ボタン（Xbox 表記 A）（`Assets/Settings/Input/KillChordInputActioMap.inputactions` OutGame/Submit・Cancel）。30_ の OG-202 の「B で破棄」はキャンセル操作の意味と読み替えた
  - 実装内の食い違い: 画質の既定は全プラットフォーム共通で High（`EnvironmentSettingsDefaultAsset.asset` `_qualityLevel: 3`）だが、`QualitySettings.asset` のプラットフォーム別既定は Standalone = Ultra、Android / iPhone = Medium（OG-227）
- 変更点:
  - 「仕様概要」節: 旧: アウトゲーム内での設定が必要なシーン → そのまま残し、ホームの歯車から開く画面であることを追記
  - 「関連ページ」節: 空 → 記入
  - 「仕様意図」節: 空 → 根拠が無いため【要確認】
  - 「変更する設定」節: コンフィグへのリンクは残し、役割分担を一文で追記（OG-222）
  - 「使用するアセット」節: 旧: ミュートボタン・チェックボックスあり → 新: 使っていないと注記、左右切り替え（< 値 >）を追加（OG-205）
  - 新節「画面の構成」（OG-205）、「環境設定」（OG-202、OG-212、OG-213、OG-227）、「オーディオ設定」（OG-204）、「開き方・閉じ方」（OG-205、OG-206）を既存の節の後ろに追加
- 決定（2026-09-22 八幡）: No.11 リズム判定タイミングは判定と表示の両方をずらす（「環境設定」節に追記）
- 決定（2026-09-22 八幡）: No.25 タイトルのデータリセットでは音量に加えて言語とリズム判定タイミングを保持する（「環境設定」節・「オーディオ設定」節に追記）
- 実装の修正が必要: No.25 データリセットで言語とリズム判定タイミングが既定値に戻る（根拠 `Assets/Scripts/Runtime/6.Composition/OutGame/Title/TitleSceneInitializer.cs:571-572, 581`）
- 織り込んだ反映項目: OG-202, OG-204, OG-205, OG-206, OG-212, OG-213, OG-222, OG-227
- 出典: 実装（`EnvironmentSettingsController.cs:9-12,75-86,186-222,298-303`、`EnvironmentSettingsPresenter.cs:56-59,119`、`EnvironmentSettingsView.cs:79-80`、`ResolutionApplier.cs:16-45`、`QualityApplier.cs:23-33,62`、`BrightnessApplier.cs`、`EnvironmentSettingsRepository.cs:36-51`、`EnvironmentSettingsData.cs:65-105`、`AudioSettingsData.cs:35-45`、`AudioSettingsController.cs:43-80,101,123-125`、`VolumeSettingsTabView.cs:25-26`、`SettingMenuView.cs:153-183`、`SettingScreenView.cs:72-90,130,202-218,258,279-360`、`ScreenInitializer.cs:231-253`、`GamepadHapticsView.cs:44-51`、`IEnvironmentSettingsCommand.cs:52`、`IAudioSettingsCommand.cs:29`）/ マスターデータ `Assets/EnvironmentSettingsDefaultAsset.asset`、`Assets/Level/Data/Master/InGame/Haptics/GamepadHapticsConfig.asset`、`ProjectSettings/QualitySettings.asset` / UXML `Setting.uxml` / PR #1391, #1426, #1573, #1574, #1601, #1648, #1673, #1675, #1701, #1823
- 要確認:
  - 仕様意図（企画に確認）
  - 「既定値に戻す」機能（処理はあるが UI が無い）を画面に出すか（要企画確認、OG-205）
  - 設定画面をホーム以外（作戦・改造・研究）から開けるか（要実機確認、OG-206）
  - プラットフォーム別の画質の初期値（要企画確認、OG-227）
  - 設定画面とコンフィグの役割分担（要企画確認、OG-222）
  - 振動の発生条件（ジャスト時のみ）はインゲーム側ページ（判定調整機能・リズム判定）と調整する

## 適用する本文

## 仕様概要
アウトゲーム内での設定が必要なシーン
ホーム画面の上部のバーにある歯車アイコンから開く。

### 関連ページ
[[コンフィグ]]
[[判定調整機能]]
[[ホーム画面]]
[[タイトル画面]]
[[セーブデータ]]

### 関連用語


## 仕様意図
【要確認: 設定画面の仕様意図を企画に確認】

## 詳細説明

### 変更する設定
[[コンフィグ]]
（設定項目の方針はコンフィグに、画面の構成・操作・値の範囲はこのページに記載する）

### 注意点
設定画面を表示するとき画面は遷移せず、ウィンドウのみを表示させる


#### 使用するアセット
- 決定ボタン
  - 設定の保存に必要
- キャンセルボタン
  - 設定の変更時のキャンセルや前の画面に戻るときに必要
- 設定変更を表示する枠
- スライダー
  - 音声や光量などの設定時に使う
- 左右の切り替え（< 値 >）
  - 選択肢がある設定変更時に使う
- ミュートボタン
  - 音声設定変更時に使う
  - 現在は使っていない
- チェックボックス
  - 選択肢がある設定変更時に使う
  - 現在は使っていない（左右の切り替えを使う）

### 画面の構成
設定のメニューは次の4つで構成する。
- 環境設定
- オーディオ設定
- タイトルへ戻る
  - 確認ダイアログを出す
    - 文言：「タイトル画面に戻りますか？」
    - ボタン：「タイトル画面に戻る」「キャンセル」
- とじる

環境設定とオーディオ設定のパネルには、メニューに戻る「<」ボタンを置く。

### 環境設定

| 項目 | 選択肢・範囲 | 既定値 |
| --- | --- | --- |
| 画面モード | フルスクリーン / ウィンドウ | フルスクリーン |
| 解像度 | 端末が対応する解像度から左右で選ぶ | 初回はモニターの解像度（取得できないときは1920×1080） |
| 画質 | Very Low / Low / Medium / High / Very High / Ultra | High |
| 明るさ | 0〜10 | 5 |
| 言語 | 日本語 / English | 日本語 |
| 振動 | 強い / 弱い / オフ | 強い |
| リズム判定タイミング | -0.30〜+0.30秒（0.05秒刻み） | 0秒 |

- 明るさは、画面全体の露出を-3〜+3の範囲で変える（5のときは変えない）
- 言語の選択肢は、どちらの言語でも「日本語」「English」と表示する
- 振動は、ジャスト判定が成立したときのゲームパッドの振動の強さである（強い＝1倍、弱い＝0.5倍、オフ＝振動しない）。ゲームパッドをつないでいないときは何もしない
- リズム判定タイミングは、リズム判定の判定と表示の両方をずらす。詳細は[[判定調整機能]]に記載する
- タイトルのデータリセットでは、言語とリズム判定タイミングは保持する（それ以外の環境設定は既定値に戻る）
- 画質の既定値はPC・スマホ共通でHighにしている【要確認: プラットフォームごとの初期値を企画に確認】
- 変更はすぐ画面に反映するが、保存するのは「設定を保存」を押したときだけである
- 保存せずにパネルを離れる（戻る、ウィンドウの外をクリック、キャンセル）と、変更は破棄する
- 既定値はプランナーが `Assets/EnvironmentSettingsDefaultAsset.asset` で変更できる

### オーディオ設定

| 項目 | 範囲 | 既定値 |
| --- | --- | --- |
| BGM | 0〜10 | 5 |
| 効果音 | 0〜10 | 4 |
| ボイス | 0〜10 | 8 |

- スライダーを動かすとすぐに反映し、その時点で保存する（保存ボタンは無い）
- タイトルのオプションはBGMと効果音だけで、ボイスは無い。どちらも同じ音量設定を変更する
- タイトルのデータリセットでは、音量設定は保持する

- 既定値に戻す機能は用意していない【要確認: 既定値に戻すボタンを置くかを企画に確認】

### 開き方・閉じ方
- 開き方
  - ホーム画面の歯車アイコンを押す
  - ゲームパッドのStart（キーボードはEsc）を押す
  - ウィンドウは画面の左から0.2秒でスライドインする
- 閉じ方
  - 「とじる」を押す
  - ウィンドウの外をクリックする
  - キャンセル（ゲームパッドのA〈下のボタン〉、キーボードのEsc）を押す（環境設定・オーディオ設定のパネルではメニューに戻る）
  - 閉じるときは左へスライドアウトする
- 設定を開いている間は、Startを押しても何もしない
- Escは開く操作と閉じる操作の両方に割り当てているため、開いた瞬間と閉じた瞬間の入力で続けて反応しないようにしている
- ホーム以外の画面（作戦・改造・研究）から開けるか【要確認: 実機で確認】
