# シナリオパート

- page_id: 3437c2c6-cc02-8035-af95-fde3f08584dd
- Notion パス: Symphony Kill Chord / **仕様概要** / シナリオパート
- スナップショットの最終更新: 2026-06-15T11:09:46.680Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 一部古い — 本文はフォーラム「シナリオパート」(3437c2c6-cc02-80c5-9173-f4d72921690e、2026-04-16) の転記のまま。操作の割り当て（TGS 対応の #1860 / #1886 / #1904）、ツールバー（#1825 / #1867 / #1890）、スキップ確認（#1613 / #1658 / #1670）、文字送りの設定値（#1791）、送りの判定ルール（#1637 / #1904）が入っていない。ログ・設定・話者の強調・場所表記は未実装
- 整合性:
  - 実装との矛盾: 「左クリックがあるまで待機」→ 送りは左クリック / Enter / ゲームパッド東（B）（`Assets/Settings/Input/KillChordInputActioMap.inputactions` の Scenario マップ）
  - 実装との矛盾: 「UI削除…画面内のどこかを押すことによって元に戻る」「オート…ボタンのどこかを押すことによって停止」の記述と、キー割り当て（HideUI=W/RB、Auto=Ctrl/X）の関係が書かれていない
  - 実装との矛盾: 「待機時には暗く表示、発言時に通常の表示」（話者以外の立ち絵の自動暗転）は無い。CSV の Fade（FadeMode=Black）で手動で黒くする（SC-04）
  - 実装との矛盾: 「左上に場所に関する記載」は無い。ログは Scenario.unity にボタンとラベルがあるが、表示の処理がコードに見当たらない。設定ボタンはツールバーに無い（SC-04）
  - 他ページとの矛盾: フォーラム「シナリオシステム テキストウィンドウについて」(3777c2c6-cc02-80a2-835c-d92af833e0de) は「適用済」だが本文が空で、TextWindow コマンドも実装に無い
  - 「関連ページ」節: 空の DB「(db)」(3447c2c6-cc02-8002-bbec-ea5c8fbae972、rows=0) だけが置かれている
- 変更点:
  - 「関連ページ」「関連用語」節: 空の DB ビューを残し、ページ名を文字で記入
  - 「仕様意図」節: そのまま残す（フォーラム「シナリオパート」と同文）
  - 「進行の流れ」節: 手順 2 の場所表記と手順 3 の立ち絵の強調に「未実装」を追記（SC-04）。手順 5 の「左クリック」に送りの割り当てを追記（SC-01）。文字送りの速さなどの設定値を追記（SC-02）
  - 「イメージ図内機能説明」節: スキップ（確認ダイアログ、SC-01）、オート（有効中の強調、SC-01）、ログ（表示処理が未確認、SC-04）、UI削除（割り当てと復帰、SC-01・SC-03）、設定（ボタンが無い、SC-04）を追記。早送りの項目を追加
  - 新節「操作の割り当て」（SC-01）、「送りの判定」（SC-03）、「設定値」（SC-02）、「未実装の機能」（SC-04）を既存の節の後ろに追加
- 決定（2026-09-22 八幡）: No.35 体験版（TGS 2026）で入った操作の割り当て（決定を東、HideUI を RB、早送りを南）は製品版の仕様として追認する（「操作の割り当て」節はそのまま。追認を問う【要確認】を外した）
- 決定（2026-09-22 八幡）: No.34 未実装の機能は消さずに「未実装」と明記して残す（「未実装の機能」節で明記済み）
- 織り込んだ反映項目: SC-01, SC-02, SC-03, SC-04
- 出典: 実装（`Assets/Settings/Input/KillChordInputActioMap.inputactions` Scenario マップ、`ScenarioUsecase.cs`、`ScenarioAdvanceGate.cs`、`ScenarioSettingsAsset.cs:44-66`、`Assets/Level/Scenes/Master/Scenario.unity:460, 1864`、`Assets/Resources/Scenario/ScenarioSkipConfirmation.prefab`）/ マスターデータ `Assets/Level/Data/Master/Scenario/ScenarioSettingsAsset.asset` / PR #1613, #1637, #1658, #1670, #1791, #1825, #1860, #1867, #1886, #1890, #1904 / フォーラム「シナリオパート」「シナリオ機能の仕様」(3417c2c6-cc02-8084-af73-ecc1c0055d65)
- 要確認:
  - ログボタンを押したときに何か起きるか（要実機確認）。ログ・設定ボタン・話者の自動強調・場所表記を製品版で実装するか（要企画確認、SC-04）
  - 早送り（0.025 秒/文字）が通常（0.03 秒/文字）とほぼ同じ速さでよいか（要企画確認、SC-02）
  - スキップ確認ダイアログのボタン構成と初期フォーカス（要実機確認、SC-01）
  - シナリオ本文のローカライズ（CSV は 1 言語）をするか（要企画確認）
  - フォーラム「シナリオシステム テキストウィンドウについて」の「適用済」を見直すか（要企画確認）

## 適用する本文

## 仕様概要
ゲームのシナリオを再生する画面。

### 関連ページ
（既存のDBビューを残す）
[[チュートリアル]]
[[体験版仕様]]
[[コンフィグ]]

### 関連用語
[[シナリオパート]]

## 仕様意図
バトルの独特なシステムや戦場の雰囲気に説得力を持たせ、よりゲームに没入感を与えるため。

## 詳細説明

### シナリオシステム

#### 進行の流れ
1. 背景の表示
2. 場面に登場するキャラクターの立ち絵の表示
左上に場所に関する記載
  - 場所の記載は未実装である
3. キャラクターの名前の明記
  1. キャラクターの立ち絵を目立たせる
・待機時には暗く表示を行い発現時に通常通りの表示を行う
  - 話者以外の立ち絵を自動で暗くする処理は未実装である。シナリオCSVのFade（黒）で手動で暗くする
4. シナリオ本文の表示の表示
  1. 本文は左から右に表示を行う
  2. アニメーションに関しては再生はどこからでも行う。
難しい場合はテキストを表示した最初に統一を行う。
5. テキストを表示しきったらプレイヤーの左クリックがあるまで待機
オートの場合は表示完了後2秒待機し次のテキストに移行
  - 送りの操作は、左クリック・Enter・ゲームパッドの東ボタン（Xbox B）である
  - 最後のテキストでも送りの入力を待つ

#### 画面イメージ図
  （既存の画像を残す）

#### イメージ図内機能説明
- スキップ
  - ボタンを押すことで、そのパートで再生されるシナリオをスキップする。
  - スキップする前に本当にスキップするかの確認画面を一度挟む。
  - スキップしたときは、すぐにシナリオの画面を閉じる
- オート
  - ボタンを押すことで、テキストの再生を自動で行いシナリオを自動進行させる。
ボタンのどこかを押すことによってオート再生は停止する。
  - 文字の表示を通常通り行いすべての文字を表示した2秒後に次のテキストに移行する。
  - オートが有効な間は、ボタンを水色で強調する
- 早送り
  - 押している間、文字の表示を速くする
- ログ
  - これまでの会話を振り返れる画面を表示する。
  - ボタンは配置しているが、表示の処理は未実装である【要確認: 実機で確認】
- UI削除
  - ボタンを押すことで表示されているUIをすべて非表示にする。
画面内のどこかを押すことによって元に戻る。
  - UIを戻した瞬間の入力では、テキストを送らない
- 設定
  - [[コンフィグ]] ←の事項を設定する画面に飛ぶことができる
  - 現在はツールバーに設定のボタンが無い【要確認: 製品版で実装するかを企画に確認】
- ツールバーの各ボタンには、アイコン・操作名（Skip / Auto / Log / Hide UI）・キーの案内を並べて表示する

#### 操作の割り当て

| 操作 | キーボード・マウス | ゲームパッド |
| --- | --- | --- |
| 送り | 左クリック / Enter | 東（Xbox B） |
| 早送り | 左Shift | 南（Xbox A） |
| ポーズ | Space | Start |
| スキップ | Esc | 北（Xbox Y） |
| オート | Ctrl | 西（Xbox X） |
| UI削除 | W | RB |

- 決定を東ボタン、取消を南ボタンにそろえる全体の方針に合わせている

#### 送りの判定
- 文字の表示の途中で送りの入力をすると、次へは進まず、そのテキストを全て表示する
- 送りの入力、オートの待機時間の終わり、オートなどの切り替えのうち、先に来た1つだけで次へ進む。余った入力は次のテキストに持ち越さない
- マウスで画面上のボタンを押したときは送らない。キーボードとゲームパッドの送りは止めない
- 送ったときにボタンの選択状態を外し、決定の入力がボタンの操作と二重にならないようにする
- シナリオの開始時と終了時に、オート・ポーズ・早送り・UI削除の状態を元に戻す

#### 設定値

| 名前 | 値 | 単位 |
| --- | --- | --- |
| 文字の表示間隔（通常） | 0.03 | 秒/文字 |
| 文字の表示間隔（早送り） | 0.025 | 秒/文字 |
| オートで次へ進むまでの待機 | 2 | 秒 |
| シナリオが終わってから画面を閉じるまで | 5 | 秒 |

（値の正本は `Assets/Level/Data/Master/Scenario/ScenarioSettingsAsset.asset`）
【要確認: 早送りと通常の速さの差が小さいため、値を見直すかを企画に確認】

#### 未実装の機能
次の機能は現在実装していない。
- ログの表示
- 設定画面へのボタン
- 話者以外の立ち絵の自動の暗転（シナリオCSVで手動で行う）
- 場所の表記
- テキストウィンドウを切り替えるコマンド
- シナリオ本文の多言語対応（シナリオCSVは1言語）
【要確認: 各機能を製品版で実装するかを企画に確認】
