# Tutorial

- page_id: 3d67c2c6-cc02-816f-aa6a-fd139a0059bf
- Notion パス: Symphony Kill Chord / システム概要 / Tutorial
- スナップショットの最終更新: 2026-09-09T04:38:10.688Z
- 書き込み許可: 可（システム概要 配下）
- 処置: 本文置き換え
- 確認日 / 確認したコミット: 2026-09-22 / 73fefeb45

## 判定

- 信頼性: 一部古い — `TutorialPhase` の 5 段階、`TutorialData` の前進限定の状態遷移、`OutGameTutorialInitializer`（Order 150）・`StageSelectInitializer`（Order 110）・`TitleSceneInitializer`（Order 20）、スキル制限の連携は実装どおり。一方で「`RunHomeTutorialAsync` はプレースホルダ」「`OnHomeTutorialStarted/Completed` の購読者は存在しない」は誤りで、ホームのボタンハイライト式チュートリアル（4 ステップ）が実装済み（#1642 / #1687、TU-11）。`CompleteOpeningScenario` / `CompleteBattle` の呼び出し元を「対象外」としている（TU-12）
- 整合性:
  - 実装との矛盾: `RunHomeTutorialAsync` は `TutorialOverlayView` で Home 画面のボタンを順にハイライトする実処理になっている（`Assets/Scripts/Runtime/6.Composition/OutGame/Tutorial/OutGameTutorialInitializer.cs:230-275`）。手順は `HOME_TUTORIAL_STEPS` の 4 件（同 `:27-41`）
  - 実装との矛盾: `OutGameUIEvent` に 3 つ目のイベント `OnHomeTutorialForceCompleteRequested` がある（`Assets/Scripts/Runtime/4.View/OutGame/Screen/OutGameUIEvent.cs:32`）。`OnHomeTutorialStarted` は体験版の `DemoRuntimeBootstrap` が購読し（`Assets/Scripts/Demo/DemoRuntimeBootstrap.cs:735`）、`OnHomeTutorialForceCompleteRequested` は体験版のホームタイマー満了で発火する（同 `:347`）。`OnHomeTutorialCompleted` の購読者は今も無い
  - 実装との矛盾: `OutGameTutorialInitializer` は `ServiceLocator` から `HomeScreenView` も取得する（`:87`）
  - 呼び出し元（旧: 対象外）: `CompleteOpeningScenario` はシナリオ完了時の `ScenarioCom` → `StageProgressSaveDataService.SaveClearAsync`（`ScenarioCom.cs:431-445`、`StageProgressSaveDataService.cs:82-84`）、`CompleteBattle` は戦闘クリア時の `SequenceInitializationModule` → `SaveClearAsync(..., stageDefinition.IsTutorial)`（`SequenceInitializationModule.cs:410-415`、`StageProgressSaveDataService.cs:49`）
  - リンク切れ: 「概要」の本文リンクが `../../../../../Docs/NotionSpecifications/...` というリポの相対パスで、Notion 上では開けない（TU-17）。リンク先の「セーブシステム」（3747c2c6-cc02-8003-b633-ed80b638f7e8）は開発記録で、現行のモジュール文書は `セーブ`（39e7c2c6-cc02-80c6-a4e3-e064efa25c2c）
  - 他ページ: `Demo`（3d67c2c6-cc02-8154-817e-c0586bac951b）のホームタイマーはホームチュートリアルの開始通知を条件にしている。`ステージセレクト / ② チュートリアル自動出撃フロー（初回起動時）`（3bf7c2c6-cc02-8127-9aa6-d13cd126eaeb）・`タイトル / ① 通常起動フロー`（3bf7c2c6-cc02-81d0-b54c-e0cc66e4e5b8）が初回導線の正本（別原稿で TU-12 を反映）
  - 担当外（判定にだけ記録）: 戦闘チュートリアルの色指定課題の表示（`TutorialAttackFeedbackPresenter` など、TU-06）と戦闘チュートリアルのローカライズの詳細（TU-14）は `ミッション`（39e7c2c6-cc02-80d5-ac4e-cd46b87fd375）または新規の Localization ページが扱う
- 変更点:
  - 「概要」の本文: リポの相対パスのリンクを Notion のページリンクに張り替え（旧: `[② チュートリアル自動出撃フロー（初回起動時）](../../../../../Docs/...)` / `[セーブシステム](../../../../../Docs/...)` → 新: `[[② チュートリアル自動出撃フロー（初回起動時）]]` / `[[セーブ]]`）。「ホームチュートリアル制御」の説明に、Home 画面のボタンを順にハイライトすることを追記
  - 「概要」の表: 旧: ステータス「実装済み（`RunHomeTutorialAsync`はプレースホルダのため、ホームチュートリアルの実処理自体は未実装）」 → 新: 「実装済み」。最終更新日を 2026-09-22 に更新
  - 「🏗️ クラス」: `TutorialOverlayView`（View）と `HomeScreenView`（View、本モジュールに関わる部分のみ）の行を追加。`OutGameTutorialInitializer` の説明に 4 ステップの実行と強制完了を追記。表の下の注記に `OnHomeTutorialForceCompleteRequested` を追加
  - 「🧩 Composition初期化情報」: 取得する型に `HomeScreenView` を追加
  - 「🔗 モジュール結合」の図: Demo モジュールと、`HomeScreenView`・`TutorialOverlayView` を追加
  - 「📥 依存しているもの」: OutGame/Screen の詳細を「購読者は現状未実装」から現行の購読状況に直し、`HomeScreenView.FindTutorialTarget` を追加
  - 「📤 依存されているもの」: OutGame/Title の「呼び出し元は本ページの対象外」を実際の呼び出し元に置き換え。Demo と Scenario・InGame/Sequence を追加
  - 「🧅レイヤー情報 / ④ View」: 「購読者は現状存在しない」を直し、`TutorialOverlayView` の見た目の数値と操作を追加。ホームチュートリアルの文言が未ローカライズであることを追記（TU-11、TU-14）
  - 「🧅レイヤー情報 / ⑥ Composition」: 初期化に失敗したときの挙動（タイトル復帰画面）への参照を追記（TU-15）
  - 「🔌 拡張ポイント」: 旧: 「ホームチュートリアル本体を実装したい」（プレースホルダ） → 新: 「ホームチュートリアルの手順を追加・変更したい」。開始/終了の購読の行の「現状どちらのイベントにも購読者がいない」を直した
  - 「🔄処理フロー」: 本文のリンク先の表記を Notion のページリンクに直した
- 織り込んだ反映項目: TU-11, TU-12（呼び出し元・注記）, TU-14（ホームチュートリアル文言の未ローカライズの注記のみ）, TU-15（参照の追記）, TU-17（相対リンクの張り替え）
- 出典: `Assets/Scripts/Runtime/6.Composition/OutGame/Tutorial/OutGameTutorialInitializer.cs:25,27-41,60-95,146-155,182-275,292`、`Assets/Scripts/Runtime/4.View/OutGame/Tutorial/TutorialOverlayView.cs:20-54,113,159-187,282`、`Assets/Scripts/Runtime/4.View/OutGame/Screen/HomeScreenView.cs:70`、`OutGameUIEvent.cs:26-32`、`Assets/Scripts/Demo/DemoRuntimeBootstrap.cs:319-347,735`、`StageSelectInitializer.cs:46,1081-1117,1854-1885`、`TitleSceneInitializer.cs:47,687-721`、`StageProgressSaveDataService.cs:29-60`、`SequenceInitializationModule.cs:400-420`、`ScenarioCom.cs:431-445`、`OutGameInitializationFailureView.cs`、PR #1529・#1642・#1687
- 要確認:
  - ホームチュートリアルの 4 つの文言を Localization のテーブルへ移すか（企画・ローカライズ担当に確認。TU-11、TU-14）
  - `OnHomeTutorialCompleted` は購読者がいない。残すか、使い道を決めるか（プログラム担当）

## 適用する本文

- カテゴリー: アウトゲーム

---

## 概要
> 💡 **モジュール概要**
> シーンを跨いだチュートリアル進行を5段階のフェーズ状態機械（`TutorialPhase`）として管理し、その進行段階に応じてホーム（作戦画面）到達時の自動処理を制御するモジュールである。Title→初回シナリオ、StageSelectの自動出撃（バトルへの直接遷移）は別モジュールの守備範囲であり、本ページは「進行フェーズの前進限定な状態遷移」と「ホームチュートリアル制御（`OutGameTutorialInitializer`。Home画面のボタンを順にハイライトして案内する）」を中心に扱う。Titleからの初回シナリオ直接遷移、およびStageSelect内の自動出撃連鎖については[[② チュートリアル自動出撃フロー（初回起動時）]]（ステージセレクトの子ページ）を参照。セーブデータ全体の永続化基盤は[[セーブ]]を参照。
| 項目 | 内容 |
|---|---|
| **モジュール名** | Tutorial |
| **カテゴリ** | OutGame / Persistent |
| **ステータス** | 実装済み |
| **最終更新日** | 2026-09-22 |
---

### 🏗️ クラス
| クラス名 | レイヤー | 役割・機能 |
|---|---|---|
| `**TutorialPhase**` | Domain | シーンを跨ぐチュートリアル進行段階を表すenum。`NotStarted`(0) → `OpeningScenarioCompleted`(1) → `BattleCompleted`(2) → `HomeStarted`(3) → `Completed`(4)の5段階 |
| `**TutorialData**` | Domain | `SaveData`に保持される進行状態。`Phase`と`IsTutorialCompleted`を公開し、`CompleteOpeningScenario`/`CompleteBattle`/`StartHome`/`Complete`の4メソッドで**前進限定**に状態を進める |
| `**PlayerActionRestrictionReason**` | Domain (InGame/Player) | プレイヤー行動制限の理由を表すenum（`None`/`Tutorial`/`Silence`）。本モジュールは`Tutorial`理由でスキル封印に関与する |
| `**SetSkillExecutionEnabledStepEntryAction**` | Domain (InGame/Mission/StepEntryAction) | ミッション目標ステップ進入時にスキル発動可否を切り替える意図を表すデータ（`IsSkillExecutionEnabled`） |
| `**PlayerActionRestrictionState**` | Adaptor (InGame/Player) | スキル発動制限を理由の`HashSet<PlayerActionRestrictionReason>`で管理し、`CanUseSkill`（制限0件で発動可）を公開する |
| `**SetSkillExecutionEnabledStepEntryActionExecutor**` | Adaptor (InGame/Mission) | `SetSkillExecutionEnabledStepEntryAction`を実行し、`PlayerActionRestrictionState`へ`Tutorial`理由の追加/解除を行う |
| `**TutorialOverlayView**` | View (OutGame/Tutorial) | ホームチュートリアルの表示。対象ボタン以外を暗幕で覆い、矢印と説明ボックスを出す。決定入力で次の手順へ進む |
| `**HomeScreenView**`（本モジュールに関わる部分のみ） | View (OutGame/Screen) | `FindTutorialTarget(elementName)`で、ハイライトする対象のボタン要素を返す |
| `**OutGameTutorialInitializer**` | Composition (OutGame) | Order=150。OutGameロード完了後にホームチュートリアルの開始判定・4手順の実行・進行保存を行う。強制完了の要求を受けると残りの手順を打ち切る |
| `**StageSelectInitializer**`（本モジュールに関わる部分のみ） | Composition (OutGame) | Order=110。`Ready()`から呼ぶ`StartAutomaticTutorialFlowAfterLoading`/`ExecuteAutomaticTutorialFlow`/`TryStartTutorialSegment`で、保存済み`TutorialPhase`に応じた自動出撃を行う |
  `OutGameUIEvent`（View/OutGame/Screen）の`OnHomeTutorialStarted`/`OnHomeTutorialCompleted`はホームチュートリアルの開始・終了を他モジュールへ通知するSignalであり、本モジュールが発火元となる。`OnHomeTutorialForceCompleteRequested`は他モジュール（体験版）が発火し、本モジュールが受けて実行中のホームチュートリアルを強制完了させる。

#### 🧩 Composition初期化情報
| 項目 | 内容 |
|---|---|
| **Initializerクラス** | `OutGameTutorialInitializer` |
| **Order** | 150（`StageSelectInitializer`の110より後） |
| **公開する ModuleContainer / ServiceLocator登録型** | なし。`ServiceLocator`からは`OutGameUIEvent`と`LoadingScreenController`（任意）、`HomeScreenView`（任意）を取得するのみで、自身は何も登録しない |
> `StageSelectInitializer`（Order 110）は本モジュール専用のComposition層クラスではないため上表では詳細を割愛するが、`Ready()`内で自動遷移フローを開始する点はTutorialモジュールの状態機械と密接に関わる。詳細は「ステージセレクト」モジュールページを参照する。
---

### 🔗 モジュール結合
```Mermaid
graph TD
    %% 定義 (接続のないレイヤーは省略)
    subgraph TutorialModule [Tutorial モジュール]
        TutorialDomain["Domain<br>TutorialPhase, TutorialData"]
        TutorialView["View<br>TutorialOverlayView"]
        TutorialComposition["Composition<br>OutGameTutorialInitializer (Order 150)"]
    end

    subgraph PersistentSavedataModule [Persistent/Savedata モジュール]
        SD_Domain["Domain<br>SaveData"]
        SD_App["Application<br>StageProgressSaveDataService"]
    end

    subgraph OutGameScreenModule [OutGame/Screen モジュール]
        Screen_View["View<br>OutGameUIEvent, HomeScreenView"]
    end

    subgraph StageSelectModule [OutGame/StageSelect モジュール]
        SS_Composition["Composition<br>StageSelectInitializer (Order 110)"]
    end

    subgraph TitleModule [OutGame/Title モジュール]
        Title_Composition["Composition<br>TitleSceneInitializer (Order 20)"]
    end

    subgraph ScenarioModule [OutGame/Scenario モジュール]
        SC_Composition["Composition<br>ScenarioCom"]
    end

    subgraph SequenceModule [InGame/Sequence モジュール]
        SQ_Composition["Composition<br>SequenceInitializationModule"]
    end

    subgraph DemoModule [Demo モジュール]
        Demo_Boot["DemoRuntimeBootstrap"]
    end

    subgraph PlayerModule [InGame/Player モジュール]
        Player_Adaptor["Adaptor<br>PlayerActionRestrictionState"]
    end

    subgraph MissionModule [InGame/Mission モジュール]
        Mission_Adaptor["Adaptor<br>SetSkillExecutionEnabledStepEntryActionExecutor"]
    end

    %% 依存関係
    SD_Domain -->|"TutorialData を保持"| TutorialDomain
    TutorialComposition -->|"Phase参照・進行保存"| SD_Domain
    TutorialComposition -->|"OnHomeTutorialStarted/Completed を発火"| Screen_View
    TutorialComposition -->|"ハイライト対象の取得"| Screen_View
    TutorialComposition -->|"手順の表示"| TutorialView
    SS_Composition -->|"Phase参照・自動出撃実行"| SD_Domain
    Title_Composition -->|"Phase == NotStarted 判定・初回シナリオ選択"| SD_Domain
    SC_Composition -->|"CompleteOpeningScenario"| SD_App
    SQ_Composition -->|"CompleteBattle"| SD_App
    SD_App -->|"Phase を進める"| TutorialDomain
    Demo_Boot -->|"OnHomeTutorialStarted 購読 / ForceComplete 発火"| Screen_View
    Mission_Adaptor -->|"Tutorial理由でスキル制限を付与/解除"| Player_Adaptor
```

#### 📥 依存しているもの
- `**Persistent/Savedata**`
  - *依存箇所*: `SaveData.Tutorial`（`TutorialData`）
  - *詳細*: `OutGameTutorialInitializer`と`StageSelectInitializer`はどちらも`SaveStore`経由でロード済みの`SaveData`を取得し、`Tutorial.Phase`を参照・`StartHome()`/`Complete()`で更新後に`SaveStore.SaveAsync<SaveData>()`で永続化する
- `**OutGame/Screen**`
  - *依存箇所*: `OutGameUIEvent`、`HomeScreenView`
  - *詳細*: `OutGameTutorialInitializer`が`ServiceLocator`から取得し、`OnHomeTutorialStarted`/`OnHomeTutorialCompleted`を発火し、`OnHomeTutorialForceCompleteRequested`を購読する。`OnHomeTutorialStarted`は体験版（Demo）が購読している。`OnHomeTutorialCompleted`の購読者は現状いない。ハイライトする対象のボタンは`HomeScreenView.FindTutorialTarget`で取得する

#### 📤 依存されているもの
- `**OutGame/StageSelect**`
  - *参照箇所*: `TutorialPhase`, `TutorialData`
  - *詳細*: `StageSelectInitializer.TryStartTutorialSegment()`が`_loadedSaveData.Tutorial.Phase`をswitchし、`OpeningScenarioCompleted`の場合にチュートリアル戦闘へ直接出撃する（詳細は後述の処理フロー②）
- `**OutGame/Title**`
  - *参照箇所*: `TutorialPhase.NotStarted`
  - *詳細*: `TitleSceneInitializer.ApplyStartDestination()`が`Tutorial.Phase != NotStarted`かどうかで、通常のホーム遷移か初回オープニングシナリオへの直接遷移かを分岐する
- `**OutGame/Scenario**・**InGame/Sequence**`
  - *参照箇所*: `TutorialData.CompleteOpeningScenario()`、`TutorialData.CompleteBattle()`（どちらも`StageProgressSaveDataService.SaveClearAsync`経由）
  - *詳細*: 冒頭のチュートリアルシナリオの再生が完了（スキップを含む）すると、`ScenarioCom`がクリアを保存し、`CompleteOpeningScenario()`が呼ばれる。チュートリアル戦闘（`IsTutorial`が有効なバトルステージ）をクリアすると、`SequenceInitializationModule`がクリアを保存し、`CompleteBattle()`が呼ばれる
- `**Demo**`
  - *参照箇所*: `OutGameUIEvent.OnHomeTutorialStarted`、`OnHomeTutorialForceCompleteRequested`
  - *詳細*: 体験版はホームチュートリアルの開始通知を受けてからホームタイマーを動かす。ホームタイマーが切れると強制完了を要求し、強制出撃へ進む
- `**InGame/Mission**`
  - *参照箇所*: `PlayerActionRestrictionReason.Tutorial`
  - *詳細*: `SetSkillExecutionEnabledStepEntryActionExecutor`がミッション目標ステップ進入時のアクションとして、チュートリアル戦闘中のスキル封印/解除を`PlayerActionRestrictionState`へ委譲する。これはチュートリアルの状態機械（`TutorialPhase`）そのものではなく、チュートリアル**戦闘中**の行動制限という別軸の連携である点に注意
---

## 詳細

### 🧅レイヤー情報

#### ① Domain
`TutorialPhase`（5段階enum）と`TutorialData`（`SaveData`配下の`[Serializable]`クラス）を持つ。`TutorialData`は`_phase`と後方互換用の`_isTutorialCompleted`の2フィールドを保持し、`Phase`プロパティは`_isTutorialCompleted`が立っていれば無条件で`Completed`を返す（旧セーブデータとの互換のため）。4つの公開メソッド（`CompleteOpeningScenario`/`CompleteBattle`/`StartHome`/`Complete`）はいずれも内部の`AdvanceTo(TutorialPhase)`を呼び、**指定フェーズが現在値以下なら何もせずfalseを返す**（後退させない・スキップも許容する設計）。`PlayerActionRestrictionReason`（`InGame/Player`）と`SetSkillExecutionEnabledStepEntryAction`（`InGame/Mission/StepEntryAction`）はTutorialモジュール専属ではないDomain型だが、チュートリアル戦闘中のスキル制限に用いられる。

#### ② Application
当モジュールでは使用していない。

#### ③ Adaptor
`PlayerActionRestrictionState`（`InGame/Player`）が理由付きのスキル制限を`HashSet<PlayerActionRestrictionReason>`で管理し、`CanUseSkill`を`PlayerAttackController.TryExecuteSkill`へ渡す。`SetSkillExecutionEnabledStepEntryActionExecutor`（`InGame/Mission`）は`IMissionStepEntryActionExecutor`を実装し、ミッション目標ステップの進入時アクションとして`Tutorial`理由の追加/削除を行う。

#### ④ View
`OutGameUIEvent`（`View/OutGame/Screen`）に`OnHomeTutorialStarted`/`OnHomeTutorialCompleted`/`OnHomeTutorialForceCompleteRequested`の3つのSignalを持つ。
`TutorialOverlayView`（`View/OutGame/Tutorial`）がホームチュートリアルを表示する。Home画面のルート要素の上に重ね、対象のボタンの周りを4枚の暗幕で覆い、矢印と説明ボックスを出す。決定入力（`NavigationSubmit`）またはクリックで次の手順へ進む。表示中は`ModalNavigationScope`でフォーカスを閉じ込める。最初の手順だけ、レイアウトの確定を待ってから位置を決める。

| 名前 | 値 | 単位 | 正本のコード |
|---|---|---|---|
| 暗幕の不透明度 | 0.75 | — | `TutorialOverlayView.cs`（`CURTAIN_ALPHA`） |
| フェード時間 | 0.2 | 秒 | 同上（`FADE_DURATION`） |
| 説明の文字サイズ | 36 | px | 同上（`MESSAGE_FONT_SIZE`） |
| 説明ボックスの最大幅 | 720 | px | 同上（`MESSAGE_BOX_MAX_WIDTH` = 320 × 2.25） |
| 説明ボックスの余白 | 36 | px | 同上（`MESSAGE_BOX_PADDING` = 16 × 2.25） |
| 説明ボックスの角丸 | 27 | px | 同上（`MESSAGE_BOX_RADIUS` = 12 × 2.25） |

ホームチュートリアルの手順と文言は`OutGameTutorialInitializer`の`HOME_TUTORIAL_STEPS`に直接書かれており、ローカライズされていない（英語設定でも日本語で出る）。
【要確認: 文言を Localization のテーブルへ移すかを企画・ローカライズ担当に確認する】

| 順 | 対象の要素名 | 文言 |
|---|---|---|
| 1 | `StageSelect` | ここから出撃するステージを選んで戦闘に挑みます。 |
| 2 | `SkillTree` | 研究を進めて新しいスキルや強化を解放できます。 |
| 3 | `SkillBuild` | 解放したスキルを組み合わせて武装を改造します。 |
| 4 | `OptionIcon` | 音量や操作方法などの各種設定を変更できます。 |

#### ⑤ Infrastructure
当モジュールでは使用していない。

#### ⑥ Composition
`OutGameTutorialInitializer`（Order 150）がホームチュートリアルの開始条件判定・実行・進行保存を担う。`StageSelectInitializer`（Order 110、本来はStageSelectモジュールのComposition）が`Ready()`から呼ぶ自動遷移フローの中で、`TutorialPhase.OpeningScenarioCompleted`時のチュートリアル戦闘自動出撃を担当する。両者はOrderの前後関係（110→150）を利用しており、同一フレームのOutGameロード完了後に順に実行される。
チュートリアル戦闘の後などにOutGameの初期化が失敗した場合は、タイトルへ戻る復帰画面を出す（詳細は[[シーンマネージャー]]を参照）。

### 🔌 拡張ポイント
| 拡張したいこと | 実装する場所 | 追加登録の要否 |
|---|---|---|
| チュートリアル進行段階を追加したい | `TutorialPhase`（Domain）へ値を追加し、`TutorialData`に対応する`CompleteXxx()`/`StartXxx()`メソッドを追加する | 必要（`AdvanceTo`は数値の大小比較のみで前進判定するため、enum値の並び順＝進行順を厳守する必要がある。`StageSelectInitializer.TryStartTutorialSegment`のswitch文にも新フェーズの分岐追記が必要） |
| ホームチュートリアルの手順を追加・変更したい | `OutGameTutorialInitializer`（Composition）の`HOME_TUTORIAL_STEPS`に、対象の要素名と文言を追加・変更する | 必要（対象の要素名は`HomeScreenView.FindTutorialTarget`で見つかる名前にする。見つからない手順は警告を出して飛ばされる） |
| ホームチュートリアルの開始/終了に演出やUIを追従させたい | `OutGameUIEvent.OnHomeTutorialStarted`/`OnHomeTutorialCompleted`を購読するControllerを新規作成する | 必要（`OnHomeTutorialStarted`は体験版だけが購読し、`OnHomeTutorialCompleted`には購読者がいないため、購読を追加しない限り何も起きない） |
| チュートリアル戦闘中に別のプレイヤー行動を制限したい | `PlayerActionRestrictionReason`（Domain）へ値を追加し、制限対象のController側で`PlayerActionRestrictionState`の該当APIを呼ぶ | 必要（`PlayerActionRestrictionState`は現状スキルの`AddSkillRestriction`/`RemoveSkillRestriction`のみを持つため、新しい制限種別にはAPI追加が要る） |

### 🔄処理フロー
主要な処理フローを2つに分けて記述する。①はホーム（作戦画面のシーン）到達時にホームチュートリアルが自動開始される流れ、②はStageSelect側で保存済みフェーズに応じてチュートリアル戦闘へ自動出撃する流れである。なお、Titleからの初回シナリオへの直接遷移（`TutorialPhase.NotStarted`時の`TitleSceneInitializer.ApplyStartDestination`）は[[② チュートリアル自動出撃フロー（初回起動時）]]（ステージセレクトの子ページ）で扱うため、本ページでは前提としてのみ触れる。
- 📄 [[① ホーム到達時のホームチュートリアル自動開始フロー（`OutGameTutorialInitializer`, Order 150）]] (3d67c2c6-cc02-817f-85b3-ef2253a42497)
- 📄 [[② StageSelect側の自動出撃フロー（`StageSelectInitializer.Ready()`起点）]] (3d67c2c6-cc02-81e7-8605-c1c3edfbaad1)
