# ① シーン追加ロード＋初期化完了待機フロー

- page_id: 3bf7c2c6-cc02-8138-8850-c3dd0d72ad24
- Notion パス: Symphony Kill Chord / システム概要 / シーンマネージャー / ① シーン追加ロード＋初期化完了待機フロー
- スナップショットの最終更新: 2026-08-17T07:40:10.037Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 一部古い — 流れ（BeginTracking → ロード → Complete → WaitForReadyAsync）は現行どおり。ロード画面付き実行（`LoadingOperationExecutor`）の中で動くこと、待機上限を超えると失敗になることが書かれていない。根拠: `Assets/Scripts/Runtime/2.Application/Persistent/SceneManagement/SceneTransitionUsecase.cs:126-140`、`SceneInitializationReadinessRegistry.cs:82-106`、`Assets/Level/Scenes/Master/Persistent.unity:2361`
- 整合性: 親ページ（39e7c2c6-cc02-8009-8530-cc10a4396fa5）の原稿と整合させた。実装との矛盾は上記の欠落のみ
- 変更点:
  - シーケンス図: `LoadingOperationExecutor` の参加者を追加し、処理全体がロードセッションの中で実行されることを示した
  - シーケンス図: 「待機上限（3600 フレーム）を超えたら false」の分岐を追加
  - 旧: 最後の矢印「ロード画面はここまで表示され続ける」 → 新: 成否を Executor へ返し、Executor がセッションを完了/失敗させる（②へ続く）
- 織り込んだ反映項目: IF-04（処理フローの補足）
- 出典: 実装（上記ファイル）、Persistent.unity の `SceneTransitionInitializer` 設定値
- 要確認: なし

## 適用する本文

```Mermaid
sequenceDiagram
    autonumber
    participant Caller as 呼び出し元（Title等）
    participant Usecase as SceneTransitionUsecase
    participant Executor as LoadingOperationExecutor
    participant Readiness as SceneInitializationReadinessRegistry
    participant Service as SceneTransitionService
    participant RootInit as 遷移先シーンのルート初期化クラス

    Caller ->> Usecase: LoadAdditiveAsync(sceneName, token)
    Usecase ->> Executor: ExecuteAsync（ロード画面付きで実行）
    Executor ->> Usecase: 処理本体を実行
    Usecase ->> Readiness: BeginTracking(sceneName)
    Usecase ->> Service: シーンオブジェクトグラフを読み込み
    Note over RootInit: シーンロード後、Init→ResourceLoadAsync→Build→Readyを実行
    RootInit -->> Readiness: Complete(sceneName, isSuccess)
    Usecase ->> Readiness: WaitForReadyAsync(sceneName, token)（フレームポーリング）
    alt 待機上限（3600フレーム）以内に完了
        Readiness -->> Usecase: isSuccess
    else 待機上限を超えた
        Readiness -->> Usecase: false
    end
    Usecase -->> Executor: 成否を返す（ロード画面はここまで表示され続ける）
    Executor -->> Caller: 成否（セッションの完了/失敗は②を参照）
```
