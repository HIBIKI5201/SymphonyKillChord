# スキル編成

- page_id: 3bf7c2c6-cc02-810f-b494-dfb647018461
- Notion パス: Symphony Kill Chord / システム概要 / スキル編成
- スナップショットの最終更新: 2026-08-17T17:49:31.216Z
- 書き込み許可: 可（システム概要 配下）
- 処置: 本文置き換え
- 確認日 / 確認したコミット: 2026-09-22 / 73fefeb45

## 判定

- 信頼性: 一部古い — 既存のクラス表のクラスはすべて実在し、Order 130 も一致する。一方で TGS 期に追加されたジャンル絞り込み・コントローラー操作・スキルのレベルアップ（改造P）・編成枠の拡張が無い。ServiceLocator への登録型と、インゲームでの装備の読み方が現行と違う（OG-121）
- 整合性:
  - 実装との矛盾:
    - ServiceLocator へ登録するのは `SkillBuildDefinition` だけで、`SkillBuildController` は登録しない（`Assets/Scripts/Runtime/6.Composition/OutGame/SkillBuild/SkillBuildInitializer.cs:270-275`。`SkillBuildController` の参照はこの Initializer の中だけ）
    - 「📤 依存されているもの / Skill」: `SkillInitializer`（Order 450）は `SkillBuildDefinition` から構成を読むのではなく、`SkillBuildRepository.LoadSkillBuild()` でセーブデータの現在の装備を読み直し、`SkillBuildDefinition.UpdateEquippedSkills` で上書きする（未登録なら新しく登録する）（`Assets/Scripts/Runtime/6.Composition/InGame/Skill/SkillInitializer.cs:80-105`、PR #1801）。`SkillBuildDefinition` を読むのは `EquipmentBgmInitializer` と `SkillEffectInitializer`
    - 編成枠は固定ではなく、`SkillBuildDefinition.INITIAL_SLOT_COUNT`（2）＋スキルツリーで解放した「編成枠を増やすノード」の数（`SkillTreeStatusEntity.SkillSlotBonus`）。`EnsureSlotCount` で枠を増やす（`SkillBuildDefinition.cs:30,54`、`SkillBuildInitializer.cs:269-281,590-593,637-640`）
  - データの問題（担当外。記録のみ）: 説明文が「装備スロットを一枠解放」の `SkillNodeData-N7.asset` に `_hasSkillSlotBonus` が設定されていない（全 48 ノードで 0 件）。このままでは編成枠が増えない（OG-110。反映先は `仕様概要 / 研究画面` 3437c2c6-cc02-80d1-8adc-d63d047f4199）
  - 他ページ: 改造画面の操作・一覧の見た目・レベルアップのコストは `仕様概要 / 改造画面`（3437c2c6-cc02-80a5-bcb6-ed675cbb7fb2）の担当（OG-117, OG-119, OG-120）。本ページには仕組みだけを書いた
- 変更点:
  - 「概要」: モジュール概要にスキルのレベルアップと編成枠の拡張を追記。最終更新日を 2026-09-22 に更新
  - 「🏗️ クラス」: `SkillBuildDefinition`・`SkillBuildUseCase`・`SkillBuildController`・`ISkillBuildCommand`・`SkillListView`・`SkillBuildRepository` の説明にレベルアップと編成枠を追記。`SkillElementControllerEquipController`・`SkillGenreFilterBarView`・`SkillGenreIconCatalogAsset` / `SkillGenreIconCatalogEntry` の行を追加
  - 「🧩 Composition初期化情報」: 旧: 「`SkillBuildDefinition`と`SkillBuildController`をServiceLocatorへ登録する」 → 新: 「`SkillBuildDefinition`だけを登録する（既に登録済みなら枠数だけを合わせる）」
  - 「🔗 モジュール結合」: SkillTree からの編成枠ボーナスと入手済みスキル変更の通知、Skill モジュールの読み方を修正し、Music（装備BGM）を追加
  - 「📥 依存しているもの」: SkillTree の依存箇所に `SkillTreeStatusEntity.SkillSlotBonus`・`OutGameUIEvent.OnOwnedSkillChanged` を追記。Persistent/Savedata にレベルアップの改造P消費を追記
  - 「📤 依存されているもの」: Skill の説明を修正し、Music と SkillEffect を追加
  - 「🧅レイヤー情報」: ② Application・④ View・⑤ Infrastructure・⑥ Composition に追記
  - 「🔌 拡張ポイント」: 変更なし
- 決定（2026-09-22 八幡）: No.23 改造画面は取得済みのキルコードだけを表示する（仕様どおり）。「🏗️ クラス」の `SkillListView` の説明を、本来の仕様と現状の併記に直した
- 実装の修正が必要: No.23 改造画面の一覧が未解放のキルコードも表示している（実装の不具合。`Assets/Scripts/Runtime/4.View/OutGame/SkillBuild/SkillListView.cs:61-113` で未開放を後半のグループに並べ、`:93` で未解放カードも選択できるようにしている）
- 織り込んだ反映項目: OG-121（01_ドキュメント棚卸し の NotionModuleDocs/SkillBuild.md 行: リポ側にしか無い情報は無い）。OG-110・OG-117 はシステム側の仕組みだけ
- 出典: `Assets/Scripts/Runtime/*/OutGame/SkillBuild/*`、`Assets/Scripts/Runtime/*/OutGame/Skill/*`、`SkillBuildInitializer.cs:43,245-290,560-640`、`SkillBuildRepository.cs:109-160`（改造P 1 消費・上限の既定 10・即時保存）、`SkillBuildController.cs:57,77`、`SkillInitializer.cs:44,80-105`、`EquipmentBgmInitializer.cs:236-250`、`SkillEffectInitializer.cs:97`、`SkillTreeInitializer.cs:522`、PR #1801・#1834・#1640・#1689
- 要確認:
  - N7 の `_hasSkillSlotBonus` が未設定のまま出荷されていないか（実機で編成枠が 3 に増えるか確認。データ修正の起票が要る。要実機確認）

## 適用する本文

- 概要: アウトゲームのスキル編成（改造画面）モジュール
- カテゴリー: アウトゲーム

---

## 概要
> 💡 **モジュール概要**
> 改造画面でのスキル編成を司るモジュールである。入手済みスキルの一覧表示、装備スロットへのドラッグ&ドロップ、構成の検証と保存を担当する。改造ポイントを使ったスキルのレベルアップと、スキルツリーの解放に応じた編成枠の拡張も扱う。スキルの解放はSkillTree、戦闘中の発動はSkillモジュールが扱う。
| 項目 | 内容 |
|---|---|
| **モジュール名** | SkillBuild |
| **カテゴリ** | OutGame |
| **ステータス** | 実装済み |
| **最終更新日** | 2026-09-22 |
---

### 🏗️ クラス
| クラス名 | レイヤー | 役割・機能 |
|---|---|---|
| `**SkillBuildDefinition**` | Domain | プレイヤーが装備しているスキル構成を表す。枠数は初期2枠（`INITIAL_SLOT_COUNT`）で、`EnsureSlotCount`で増やす |
| `**EquippedSkill**` | Domain | 所持しているスキル1件を表す値型 |
| `**SkillBuildUseCase**` | Application | 装備構成の検証・保存・確定と、スキルのレベルアップ（改造Pを1消費） |
| `**ISkillBuildRepository**` / `**IOwnedSkillRepository**` | Application | 装備構成と入手済みスキルの永続化境界 |
| `**SkillBuildController**` | Adaptor | 改造画面の保存要求とレベルアップ要求をユースケースへ伝える |
| `**ISkillBuildCommand**` | Adaptor | 編成操作（保存`SaveAsync`・レベルアップ`LevelUpAsync`）をApplication層へ伝えるコマンド契約 |
| `**SkillBuildSlotState**` | Adaptor | 装備スロットの保存済み状態と編集中状態を保持する |
| `**SkillBuildPresenter**` | Adaptor | 編成状態をViewへ渡すDTOへ変換する |
| `**SkillBuildViewDTO**` / `**SkillBuildSlotData**` / `**SkillViewData**` | Adaptor | 画面・スロット・スキル1件それぞれの表示データ |
| `**ISkillBuildViewModel**` / `**ISkillBuildViewModelWriter**` | Adaptor | ViewModelの読み取り契約と書き込み契約 |
| `**SkillBuildViewModel**` | View | 編成画面の表示状態を保持する |
| `**SkillListView**` | View | スキル一覧の表示。仕様では取得済みのキルコードだけを表示する。現状は解放済みスキルの後に未開放のスキルを区切り線で分けて並べている（実装の不具合） |
| `**SkillElementView**` | View | スキルアイコン1件の表示 |
| `**SkillElementDragAndDropManipulator**` / `**SkillElementDragAndDropSetup**` | View | スキルアイコンのドラッグ&ドロップ操作 |
| `**SkillElementControllerEquipController**` | View | コントローラーでの装備操作（1回目の決定で選択、同じカードへの2回目の決定で持ち上げ、スロットを選んで装備） |
| `**SkillGenreFilterBarView**` | View | ジャンル絞り込みのボタン（タブ）一覧。「全て」と、一覧に存在するジャンルのボタンを並べる |
| `**SkillBuildSlotLayout**` | View | 装備スロットの状態をUI配置へ反映する |
| `**SkillDetailView**` | View | スキル詳細の表示 |
| `**SkillDisplayTextFormatter**` / `**SkillEffectDescriptionFormatter**` / `**SkillDisplayText**` | Adaptor（`OutGame/Skill`） | スキル名・効果説明の整形。編成画面と詳細表示で使う |
| `**SkillGenreIconCatalogAsset**` / `**SkillGenreIconCatalogEntry**` | Infrastructure（`OutGame/Skill`） | スキルジャンル（Attack / Buff / Debuff）のアイコンのカタログ |
| `**InitialSkillLoadoutAsset**` | Infrastructure | ゲーム開始時点の初期解放・初期装備スキルを定義するアセット |
| `**SkillBuildRepository**` / `**OwnedSkillRepository**` | Infrastructure | 装備構成と入手済みスキルの実装。レベルアップは改造Pを1消費してすぐにセーブデータへ保存する（上限はスキルごと。定義が無ければ10） |
| `**SkillBuildInitializer**` | Composition | 上記一式の構築とServiceLocatorへの登録 |

#### 🧩 Composition初期化情報
| 項目 | 内容 |
|---|---|
| **Initializerクラス** | `SkillBuildInitializer` |
| **Order** | 130（`SkillTreeInitializer`(120)の後。解放済みスキルが確定してから編成を組む） |
| **公開する ModuleContainer / ServiceLocator登録型** | 専用の`ModuleContainer`は無く、`SkillBuildDefinition`だけをServiceLocatorへ登録する。既に登録済み（インゲームから戻った場合など）のときは登録せず、枠数だけを合わせる |
---

### 🔗 モジュール結合
```Mermaid
graph TD
    %% 定義 (接続のないレイヤーは省略)
    subgraph SkillBuildModule [SkillBuild モジュール]
        SB_Domain["Domain<br>SkillBuildDefinition"]
        SB_App["Application<br>SkillBuildUseCase"]
        SB_Adaptor["Adaptor<br>SkillBuildController, SkillBuildSlotState"]
        SB_Infra["Infrastructure<br>SkillBuildRepository, OwnedSkillRepository"]
        SB_Adaptor --> SB_App
        SB_App --> SB_Domain
        SB_Infra --> SB_App
    end

    subgraph SavedataModule [Persistent/Savedata モジュール]
        SD_Store["SymphonyFrameWork<br>SaveStore"]
        SD_App["Application<br>InitialSkillLoadoutService"]
    end

    subgraph SkillTreeModule [SkillTree モジュール]
        ST_Domain["Domain<br>SkillTreeStatusEntity（解放済みスキル・編成枠ボーナス）"]
    end

    subgraph SkillModule [Skill モジュール]
        SK_Composition["Composition<br>SkillInitializer, SkillEffectInitializer"]
    end

    subgraph MusicModule [Music モジュール]
        MU_Composition["Composition<br>EquipmentBgmInitializer"]
    end

    subgraph ScreenModule [Screen モジュール]
        SC_View["View<br>SkillBuildScreenView"]
    end

    %% 依存関係
    SB_Infra -->|"装備構成・入手済みスキル・スキルレベル・改造Pの読み書き"| SD_Store
    SD_App -->|"初期解放・初期装備の補完"| SD_Store
    ST_Domain -->|"装備の選択肢と編成枠ボーナスを提供"| SB_App
    SK_Composition -->|"セーブデータの装備で上書き・参照"| SB_Domain
    MU_Composition -->|"装備中スキル構成を取得"| SB_Domain
    SC_View -->|"画面のコンテナを提供"| SB_Adaptor
```

#### 📥 依存しているもの
- `**Persistent/Savedata**`
  - *依存箇所*: `SaveStore`, `SkillBuildData`, `ResourceInventoryData`, `InitialSkillLoadoutService`
  - *詳細*: 装備構成・入手済みスキル・スキルレベルを読み書きする。レベルアップでは改造Pを消費する。初回起動時とセーブデータリセット後は`InitialSkillLoadoutService`が`InitialSkillLoadoutAsset`の内容で補完する
- `**SkillTree**`
  - *依存箇所*: 解放済みスキル, `SkillTreeStatusEntity.SkillSlotBonus`, `OutGameUIEvent.OnOwnedSkillChanged`
  - *詳細*: ツリーで解放したスキルが装備の選択肢になる。解放済みの「編成枠を増やすノード」の数だけ編成枠が増える。ツリーでノードを解放すると`OnOwnedSkillChanged`が届き、一覧と枠数を更新する
- `**Screen**`
  - *依存箇所*: `SkillBuildScreenView`
  - *詳細*: 改造画面のコンテナはScreenモジュールが持ち、その中身を当モジュールが構築する

#### 📤 依存されているもの
- `**Skill**`
  - *参照箇所*: `SkillBuildDefinition`
  - *詳細*: `SkillInitializer`（Order 450）が戦闘のたびにセーブデータの現在の装備を読み直し、`SkillBuildDefinition`を上書きする（未登録なら登録する）。インゲームで発動できるスキルはこの構成で決まる。`SkillEffectInitializer`も装備構成を参照する
- `**Music**`
  - *参照箇所*: `SkillBuildDefinition`
  - *詳細*: `EquipmentBgmInitializer`が装備中のスキル構成から装備BGMを決める。未登録の場合は装備なしとして扱う
---

## 詳細

### 🧅レイヤー情報

#### ① Domain
装備しているスキル構成（`SkillBuildDefinition`）と、所持スキル1件（`EquippedSkill`）を保持する。

#### ② Application
`SkillBuildUseCase`が構成の検証・保存・確定を行う。スキルのレベルアップもここから要求する。永続化は2つのリポジトリ境界の先にある。

#### ③ Adaptor
`SkillBuildController`が保存要求とレベルアップ要求を仲介し、`SkillBuildSlotState`が「保存済み」と「編集中」の2つの状態を持つ。編集途中の内容は確定するまで保存済み状態へ反映されない。

#### ④ View
入手済みスキル一覧、スキルアイコン、装備スロットの配置、スキル詳細を担当する。装備の操作はドラッグ&ドロップで行い、`SkillElementDragAndDropManipulator`が扱う。コントローラーでの装備は`SkillElementControllerEquipController`、ジャンルでの絞り込みは`SkillGenreFilterBarView`が扱う。

#### ⑤ Infrastructure
装備構成と入手済みスキルの実装を持つ。初期状態は`InitialSkillLoadoutAsset`が定義する。レベルアップは編成の保存とは別に、実行した時点でセーブデータへ保存する。

#### ⑥ Composition
`SkillBuildInitializer`（Order 130）が一式を構築する。SkillTree（120）の後に走るため、解放済みスキルと編成枠ボーナスが確定した状態で編成を組める。

### 🔌 拡張ポイント
| 拡張したいこと | 実装する場所 | 追加登録の要否 |
|---|---|---|
| 初期解放・初期装備スキルを変えたい | `InitialSkillLoadoutAsset` | 不要（コード変更なし） |
| 編成操作を増やしたい | `ISkillBuildCommand`（Adaptor）へ操作を追加し、`SkillBuildUseCase`で処理する | 必要（ViewからCommandを呼ぶ経路も繋ぐ） |

### 🔄処理フロー
主要な処理フローは、それぞれ子ページに分けている。
- 📄 [[① スキル装備フロー（ドラッグ&ドロップ）]] (3bf7c2c6-cc02-81fb-9fd0-d9f1e1f3b2ab)
- 📄 [[② 初期スキルの補完フロー（初回起動・リセット後）]] (3bf7c2c6-cc02-814f-9e0a-e828a52bab0d)
