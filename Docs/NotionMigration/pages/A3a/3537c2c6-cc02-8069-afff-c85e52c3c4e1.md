# Application層からView層の多態な処理を実行する方法

- page_id: 3537c2c6-cc02-8069-afff-c85e52c3c4e1
- Notion パス: Symphony Kill Chord / システム概要 / 設計思想 / Application層からView層の多態な処理を実行する方法
- スナップショットの最終更新: 2026-05-01T13:05:07.231Z
- 書き込み許可: 可（システム概要 配下）
- 処置: 本文置き換え
- 確認日 / 確認したコミット: 2026-09-22 / 73fefeb45

## 判定

- 信頼性: 一部古い — パターンの考え方（Application に interface、Adaptor が実装して View の多態な処理へ振り分け、Composition で結ぶ）は現在のレイヤー参照（View→Adaptor、Adaptor→Application）と合う。ただしサンプルコードに誤りがあり、そのままではコンパイルできない
- 整合性:
  - Composition のサンプルで `skillUseCase = new (action);` の `action` が未定義（直前で作った `controller` を渡すべき）
  - View のサンプルの `[SerializeField] private int id;`、Composition の `private SkillUseCase skillUseCase;` が コード規定（2a87c2c6-cc02-80f9-a10a-f0b72b95fbb3）の CG-030（フィールドは `_` + キャメルケース）・CG-027（SerializeField に Tooltip）に反する
  - 見出し「Adapter」は、設計思想（31c7c2c6-cc02-80f3-92c0-e6929d962476）のレイヤー名「Adaptor」と表記が違う
  - 反映項目の対象ページではない（10〜60 に該当項目なし）
- 変更点:
  - 見出し: 旧: 「Adapter」 → 新: 「Adaptor」
  - 「View」のコード: 旧: `public int Id => id;` / `[SerializeField] private int id;` → 新: `public int Id => _id;` / `[SerializeField, Tooltip("スキルの ID")] private int _id;`
  - 「Composition」のコード: 旧: `private SkillUseCase skillUseCase;` / `skillUseCase = new (action);` → 新: `private SkillUseCase _skillUseCase;` / `_skillUseCase = new (controller);`
  - インデントのタブと空白の混在を空白にそろえた（コードの意味は変えていない）
- 織り込んだ反映項目: なし
- 出典: 設計思想ページのレイヤー定義、各レイヤーの asmdef の参照設定、コード規定
- 要確認: なし

## 適用する本文

Application
```C#
public interface IViewAction
{
    void Execute(int id);
}

public class SkillUseCase
{
    private readonly IViewAction _action;

    public SkillUseCase(IViewAction action)
    {
        _action = action;
    }

    public void Execute(int id)
    {
        _action.Execute(id);
    }
}
```
Adaptor
```C#
public interface ISkillVisual
{
    int Id { get; }
    void Execute();
}

public class SkillController : IViewAction
{
    private readonly Dictionary<int, ISkillVisual> _visuals;

    public SkillController(ISkillVisual[] visuals)
    {
        _visuals = new Dictionary<int, ISkillVisual>();
        for (int i = 0; i < visuals.Length; i++)
        {
            _visuals.Add(visuals[i].Id, visuals[i]);
        }
    }

    public void Execute(int id)
    {
        _visuals[id].Execute();
    }
}
```
View
```C#
public class SkillView : MonoBehaviour, ISkillVisual
{
    public int Id => _id;

    [SerializeField, Tooltip("スキルの ID")] private int _id;

    public void Execute()
    {
        //実際のViewで起こる演出など
    }
}
```
Composition
```C#
public class SkillInitializer : MonoBehaviour
{
    private SkillUseCase _skillUseCase;

    public void Initialize(ISkillVisual[] skills)
    {
        SkillController controller = new (skills);
        _skillUseCase = new (controller);
    }
}
```
