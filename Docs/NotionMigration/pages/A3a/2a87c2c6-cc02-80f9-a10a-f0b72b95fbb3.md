# コード規定

- page_id: 2a87c2c6-cc02-80f9-a10a-f0b72b95fbb3
- Notion パス: Symphony Kill Chord / システム概要 / コード規定
- スナップショットの最終更新: 2026-07-28T08:05:21.892Z
- 書き込み許可: 可（システム概要 配下）
- 処置: 本文置き換え
- 確認日 / 確認したコミット: 2026-09-22 / 73fefeb45

## 判定

- 信頼性: 一部古い — 既存の 6 節（設定・順序・サマリー・コメント・命名規則・その他・UnityAPI）は `Assets/Scripts/CodeGuidelines.md` とほぼ同文で、実装とも概ね合う。一方でリポ版にだけある規則が 4 件ある（RL-04）。「AI によるファイル変更は…に留める」は現在の運用と合わない（RL-07）
- 整合性:
  - リポ文書との差: `Assets/Scripts/CodeGuidelines.md`（2026-07-17）に、名前空間・`public set` 禁止・1 ファイル 1 公開型・ログ出力の 4 規則がある。`Assets/Docs/ScriptsDocs/CodingConventions.txt` には、`static readonly` 定数もアッパースネーク、Tooltip は日本語、クラスにもサマリー、古いログ形式が残る、の補足がある
  - 規則どうしの矛盾: 「順序」15 番は「プレフィックスが Handle のタイプ」、「命名規則」は「イベントハンドラメソッドのサフィックスは Handler」。実装は `Handle〇〇(` 形式のメソッド定義が 284 件、`〇〇Handler(` 形式が 142 件で両方ある（`git grep`、`Assets/Scripts/Runtime`）。code-guideline-check スキルも両方を検査基準にしている（`.claude/skills/code-guideline-check/SKILL.md:22,26`）→ 決定（2026-09-22 八幡）No.8 で使い分けを規則にした
  - 実装との矛盾: CodingConventions.txt の例 `if (_isTutorialCompleted) return false;` は「一行のブロックでも波カッコ」に反する（例の方が誤り。本文では波カッコ付きに直した）。同じく例の `INITIAL_UNLOCKED_SKILL_IDS` はもうコードに無いため、実在する `AttackPowerIncreaseBuff.cs:38` の `EFFECT_ID` に差し替えた。`static readonly` 定数はアッパースネーク 50 件・パスカル 7 件・キャメル 2 件で、概ね規則どおり
  - 運用との矛盾: 「AI によるファイル変更の使用はサマリー追加、コメント追加、タイポ修正の編集に留める」に対し、2026-08-15〜09-22 のマージ済み PR 506 件のうち `agent/` ブランチが 40 件あり、機能実装・CI 改修を含む（#1534、#1575、#1827）。`AGENTS.md:6-15` は AI エージェントのブランチ・PR 運用を定めている → 他ページ: `GitHub 運用規定`（2a87c2c6-cc02-804b-9b05-d384f4d76dbc）の AI 運用節と揃える必要がある
- 変更点:
  - 全体: ツール用キャッシュ（`Library/NotionCache/rules/code-guidelines.md`）の対象になるため、各規則の先頭に変えない ID（`CG-001`〜）を付けた（W-09・W-10）。節の構成・見出し名・順番・文言は既存のまま
  - 「設定」: CG-002 を決定 No.7 に合わせて書き換えた（RL-07。下の決定の行）
  - 「順序」: 15 番は既存のまま（決定 No.8 と合う）
  - 「サマリー・コメント」: クラスにもサマリーを付ける規則（CG-028）と、Tooltip は日本語で書く旨（CG-027 に追記）を追加（CodingConventions.txt）
  - 「命名規則」: 定数の規則に「`static readonly` を含む」を追記（CG-033）。名前空間の規則（CG-040）を追加（RL-04）。イベントハンドラ規則（CG-035）を決定 No.8 に合わせて書き換えた
  - 「その他」: 旧: 「フィールド変数を公開する際はプロパティを使用する」 → 新: 同文＋「変更を伴う場合は `public set` ではなく `SetXxx` / `RecordXxx` のような意図が伝わるメソッドを介する」（CG-051、RL-04）。1 ファイル 1 公開型（CG-055）を追加（RL-04）
  - 「その他」: コード例の全角引用符を修正。旧: `{ Console.WriteLine(”message”); }` → 新: `{ Console.WriteLine("message"); }`
  - 新節「ログ出力」（CG-080・CG-081）を既存の節の後ろに追加（RL-04、CodingConventions.txt）
- 決定（2026-09-22 八幡）: No.7 AI も実装・PR 作成を行い、レビューは人が行う。「設定」の CG-002 を旧: 「AIによるファイル変更の使用はサマリー追加、コメント追加、タイポ修正の編集に留める」 → 新: 「AIもコードの実装・修正とPRの作成を行う。AIの変更を取り込むPRのレビューは人が行う。ブランチ・PRの運用はGitHub 運用規定の「AI エージェントの運用」（`AGENTS.md`）に従う」に直し、【要確認】を外した
- 決定（2026-09-22 八幡）: No.8 メソッド名は `Handle〇〇`、クラス名は `〇〇Handler`。「命名規則」の CG-035 を旧: 「イベントハンドラメソッドのサフィックスは `Handler`」 → 新: メソッドは `Handle〇〇`、クラスは `〇〇Handler` の規則に直し、実在の例（`EnemyAttackReservationUsecase.cs:122` の `HandleReservedTimingReached`、`TextEventHandler.cs:13`）を添えた。「順序」15 番と CG-035 の【要確認】を外した
- 決定（2026-09-22 八幡）: No.9 必須の依存は `GetInstance`、無くてもよい依存は `TryGetInstance`。「その他」に CG-056 を追加した（設計思想 DP-302 と同じ規則）
- 織り込んだ反映項目: RL-04, RL-07（01_ドキュメント棚卸し の CodeGuidelines.md・CodingConventions.txt の行も反映）
- 出典: `Assets/Scripts/CodeGuidelines.md`、`Assets/Docs/ScriptsDocs/CodingConventions.txt`、`AGENTS.md:6-15`、`.editorconfig`（`charset = utf-8`）、`prs-list.tsv`（ブランチ先頭語の集計）、`git grep` による命名の集計
- 要確認:
  - CG-035: 既存の `〇〇Handler(` 形式のメソッド（142 件。例 `MissionDialogueController.cs:171` の `ObjectiveStepChangedHandler`）を `Handle〇〇` へ改名するか、新しく書くコードだけに適用するか（リードプログラマー判断）
  - キャッシュ化の手順（T-2・T-3）が済むまで、code-guideline-check などのスキルはリポの `Assets/Scripts/CodeGuidelines.md` を読み続ける。Notion を正本にした後にリポ版を消すかは移行手順（段階 5）で扱う

## 適用する本文

### 設定
- `CG-001` cs, md, txtのファイルはUTF-8を使用する
- `CG-002` AIもコードの実装・修正とPRの作成を行う。AIの変更を取り込むPRのレビューは人が行う。ブランチ・PRの運用はGitHub 運用規定の「AI エージェントの運用」（リポジトリの `AGENTS.md`）に従う

### 順序
`CG-010` クラス・構造体のメンバーは次の順に並べる。
1. **コンストラクタ**
2. **イベント (public events)**
3. **パブリックプロパティ**
4. **インターフェースプロパティ**
5. **パブリック定数(const/readonly)**
6. **パブリックメソッド**
7. **パブリックインターフェースメソッド**
8. **パブリックEnum定義**
9. **パブリックclass定義**
10. **パブリックStruct定義**
11. **定数 (const/readonly)**
12. **インスペクター表示フィールド ([SerializeField])**
13. **プライベートフィールド**
14. **Unityライフサイクルメソッド (Awake, Start, Updateなど)**
15. **イベントハンドラメソッド (プレフィックスがHandleのタイプ)**
16. **プロテクトインターフェース・仮想メソッド**
17. **プライベートメソッド**
18. **内部メソッド、抽出メソッド**
19. **プライベートEnum定義**
20. **プライベートclass定義**
21. **プライベートStruct定義**
22. **デバッグ機能**

### サマリー・コメント
- `CG-020` 全てのメソッドにはサマリーを付ける
- `CG-021` パブリックなプロパティとイベントにはサマリーを付ける
- `CG-022` プロパティ, イベント, フィールド変数かつ一行のサマリーは以下のように一行かつスペースを入れて書く
/// <summary> comment </summary> 
- `CG-023` クラス, メソッドまたは二行以上のサマリー以下のように改行してインデントを上げて書く
/// <summary>
///     comment1
///     comment2
/// </summary> 
- `CG-024` コメントは基本的に日本語を使用する
- `CG-025` 日本語の最後は「。」で終える
- `CG-026` メソッド内の処理には適宜コメントを付ける
- `CG-027` SerializeField属性を使用するフィールドには、TooltipAttributeでの説明を付ける。説明は日本語で書く
```C#
[SerializeField, Tooltip("研究ポイント")]
private int _researchPoint;
```
- `CG-028` クラスにもサマリーを付ける

### 命名規則
- `CG-030` C#のフィールド変数はキャメルケースでプレフィックスに `_`
- `CG-031` C#のクラス、プロパティ、メソッドはパスカルケース
- `CG-032` C#の引数はキャメルケース
- `CG-033` 定数はアッパースネークケース。`const` だけでなく `static readonly` の定数も含む
```C#
private const int MAX_STAGE_READY_WAIT_FRAME = 120;
private static readonly StatusEffectId EFFECT_ID = new("Skill07.AttackPowerIncreaseBuff");
```
- `CG-034` イベントのプレフィックスは `On` 
- `CG-035` イベントハンドラのメソッド名はプレフィックスを `Handle` にする（`Handle〇〇`）。イベントを処理するクラスの名前はサフィックスを `Handler` にする（`〇〇Handler`）
```C#
// メソッド名（EnemyAttackReservationUsecase.cs）
private void HandleReservedTimingReached()
// クラス名（TextEventHandler.cs）
public class TextEventHandler : IScenarioEventHandler<TextEvent>
```
- `CG-036` インターフェースは `I` で始める
- `CG-037` メソッドは動詞で始める
- `CG-038` bool型のプロパティは `Is` または `Has` で始める
- `CG-039` uxml, ussの命名はロウワーチェーンケース
- `CG-040` 名前空間は、レイヤーフォルダの先頭の番号を除いたフォルダ構成をそのまま反映する
```
Assets/Scripts/Runtime/1.Domain/InGame/Skill/SkillDefinition.cs
  → namespace KillChord.Runtime.Domain.InGame.Skill
Assets/Scripts/Runtime/6.Composition/InGame/Player/PlayerInitializer.cs
  → namespace KillChord.Runtime.Composition.InGame.Player
```

### その他
- `CG-050` `using` ディレクティブはファイルの先頭にまとめて記述する
- `CG-051` フィールド変数を公開する際はプロパティを使用する。変更を伴う場合は `public set` ではなく、`SetXxx` / `RecordXxx` のような意図が伝わるメソッドを介する
```C#
public bool IsCompleted => _isCompleted;

public bool Complete()
{
    if (_isCompleted) { return false; }
    _isCompleted = true;
    return true;
}
```
- `CG-052` アクセス修飾子は必ず明示的に記述する
- `CG-053` マジックナンバーは排除し、代わりに定数で定義する
- `CG-054` 一行のブロックの場合でも以下のように、波カッコを使用する
{ Console.WriteLine("message"); }
- `CG-055` 1ファイルには1つの公開型（class / struct / interface / enum）のみを定義し、ファイル名を型名と一致させる。同じファイルに複数の型が要る場合は、片方をもう一方のプライベートなネスト型にする
- `CG-056` ServiceLocatorから依存を取得するときは、必須の依存は `GetInstance`、無くてもよい依存は `TryGetInstance` を使う（設計思想の DP-302 を参照）

### UnityAPI
- `CG-070` データコンテナとして使用するScriptableObject（CreateAssetMenuが付く物）は
外部からのアクセスにはパラメータを読み取り専用にして
SerializeFieldのみで変更できるようにする

### ログ出力
- `CG-080` `Debug.LogError` / `Debug.LogWarning` は `[{nameof(ClassName)}] メッセージ` の形式で書き、MonoBehaviourの場合は第2引数に `this` を渡す
```C#
Debug.LogError($"[{nameof(IngameComposition)}] バトルステージが選択されていません。", this);
```
- `CG-081` この形式に従っていない古いログ（特に `5.InfraStructure` と `4.View` の一部）は、見つけても修正を必須としない。新しく書くログは必ず CG-080 の形式にする
