# シーンマネージャー

- page_id: 39e7c2c6-cc02-8009-8530-cc10a4396fa5
- Notion パス: Symphony Kill Chord / システム概要 / シーンマネージャー
- スナップショットの最終更新: 2026-08-24T13:51:08.206Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 一部古い — クラス表・依存関係の骨格は正しいが、`SceneTransitionController` を「薄いパススルー」としている点、`EventNotificationView`/`EventNotificationInitializer`/`LoadingTip`/`OutGameInitializationFailureView` が無い点が古い。根拠: `Assets/Scripts/Runtime/3.Adaptor/Persistent/SceneManagement/SceneTransitionController.cs:12-240`（常駐寿命トークン、`~WithPersistentLifetimeAsync`、シナリオ戦闘出撃の専用経路）、`6.Composition/Persistent/SceneManagement/EventNotificationInitializer.cs:17`（Order 40）、`SceneTransitionInitializer.cs:35,43-72,241-245`、`Assets/Level/Scenes/Master/Persistent.unity:2361-2362`、PR #1529 / #1874 / #1575 / #1605
- 整合性:
  - 実装との矛盾: `SceneTransitionController` は薄いパススルーではない（常駐寿命・シナリオ戦闘出撃の受付・初期化停止・決着・手動復帰を持つ）
  - 実装との矛盾: 初期化失敗時の復帰画面（IMGUI）が記載されていない。50_ の IF-03 は「文言は日本語直書き・ローカライズ対象外」としているが、現行コードは保存済みの言語設定が English なら英語文言を渡す（`OutGameSceneInitializer.cs:224-235`、`SceneTransitionInitializer.cs:61-67`）。日英ともコード直書きで、Unity Localization のテーブルは使っていない
  - 他ページとの関係: ロード画面の Tips 内容・演出値は新規「仕様概要 / ロード画面」（OG-217、別担当）の範囲。本ページはクラス構成と編集方法だけを持つ
  - 他ページとの関係: 初期化フェーズの説明は「システム概要 / 初期化ライフサイクル」と重なる。IF-03 の反映先候補は両ページだが、本原稿ではシーンマネージャー側に「初期化失敗時の復帰」節を置いた（初期化ライフサイクル側からはリンクだけにするのがよい）
- 変更点:
  - 「概要」表: 旧: 最終更新日 2026-08-23 → 新: 2026-09-22
  - 「🏗️ クラス」: 旧: `SceneTransitionController` = 「薄いパススルー」 → 新: 常駐寿命トークンとシナリオ戦闘出撃の専用経路を持つ窓口
  - 「🏗️ クラス」: `LoadingTip`、`EventNotificationView`、`OutGameInitializationFailureView`、`EventNotificationInitializer` を追加。`LoadingTipsConfig` に Addressables 登録と Planner Master Data からの編集を追記
  - 「🧩 Composition初期化情報」: `EventNotificationInitializer`（Order 40）と、`SceneTransitionInitializer` の設定値（初期化待機 3600 フレーム、ロード画面の最低表示 4 秒）を追記
  - 「📤 依存されているもの」: Title の参照箇所を現行 API（`LoadAdditiveAsync` / `UnloadWithPersistentLifetimeAsync`）に更新。常駐通知 View の利用者（Title・Demo）と Scenario の専用出撃経路を追加
  - 「⑤ Infrastructure」「⑥ Composition」: 常駐寿命と手動復帰画面の所有を追記
  - 新節「⚠️ 初期化失敗時の復帰」を処理フローの前に追加（IF-03）
- 織り込んだ反映項目: IF-04, IF-03, OG-217（クラス構成と Tips の編集方法のみ）
- 出典: 実装（上記ファイル）、`Assets/Level/Scenes/Master/Persistent.unity:2361-2362`、`ProjectSettings/SourceDataProviderSettings.asset:59,153-156`、`Assets/AddressableAssetsData/AssetGroups/GameData.Shared.asset:139`、PR #1529（初期化失敗時の復帰）、#1874（通知）、#1575（Planner Master Data）、#1605（Tips にタイトル）、commit b17f82dca / 180d02f2c（50_ IF-04 の記載）
- 決定（2026-09-22 八幡）: R54 全表示を日英に対応させる（#2067）。「⚠️ 初期化失敗時の復帰」の文言は既に日英に対応している（コード直書き）ので本文は変えず、「要確認」の「復帰画面の文言を Localization テーブルへ移すか」を消した
- 要確認:
  - 復帰画面の実機表示は未確認（#1529 本文に未実施とある）→ 要実機確認
  - LoadingTip を編集する Planner Master Data のページ名（IF-04 は「UI」ページとするが、コードで確認できなかった）→ ツール担当に確認

## 適用する本文

- 概要: シーンのマネジメントモジュール
- カテゴリー: 常駐

---

## 概要
> 💡 **モジュール概要**
> シーンの追加ロード・アンロード・切り替えと、ロード画面の表示・非表示を司る常駐モジュールである。単にシーンオブジェクトグラフの読み込みが終わるだけでなく、遷移先シーンの初期化モジュール（`Init→ResourceLoadAsync→Build→Ready`）が完了するまでロード画面を表示し続ける。常駐シーンの寿命に従うシーン操作、シナリオ終了後の戦闘出撃の専用経路、初期化失敗時の復帰画面、操作を保留する中央通知もこのモジュールで扱う。
| 項目 | 内容 |
|---|---|
| **モジュール名** | SceneManagement |
| **カテゴリ** | Persistent |
| **ステータス** | 実装済み |
| **最終更新日** | 2026-09-22 |
---

### 🏗️ クラス
| クラス名 | レイヤー | 役割・機能 |
|---|---|---|
| `**ISceneTransitionService**` | Application | シーンロード処理そのものの抽象（実装はInfrastructure層） |
| `**ISceneInitializationReadiness**` | Application | シーン名をキーに初期化完了を追跡・待機する契約 |
| `**SceneInitializationReadinessRegistry**` | Application | `ISceneInitializationReadiness`実装。シーン名ごとの完了状態を保持し、フレームポーリングで待機する。上限フレーム数を超えると失敗（`false`）として返す |
| `**SceneTransitionUsecase**` | Application | シーンロードと初期化完了待機を1つの継続的な処理としてまとめるユースケース |
| `**ILoadingOperationExecutor**` | Application | ロード画面付き処理の実行機能の抽象 |
| `**LoadingOperationExecutor**` | Application | ロードセッションの開始〜完了/失敗までを管理する実装。成功時はロード画面の最低表示時間を待ってから完了させる |
| `**ILoadingSession**`** / **`**ILoadingSessionFactory**` | Application | ロードセッションの抽象契約 |
| `**LoadingExecutionOptions**`** / **`**LoadingProgressRange**` | Application | ロード進捗の範囲・継続オプションを表す値オブジェクト |
| `**SceneTransitionController**` | Adaptor | Viewからのシーン遷移要求を受け取り`SceneTransitionUsecase`へ委譲する窓口。常駐シーンの寿命トークン（`PersistentLifetimeToken`）と、それに従う`~WithPersistentLifetimeAsync`系のAPI（Change/Reload/Unload/UnloadAndSetActive/KeepingLoading）を持つ。シナリオ終了後の戦闘出撃は専用経路（`TryBeginScenarioBattleSortie`〜`SettleScenarioBattleInitializationAsync`〜`EndScenarioBattleSortie`）で1件だけ受け付け、進行中は`HasScenarioBattleSortie`が`true`になる |
| `**LoadingScreenController**` | Adaptor | ロードセッションの開始・成功・失敗を管理し`LoadingStarted`/`LoadingCompleted`イベントを発行 |
| `**SceneTransitionView**` | View | シーン遷移状態のデバッグ表示 |
| `**LoadingScreenView**` | View | ロード画面本体の表示/非表示（`LoadingScreenController`のイベントを購読）。表示中はTipsを1件選んで出す |
| `**LoadingTipsConfig**` | View | ロード画面に表示するTipsを保持するScriptableObject。`GetRandomTip()`でランダムに1件返す。Addressables（`GameData.Shared`、アドレス`LoadingTipsConfig`）に登録され、SourceDataProviderのCollection`LoadingTip`としてPlanner Master Dataから編集する |
| `**LoadingTip**` | View | Tips1件分のデータ（タイトルと本文） |
| `**EventNotificationView**` | View | 操作を保留する中央通知。非スケール時間で3秒表示してから0.5秒でフェードアウトし、完了まで後続の処理を待たせる。体験版の期限切れ通知やセーブデータリセット通知に使う |
| `**OutGameInitializationFailureView**` | View | シーン初期化に失敗したとき、通常のUIやロード済みアセットに依存せず（IMGUI）復帰操作を表示する |
| `**SceneTransitionService**` | Infrastructure | `ISceneTransitionService`実装。実際に`SceneLoader`（Unity）を呼び出す |
| `**SceneTransitionInitializer**` | Composition | 上記スタック一式の構築・ServiceLocatorへの登録。シナリオ戦闘出撃が失敗したときの手動復帰画面も所有する |
| `**EventNotificationInitializer**` | Composition | 常駐通知View（`EventNotificationView`）を入力初期化の前にServiceLocatorへ公開し、常駐終了時に解除する |
  > ロード画面まわりのクラス（`ILoadingOperationExecutor`・`LoadingOperationExecutor`・`ILoadingSession`・`ILoadingSessionFactory`・`LoadingExecutionOptions`・`LoadingProgressRange`・`LoadingScreenController`・`LoadingScreenView`・`LoadingTipsConfig`・`LoadingTip`・`EventNotificationView`）は`Persistent/Load/`配下にある。シーン遷移と一体で動くため、独立したモジュール文書は設けず本書で扱う。`OutGameInitializationFailureView`は`4.View/OutGame/Screen/`にある。

#### 🧩 Composition初期化情報
| 項目 | 内容 |
|---|---|
| **Initializerクラス** | `SceneTransitionInitializer`／`EventNotificationInitializer` |
| **Order** | 0（`SceneTransitionInitializer`。Persistentシーン内で最初に初期化される）／40（`EventNotificationInitializer`） |
| **公開する ModuleContainer / ServiceLocator登録型** | 専用の`ModuleContainer`は無し。`LoadingScreenController`、`ILoadingSessionFactory`、`ILoadingOperationExecutor`、`ISceneTransitionService`、`ISceneInitializationReadiness`、`SceneTransitionUsecase`、`SceneTransitionController`、`SceneTransitionInitializer`自身、`EventNotificationView`を個別にServiceLocatorへ登録 |

| 設定値 | 値 | 単位 | 正本 |
|---|---|---|---|
| 初期化完了の待機上限（`_sceneInitializationTimeoutFrameCount`） | 3600 | フレーム | `Assets/Level/Scenes/Master/Persistent.unity`（`SceneTransitionInitializer`） |
| ロード画面の最低表示時間（`_minimumLoadingScreenDisplayTime`） | 4 | 秒 | 同上（コード既定値は0.8） |
| 中央通知の表示時間 | 3 | 秒 | `EventNotificationView.cs`（`HOLD_SECONDS`） |
| 中央通知のフェードアウト | 0.5 | 秒 | `EventNotificationView.cs`（`FADE_SECONDS`） |
---

### 🔗 モジュール結合
```Mermaid
graph TD
    %% 定義 (接続のないレイヤーは省略)
    subgraph SceneManagementModule [SceneManagement モジュール]
        SM_App["Application<br>SceneTransitionUsecase, ISceneInitializationReadiness"]
        SM_Adaptor["Adaptor<br>SceneTransitionController, LoadingScreenController"]
        SM_View["View<br>LoadingScreenView, EventNotificationView"]
        SM_Composition["Composition<br>SceneTransitionInitializer, EventNotificationInitializer"]
        SM_App --> SM_Adaptor
        SM_Adaptor --> SM_Composition
        SM_View --> SM_Composition
    end

    subgraph AllScenesModules [各シーンのルート初期化クラス群]
        AS_Composition["Composition<br>OutGameSceneInitializer, IngameComposition, TitleSceneInitializer 等"]
    end

    subgraph TitleModule [Title モジュール]
        T_Adaptor["Adaptor<br>TitleStartController"]
    end

    subgraph ResultModule [Result モジュール]
        R_Adaptor["Adaptor<br>StageResultController"]
    end

    subgraph DemoModule [Demo]
        D_Bootstrap["DemoRuntimeBootstrap"]
    end

    %% 依存関係
    AS_Composition -->|"Init→ResourceLoadAsync→Build→Readyの完了を通知"| SM_App
    T_Adaptor -->|"シーン遷移要求"| SM_Adaptor
    R_Adaptor -->|"シーン遷移要求"| SM_App
    D_Bootstrap -->|"シーン遷移・通知"| SM_Adaptor
```

#### 📥 依存しているもの
- なし
  - *詳細*: 本モジュールは他モジュールのDomain/Application型に依存しない、独立した基盤モジュールである。復帰画面の言語を決めるため、ロード済みのセーブデータ（`SaveData.EnvironmentSettings.Language`）だけを参照する

#### 📤 依存されているもの
- **各シーンのルート初期化クラス（**`**OutGameSceneInitializer**`**、**`**IngameComposition**`**、**`**TitleSceneInitializer**`**等）**
  - *参照箇所*: `ISceneInitializationReadiness.BeginTracking`/`Complete`
  - *詳細*: 各シーンのComposition初期化が完了した際にこのレジストリへ完了を通知する。通知が来るまで、シーン遷移を要求した側のロード画面は表示され続ける
- `**Title**`
  - *参照箇所*: `SceneTransitionController.LoadAdditiveAsync`/`UnloadWithPersistentLifetimeAsync`、`EventNotificationView.ShowAsync`
  - *詳細*: `TitleStartController.StartGameAsync`が遷移先を追加ロードしたあと、常駐シーンの寿命でTitleシーンをアンロードする（Titleのアンロードで自身のViewが破棄されるため）。シナリオ戦闘出撃の進行中は開始しない。セーブデータリセット成功時は中央通知を出す
- `**Result**`
  - *参照箇所*: `SceneTransitionUsecase.UnloadThenChangeSceneAsync`/`UnloadThenReloadSceneAsync`
  - *詳細*: `StageResultController`の完了/リトライボタンから使用される
- `**StageSelect**`** / **`**Scenario**`** / **`**Sortie**`
  - *参照箇所*: `SceneTransitionUsecase.LoadAdditiveAsync`/`ChangeSceneAsync`、`SceneTransitionController`のシナリオ戦闘出撃API
  - *詳細*: 出撃・シナリオ再生開始時のシーン遷移に使用される。シナリオ終了後にそのまま戦闘へ出撃するときは、専用経路で1件だけ受け付け、InGameの初期化停止と決着を管理する
- `**Demo**`
  - *参照箇所*: `SceneTransitionController`、`EventNotificationView`
  - *詳細*: 体験版の期限切れ・ホームタイマー満了の通知と、DemoEndへの遷移に使用される
---

## 詳細

### 🧅レイヤー情報

#### ① Domain
当モジュールでは使用していない。

#### ② Application
`SceneTransitionUsecase`がシーンロードと初期化完了待機を1つの継続処理として扱い、`ISceneInitializationReadiness`がシーン名ごとの完了状態を管理する。`ILoadingOperationExecutor`/`LoadingOperationExecutor`がロードセッションの実行制御を担う。

#### ③ Adaptor
`SceneTransitionController`がViewからの要求を仲介し、`LoadingScreenController`がロードセッションの開始・成功・失敗イベントを発行する。`SceneTransitionController`は常駐シーンの寿命トークンを持ち、遷移元シーンが破棄されても遷移先の初期化とロード画面の終了まで処理を続けられる。

#### ④ View
`LoadingScreenView`がロード画面の表示/非表示を、`SceneTransitionView`がデバッグ表示を担当する。`EventNotificationView`が操作を保留する中央通知を、`OutGameInitializationFailureView`が初期化失敗時の復帰画面を表示する。

#### ⑤ Infrastructure
`SceneTransitionService`が実際のUnityシーンロード（`SceneLoader`）を呼び出す。`ChangeSceneAsync`はロードとアンロードだけを行い、アクティブシーンは切り替えない。切り替えるのは`UnloadAndSetActiveAsync`と`ReloadSceneAsync`の2つで、いずれも`TrySetActiveScene`で成否を確認し、失敗した場合はエラーログを出して`false`を返す。`ReloadSceneAsync`は再読み込み中の退避先として、別のシーンを一時的にアクティブへ切り替える。

#### ⑥ Composition
`SceneTransitionInitializer`（Order 0）が上記スタック全体を構築し、既存インスタンスが無い場合のみ新規登録する（Persistentシーンの再読み込み等での二重登録を防止）。常駐シーンの寿命トークンを作り、終了時に取り消す。`EventNotificationInitializer`（Order 40）が常駐通知Viewを入力初期化の前に公開する。

### 🔌 拡張ポイント
> 現状、ポリモーフィックな拡張ポイント（`SubclassSelector`等）はない。ロード画面のTipsを増減させる場合は、Planner Master Dataから`LoadingTip`を編集する（コードの変更は要らない）。【要確認: Planner Master Data 上のページ名をツール担当に】

### ⚠️ 初期化失敗時の復帰
- OutGame・Titleシーンの初期化に失敗すると、そのシーンの`UIDocument`を無効にし、`OutGameInitializationFailureView`（IMGUI）の復帰画面を出す（`OutGameSceneInitializer`）。
- 文言は「画面の読み込みに失敗しました。」「タイトルへ戻る」（Titleシーン自身の失敗時は「もう一度読み込む」）「画面を読み込めませんでした。もう一度お試しください。」「読み込み中…」である。ロード済みの言語設定がEnglishなら英語の文言を出す。文言はコードに直書きで、Localizationテーブルは使わない（Localization自体の初期化失敗でも表示できるようにするため）。
- マウスまたはEnterで復帰する。OutGameからはタイトルへ遷移し、Titleシーン自身の失敗時は再読み込みする。いずれも常駐シーンの寿命で実行する。
- 次の間は復帰を始めない: シナリオ戦闘出撃の専用処理が進行中、元のロードセッションが終わっていない、常駐シーンの寿命が取り消し済み。連打は受け付けない。
- シナリオ戦闘出撃が失敗したときは、常駐シーンの`SceneTransitionInitializer`が同じ復帰画面を1つだけ所有し、入力を無効にしてから表示する。

### 🔄処理フロー
主要な処理フローは、それぞれ子ページに分けている。
- 📄 [[① シーン追加ロード＋初期化完了待機フロー]] (3bf7c2c6-cc02-8138-8850-c3dd0d72ad24)
- 📄 [[② ロードセッションの成功/失敗通知フロー]] (3bf7c2c6-cc02-81a8-b788-d28cdbfa017d)
