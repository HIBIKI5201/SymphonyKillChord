# ① 通常バトル出撃フロー（ステージ選択→戦闘準備画面）

- page_id: 3bf7c2c6-cc02-8195-beb6-eb3f24920cc8
- Notion パス: Symphony Kill Chord / システム概要 / ステージセレクト / ① 通常バトル出撃フロー（ステージ選択→戦闘準備画面）
- スナップショットの最終更新: 2026-08-17T17:36:24.695Z
- 書き込み許可: 可（システム概要 配下）
- 処置: 本文置き換え（ページタイトルも変更: 「① 通常バトル出撃フロー（ステージ選択→バトル開始）」）
- 確認日 / 確認したコミット: 2026-09-22 / 73fefeb45

## 判定

- 信頼性: 陳腐化 — 最後の段が「戦闘準備画面を表示（IOutGameSortieOutputPort）」のまま。戦闘準備画面は 2026-09-16 の commit b219a1de3「作戦準備画面の廃止」でバトル直行に変わり、PR #1839（2026-09-18）で実装ごと削除された。現在は `OutGameSortieUseCase.RequestSortieAsync` がバトルなら `StartBattle()` を呼び、`ScreenInitializer` が InGame シーンへ遷移する（OG-102）
- 整合性:
  - 実装との矛盾: `IOutGameSortieOutputPort` に戦闘準備画面の表示は無い（`Assets/Scripts/Runtime/2.Application/OutGame/Sortie/IOutGameSortieOutputPort.cs`）
  - 追加の事実: 出撃ボタンを押すと、まず自動遷移の予約（`ReserveNodeTransitionChain`）を行う（`StageSelectInitializer.cs:660`）。ミッション・シーン設定が不正で選択状態を作れない場合は、予約を破棄して何もしない（`:661-668`）
  - 他ページ: 親 `ステージセレクト`（39e7c2c6-cc02-80c3-b671-d3162c5f9d3b）のリンク文字も変更する（親の原稿に反映済み）。ページタイトルを変えるので、module-docs.md の手順（タイトルで突き合わせる同期スクリプト）はもう使わない前提（02 §2.6 T-8）
- 変更点:
  - ページタイトル: 旧: 「ステージ選択→戦闘準備画面」 → 新: 「ステージ選択→バトル開始」
  - 導入文: 出撃ボタンの後、戦闘準備画面を挟まずにバトルが始まることを追記
  - シーケンス図: 旧: 最後の段「戦闘準備画面を表示（IOutGameSortieOutputPort）」 → 新: 「StartBattle()」→`OutGameUIEvent.OnStartGame`→`ScreenInitializer`が InGame シーンへ遷移。予約と選択状態の設定の段を追加
- 織り込んだ反映項目: OG-102, OG-101（システム側）
- 出典: `Assets/Scripts/Runtime/6.Composition/OutGame/StageSelect/StageSelectInitializer.cs:643-745`、`Assets/Scripts/Runtime/2.Application/OutGame/Sortie/OutGameSortieUseCase.cs:31-49`、`Assets/Scripts/Runtime/6.Composition/OutGame/Sortie/OutGameSortieOutputPort.cs:49-52`、`Assets/Scripts/Runtime/6.Composition/OutGame/Screen/ScreenInitializer.cs:436,569-619`、PR #1839、commit b219a1de3
- 要確認: なし

## 適用する本文

プレイヤーがステージノードを選択し、出撃ボタンを押してバトルが始まるまでの流れである。戦闘準備画面は廃止済みで、出撃ボタンを押すとそのままバトルを開始する。装備の確認は、ステージ詳細画面の装備スキル表示と「編成」ボタン（改造画面へ移る）で行う。
```Mermaid
sequenceDiagram
    autonumber
    actor Player as プレイヤー
    participant NodeView as StageNodeView
    participant Init as StageSelectInitializer
    participant Controller as StageSelectController
    participant SortieCtrl as OutGameSortieController
    participant SortieUC as OutGameSortieUseCase
    participant Port as OutGameSortieOutputPort
    participant Screen as ScreenInitializer

    Player ->> NodeView: ノードをクリック
    NodeView -->> Init: OutGameUIEvent.OnStageNodeSelected
    Init ->> Controller: OnStageNodeSelected(nodeId)
    Controller ->> Controller: StageDetailPresenter.Push → 詳細画面表示
    Player ->> Init: 出撃ボタン押下 (OnSortieRequested)
    Init ->> Init: 自動遷移の予約（ReserveNodeTransitionChain）
    Init ->> Init: SelectedBattleStageState / SelectedMissionState を設定（BattleSortieSelectionService）
    Init ->> SortieCtrl: RequestSortieAsync(Battle, ...)
    SortieCtrl ->> SortieUC: RequestSortieAsync
    SortieUC ->> Port: StartBattle()
    Port -->> Screen: OutGameUIEvent.OnStartGame
    Screen ->> Screen: InGameシーンへ遷移（ロード画面を保ったまま）
```
