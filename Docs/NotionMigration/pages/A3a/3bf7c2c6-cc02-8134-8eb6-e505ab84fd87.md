# ① 画面遷移フロー（ボタン押下→新画面表示）

- page_id: 3bf7c2c6-cc02-8134-8eb6-e505ab84fd87
- Notion パス: Symphony Kill Chord / システム概要 / スクリーン / ① 画面遷移フロー（ボタン押下→新画面表示）
- スナップショットの最終更新: 2026-08-24T13:49:49.314Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 一部古い — 流れは現行どおり。`ShowStageSelect(token)` はトークン引数が無くなっている。完了イベント `OnStageSelectScreenCompleted` は `ScreenInitializer` ではなく `StageSelectScreenView` がフェード完了時に自分で発火する。根拠: `Assets/Scripts/Runtime/3.Adaptor/OutGame/Screen/ScreenController.cs:59-61`、`Assets/Scripts/Runtime/6.Composition/OutGame/Screen/ScreenInitializer.cs:505-516`、`Assets/Scripts/Runtime/4.View/OutGame/Screen/StageSelectScreenView.cs:44-57`、`HomeScreenView.cs:230-237`
- 整合性: 親ページ「スクリーン」（39e7c2c6-cc02-8012-b554-eb7b8e326fe5）の原稿と整合させた
- 変更点:
  - シーケンス図: 旧: `ShowStageSelect(token)` → 新: `ShowStageSelect()`
  - シーケンス図: 旧: `Registry -->> Init: トランジション完了` / `Init -->> Init: OnStageSelectScreenCompleted 発火` → 新: `StageSelectScreenView` がフェード完了後に `OnStageSelectScreenCompleted` を発火
  - シーケンス図: `ScreenViewApplicator` を Presenter と Registry の間に追加
- 織り込んだ反映項目: なし（実装照合による修正）
- 出典: 実装（上記ファイル）
- 要確認: なし

## 適用する本文

Home画面からStageSelect画面への遷移を例にした標準的な流れである。
```Mermaid
sequenceDiagram
    autonumber
    actor Player as プレイヤー
    participant Home as HomeScreenView
    participant Init as ScreenInitializer
    participant Controller as ScreenController
    participant UseCase as ShowScreenUseCase
    participant Presenter as ScreenPresenter
    participant Applicator as ScreenViewApplicator
    participant Registry as ScreenViewRegistry
    participant StageSelect as StageSelectScreenView

    Player ->> Home: 「作戦」ボタン押下
    Home -->> Init: OutGameUIEvent.OnShownStageSelectionScreen
    Init ->> Controller: ShowStageSelect()
    Controller ->> UseCase: Execute(ShowScreenCommand(StageSelect))
    UseCase ->> UseCase: ルール取得・履歴更新・表示/非表示を判定
    UseCase ->> Presenter: Present(ScreenTransitionResult)
    Presenter ->> Applicator: ScreenViewDTO
    Applicator ->> Registry: Hide(Home) → Show(StageSelect)
    Registry ->> StageSelect: Show()
    StageSelect -->> StageSelect: フェード完了後に OutGameUIEvent.OnStageSelectScreenCompleted 発火
```
---
