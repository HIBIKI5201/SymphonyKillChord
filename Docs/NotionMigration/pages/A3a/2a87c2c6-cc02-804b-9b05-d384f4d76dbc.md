# GitHub 運用規定

- page_id: 2a87c2c6-cc02-804b-9b05-d384f4d76dbc
- Notion パス: Symphony Kill Chord / システム概要 / GitHub 運用規定
- スナップショットの最終更新: 2026-09-09T03:25:14.661Z
- 書き込み許可: 可（システム概要 配下）
- 処置: 本文置き換え
- 確認日 / 確認したコミット: 2026-09-22 / 73fefeb45

## 判定

- 信頼性: 一部古い — ブランチ構成・AutoCreateMasterBranch・ValidateFeaturePRTarget・PR 作成ガイドラインの本文は現行の `.github/workflows/*.yml` と `.github/PULL_REQUEST_TEMPLATE.md` に合う。一方で、feature ブランチの命名、自動 Draft PR、AI エージェントの運用、develop の保護ルール、CodeRabbit が書かれていない（RL-08）。PR 作成ガイドラインの節番号が「0 / 1 / 1 / 3」と崩れている（RL-09）
- 整合性:
  - 命名の実態: 2026-08-15〜09-22 のマージ済み PR 506 件の作業ブランチ先頭語は feature 263 / hotfix 161 / agent 40 / develop 29 / fix 3 / designer 3 / spec 2 / chore 2（`prs-list.tsv`）。feature の 2 階層目は demo 224 / release 19 / research 14 / tools 2 などで、レイヤーではなく開発段階になっている
  - 他ページとの矛盾: `クライアント部門 Unityプロジェクト開発規定`（34a7c2c6-cc02-8007-a640-cc8a457ddd69）は `feature/[段階]/[プロダクト名]/[個人名]` とし、本ページの旧記述「feature/レイヤー/作業名/作業者」と食い違っていた。実態は開発規定の方に合うので、本ページを実態に合わせた
  - 他ページとの矛盾: `コード規定`（2a87c2c6-cc02-80f9-a10a-f0b72b95fbb3）の「AI によるファイル変更はサマリー・コメント・タイポに留める」と、`AGENTS.md:6-15` の AI エージェント運用（機能実装を含む）が食い違う（RL-07）。本ページの新節「AI エージェントの運用」は AGENTS.md の事実を書き、許可範囲は【要確認】とした
  - main への反映: 旧記述は「リリースやバーティカルスライスの完成時に develop からマージ」。実際は同期間に develop→main の PR が 23 件あり、TGS 前は 1 日に複数回反映していた（例 #1627〜#1787、2026-09-16）。main への push でホームページが、develop / main への PR マージでビルドとリリースが自動で走るため、main への反映は配布物の更新を兼ねている
  - 実装との矛盾: `.coderabbit.yaml:7` はレビュー規約として存在しない `Assets/Docs/AGENTS.md` を指している（02 §2.6 T-6 で修正予定。本文には書かない）
  - PR ガイドラインの適用範囲: 本文は「マスターブランチから develop へ PR を送る際にのみ」とするが、hotfix（161 件）・agent（40 件）は作業ブランチから develop へ直接 PR を出しており、PR テンプレートも全 PR に自動挿入される（RL-09）
- 変更点:
  - 「ブランチ / 構成 / main ブランチ」: マージの説明に、配布物の更新として develop から随時反映している実態を追記
  - 「ブランチ / 命名規則」: 旧: 「featureはfeature/レイヤー/作業名/作業者」 → 新: 「featureはfeature/段階/機能名/作業者」。agent ブランチの命名を追加
  - 「ブランチ / 自動化」: AutoCreateDevelopPullRequest、Unity Build and Release、ホームページのデプロイ、CodeRabbit の自動レビュー、develop の保護ルールを追加（RL-08）
  - 「PR作成ガイドライン」: 見出しの番号を直した。旧: 「1. PR作成後の必須フロー」「3. PRテンプレートについて」 → 新: 「2. PR作成後の必須フロー」「3. PRテンプレートについて」（RL-09）
  - 「PR作成ガイドライン / 0. PR提出前の必須フロー」: C# ・シーンを変えない PR での必須チェックの扱いを【要確認】として追加（RL-09）
  - 「PR作成ガイドライン / 目的」: 適用範囲（master→develop のみか、develop 向けの全 PR か）に【要確認】を追加
  - 新節「AI エージェントの運用」を既存の節の後ろに追加（RL-08、`AGENTS.md:6-15`）
- 織り込んだ反映項目: RL-08, RL-09（01_ドキュメント棚卸し の PR テンプレート・AGENTS.md の行も反映）
- 出典: `.github/workflows/AutoCreateMasterBranch.yml`・`ValidateFeaturePRTarget.yml`・`AutoCreateDevelopPullRequest.yml`（`pull_request_target: closed`、`feature/**/master`、`merged == true`、Draft、422「No commits between」なら作らない）・`BuildAndRelease.yml`（develop / main への PR の closed かつ merged、または手動実行）・`HomePageDeploy.yml`（main への push）、`.coderabbit.yaml`（`auto_review.enabled: true`、`drafts: false`、`language: ja-JP`、`profile: chill`）、`AGENTS.md:6-15`、`.github/PULL_REQUEST_TEMPLATE.md`、PR #1528 本文（develop の ruleset）、#1539（自動 Draft PR の追加）、#1827（必須チェックの適用範囲）、`prs-list.tsv`・`prs-all.json`（ブランチの集計）
- 要確認:
  - develop の保護ルール（PR 必須・承認 1 件・最終 push 後の別承認必須）は PR #1528 本文の記述。現在の GitHub の ruleset 設定を管理者が確認する（要実機確認）。main の保護ルールは不明
  - PR 作成ガイドラインの適用範囲と、CI・ドキュメント・ツールだけを変える PR での必須チェック（「PlayMode を通した」）の扱い（リードプログラマー判断。RL-09）
  - AI エージェントに許す作業の範囲（コード規定 CG-002 と同時に決める。RL-07）
  - 命名の「段階」に使ってよい語の一覧（demo / release / research / tools / docs など）を固定するか（リードプログラマー判断）

## 適用する本文

## ブランチ

### 構成

#### main ブランチ
- **目的**：安定して動作するバージョンの保持
- **内容**：
  - 動作確認済みのビルド版（Vertical Slice：垂直スライス）を保存
  - 最終的なバックアップとして機能
- **マージ**：リリースやバージカルスライスの完成時に `develop` からマージ
  - 配布ビルドやホームページを更新するときにも `develop` から随時マージする（main への反映でホームページのデプロイとビルドが自動で走るため。下記「自動化」参照）
---

#### develop ブランチ
- **目的**：最新の開発バージョンを統合
- **内容**：
  - 基本的には動作可能
  - 機能追加や修正によりバグが発生する可能性あり
- **マージ**：各 `feature` ブランチや `hotfix` ブランチからのマージ先
---

#### feature ブランチ
- **目的**：新機能や改善の開発
- **内容**：
  - 開発中の個別機能を隔離して作業
- **マージ**：完了後に `develop` へマージ
---

#### hotfix ブランチ
- **目的**：致命的なバグの即時修正
- **内容**：
  - `develop` へのマージ後などに発生した不具合に対応
- **マージ**：修正後は `develop`（必要に応じて `main` にも）へマージ

### 命名規則
- ロウワーチェーンケース
- featureはfeature/段階/機能名/作業者
  - 段階は開発の段階を表す（例: `demo`、`release`、`research`、`tools`）
  - 機能開発マスターブランチは feature/段階/機能名/master
  - AI エージェントが作業するときは、作業者の位置を `agent` にする（例: `feature/demo/home-tutorial/agent`）
- hotfixはhotfix/作業対象
- 機能ブランチの指定が無い AI エージェントの作業は agent/作業名（例: `agent/cbt-qa-sheet`）
```Mermaid
sequenceDiagram
    participant Main as main<br>安定版バックアップ
    participant Develop as develop<br>開発用マスター
    participant FeatureMaster as feature/機能名/master<br>機能開発マスターブランチ
    participant FeatureWorker as feature/機能名/作業者<br>機能開発個人ブランチ
    participant Hotfix as hotfix/修正A<br>修正ブランチ

    Note over Develop: 開発サイクル開始

    Develop->>FeatureMaster: チェックアウト
    FeatureMaster->>FeatureWorker: チェックアウト
    FeatureWorker->>FeatureWorker: 機能開発を行う
    FeatureWorker->>FeatureMaster: マージ
    FeatureMaster-->>Develop: 開発完了 → developへマージ

    Develop->>Hotfix: 不具合発生 → hotfixブランチ作成
    Hotfix->>Hotfix: 修正作業
    Hotfix-->>Develop: 修正完了 → developへマージ

    Develop-->>Main: 安定状態を確認 → mainへマージ
    Note over Main: バーティカルスライス（安定版）更新

```

### 自動化
上記のブランチ運用は、GitHub Actionsにより一部自動チェック・自動化されている。
- **AutoCreateMasterBranch**（`.github/workflows/AutoCreateMasterBranch.yml`）
`feature/`から始まるブランチが作成されると発火する。ブランチ名が4階層以上（`/`が3つ以上）かつ末尾が`master`でないことを条件に、同じ階層の`feature/.../master`ブランチが存在しなければ自動作成する。作業者が個人ブランチを切るだけで、対応する機能開発マスターブランチが用意される。
- **ValidateFeaturePRTarget**（`.github/workflows/ValidateFeaturePRTarget.yml`）
PRの作成・更新時に発火する。マージ元が`feature/`配下の個人ブランチ（4階層以上かつ末尾が`master`でない）の場合、マージ先が対応する`feature/.../master`ブランチであるかを検証し、結果をコミットステータス（`feature-pr-target`）として表示する。マージ先が不正な場合はステータスが失敗となり、正しいマージ先が提示される。
- **AutoCreateDevelopPullRequest**（`.github/workflows/AutoCreateDevelopPullRequest.yml`）
`feature/**/master` へのPRがマージされると発火する。その`master`から`develop`へのDraft PRを、PRテンプレートの構成の本文付きで自動作成する。同じ`master`から`develop`への開いているPRが既にある場合と、`develop`との差分が無い場合（「No commits between」）は作成しない。
AutoCreateMasterBranchは個人ブランチ作成時点の内容で`master`を作るため、個人ブランチと`master`が同じコミットのまま一度もマージが起きないことがある。この場合はDraft PRが作られないので、`develop`へのPRを手動で作成する。
- **Unity Build and Release**（`.github/workflows/BuildAndRelease.yml`）
`develop`または`main`へのPRがマージされると発火し、ビルドとリリースを行う。手動実行もできる。詳細はワークフローの「ビルドとリリース」ページを参照する。
- **Deploy Astro to GitHub Pages**（`.github/workflows/HomePageDeploy.yml`）
`main`へのpushで発火し、ホームページをビルドしてGitHub Pagesへデプロイする。詳細は [[ホームページ仕様]] を参照する。
- **CodeRabbit**（`.coderabbit.yaml`）
PRを日本語で自動レビューする。Draft PRはレビューしない。`.meta`・`.unity`などはレビュー対象から外している。
- **develop の保護ルール**
`develop`への変更はPR必須で、承認1件と、最後のpushの後の別の承認が必要である。【要確認: PR #1528 作成時点の設定。現在の ruleset を管理者が GitHub の設定画面で確認する】

## PR作成ガイドライン

### 目的
レビュアー（リードプログラマー）が差分やDiscordを探索することなく、
**このPR文だけでApprove/Reject/意思決定のいずれかを判断できる状態**にすること。
情報の多さではなく、判断に必要な事実が過不足なく揃っていることを優先する。
あくまでレビューを高速化するためのものであるため、マスターブランチからDevelopブランチにプルリクエストを送る際にのみこれらの手順を踏む。
【要確認: hotfix・agent などの作業ブランチも develop へ直接PRを出しており、PRテンプレートは全てのPRに挿入される。適用範囲を「master から develop へのPRのみ」とするか「develop へのPR全て」とするかをリードプログラマーに確認する】
---

### 0. PR提出前の必須フロー（最優先・例外なし）
以下の2つを満たさないPRは、内容の出来不出来に関わらず**提出禁止**とする。
1. **コンパイルエラーが無いことを確認する**
コンパイルエラーが残ったままのPRは論外。レビュー以前の問題であり、レビュアーの時間を奪う。
2. **変更が関係しうるシーンでPlayModeを最低1回通す**
コンパイルが通ることと、実際に動くことは別問題。致命的なバグ（起動不可・進行不能・クラッシュなど）を
レビュー前に自分の手で潰しておく。「関係しうるシーン」は、変更したスクリプト・アセットが
参照/使用されている全シーンを指す。複数ある場合は主要なものだけで済ませず、思いつく範囲は通すこと。
この2点は本文の「確認済みの内容」欄より前に、PRテンプレート冒頭のチェックボックスとして必須項目化されている。
チェックが入っていない・入れられない状態のPRは出さないこと。
【要確認: C#・シーン・アセットを変えないPR（CI・ドキュメント・ツールだけの変更）では「PlayModeを通した」にチェックできない。現在は本文で適用範囲を説明している（例 #1827）。このようなPRでの扱いをリードプログラマーに確認する】
---

### 1. 本文構成
以下の見出しで整形する。

#### 概要
何が起きていた不具合・要望で、何を実現したかを1〜2文で。

#### 原因
表面的な現象ではなく、技術的な原因（何と何にズレ・矛盾があったか）を書く。

#### 対処
実際に変更したコード・仕組みを具体的に。
クラス名、メソッド名、フラグ名などメモにあれば必ず含める。
「〜するように修正しました」で終わらせず、何を使ってどう実現したかまで書く。

#### 確認済みの内容
- 正常系・異常系・境界値のうち、メモに含まれるものを箇条書きで。
- どの方法で確認したか（PlayMode/実機/ユニットテストなど）を各項目に明記する。
- 影響範囲の確認（他機能への副作用チェック）があれば含める。

#### 未確認・残論点
- 確認できていない項目を隠さず明記する。
- 「多分大丈夫」で終わらせず、そう考える技術的根拠がメモにあれば併記する。根拠がなければ「根拠は未整理」と正直に書く。
- リードプログラマーの意思決定が必要な論点があれば、選択肢とそれぞれの利点・代償、自分の推しを添える。

#### 注意事項
- 断定的な自己弁護や過剰な謙遜は不要。事実ベースで簡潔に。
- 最後に「ご確認いただけますと幸いです」などの一言で締める。
- DevelopブランチへのPRの際にのみこれらを適用する
---

### 2. PR作成後の必須フロー
PRが承認され、マージする際に以下を行う。
1. 自分の作業ブランチ(名前ブランチ)の削除、マスターブランチの削除を行う
ブランチが増えすぎて、100を超えるなどの事態を防止する
2. リードプログラマーに対してマージを完了した旨を報告する
---

### 3. PRテンプレートについて
上記構成と必須チェック項目は、リポジトリの `.github/PULL_REQUEST_TEMPLATE.md` に反映済み。
GitHub上で新規PRを作成すると本文欄に自動挿入されるため、このガイドラインを見ながら手動整形する必要はない。
表示された項目を埋めるだけで、自然にこの構成に従うことになる。

## AI エージェントの運用
自律 AI エージェントのブランチ・PR 運用は、リポジトリの `AGENTS.md` で次のとおり定めている。
【要確認: AI エージェントに許す作業の範囲（コード規定「設定」の AI によるファイル変更の規則と食い違う）をリードプログラマーに確認する】
- 編集・修正・コミットは、指定された作業ブランチ（例: `feature/demo/just-judgement/agent`）で行う。レビュー対応も同じブランチで行う
- 同じ階層の `master` では直接編集・コミットしない。`master` は作業ブランチからのセルフマージでだけ更新する
- `feature/` で始まる作業ブランチは、同じ階層の `master` へセルフマージしてよい（例: `feature/demo/just-judgement/agent` → `feature/demo/just-judgement/master`）
- 作業ブランチから直接 `develop` へ PR を作らない。`master` への取り込み後、上記の AutoCreateDevelopPullRequest が `develop` 向けの Draft PR を作る
- `feature/` で始まらない作業ブランチ（`agent/〇〇` など）の PR 先は `develop` とする
- PR 本文は PR テンプレートの構成で書き、確認済み・未確認を正確に書く
- テストの実行と `develop` へのマージの可否は、そのときのユーザーの指示に従う
