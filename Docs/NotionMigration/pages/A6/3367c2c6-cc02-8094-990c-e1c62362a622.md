# 戦闘音楽

- page_id: 3367c2c6-cc02-8094-990c-e1c62362a622
- Notion パス: Symphony Kill Chord / ステータスリスト / 管理用アセットリスト / 戦闘音楽
- スナップショットの最終更新: 2026-07-27T09:40:07.488Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 一部古い — ゲーム側の BPM は 84（`Assets/Level/Scenes/Master/InGame.unity:84252-84253` `_testCue: BGM_Ingame` / `_testBpm: 84`、BT-07）。このページの「BPM90」と、BPM90 で計算した秒数（4 小節 約10.6秒 など）が食い違う。スキルに応じたループの組み替えは実装済み（`Assets/Level/Data/Master/InGame/Music/BgmSelectorLabelTableAsset.asset`、`Assets/Scripts/Runtime/1.Domain/InGame/Music/SkillBgmSelectorTable.cs`）。最終確認 2026-09-22 / 73fefeb45
- 整合性:
  - モーション発注ページ 5 件（拳銃系スキル 38e7c2c6-cc02-8005-80d7-f4b463c707a0 ほか）の「本ゲームの現状のBPMは84」は実装と一致する。60_アセット.md AS-11 は「戦闘音楽ページの値を正とする」としていたが、実装値は 84 である
  - 用語「BPM」(3a37c2c6-cc02-808d-a158-d4278e95046e、他領域) も「現状84」
- 変更点:
  - 概要: 旧: 「BPM90。」 → 新: 「BPM90。」に【要確認】を添え、ゲーム側の設定値が 84 であることを書いた（楽曲そのものの BPM は ACB を開かないと確かめられないため、値は書き換えていない）
  - 時間: 秒数は BPM90 での計算値であることを明記し、BPM84 での値を並べた（数値が 3 つ以上並ぶため表にした）。BPM90 の値も計算し直した（旧: イントロ・アウトロ 約10.6秒 / ループ 約42.2秒 → 新: 約10.7秒 / 約42.7秒。1 小節 = 4 拍として 4×4×60÷90 = 10.67 秒、16×4×60÷90 = 42.67 秒）
  - 実装状況（新しい節、末尾）: Cue 名、BPM の設定場所、ループ組み替えの実装（2 スキル分）を足した
- 織り込んだ反映項目: AS-03, AS-11, BT-07（参照のみ）
- 出典: `InGame.unity:84252-84253`、`Assets/Scripts/Runtime/0.Utility/Constant/MusicConstants.cs:11,17,31`、`BgmSelectorLabelTableAsset.asset`（`_originalLabel: SelectorLabel_S00`、`skill_01` → `SelectorLabel_S01`、`skill_02` → `SelectorLabel_S03`）
- 要確認:
  - 楽曲の BPM は 84 と 90 のどちらか（サウンド担当。ACB の中身とゲーム側の設定値 84 がずれていると、拍の判定と音楽が合わない）
  - 組み替えの対象が 2 スキルだけなのは体験版の範囲か（企画）
  - 心臓の鼓動が 1 小節に 2 回鳴る仕様が楽曲に入っているか（サウンド担当）

## 適用する本文

## 概要
インゲームで流れるBGM。
月光ソナタ 第三楽章の電子クラシック アレンジ。
BPM90。【要確認: ゲーム側の設定値は BPM84（`InGame.unity` の `_testBpm`）。楽曲の BPM と一致しているかをサウンド担当へ】

### 時間
イントロ、ループ、アウトロで構成される。

| 部分 | 小節数 | BPM90 のとき | BPM84 のとき |
| --- | --- | --- | --- |
| イントロ | 4小節 | 約10.7秒 | 約11.4秒 |
| ループ | 16小節 | 約42.7秒 | 約45.7秒 |
| アウトロ | 4小節 | 約10.7秒 | 約11.4秒 |

インゲーム開始時にイントロ、戦闘中はループがリピートされ、戦闘終了後のリザルト演出中はアウトロに移行する。
音楽中は心臓の鼓動が1小節に2回鳴る。

### ループの仕様
ループ部分に特殊仕様がある。
4小節を一つの単位として分割して、組み替え可能にする。
組み替えはCRIによって動的に行えるようにする。
4小節のうち1,3小節目は変わらず、2,4小節目の変更を基本とする。
例えば、小節の種類が A, B, C, Dとあった時に、
元の4小節が A B A C と並んでいた場合、
CRIによって2小節目をD、4小節目をBに置き換えると
A D A B となるようにする。
変更を行えるようにしたい理由は、[[31f7c2c6-cc02-8024-9186-c6e8c9e3030b]] のスキルコマンドと整合性を取るため。
装備しているスキルの種類によってプレイヤーが求めるリズムが変わるため、それに対応したフレーズがゲームに登場しやすくしたい。

### 実装状況
- 実装済み: Cue `BGM_Ingame`（CueSheet `CueSheet_0`）。
- ゲーム側の BPM は 84 で、全ステージ共通である（`Assets/Level/Scenes/Master/InGame.unity` の `MusicSyncInitializer`、`_testBpm: 84`）。
- ループの組み替えは、装備スキルに応じて CRI のセレクターラベルを切り替える方式で実装されている。対応表は `Assets/Level/Data/Master/InGame/Music/BgmSelectorLabelTableAsset.asset` にある。

| 装備スキル | セレクターラベル |
| --- | --- |
| （原曲） | `SelectorLabel_S00` |
| `skill_01` | `SelectorLabel_S01` |
| `skill_02` | `SelectorLabel_S03` |

- 【要確認: 組み替えの対象が 2 スキルだけなのは体験版の範囲か】
