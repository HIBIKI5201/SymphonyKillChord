# Unity Loop MCP (uloop-*)

- page_id: 3d67c2c6-cc02-81af-adb7-de390511bffe
- Notion パス: Symphony Kill Chord / システム概要 / Unity Loop MCP (uloop-*)
- スナップショットの最終更新: 2026-09-09T04:38:31.098Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 一部古い — 確認コミット 73fefeb45。載っている 8 ツールと record-input の削除（録画は Window > Unity CLI Loop > Recordings から行う: `.claude/skills/uloop-replay-input/SKILL.md:11`）は実装どおり。一方、`.claude/skills/` の uloop スキルは 18 個あり、control-play-mode / hot-reload / pause-point / record-video / replay-input / run-tests / set-game-view-size / simulate-keyboard / simulate-mouse-input / simulate-mouse-ui の 10 個が載っていない。「場所」欄が空。パッケージは `Packages/manifest.json:50` の `io.github.hatayama.uloopmcp`（unity-cli-loop の git）。Codex 用に同じ内容が `.agents/skills/uloop-*`（18 個）にある。V2→V3 の構文移行は #1505
- 整合性: 他ページとの矛盾は無い
- 変更点:
  - 「プロパティ」表の「場所」: 空欄 → パッケージとスキルの置き場所を記入した
  - 「説明」節: スキルの置き場所に `.agents/skills/uloop-*/`（Codex 用）を追加した。壊れたリンク「配下にツールごとのSKILL.md」（http://配下に… になっていた）を普通の文に直した
  - 「ツール一覧」節: 10 ツールを 1 節ずつ追加した（既存 8 ツールの後ろ、record-input の前）
  - 「uloop compile」節: `uloop fix` の存在を確認できなかったため【要確認】を付けた
- 織り込んだ反映項目: TL-15
- 出典: `.claude/skills/uloop-*/SKILL.md`（各 description）、`Packages/manifest.json:50`、PR #1505
- 要確認: `uloop fix`（ロックファイル除去）がV3に存在するか。`.claude/skills/uloop-compile/SKILL.md` には記載が無い（AI 基盤担当に確認）

## 適用する本文

# Unity Loop MCP (uloop-*) {color="gray_bg"}

## プロパティ
| プロパティ | 値 |
|---|---|
| カテゴリー | 開発用 |
| 場所 | パッケージ: `Packages/manifest.json`の`io.github.hatayama.uloopmcp`（unity-cli-loop）/ スキル: `.claude/skills/uloop-*/`（Claude Code用）、`.agents/skills/uloop-*/`（Codex用） |
| 日付 |  |
| 概要 | AIエージェントからUnity Editorを外部操作するための`uloop`コマンド群について |
---

# 説明 {color="gray_bg"}
`npx uloop-cli`経由でUnity Editorを外部から操作する仕組みである。
Claude Code等のAIコーディングエージェントがCLI経由でUnity Editorへコマンドを発行し、コンパイル、シーン検査、動的コード実行、ログ取得、スクリーンショット撮影などを行える。
本プロジェクトでは`.claude/skills/uloop-*/`配下（Codex用は`.agents/skills/uloop-*/`配下）にツールごとのSKILL.mdとしてラップされており、エージェントは用途に応じて該当するSKILLを呼び出す。

# 詳細 {color="gray_bg"}

## 仕組み

### 前提
Unity Editorが起動しており、CLIから接続できる状態であることが前提となる。
Editorが未起動、またはクリーンな再起動が必要な場合は`uloop launch`を使用する。
- `-r, --restart`でUnityを再起動できる
- `-p, --platform`でビルドターゲットを指定できる
- Unityのバージョン検出、プロジェクトパスの探索、Unity Hubへの登録も行う

### コマンド共通仕様
各コマンドは`--project-path`オプションでプロジェクトパスを明示的に指定できる（省略時はカレントディレクトリを対象とする）。
V3のブール系オプションは値を取らないフラグ形式であり、存在すれば有効、存在しなければ無効となる。

## ツール一覧

### uloop launch
Unity Editorを起動する。既に起動している場合は既存ウィンドウにフォーカスし、起動が必要な場合はCLI操作が可能になるまで待機してから終了する。

### uloop compile
プロジェクトのコンパイルを実行し、エラー・警告件数を返す。
`--force-recompile`で強制的にフルリコンパイル（ドメインリロードを伴う）を行える。
`compiling.lock`等のロックファイルが残ってCLIが固まる場合は`uloop fix`でロックファイルを除去してから再試行する。【要確認: `uloop fix`がV3に存在するか。スキルの説明には載っていない】

### uloop execute-dynamic-code
既存のuloopツールでは調査・編集しきれない場合に、Unity APIを含むC#コードを動的に実行する。
シーン操作、プレハブ操作、SerializedObject経由の編集、AssetDatabaseのリフレッシュや.meta生成、メニュー実行、PlayMode自動化などに使用する。
`--code`（単行）または`--code-file`（複数行、PowerShellでの行落ち対策として推奨）でC#の直接記述（クラス/名前空間/メソッドのラップ不要）を渡し、コンパイル結果と実行結果、ログをJSONで受け取る。
`System.IO.*`やアセット作成系API（`AssetDatabase.CreateFolder`等）、`.cs`/`.asmdef`ファイルの作成・編集はコンパイル時に拒否される。

#### 運用上の注意
動的コード実行によるUnity API呼び出しは、対象APIによってはUnity Editor自体をフリーズさせるリスクがある。
特にNavMesh関連API（`AddNavMeshData`、`CalculateTriangulation`等）を経由した動的コード実行は、過去にUnityエディタのフリーズを引き起こしたことがある。
このようなリスクのあるAPIを動的コードから呼び出す場合は、影響範囲を切り分けたうえで慎重に実行し、事前にユーザーへ確認を取ることが望ましい。

### uloop find-game-objects
名前パターン・タグ・レイヤー・コンポーネントなどの条件でGameObjectを検索する。
`--search-mode Selected`を指定すると、Unity Editorで現在選択中のGameObjectの詳細（コンポーネントとそのプロパティ）を取得できる。
複数選択時は結果がインラインではなくファイル出力（`.uloop/outputs/FindGameObjectsResults/`）になる。

### uloop get-hierarchy
シーンの階層構造をツリー形式で取得する。
`--root-path`でルートを指定、または`--use-selection`で現在選択中のGameObjectをルートとして扱える。
取得結果はレスポンスに含まれず、指定されたJSONファイルへ書き出される。

### uloop get-logs
Unity Console上のログ（Log/Warning/Error）を取得する。
`--search-text`によるテキスト検索（正規表現も可）や、スタックトレースの取得・検索にも対応する。
コンパイル、テスト、PlayMode実行、動的コード実行の後に結果を確認する用途で使う。

### uloop clear-console
Unity Consoleのログをクリアする。
コンパイルやテスト、デバッグ作業の前に古いログが結果を紛らわせないよう、事前にクリアする用途で使う。

### uloop screenshot
Unity EditorのウィンドウやGame Viewの描画をPNGとして撮影する。
`--capture-mode rendering`ではPlayMode中のゲーム描画のみをキャプチャし、座標系がマウス入力シミュレーション（simulate-mouse系ツール）と一致する。
`--annotate-elements`（UI要素への注釈）や`--annotate-raycast-grid`（3Dレイキャスト候補点への注釈）を併用することで、クリック可能な座標をAIエージェントが特定しやすくなる。
ビジュアル確認、デバッグ、ドキュメント作成、UI要素の座標特定に使用する。

### uloop control-play-mode
PlayModeを操作する。Play（Resumeも同じ）・Stop・Pause・Stepのほか、状態を変えずに現在の状態だけを取得するStatusがある。実行時の挙動確認やフレーム単位の確認に使う。

### uloop hot-reload
メソッド本体の変更や、メソッド・フィールドの追加を、フルコンパイルせずにUnityへ反映する。同じアセンブリ内の新しいトップレベル型も反映できる。アセンブリをまたぐ利用、asmdefの変更、他アセンブリから参照されるメンバーの変更などは`uloop compile`が必要になる。

### uloop pause-point
ソースファイルの指定行でPlayModeを一時停止し、その時点のローカル変数・引数・インスタンスフィールドのスナップショットを返す。コードの編集や再コンパイルは不要である。不具合調査や、特定の処理が実行されたかの確認に使う。

### uloop record-video
Game ViewまたはEditorウィンドウを動画（H.264の`.mp4`、またはVP8の`.webm`）として録画する。アニメーションや画面遷移など、静止画では足りない確認に使う。録画中も他のuloopコマンドを使える。

### uloop replay-input
記録したPlayModeのキーボード・マウス入力（JSON）を再生する。ゲームプレイの正確な再現、E2Eの実行、同じ内容のデモに使う。記録ファイルはUnity Editorの **Window > Unity CLI Loop > Recordings** で作る（CLIからは記録できない）。

### uloop run-tests
Unity Test Runnerを実行し、結果の詳細を返す。EditModeとPlayModeのテストに対応する。変更の検証や失敗の調査に使う。

### uloop set-game-view-size
Game Viewのカスタム解像度を設定する、または現在の値を取得する。

### uloop simulate-keyboard
Input System経由で、PlayMode中のキーボード入力を再現する。キーの押下・長押し・離す操作に対応する。Input Systemパッケージが必要である。

### uloop simulate-mouse-input
Input Systemの`Mouse.current`経由で、PlayMode中のマウス入力（クリック・長押し・移動量・スクロール）を再現する。ゲームプレイ中のマウス操作に使う。UIの操作には`simulate-mouse-ui`を使う。`--dry-run`を付けると、Game Viewの座標が3D物理で何に当たるかだけを確認できる。

### uloop simulate-mouse-ui
PlayMode中のEventSystem経由のUI操作（クリック・長押し・ドラッグ）を、画面座標で再現する。`screenshot`の注釈付き画像から座標を決めて使う。

### uloop record-input（V3で削除）
`uloop record-input`はV3で削除されたコマンドである。
CLIからPlayMode入力を記録する手段はなくなっており、記録操作はUnity Editorの **Window > Unity CLI Loop > Recordings** からGUI経由で行う必要がある。
CLIエージェント経由でこの操作を自動化したい場合は、`uloop-execute-dynamic-code`による代替実装、または人手によるGUI操作を検討する。
決定論的リプレイに関する設計上の注意点自体は、記録方法の変更後も変わらず有効である。
