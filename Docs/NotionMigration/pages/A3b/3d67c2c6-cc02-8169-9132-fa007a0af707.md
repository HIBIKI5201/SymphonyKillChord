# SourceDataProvider

- page_id: 3d67c2c6-cc02-8169-9132-fa007a0af707
- Notion パス: Symphony Kill Chord / システム概要 / SourceDataProvider
- スナップショットの最終更新: 2026-09-09T04:38:01.101Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 一部古い — プレビュー 4 クラス（`PlannerEnemyStatusPreview` ほか）と `GameDataVariantBuildPreprocessor` は存在しない（`[CustomEditor]` 群 `Assets/Editor/Scripts/Inspectors/SourceData/` と `GameDataVariantBuildProcessor.cs` へ移行、PR #1564 / #1788）。Variant の正本は Build Profile（`GameDataVariantProfiles.cs:26-44`、#1534）。Window は値を編集しない。`PlannerMasterDataEditorSettings.asset` の実パスは `ProjectSettings/KillChord/`（`ProviderConst.cs:15`）。「Window はどちらの Variant でも両方の Group を表示する」は誤り（`SourceDataProviderRepositoryResolver.cs:23-31`）。それ以外のクラス表・Drawer・DataID の記述は実装どおり。最終確認 2026-09-22 / 73fefeb45
- 整合性:
  - 子 ③ (3d67c2c6-cc02-81c5-bdd1-fad0d552e2d2) と同じ誤り（メニュー・EditorPrefs・Preprocessor）を持っていたので、両方を直した
  - マスターデータ運用ガイド (39e7c2c6-cc02-809f-a6df-ee406c242e70) と Planner Master Data の説明を揃えた（Window は選択と Ping、編集は Inspector）
  - リポ側 `NotionModuleDocs/SourceDataProvider.md`（2026-09-13）は Preprocessor とプレビュー 4 クラスの記述が古い（01_ドキュメント棚卸し.md:210）
  - `StageEffect` はコードで使う CollectionKey だが、`ProjectSettings/SourceDataProviderSettings.asset` の Collection に無い。`BarrageTurret` はシーン内完結の CollectionKey だが、`SceneScopedDataIDOptionResolver` は `SpawnPoint` しか解決しない（候補ゼロで手入力のみ）
- 変更点:
  - 概要: 最終更新日を 2026-09-22 にした
  - 🏗️ クラス: 旧: `PlannerEnemyStatusPreview` / `PlannerEnemyWavePreview` / `PlannerStageTreeGraphRenderer` / `PlannerBattleSceneMapRenderer` の 4 行 → 新: Inspectors 群（CharacterDefinitionAssetEditor ほか 11 クラス）の行に置き換えた。旧: `GameDataVariantEditorState`「EditorPrefs で保持し変更時に Apply」 → 新: Window の表示と生成先の選択だけを保持。旧: `GameDataVariantBuildSettings / GameDataVariantBuildPreprocessor` → 新: `GameDataVariantBuildSettings`・`GameDataVariantProfiles`・`GameDataVariantBuildProcessor`・`BuildProfileToolbar` の 4 行。`PlannerNavigationHub`・`SourceCollectionElementDisplay`・`SourceDataAssetIndex`・`SourceDataProjectWindowIcons` を追加した。`PlannerMasterDataEditorSettings` のパスと `PlannerMasterDataWindow` の役割を直した
  - 🔗 モジュール結合: Window の矢印を「選択・Ping」に、Variant を Build Profile 起点に直し、Inspectors と ProjectWindow を追加した
  - 🧩 構成要素: フォルダ構成（Core / Wiki / Inspectors / ProjectWindow）を追記した。Editor拡張(Drawer / Window) の Window の説明を直した。Editor拡張(Variant) の「Window は全 SourceAsset を表示する」を削除し、Build Profile 正本・ビルド時の検証・データ配置の規約を追記した。Editor拡張(Inspector / ProjectWindow) の小節を追加した
  - 🔌 拡張ポイント: 旧: 「専用プレビューは `PlannerMasterDataWindow.DrawSourceAssetPreview` 等の分岐へ追記」 → 新: 対象型の `[CustomEditor]` を `Inspectors/SourceData/` に追加。第三の Variant の行を Build Profile 方式に直した
  - 📋 登録済み CollectionKey 一覧（新しい節、末尾）: CollectionKey・SourceAsset・Planner のページの表を追加した
- 決定（2026-09-22 八幡）: No.2 「🏗️ クラス」表の `EnemyWaveDefinitionAssetEditor` 行の説明「Wave定義」を「ウェーブ定義」に直した（「参照Waveのシーンマップ」は Inspector の表示名、CollectionKey `Wave` は識別子なので変えていない）
- 織り込んだ反映項目: IF-07, IF-08, IF-09, IF-10, IF-11（IF-13 は子 ② に織り込んだ）
- 出典: `Assets/Editor/Scripts/SourceDataProvider/Core/*.cs`、`Wiki/*.cs`、`Assets/Editor/Scripts/Inspectors/SourceData/*.cs`、`Assets/Editor/Scripts/ProjectWindow/*.cs`、`GameDataVariantBuildProcessor.cs:15-61`、`GameDataVariantBuildSettings.cs`（`[InitializeOnLoadMethod]` の再適用）、`ProjectSettings/SourceDataProviderSettings.asset:77-168`、`ProjectSettings/KillChord/PlannerMasterDataEditorSettings.asset:16-96`、`PlannerCollectionItemCreator.cs:190-211,381-385`、`EditorWindowPathConst.cs:10,14`、PR #1508 / #1534 / #1564 / #1565 / #1575 / #1588 / #1788
- 要確認:
  - `StageEffect` を Collection 未登録のままにしてよいか、Inspector で警告が出ていないか（実機確認、IF-10）
  - バッジの色の具体値（実機確認、IF-9）
  - 旧「配信版」の StreamingBind 3 件（製品版・体験版それぞれ）の扱い（企画、IF-11）

## 適用する本文

# 概要 {color="gray_bg"}
> 💡 **モジュール概要**
> プランナーがコードを触らずにマスターデータ（ScriptableObject）を追加・編集できるようにするためのエディタ拡張基盤である。DataIDの採番とハッシュ衝突検出、Addressableアセットの登録レジストリ、Planner Master Data Windowによるデータの検索・選択UIとInspectorプレビュー、Release/Demoのマスターデータ出し分け（GameDataVariant）の4つを扱う。「マスターデータ運用ガイド（プランナー向け）」がプランナー向けの操作手順書であるのに対し、本ページは仕組みと拡張方法を扱うエンジニア向け設計書である。
| 項目 | 内容 |
|---|---|
| **モジュール名** | SourceDataProvider |
| **カテゴリ** | Editor拡張 / Tooling |
| **ステータス** | 実装済み |
| **最終更新日** | 2026-09-22 |
---

## 🏗️ クラス
  Clean Architectureの6層はEditor拡張には厳密対応しないため、実態に即して分類する。
| クラス名 | 分類 | 役割・機能 |
|---|---|---|
| `**DataID**` | Runtime基盤 | プランナーが設定する人間可読な文字列ID（Editorのみ保持）と、実行時に使う焼き込み済み数値ID(`_hashId`)を保持するstruct |
| `**DataIDHasher**` | Runtime基盤 | `CollectionKey + ":" + id` を `Animator.StringToHash` へ渡し、安定した数値IDを生成する |
| `**SourceDataCollectionAttribute**` | Attribute | `DataID`フィールドが属するCollectionKeyを指定する。`isSceneScoped`でSourceDataProviderに登録しないシーン内完結IDを区別する |
| `**SourceDataAddressAttribute**` | Attribute | `string`フィールドに付与し、登録済みAddressableキーのドロップダウン選択にする |
| `**RepositoryAddressSelectorAttribute**` | Attribute | `SourceDataAddressAttribute`への移行前の旧属性。`[Obsolete]`、新規コードでは使用しない |
| `**SourceDataProvider**` | Editor拡張 | 本エディタ機能群の所属を表すだけのマーカー型。ロジックは持たない |
| `**SourceDataProviderSettings**` | 設定・レジストリ | `ProjectSettings/SourceDataProviderSettings.asset`に永続化される`ScriptableSingleton`。SourceAsset一覧・collection一覧・既定マッピング（`CreateDefaultMappings`）を保持し、`RefreshSourceAssetsFromAddressables()`でAddressables側との自動同期を行う |
| `**SourceDataProviderSettingsProvider**` | 設定・レジストリ | Project Settings「KillChord/Source Data Provider」画面。`SourceDataProviderSettings`のSourceAsset設定・collection設定をInspector風に編集する |
| `**SourceDataProviderRepositoryResolver**` | Editor拡張(解決) | Addressableキーからアセット実体を解決し、collectionの要素型取得、指定CollectionKeyへ登録済みの`DataID`列挙（`GetOptions`）、対象フィールドが「生成側（authoring）」か「参照側」かの判定（`IsAuthoringProperty`）を行う中核クラス。SourceAssetは選択中Variantに対応するGroupから解決する |
| `**SerializedPropertyFieldResolver**` | Editor拡張(解決) | `SerializedProperty.propertyPath`（`Array.data[n]`表記込み）から、継承元を辿って宣言元`FieldInfo`を逆引きする |
| `**DataIDCollisionDetector**` | Editor拡張(検証) | 同一CollectionKey内でのID重複・異なるIDによるハッシュ衝突を検出する |
| `**SourceDataIDOption**` | Editor拡張(データ) | DataID選択UIに表示する候補（文字列ID・数値ID・登録元オブジェクト）を保持するreadonly struct |
| `**SceneScopedDataIDOptionResolver**` | Editor拡張(解決) | `isSceneScoped`なCollectionKey（現状は`SpawnPositionPair`のスポーン地点ID）専用の候補解決。`BattleSceneDataReader`経由でシーンファイルを直接パースする |
| `**BattleSceneDataReader**` | Editor拡張(解析) | ステージの`.unity`ファイルをテキストとして直接パースし、シーンを開かずにスポーンポイントとNavMeshの三角形メッシュを取得する。結果はキャッシュされる |
| `**DataIDPropertyDrawer**` | Editor拡張(Drawer) | `DataID`型の`CustomPropertyDrawer`。文字列ID入力or登録済みID選択、ハッシュ表示・コピー、Plannerジャンプボタン、警告表示（`DataIDCollisionDetector`/`SourceDataProviderRepositoryResolver`を利用）を行う |
| `**SourceDataAddressSelectorDrawer**` | Editor拡張(Drawer) | `[SourceDataAddress]`が付いた`string`フィールドの`CustomPropertyDrawer`。登録済みAddressableキーのポップアップ・Ping・Plannerジャンプを提供する |
| `**SourceDataRegistrationHeader**` | Editor拡張(Drawer) | `Editor.finishedDefaultHeaderGUI`を購読し、任意ScriptableObjectのInspectorヘッダーへ「どのcollectionに登録/未登録か」を表示し、その場で登録・登録解除できるボタンを追加する |
| `**SpawnPositionPairEditor**` | Editor拡張(Drawer) | `SpawnPositionPair`専用の`CustomEditor`。シーン内完結IDのため、シーン内の他インスタンスとのID重複だけを独自にチェックする |
| `**PlannerNavigationHub**` | Editor拡張(解決) | Core（Drawer・Inspector）からPlanner Master Data Windowを開いて対象へ移動するための窓口。CoreがWikiに直接依存しないよう、依存の向きを逆にする |
| `**SourceCollectionElementDisplay**` | Editor拡張(データ) | Collection要素の表示名と検索キー（Tips本文・BGMラベル・アイコン種別など）を取り出す |
| `**PlannerMasterDataEditorSettings**` | 設定・レジストリ | `ProjectSettings/KillChord/PlannerMasterDataEditorSettings.asset`に永続化される`ScriptableSingleton`。Planner Master Data Windowのサイドバー「Page」定義（`PageDefinition`：表示名・SourceAssetキー一覧・CollectionKey一覧）を保持する |
| `**PlannerMasterDataEditorSettingsProvider**` | 設定・レジストリ | Project Settings「KillChord/Planner Master Data」画面。`PageDefinition`の追加・編集を行う |
| `**PlannerMasterDataWindow**` | Editor拡張(Window) | プランナー向けの検索・選択画面本体（`EditorWindow`、メニュー `Window/KillChord/PlannerMasterDataWindow`）。Page切り替え、SourceAsset単位／Collection単位のナビゲーション、横断検索、表示順のソート、Collectionへの追加・登録解除、ツールバーのVariant切替を行う。値は編集せず、選択した行の実アセットをProject/Inspectorで選択・Pingする |
| `**PlannerCollectionItemCreator**` | Editor拡張(生成) | Collectionへ新規要素を追加する処理。要素型がScriptableObjectなら`AssetCreationDirectory`配下へアセット生成（`{Variant}`は`Master`/`Demo`へ置換）、そうでなければ配列へインライン要素を追加し型の既定値にリセットする |
| `**CharacterDefinitionAssetEditor**` / `**EnemyStatusRadarChart**` | Editor拡張(Inspector) | `CharacterDefinitionAsset`のInspectorに主要ステータスのレーダーグラフを表示する |
| `**EnemyWaveDefinitionAssetEditor**` / `**EnemyWaveSummaryDrawer**` / `**BattleSceneMapRenderer**` | Editor拡張(Inspector) | ウェーブ定義のInspectorに概要と、`BattleSceneDataReader`の結果を使ったスポーンポイント・NavMeshの2Dマップを表示し、スポーン候補地を編集させる |
| `**StageTreeAssetEditor**` / `**StageAssetEditor**` / `**StageBindAssetEditor**` / `**StageTreeGraphView**` | Editor拡張(Inspector) | Stage・Bind・StageTreeのInspectorにステージとBindの構成をグラフ表示する（ドラッグで移動、ノードのクリックで選択） |
| `**StageWavePreview**` | Editor拡張(Inspector) | BattleStageのInspectorに「参照Waveのシーンマップ」を表示する |
| `**SourceCollectionItemInspector**` | Editor拡張(Inspector) | カタログ等のインライン要素を、所有アセットのInspector先頭に「Plannerで選択中の項目」として表示する |
| `**SourceDataAssetIndex**` / `**SourceDataProjectWindowIcons**` | Editor拡張(ProjectWindow) | Projectウィンドウの各アセットに、Addressables登録済み／Collection登録済みのバッジを描画する。GUIDからの逆引きインデックスを持ち、Addressablesの変更・アセットのインポート・Variant切替で作り直す |
| `**DataIDRebuildMenu**` | Editor拡張(一括処理) | プロジェクト内の全ScriptableObject／Prefab／Sceneを走査し、`DataID`のハッシュを`DataIDHasher`で再計算・再焼き込みするメニュー。走査中にハッシュ衝突も検出しログ出力する |
| `**SpawnPositionPairIndexMigrationMenu**` | Editor拡張(一括処理) | ステージシーン配下の`SpawnPositionPair`へGameObject名ベースのIDを一括付与する移行用メニュー |
| `**GameDataVariant**` | Editor拡張(Variant) | Editor上で扱うゲームデータ種別を表す列挙（`Release` / `Demo`） |
| `**GameDataVariantEditorState**` | Editor拡張(Variant) | Planner Master Data WindowのVariant選択を`EditorPrefs`（キー `KillChord.GameDataVariant`、ユーザー単位）で保持する。表示するデータとアセットの生成先にだけ効き、ビルドには影響しない。Group名の定数（`GameData.Release` / `GameData.Demo` / `GameData.Shared`）を持つ |
| `**GameDataVariantProfiles**` | Editor拡張(Variant) | アクティブなBuild Profileのスクリプティングシンボルに`KILLCHORD_DEMO`があればDemo、無ければReleaseと判定する（ゲームデータ種別の正本）。体験版Profileのシーン一覧を「グローバルのシーン一覧＋`Demo/DemoEnd.unity`」へ揃える |
| `**GameDataVariantBuildSettings**` | Editor拡張(Variant) | 判定した種別に応じてAddressables Groupの`IncludeInBuild`を切り替える（Sharedは常に含める）。Editorの起動・再読み込み時にもアクティブなProfileで再適用する |
| `**GameDataVariantBuildProcessor**` | Editor拡張(Variant) | `BuildPlayerProcessor.PrepareForBuild`（callbackOrder -1000。Addressablesのビルドより前）で種別を再適用し、検証に失敗したら`BuildFailedException`でビルドを止める |
| `**BuildProfileToolbar**` | Editor拡張(Variant) | メインツールバーの「KillChord/Build Profile」。Build Profileを切り替える |
---

## 🔗 モジュール結合
```Mermaid
graph TD
    subgraph SourceDataProviderModule [SourceDataProvider モジュール]
        Attribute["Attribute<br>SourceDataCollectionAttribute, SourceDataAddressAttribute"]
        Registry["設定・レジストリ<br>SourceDataProviderSettings"]
        Resolver["Editor拡張(解決)<br>SourceDataProviderRepositoryResolver"]
        Drawer["Editor拡張(Drawer)<br>DataIDPropertyDrawer"]
        Inspector["Editor拡張(Inspector)<br>StageTreeAssetEditor ほか"]
        Window["Editor拡張(Window)<br>PlannerMasterDataWindow"]
        Variant["Editor拡張(Variant)<br>GameDataVariantBuildProcessor"]
    end

    subgraph AddressablesModule [Addressable Asset System（外部パッケージ）]
        AddressablesSettings["Infrastructure<br>AddressableAssetSettings"]
    end

    subgraph UnityBuild [Unity Build Profile]
        Profile["Build Profile<br>Release_* / Demo_*"]
    end

    subgraph AllMasterDataModules [各マスターデータ保持クラス（Stage/Skill/Enemy等 各モジュール）]
        MasterDataAsset["Runtime基盤<br>ScriptableObject + DataIDフィールド"]
    end

    Registry -->|Addressableエントリを走査してSourceAsset自動登録| AddressablesSettings
    Resolver -->|Addressableキーからアセット実体を解決| AddressablesSettings
    Resolver -->|collection配下のDataIDを収集| MasterDataAsset
    Drawer -->|SourceDataCollection属性を読み取りcollectionを解決| Resolver
    Attribute -->|DataIDフィールドへ付与される| MasterDataAsset
    Window -->|SourceAsset/collectionを選択・Ping| MasterDataAsset
    Inspector -->|Inspectorにプレビューを描画| MasterDataAsset
    Variant -->|KILLCHORD_DEMOの有無で種別を判定| Profile
    Variant -->|Group単位でIncludeInBuildを切替| AddressablesSettings
```

### 📥 依存しているもの
- `**Addressable Asset System**`**（Unityパッケージ）**
  - *依存箇所*: `SourceDataProviderRepositoryResolver.TryResolveAsset`、`SourceDataProviderSettings.SynchronizeSourceAssetsFromAddressables`、`GameDataVariantBuildSettings.EnsureGroup`
  - *詳細*: SourceAssetの実体解決、Addressableエントリの自動検出、Release/Demo/SharedのGroup作成とビルド含有切替をすべてAddressablesのAPI（`AddressableAssetSettingsDefaultObject.Settings`）経由で行う。Addressablesに登録されていないScriptableObjectはPlanner Master Data Windowへ現れない
- `**Build Profile**`**（Unity 6）**
  - *依存箇所*: `GameDataVariantProfiles.GetActiveVariant`、`GameDataVariantBuildProcessor.PrepareForBuild`
  - *詳細*: `Assets/Settings/Build Profiles/{Release,Demo}_{Dev,Master}_{Android,iOS,macOS,Windows}.asset`の16本のうち、Demoの8本だけが`KILLCHORD_DEMO`を持つ。共通のPlayer Settingsにはこのシンボルを置かない

### 📤 依存されているもの
- **各マスターデータ保持クラス（Stage / Skill / Enemy / Scenario / Character 等の全モジュール）**
  - *参照箇所*: `[SourceDataCollection]`を付与した`DataID`フィールド、`[SourceDataAddress]`を付与した`string`フィールド
  - *詳細*: マスターデータを保持するあらゆるScriptableObject／MonoBehaviourが、ID管理とAddressableキー選択をこのモジュールへ委譲する。新しいマスターデータ種別を追加するたびに、後述の拡張ポイント手順でこのモジュール側へ登録が必要になる
- **AutoBuilder / CI（BuildAndRelease.yml）**
  - *参照箇所*: `-gameDataVariant release|demo`引数
  - *詳細*: 指定した種別のBuild Profileを集めてビルドする。ビルド時の種別の適用と検証は`GameDataVariantBuildProcessor`が行う
---

# 詳細 {color="gray_bg"}

## 🧩 構成要素
Editor拡張という性質上、Domain/Application/Adaptor/View/Infrastructure/Compositionの6層区分は当てはまらない。代わりに責務単位で構成を示す。
フォルダは`Assets/Editor/Scripts/SourceDataProvider/Core/`（登録・解決・DataID・Variant）、`SourceDataProvider/Wiki/`（Planner Master Data Windowと、Collectionへの追加・削除）、`Assets/Editor/Scripts/Inspectors/SourceData/`（Inspectorのプレビュー）、`Assets/Editor/Scripts/ProjectWindow/`（Projectウィンドウのバッジ）に分かれる。

### Runtime基盤 / Attribute
`DataID`・`DataIDHasher`・`SourceDataCollectionAttribute`・`SourceDataAddressAttribute`は`Assets/Scripts/Runtime/0.Utility/Identity/`に置かれ、実行時にもビルドへ含まれる唯一の層である。`DataID._id`は`#if UNITY_EDITOR`ガードでEditorビルドのみ保持し、実機ビルドには焼き込み済みの`_hashId`のみが残る。

### 設定・レジストリ
`SourceDataProviderSettings`（`ProjectSettings/SourceDataProviderSettings.asset`）と`PlannerMasterDataEditorSettings`（`ProjectSettings/KillChord/PlannerMasterDataEditorSettings.asset`）はいずれも`ScriptableSingleton<T>`＋`[FilePath]`でアセット化され、リポジトリにコミットされる（チーム全員で共有される設定）。前者は「何がSourceAssetで、どの配列がcollectionか」、後者は「Planner Master Data Windowのどのページに何を出すか」を分離して保持する。

### Editor拡張(解決・検証)
`SourceDataProviderRepositoryResolver`が中心で、Addressable解決・collection走査・authoring判定を担う。`SerializedPropertyFieldResolver`はリフレクションで宣言元フィールドを逆引きし、`SourceDataCollectionAttribute`の取得に使われる。`DataIDCollisionDetector`と`SceneScopedDataIDOptionResolver`はそれぞれSourceDataProvider管理下・シーン内完結のID検証を担当する。

### Editor拡張(Drawer / Window)
`DataIDPropertyDrawer`と`SourceDataAddressSelectorDrawer`がInspector上のUIを差し替え、`SourceDataRegistrationHeader`がInspectorヘッダーに登録状態を追加する。`PlannerMasterDataWindow`はこれらとは独立したEditorWindowで、Page単位のナビゲーションと検索を提供する。値の編集とプレビューはWindowでは行わず、選択した実アセットのInspectorで行う。DrawerからWindowを開くときは`PlannerNavigationHub`を経由する。

### Editor拡張(Inspector / ProjectWindow)
プレビューは対象型ごとの`[CustomEditor]`として`Inspectors/SourceData/`に置く。Windowを開いていなくても、Inspectorで対象アセットを選べば表示される。Projectウィンドウのバッジも同様にWindowと独立して動く。

### Editor拡張(Variant)
`GameDataVariant`／`GameDataVariantProfiles`／`GameDataVariantBuildSettings`／`GameDataVariantBuildProcessor`はマスターデータそのものではなく、「どのAddressables GroupをビルドへIncludeするか」を切り替える層である。種別の正本はアクティブなBuild Profileで、`GameDataVariantEditorState`（Windowのツールバー「Variant」）はEditor上の表示と生成先を切り替えるだけである。
- ビルド時は`GameDataVariantBuildProcessor`が次を検証し、1つでも満たさなければ`BuildFailedException`でビルドを止める。
  - Build Profileの種別と、コンパイル済みコードの種別（`KILLCHORD_DEMO`の有無）が一致する。共通のPlayer Settingsへ`KILLCHORD_DEMO`を置くとReleaseのProfileでも不一致になる
  - 対象GroupがIncludeInBuildかつアドレス付きでビルドされる
  - 必須エントリ（`StageTreeAsset` / `EnemyMissionKeyRepository` / `MissionDefinitionRepository`、Demoではさらに`DemoRuntime` / `DemoExperienceConfig`）が対象Groupにある
- データ配置の規約:
  - 製品版のデータは`Assets/Level/Data/Master/`、体験版は`Assets/Level/Data/Demo/`に置く
  - 両版共通のものは`GameData.Shared`へ入れる。版ごとに内容が違うものは、同じアドレスで`GameData.Release`と`GameData.Demo`の両方へ入れる
  - 同じアドレスを`GameData.Shared`にも残すとRelease側と重複してビルドが失敗する
  - 旧「配信版」用の`StageBinds/StreamingBind/`配下（製品版・体験版それぞれ3件）はAddressablesから外れたまま残っている【要確認: 削除するかを企画に】

## 🔌 拡張ポイント
| 拡張したいこと | 実装する場所 | 追加登録の要否 |
|---|---|---|
| 既存Addressable ScriptableObjectを新たにSourceAssetとして表示したい | Addressablesへ address を設定して登録するだけでよい | 不要（`SourceDataProviderSettings.RefreshSourceAssetsFromAddressables()`がWindowの`OnEnable`／Refreshボタン／設定画面`OnActivate`で自動的にSourceAssetMappingへ追加する） |
| 新しいSourceDataCollection（マスターデータ種別）を追加したい | ① 対象ScriptableObjectをAddressable登録 → ② Project Settings「KillChord/Source Data Provider」で`SourceCollectionMapping`（CollectionKey・SourceAsset Addressableキー・配列のプロパティパス・ScriptableObjectならAsset Creation Directory）を追加 → ③ Project Settings「KillChord/Planner Master Data」で対象Pageの`SourceAssetAddressableKeys`/`CollectionCategories`へキーを追加 → ④ データクラスの`DataID`フィールドへ`[SourceDataCollection("YourCollectionKey")]`を付与 | 必要（4箇所すべて手動。①だけ行うとSourceAssetとしては見えるがCollection編集ができず、②③を忘れると`DataIDPropertyDrawer`が「SourceDataProviderにCollectionKeyが登録されていません」警告を出し、Planner Master Data Windowのサイドバーにも出現しない。③だけ忘れた場合は検索からだけ開ける） |
| Addressableキーをコード上で安全に選択したい（typoを防ぎたい） | 対象`string`フィールドへ`[SourceDataAddress]`を付与する | 不要（`SourceDataAddressSelectorDrawer`が`CustomPropertyDrawer(typeof(SourceDataAddressAttribute))`で自動的にポップアップ選択UIへ差し替える） |
| シーン内で完結する（SourceDataProviderへ登録しない）新しいDataID種別を追加したい | `SourceDataCollectionAttribute`のコンストラクタへ`isSceneScoped: true`を指定 | 必要（候補列挙ロジックは自動化されないため、`SceneScopedDataIDOptionResolver.GetOptions`内の分岐へ対象CollectionKeyの解決処理を追記する必要がある。追記を忘れると`DataIDPropertyDrawer`は候補ゼロ扱いとなり手入力のみ可能な状態で警告は出ない。現状`BarrageTurret`はこの状態である） |
| 専用プレビューを追加したい | 対象型の`[CustomEditor]`を`Assets/Editor/Scripts/Inspectors/SourceData/`に追加する | 不要（Inspectorで対象アセットを選べば表示される。Windowのコードは変更しない） |
| Release/Demo以外の第三のGameDataVariantを追加したい | `GameDataVariant` enumへ値を追加し、`GameDataVariantProfiles`の判定（シンボル）、`GameDataVariantEditorState`のGroup名定数、`GameDataVariantBuildSettings.Apply`のGroup切替、`GameDataVariantBuildProcessor`の検証、対応するBuild Profileを追加する | 必要（enum追加だけでは判定とGroup切替に反映されず、Groupが常にビルドへ含まれたまま／除外されたままになる） |

## 🔄処理フロー
- 📄 [[① 新しいマスターデータ種別を登録する手順]] (3d67c2c6-cc02-8140-9293-d9da999c8519)
- 📄 [[② DataID採番とハッシュ衝突検出]] (3d67c2c6-cc02-8160-ad80-da71dd5416cf)
- 📄 [[③ GameDataVariant切替でRelease/Demoのデータを出し分ける仕組み]] (3d67c2c6-cc02-81c5-bdd1-fad0d552e2d2)

## 📋 登録済み CollectionKey 一覧
`ProjectSettings/SourceDataProviderSettings.asset`に登録されたCollectionと、Planner Master Data Windowでの所属ページである。同じCollectionが複数のページに出ることがある。
| CollectionKey | SourceAsset（Addressableキー） | 所属ページ |
|---|---|---|
| `StageAsset` | `StageTreeAsset` | Stage Select |
| `StageBind` | `StageTreeAsset` | Stage Select |
| `Wave` | `EnemyWaveDefinitionRepository` | Stage Select |
| `EnemyData` | `EnemyDefinitionRepository` | Stage Select / Enemy |
| `EnemyMissionKey` | `EnemyMissionKeyRepository` | Stage Select |
| `Mission` | `MissionDefinitionRepository` | Stage Select |
| `PlayerAttack` | `Player` | Player |
| `Skill` | `OutGameSkillRepository` | Player / Skill Tree |
| `SkillGenreIcon` | `SkillGenreIconCatalogAsset` | Player |
| `TitleScreenRule` | `TitleScreenRuleData` | UI |
| `OutGameScreenRule` | `OutGameScreenRuleData` | UI |
| `LoadingTip` | `LoadingTipsConfig` | UI |
| `BossAttackEntry` | `BossAttackEntryRepo` | Enemy |
| `Character` | `CharacterDefinitionRepository` | Enemy |
| `BgmSelectorLabel` | `BgmSelectorLabelTableAsset` | Enemy |
| `SkillNode` | `SkillNodeDataRepo` | Skill Tree |
| `SkillNodeBind` | `SkillNodeBindRepo` | Skill Tree |
| `SkillNodePhaseBind` | `SkillNodePhaseBindDataRepo` | Skill Tree |
| `StatusBonusEffectIcon` | `StatusBonusEffectIconCatalogAsset` | Skill Tree |
| `ScenarioBackground` | `BackgroundCatalogAsset` | Scenario |
| `ScenarioAnimation` | `AnimationCatalogAsset` | Scenario |
| `ScenarioPortrait` | `PortraitCatalogAsset` | Scenario |
| `GameResource` | `GameResourceDefinitionRepository` | Resource |
コードでは使うが上表に無いCollectionKeyは次のとおりである。
- `SpawnPoint`（`SpawnPositionPair`のスポーン地点）と`BarrageTurret`（弾幕の砲台）: シーン内完結（`isSceneScoped: true`）。候補を解決するのは`SpawnPoint`だけである
- `StageEffect`（`StageEffectAssetBase`と`StageEffectView`の対応付け）: Collection未登録【要確認: 登録するか、Inspectorで警告が出ていないかをエンジニアに】
