# スキル　2026-05-09

- page_id: 35b7c2c6-cc02-8038-ad3e-fb1914ecee56
- Notion パス: Symphony Kill Chord / システム概要 / スキル　2026-05-09
- スナップショットの最終更新: 2026-05-09T09:26:47.224Z
- 書き込み許可: 可
- 処置: アーカイブ推奨

## 判定

- 信頼性: 陳腐化 — `SkillData`・`SkillDataAsset` は現存せず、現行は Domain の `SkillTemplate`（`Assets/Scripts/Runtime/1.Domain/Player/SkillTemplate.cs`）と Infrastructure の `SkillTemplateAsset`（`5.InfraStructure/Player/SkillTemplateAsset.cs`）を `SkillRepository` が変換する。`ConvertSkillDefinitions` は 0 件。`SkillCheckService` は非 static の class で、`SkillInitializer` が生成して渡す（`SkillInitializer.cs:177`）。つまり「修正」節の方針は、名前を変えて実装されている
- 整合性: 現行のモジュール文書 `システム概要 / スキル`（3957c2c6-cc02-806e-bbe8-e391a7ad8c86）と題材が重複する
- 変更点:
  - ページ冒頭（「クラス図」の前）: アーカイブのコールアウトと、残す設計判断の要点を追加
- 織り込んだ反映項目: BT-41, RL-14
- 出典: 実装（上記ファイル）、`10_インゲーム戦闘コア.md` BT-41
- 要確認: アーカイブの置き方（RL-14、要企画確認）。W-23 のキャッシュマップ確認

## 適用する本文

（ページ冒頭、「クラス図」の前に挿入する）

> 📦 **アーカイブ（2026-05 のリファクタリング記録）**
このページは 2026-05 に行ったスキルモジュールのリファクタリングの記録である。ここに出てくる `SkillData`・`SkillDataAsset` は、現在 `SkillTemplate`（Domain）と `SkillTemplateAsset`（Infrastructure）になっている。現行の構成は「システム概要 / スキル」を参照する。
現行実装に残っている設計判断は次のとおり。
- Domain のスキル定義は Unity の属性（`SerializeField` など）を持たない。Inspector で編集するデータは Infrastructure の Asset に置き、Repository で Domain へ変換する
- `SkillCheckService` は static にせず、Composition 層（`SkillInitializer`）で生成して注入する
