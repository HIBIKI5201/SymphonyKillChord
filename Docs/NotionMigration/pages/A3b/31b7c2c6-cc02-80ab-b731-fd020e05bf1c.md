# SinfoniaOperator

- page_id: 31b7c2c6-cc02-80ab-b731-fd020e05bf1c
- Notion パス: Symphony Kill Chord / システム概要 / SinfoniaOperator
- スナップショットの最終更新: 2026-09-09T03:24:59.311Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 一部古い — 確認コミット 73fefeb45。仕様検索 Bot は Gemini 要約を実装済み（`SinfoniaOperator/SinfoniaOperator/GeminiSummarizer.cs`、`DiscordBotManager.cs:243-258`、PR #1502）。Writer は pull / push / create / set-properties / rename の 5 コマンドで、`push --whole` で全文置換もできる（`NotionMarkdownWriter/Program.cs:37-41,80-89`）。CLI に `branches` と Discord `/branches` がある（`SinfoniaOperator.cs:37`、`DiscordBotManager.BranchCleanup.cs`、#1525）。VM への配備は GitHub Actions で自動化された（`.github/workflows/DeploySinfoniaOperator.yml`、#1538）
- 整合性:
  - 子ページ「仕様検索Bot」(3d47c2c6-cc02-8186-b646-c785716fedde) と本ページがともに「外部 AI API への課金は発生しない」としていたが、実装と矛盾する（Gemini 要約は `GEMINI_API_KEY` がある場合に有効）
  - 「ワークフロー / 社内ツール」(3c87c2c6-cc02-815e-b882-f8e5182ea23f) は Exporter を「全ページの再取得」と書き、本ページの「差分エクスポート」と食い違う。実装は差分対応（`NotionMarkdownExporter/ExportManifest.cs`、#1308）で、本ページが正しい
  - Exporter の出力先 `Docs/NotionSpecifications` は、2026-09-22 の決定（キャッシュは `Library/` 配下）で変わる予定。未実装のため、本文は現行のまま残し「今後の仕様（未実装）」節に分けた
- 変更点:
  - 「説明」節の表 NotionMarkdownWriter 行: 旧「更新は部分置換（`update_content`）のみに対応し、全文置換や子ページ削除は実装されていない」→ 新「pull / push / create / set-properties / rename の 5 コマンド。`push` は既定で差分、`--whole` で本文全体を置換。子ページ削除は無い」
  - 「説明」節の本文: 常駐 Bot の機能に `/branches` を追加した
  - 「全体構成」節: Exe の CLI サブコマンドに `branches` を追加した（旧 3 つ → 新 4 つ）
  - 「主な機能」節: 旧「ローカルの埋め込みモデルのみで動作し、外部AI APIへの課金は発生しない」→ 新「検索はローカルの埋め込みモデルで行い、`GEMINI_API_KEY` を設定した場合だけ Gemini API で要約する（課金の可能性あり）」。`/branches` と VM への自動配備を追加した
  - 新節「今後の仕様（未実装）」: Exporter のキャッシュ化（`Library/` 配下・オンデマンド更新）を追加した
- 織り込んだ反映項目: TL-11, TL-12, TL-13, TL-14
- 出典: 実装（上記ファイル）、PR #1502 / #1525 / #1538 / #1308 / #1507、`Docs/NotionMigration/02_キャッシュ運用とNotion記述規約.md` §0・§2
- 要確認: 本番 VM で `GEMINI_API_KEY` が設定され要約が有効か（要実機確認、TL-11）。Gemini API の課金プラン（無料枠か）は未確認

## 適用する本文

# 説明 {color="gray_bg"}
Discord及びNotionの管理を行うボット群。
SinfoniaOperatorディレクトリに、ゲーム本体とは別の`.NET`ソリューションとして設計されている。
毎朝のタスク・スプリント通知に加え、Oracle Cloud上に常駐させた仕様検索Bot（Discordの`/spec`コマンド）とブランチ整理コマンド（`/branches`）も同じソリューションに含まれる。
また同ディレクトリには、Discord/Notion通知ボット以外に以下3つの独立したツールが同居している。

| ツール | 役割 |
|---|---|
| NotionMarkdownExporter | Notionの仕様書ページ以下をEnhanced Markdown APIで再帰取得し、Markdownとしてローカル（`Docs/NotionSpecifications`）へ保存する。子ページ参照は相対リンクへ変換される。再実行時はNotionページの最終更新日時を前回エクスポート完了日時と比較し、更新・新規ページのみを再取得する差分エクスポートに対応している。 |
| NotionMarkdownWriter | ローカルで編集したMarkdownをNotionへ書き戻す。書き込み対象は`sinfonia-operator.env.json`の`NOTION_WRITE_ALLOWED_ROOTS`に列挙した許可ページの配下に限定され、それ以外への送信は拒否される。コマンドは`pull`（編集の基準になる原文の取得）/`push`（差分の反映。`--whole`を付けると本文全体の置換1件として送る）/`create`（許可ルート配下へのページ作成）/`set-properties`（DB行のプロパティ更新）/`rename`（タイトル変更）の5つ。子ページの削除は実装されていない。`--confirm`を付けるまで実際の送信は行われない。 |
| DiscordLogExporter | 設定したDiscordチャンネル・フォーラムの全メッセージをテキストファイルへ出力する。通常チャンネルはチャンネルごと、フォーラムはページ（スレッド）ごとに1ファイルを生成し、`Docs/DiscordLog/`へ保存する。 |

いずれもWindows向けの単体exeとして配布されており、Notion/Discordの通知ボット本体とは別の実行系として動作する。

## 参考資料
（既存のリンクを残す）

# 詳細 {color="gray_bg"}
機能ごとの詳細は子ページに分けている。まずここで全体像をつかんでから、目的の子ページを開く。

## 全体構成
SinfoniaOperatorは3つの`.NET`プロジェクトから成る。
- `SinfoniaOperator.Core`：Notion連携・Discord送信・設定管理の共通ライブラリ。Unity Editor拡張からも参照される。
- `SinfoniaOperator`（Exe）：Bot本体。CLIサブコマンドで「日次通知」「検索インデックス生成」「常駐Bot」「ブランチ一覧」の4つの動作を切り替える。
- `SinfoniaOperator.SpecSearch`：仕様検索専用ライブラリ。Markdownのチャンク分割・埋め込みベクトル生成・検索インデックス・検索の重み付けを担う。

## 主な機能
- 毎日朝7時（JST）、GitHub Actionsから実行され、Notionのタスク・スプリントデータベースを読み込んでDiscordへ通知する。
- Oracle Cloud上でsystemdサービスとして常駐し、Discordの`/spec query:<質問文>`コマンドから仕様書をセマンティック検索できる。検索そのものはローカルの埋め込みモデルだけで行う。`GEMINI_API_KEY`を設定した場合に限り、検索結果の抜粋を根拠にGemini APIで回答を要約する（外部AI APIの利用になるため、設定時は課金が発生しうる）。
- 同じ常駐Botで、Discordの`/branches`コマンドから、マージ済みで削除できるリモートブランチの一覧（最大25件）を表示できる。GitHub APIを参照するため、Botのホストにリポジトリのクローンは要らない。
- 常駐Botは、`develop`へのマージでSinfoniaOperator関連のソースが変わると、GitHub Actions（`DeploySinfoniaOperator.yml`）が自動でVMへ配備する。

## 子ページ
- 📄 [[アーキテクチャ]] (3d47c2c6-cc02-8122-b5c1-e06b79d59012)
- 📄 [[日次通知機能]] (3d47c2c6-cc02-8102-8c10-fb2512a3fa72)
- 📄 [[仕様検索Bot]] (3d47c2c6-cc02-8186-b646-c785716fedde)
- 📄 [[設定・環境変数リファレンス]] (3d47c2c6-cc02-81e6-93d5-fdca6adc65f9)
- 📄 [[Oracle Cloudデプロイ手順]] (3d47c2c6-cc02-81df-a3fd-ebaaff186a65)

## 今後の仕様（未実装）
2026-09-22の決定により、仕様書の正本はNotionになり、リポジトリ側の文書はNotionの写し（キャッシュ）になる。これに合わせてNotionMarkdownExporterを次のように変える。いずれも未実装である。
- 出力先を`Docs/NotionSpecifications`から`Library/NotionSpecifications`へ移す。`Library/`はUnityの生成物置き場で、もともとgitの対象外である。
- スキルやツールが固定パスで読むページは、キャッシュマップ（`SinfoniaOperator/notion-cache-map.json`、gitで管理する）でpage idと`Library/NotionCache/`内のパスを結び付ける。ページ名を変えてもパスは変わらない。
- キャッシュファイルの先頭に、正本のURL・page id・Notion側の最終更新日時・取得日時を書いたヘッダを付ける。キャッシュファイルは直接編集しない。
- 次のモードを追加する。`--pages <id,...>`（指定したページだけ取得）、`--cache-map <path>`（キャッシュマップのページだけ取得）、`--check`（本文を取らずに更新日時だけを比べる）。
- 更新は各自の手動実行と、スキルからのAI実行で行う。CIでの定期実行はしない。定期的な全件の取り直しもしない（Notion APIの制限に掛かるため）。必要なときに関連ページの更新日時だけを先に取り、更新があったページだけを取り直す。
- 仕様検索Botのインデックス生成も、出力先の変更に合わせて参照先を直す（「仕様検索Bot」ページを参照）。
