# スキルツリー開発について

- page_id: 36b7c2c6-cc02-80c2-b838-f9267cb45088
- Notion パス: Symphony Kill Chord / システム概要 / スキルツリー開発について
- スナップショットの最終更新: 2026-05-28T10:35:25.494Z
- 書き込み許可: 可
- 処置: アーカイブ推奨

## 判定

- 信頼性: 陳腐化 — 本文が使う `IAlgorithmService`・`IPointRepository`・`CanUnlockUsecase`・`UnlockUsecase`・`NodeVisibleController`・`SkillTreeRepository.GetNodesByPhase` は `Assets/Scripts` に 1 件も存在しない（git ls-files / git grep で 0 件、73fefeb45）。現行は `SkillTreeService`（`Assets/Scripts/Runtime/2.Application/OutGame/SkillTree/SkillTreeService.cs`）・`SkillTreeController`・`SkillNodePhaseBindDataRepo`。研究版は `Assets/DevelopProducts/Research/SkillTree/` に残っている
- 整合性: 現行のモジュール文書 `システム概要 / スキルツリー`（3bf7c2c6-cc02-81e0-bbc6-c0008d16f3bf）と同じ題材で、クラス構成が食い違う。システムリスト上で現行ページと並んでいる
- 変更点:
  - ページ冒頭（「1.クラス関係図」の前）: アーカイブのコールアウトを追加
  - 既存の図・本文は変更しない（経緯資料として残す）
- 織り込んだ反映項目: OG-122, RL-14
- 出典: 実装（上記クラスの有無、`SkillTreeService.cs:36` `TryGetTotalCost`）、`30_アウトゲーム.md` OG-122
- 要確認: アーカイブの置き方（`システム概要 / アーカイブ` を新設して移すか、カテゴリーを変えるか）を企画に確認（RL-14 は要企画確認）。移動・削除の前にキャッシュマップ（`notion-cache-map.json`）に載っていないか確認する（W-14）

## 適用する本文

（ページ冒頭、「1.クラス関係図」の前に挿入する）

> 📦 **アーカイブ（2026-05 時点の旧設計）**
このページは 2026-05 の研究段階の設計資料であり、ここに出てくる `IAlgorithmService`・`IPointRepository`・`CanUnlockUsecase`・`UnlockUsecase`・`NodeVisibleController` は現在のコードに存在しない。現行の仕組みは「システム概要 / スキルツリー」を参照する。
現行実装に引き継がれた考え方は次のとおり。
- 目標ノードまでの未解放ノードを根元からたどり、経路上のコスト合計を解放コストとする（現行は `SkillTreeService.TryGetTotalCost`）
- 親ノードのうち 1 つでも解放済みなら、そのノードは到達可能とする
