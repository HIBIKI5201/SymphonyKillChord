# プレイヤー　2026-05-08

- page_id: 35a7c2c6-cc02-80c1-98a8-d73e88fe0b05
- Notion パス: Symphony Kill Chord / システム概要 / プレイヤー　2026-05-08
- スナップショットの最終更新: 2026-05-13T09:23:31.963Z
- 書き込み許可: 可
- 処置: アーカイブ推奨

## 判定

- 信頼性: 陳腐化 — 「解決済み」に挙げた `DodgeSpeedVO`・`DodgeDurationVO`・`DodgeCooldownVO` と「スクリプトの概略」の `PlayerMoveParameter` は現存しない（`Assets/Scripts/Runtime/1.Domain/InGame/Player/` には `PlayerMoveSpec.cs` と `PlayerActionRestrictionReason.cs` のみ）。`PlayerMovementApplication`・`IPlayerApplication`・`IPlayerController` は現存する
- 整合性: 現行のモジュール文書 `システム概要 / プレイヤー`（3957c2c6-cc02-8017-9b5f-c04dd480c816）と題材が重複する
- 変更点:
  - ページ冒頭（「各レイヤーの色分け」の前）: アーカイブのコールアウトと、残す設計判断の要点を追加
- 織り込んだ反映項目: BT-41, RL-14
- 出典: 実装（上記ファイル）、PR #532（ページ内リンク）、`10_インゲーム戦闘コア.md` BT-41
- 要確認: アーカイブの置き方（RL-14、要企画確認）。W-14 のキャッシュマップ確認

## 適用する本文

（ページ冒頭、「各レイヤーの色分け」の前に挿入する）

> 📦 **アーカイブ（2026-05 のリファクタリング記録）**
このページは 2026-05 に行ったプレイヤーモジュールのリファクタリング（PR #532）の記録である。ここに出てくる `DodgeSpeedVO` などの値オブジェクトと `PlayerMoveParameter` は、その後 `PlayerMoveSpec` にまとめられ、現在のコードには存在しない。現行の構成は「システム概要 / プレイヤー」を参照する。
現行実装に残っている設計判断は次のとおり。
- Application 層のクラス名は「○○Application」に揃える（例: `PlayerMovementApplication`）
- 外側の層が内側を参照するときはインターフェイスを介する（`IPlayerApplication`・`IPlayerController`）
