# 設計方針

- page_id: 32d7c2c6-cc02-80c2-b202-c911d9844c4c
- Notion パス: Symphony Kill Chord / システム概要 / SinfoniaAssetManager / 設計方針
- スナップショットの最終更新: 2026-03-26T03:32:01.106Z
- 書き込み許可: 可
- 処置: 追記のみ

## 判定

- 信頼性: 陳腐化 — 2026-03 時点の計画である。`Assets/AssetStoreTools/` を gitignore して外部リソースを集める方針は現行も有効（`.gitignore:25,79`）。一方、自動ビルドの起動条件「main ブランチの PR が承認された時」は実装と違う。実装は develop / main への PR マージ（`pull_request` の `closed`）と手動実行で起動する（`.github/workflows/BuildAndRelease.yml:3-8`）。「仮想 Unity プロジェクト」でのパッケージ化は実装されていない（DriveImportSync は `Assets/` 配下へ直接配置する）
- 整合性: 親ページ「SinfoniaAssetManager」(32d7c2c6-cc02-8026-8e09-f37a558a73bd) と同じ。現行の CI の説明は「AutoBuilder」(3d67c2c6-cc02-8130-9cb7-ec47e253a290)
- 変更点:
  - ページ冒頭（「環境」節の前）に、計画と実装の違いを示すコールアウトを追加した。計画本文は残す
- 織り込んだ反映項目: TL-19, TL-07, RL-14
- 出典: `.github/workflows/BuildAndRelease.yml`、`Assets/Editor/Scripts/AssetManagement/DriveImportSync.cs`、`.gitignore`
- 要確認: 「AssetStoreTools のパッケージ配布」（ログ差分のマージと zip 化）が `AssetStoreToolsPackagePipeline_Combine.asset`（`Assets/Editor/SymphonyFrameWork/Configs/`）の仕組みで実現済みかは照合していない（リードプログラマーに確認）

## 適用する本文

挿入位置: ページ冒頭（「### 環境」の前）

> 💡 このページは 2026-03 時点の設計方針である。現行の実装とは次の点が違う。
> - 自動ビルドは、`develop` / `main` への PR のマージ、または手動実行で起動する（承認時ではない）。実体は `BuildAndRelease.yml` であり、説明は「AutoBuilder」ページにある
> - Google Drive のアセット取り込みは `DriveImportSync` が行い、`Assets/` 配下へ直接配置する。仮想 Unity プロジェクトでの unitypackage 化は実装していない
> - `Assets/AssetStoreTools/` を git 管理外にして外部リソースを集める方針は、現在も有効である
