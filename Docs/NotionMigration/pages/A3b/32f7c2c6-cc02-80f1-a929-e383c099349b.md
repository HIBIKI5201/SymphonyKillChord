# Adaptive Performance Package

- page_id: 32f7c2c6-cc02-80f1-a929-e383c099349b
- Notion パス: Symphony Kill Chord / システム概要 / Adaptive Performance Package
- スナップショットの最終更新: 2026-04-05T19:43:48.568Z
- 書き込み許可: 可（システム概要 配下）
- 処置: 追記のみ
- 確認日 / コミット: 2026-09-22 / 73fefeb45

## 判定

- 信頼性: 一部古い — 調査内容（Provider が必要、iOS 向けの正式 Provider が無い）は調査メモとして残せる。ただしプロジェクトでは採用されておらず、それが書かれていないため、導入済みの仕組みと誤読されうる
  - `Packages/manifest.json` にあるのは組み込みモジュール `com.unity.modules.adaptiveperformance` 1.0.0 だけで、本文が導入手順に挙げる「Adaptive Performance」「Adaptive Performance Android」のパッケージ行は無い
  - `Assets/Scripts` に `UnityEngine.AdaptivePerformance` を使うコードは無い（git grep 0 件）
  - Android の 4 つのビルドプロファイル（`Assets/Settings/Build Profiles/{Release,Demo}_{Dev,Master}_Android.asset`）はすべて `m_AdaptivePerformanceEnabled: 0`
- 整合性: 他ページとの矛盾なし。本文のサンプルコード 2 本（Samsung / ChatGPT）はプロジェクトのコード規定に沿っていない（命名・`Debug.Log` の書式・Singleton）。参考コードとして扱う旨を明記する。
- 変更点:
  - 新節「プロジェクトでの採用状況」を「詳細」の最後（「ChatGPTからもらったサンプルコード」の後ろ）に追加
- 織り込んだ反映項目: なし（10〜60 に本ページを反映先とする項目は無い。50_基盤・ツール・規約.md の「未調査」欄で技術調査ページとして挙がっているだけ）
- 出典: `Packages/manifest.json:54`、`Assets/Settings/Build Profiles/Release_Master_Android.asset:64`、`Demo_Master_Android.asset:65`、`Release_Dev_Android.asset:1034`、`Demo_Dev_Android.asset:1035`、`ProjectSettings/ProjectVersion.txt`（6000.3.10f1）
- 要確認:
  - Adaptive Performance を今後採用する予定があるか（クライアントリード・技術班に）
  - Unity 6000.3 では Adaptive Performance が組み込みモジュールとして入っている。本文の「Package Manager で検索してインストールする」手順が 6000.3 でもそのまま当てはまるかは未確認（実機・エディタ確認が必要）

## 適用する本文

（追記: 挿入位置は「詳細」の最後、「ChatGPTからもらったサンプルコード」節の後ろ）

### プロジェクトでの採用状況
  2026-09-22 時点で、Symphony Kill Chord は Adaptive Performance を使っていない。このページは調査メモとして残す。
  - `Packages/manifest.json` には組み込みモジュール `com.unity.modules.adaptiveperformance` だけがあり、「Adaptive Performance」「Adaptive Performance Android」のパッケージは入っていない。
  - Adaptive Performance の API を使うコードは無い。
  - Android のビルドプロファイル 4 種（Release / Demo × Dev / Master）は、すべて Adaptive Performance が無効になっている。
  - 上の 2 本のサンプルコードは参考用であり、プロジェクトのコード規定には沿っていない。採用するときは、コード規定・設計思想に合わせて作り直す。
  【要確認: Adaptive Performance を今後採用する予定があるか、クライアントリード・技術班に】
