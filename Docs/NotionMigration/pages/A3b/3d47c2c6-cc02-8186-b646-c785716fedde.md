# 仕様検索Bot

- page_id: 3d47c2c6-cc02-8186-b646-c785716fedde
- Notion パス: Symphony Kill Chord / システム概要 / SinfoniaOperator / 仕様検索Bot
- スナップショットの最終更新: 2026-09-07T17:10:23.166Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 一部古い — 確認コミット 73fefeb45。チャンク分割（800 文字・重なり 100 文字、`MarkdownChunker.cs:66-72`）、E5 のプレフィックス、`SpecIndex` の `FILE_VERSION = 1`（`SpecIndex.cs:149`）、`DeferAsync` は実装どおり。一方、「LLM で要約しない」は誤り。`GeminiSummarizer`（`SinfoniaOperator/SinfoniaOperator/GeminiSummarizer.cs`）があり、`DiscordBotManager.cs:243-258` は要約器が設定されていれば Gemini で要約する（PR #1502「SinfoniaOperatorを仕様検索Bot化(Oracle Cloud + Gemini要約)」）。検索スコアへのパス別の重み付け（`SpecIndex.cs:59`、`SpecPriorityTable.cs`、`SinfoniaOperator/spec-search-priority.json`）が記載されていない。VM への配備は自動化された（`DeploySinfoniaOperator.yml`、#1538）
- 整合性:
  - 親ページ「SinfoniaOperator」(31b7c2c6-cc02-80ab-b731-fd020e05bf1c) の「外部 AI API への課金は発生しない」と同じ誤り（同ページも修正）
  - リポジトリの `SinfoniaOperator/SpecSearchBot実装レポート.md` も「要約しない」のままで、実装と食い違う（01 棚卸し）
  - `MarkdownChunker.cs:69` は `Docs/NotionSpecifications` を前提にしている。2026-09-22 の決定（キャッシュを `Library/` へ移す）で要修正（02 §2.4・T-5）。未実装なので「今後の仕様（未実装）」に分けた
- 変更点:
  - 冒頭の段落: 旧「ローカルの埋め込みモデルだけで動作し、外部AI APIへの課金は発生しない」→ 新「検索はローカルの埋め込みモデルで行い、`GEMINI_API_KEY` がある場合だけ Gemini で要約する」
  - 「全体の流れ」節の図: 重み付けと Gemini 要約のステップを追加した
  - 「なぜLLMで要約しないか」節 → 見出しを「AI要約（Gemini）」に改め、現行の挙動（根拠は抜粋だけ、失敗時の文言、モデル名の既定値、タイムアウト）を書いた。要約を任意にしている理由（ハルシネーション対策・VM の負荷）は残した
  - 「`SpecIndex`（検索インデックス）」節: 重み付け（`spec-search-priority.json`）の説明と重みの表を追加した
  - 「Discord側の実装」節: 要約あり・なしで Embed の中身が変わることを追加した
  - 「仕様書を更新したときの運用」節: Bot 本体の配備は自動になったこと、インデックスの元になる Markdown の用意方法が未確認であることを追加した
  - 新節「今後の仕様（未実装）」: 仕様書キャッシュの移動に伴う参照先の変更を追加した
- 織り込んだ反映項目: TL-11, TL-12, TL-13
- 出典: 実装（上記ファイル、`SinfoniaOperator.cs:95,305-319,562-573`）、PR #1502 / #1538、`SinfoniaOperator/GeminiSummarizer実装レポート.md`、`Docs/NotionMigration/02_キャッシュ運用とNotion記述規約.md` §2.4・§2.6 T-5
- 要確認:
  - 本番 VM で `GEMINI_API_KEY` が設定され、要約が有効か（要実機確認）
  - VM 上でインデックスの元になる `Docs/NotionSpecifications` をどう用意しているか。`deploy/oracle-vm-setup.md` の sparse-checkout 手順は、同フォルダが `.gitignore` の対象のため機能しない（TL-13、D-09。VM 管理者に確認）
  - 現状の件数「2000ファイル・数千チャンク」は 09-07 時点の値で、再確認していない

## 適用する本文

Discordの`/spec query:<質問文>`スラッシュコマンドから、`Docs/NotionSpecifications`配下の仕様書をセマンティック検索する機能。検索はローカルの埋め込みモデルだけで行う。`GEMINI_API_KEY`を設定した場合に限り、検索でヒットした抜粋を根拠にGemini APIで回答を要約して返す（この場合は外部AI APIの利用になる）。

## 全体の流れ
```Mermaid
graph LR
    A["仕様書 Markdown"] -->|"MarkdownChunker"| B["チャンク（見出し単位・800文字）"]
    B -->|"OnnxEmbeddingModel"| C["埋め込みベクトル"]
    C -->|"SpecIndex.Save"| D["spec-index.bin"]
    E["/spec query"] -->|"OnnxEmbeddingModel"| F["クエリベクトル"]
    D -->|"SpecIndex.Load"| G["SpecIndex.TopK（パス別の重み付き）"]
    F --> G
    G -->|"GEMINI_API_KEY なし"| H["Discord Embedで返信（抜粋+Notionリンク）"]
    G -->|"GEMINI_API_KEY あり"| I["GeminiSummarizer"]
    I --> J["Discord Embedで返信（要約+Notionリンク）"]
```
インデックス生成（`index`サブコマンド）と検索応答（`serve`サブコマンド）は別プロセス・別タイミングで動く。仕様書を更新したら`index`を再実行しない限り、検索結果には反映されない。

## AI要約（Gemini）
`GEMINI_API_KEY`が設定されている場合、`serve`は`GeminiSummarizer`を生成し、検索でヒットしたチャンクを根拠に質問への回答を要約する。未設定の場合は要約器を作らず、従来どおりチャンクの抜粋とNotionリンクをそのまま返す。
- プロンプトは「以下の仕様書の抜粋だけを根拠に、質問に日本語で簡潔に答えてください。抜粋に無い情報は答えないでください。」という指示に、チャンクの見出しパンくずと本文、質問文を続けたものである。
- 使うモデルは`GEMINI_MODEL`で指定する。未設定なら`gemini-3.6-flash`を使う。
- リクエストのタイムアウトは15秒である。HTTPエラー、生成が正常終了しない（`finishReason`が`STOP`以外）、要約文が空、のいずれかの場合は、要約欄に「AI要約の生成に失敗しました。」と表示し、出典のNotionリンクは返す。

要約を必須にせず、キーを設定したときだけ有効にしているのは次の理由による。
- 要約はハルシネーション（誤った内容の生成）のリスクを持つ。仕様書は正確性が求められるため、根拠の抜粋に限定したプロンプトを使い、出典のNotionリンクを必ず併記する。
- 要約無しであれば埋め込みモデル（軽量）だけで完結し、生成モデル（重量級）をVMで動かす必要が無い。要約は外部APIに任せるため、Oracle Cloud無料枠の非力なVMでも動かせる規模に収まる。

## `MarkdownChunker`（チャンク分割）
`Docs/NotionSpecifications/**/*.md`を再帰的に読み込み、次の規則でチャンクへ分割する。
- `assets/`ディレクトリ配下と`_database.md`は除外する（画像・データベース定義であり検索対象として有用でないため）。
- `#`・`##`・`###`の見出しから、パンくず文字列（例：「設計思想 > バトルシステム > ダメージ計算」）を構築する。検索結果の見出しとして表示するのに使う。
- セクション本文は800文字ごと、100文字のオーバーラップを持たせて分割する。日本語は単語区切りが無いため、文字数ベースの分割を採用している（英語のような単語数ベースの分割は行っていない）。
- 各ファイル冒頭付近にある`[Notionで開く](https://...)`のリンクを正規表現で抽出し、そのファイルのNotion URLとして全チャンクに引き継ぐ。

チャンクサイズ（800文字）とオーバーラップ（100文字）は`MarkdownChunker`のコンストラクタ引数で調整できる。埋め込みモデルの最大トークン長（512トークン）を超えないよう、変更する場合は上限に注意する。

## `OnnxEmbeddingModel`（埋め込み生成）
多言語対応の軽量埋め込みモデル`intfloat/multilingual-e5-small`（384次元、ONNX形式）を`Microsoft.ML.OnnxRuntime`で実行する。トークナイズにはXLM-RoBERTa系のSentencePieceモデルが必要で、`Lokad.Tokenizers`の`XLMRobertaTokenizer`を使っている。
推論結果はトークンごとの隠れ状態を平均プーリングし、L2正規化したベクトルを返す。E5系モデルの慣例に従い、インデックス生成時は`passage: <パンくず>\n<本文>`、検索時は`query: <検索文>`というプレフィックスを付けてからモデルへ渡す（`SpecIndexBuilder`と`DiscordBotManager`でそれぞれ付与）。**このプレフィックスを省略すると検索精度が大きく落ちる。** E5系モデル特有の学習前提であり、他の埋め込みモデルに差し替える場合は前提を確認すること。
`EmbedAsync`の呼び出しは`SemaphoreSlim`で直列化している。ONNX Runtimeのセッションはスレッドセーフだが、非力なVM上で同時に複数推論を走らせるとメモリを圧迫するため、意図的に1件ずつ処理する設計にしている。

## `SpecIndex`（検索インデックス）
チャンクとその埋め込みベクトルを配列として保持し、コサイン類似度によるTopK検索を提供する。件数が数千件規模（現状2000ファイル・数千チャンク）であるため、総当たり比較で十分な速度が出ており、外部のベクトルデータベースは導入していない。件数が数万を超える規模になったら再検討する。
保存形式は独自の単純なバイナリで、ファイル識別子とバージョン番号を先頭に持つ。フォーマットを変更する場合は`FILE_VERSION`を上げ、旧バージョンの読み込み可否を明示すること。

### 検索の重み付け
`SPEC_SEARCH_PRIORITY_PATH`で重み付けのJSON（リポジトリでは`SinfoniaOperator/spec-search-priority.json`）を指定すると、コサイン類似度にチャンクの元ファイルのパスに応じた重みを掛けてから順位を付ける（`SpecPriorityTable`）。確定した仕様のツリーを上に、議論の途中経過を下に出すための仕組みである。ルールは上から順に照合し、最初に当てはまったものを使う。どれにも当てはまらないファイルは既定の重みになる。未設定の場合は重み付けをしない。

| パスに含む文字列 | 重み |
|---|---|
| `Symphony Kill Chord/基本情報.md` | 1.5 |
| `Symphony Kill Chord/仕様概要/` | 1.5 |
| `Symphony Kill Chord/システム概要/` | 1.5 |
| `Symphony Kill Chord/世界観設定/`・`背景設定/`・`キャラクター設定/`・`ステータスリスト/`・`用語/`・`マスターデータ/`・`ワークフロー/` | 1.2 |
| `Symphony Kill Chord/議事録/`・`スケジュール/` | 0.7 |
| `Symphony Kill Chord/仕様検討フォーラム/`・`スケジュール/タスク管理/` | 0.4 |

現在値の正本は`SinfoniaOperator/spec-search-priority.json`である。

## Discord側の実装（`DiscordBotManager`）
`serve`サブコマンド実行時、`ConfigureSpecSearch`でインデックスと埋め込みモデル（あわせて重み付け表とGemini要約器）を受け取り、`Ready`イベントで`/spec`スラッシュコマンドを登録する。`SPEC_SEARCH_DISCORD_GUILD_ID`を設定していればそのGuildへ即時登録（開発向け、反映が速い）、未設定ならグローバル登録（反映に最大1時間程度かかる）を行う。
`InteractionCreatedHandler`は`DeferAsync()`で応答を保留してから検索を行う。埋め込み推論やAI要約に数秒かかる場合があるため、Discordの3秒応答タイムアウトを避ける目的である。検索結果は`SPEC_SEARCH_TOP_K`件（既定3件）を返す。
- 要約なしの場合: Discord Embedのフィールドとして返し、フィールド名に見出しパンくず、値に本文抜粋（400文字まで）とNotionリンクを入れる。
- 要約ありの場合: Embedの説明欄に検索文と要約を入れ、各フィールドには見出しパンくずとNotionリンクだけを入れる（本文抜粋は載せない）。

## 仕様書を更新したときの運用
1. Notion側の仕様書を更新する。
2. `NotionMarkdownExporter`で`Docs/NotionSpecifications`のミラーを最新化する。
3. VM上で`index`サブコマンドを再実行し、`spec-index.bin`を作り直す。
4. `serve`プロセスを再起動する（`SpecIndex`はプロセス起動時に一度だけ読み込むため、再起動しないと新しいインデックスが反映されない）。

手順3・4を自動化するには、systemdタイマーや`ExecStartPre`でのインデックス再生成などが候補になるが、現状は手動運用としている。
なお、Bot本体のプログラムの配備は自動化されている（`develop`へのマージで`DeploySinfoniaOperator.yml`が実行される。「Oracle Cloudデプロイ手順」ページを参照）。自動配備はプログラムを入れ替えてサービスを再起動するだけで、インデックスは作り直さない。
【要確認: VM上で手順2のミラーをどこで用意しているか（`Docs/NotionSpecifications`はgit管理外のため、リポジトリのクローンでは取得できない）を、VM管理者に確認する】

## 今後の仕様（未実装）
2026-09-22の決定により、NotionMarkdownExporterの出力先は`Docs/NotionSpecifications`から`Library/NotionSpecifications`へ移る。あわせて次を直す。いずれも未実装である。
- `MarkdownChunker`（`SPECIFICATION_RELATIVE_PATH`）と`index`サブコマンドのリポジトリルート探索が`Docs/NotionSpecifications`を前提にしているので、新しい出力先を読むようにする。
- VM上で仕様書のミラーを用意する手順を、VM側でエクスポーターを動かす構成に合わせて書き直す（`deploy/oracle-vm-setup.md`のsparse-checkout手順は廃止する）。
- エクスポーターは定期的な全件の取り直しをしない方針になった（Notion APIの制限のため）。インデックスを作り直す前に、更新日時だけを先に確かめて変わったページだけを取り直す。
