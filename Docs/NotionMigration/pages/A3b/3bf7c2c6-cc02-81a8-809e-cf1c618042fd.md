# ① シーン初期化のフェーズ実行フロー

- page_id: 3bf7c2c6-cc02-81a8-809e-cf1c618042fd
- Notion パス: Symphony Kill Chord / システム概要 / 初期化ライフサイクル / ① シーン初期化のフェーズ実行フロー
- スナップショットの最終更新: 2026-08-17T18:19:22.930Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 一部古い — 「同じフェーズを全モジュールで終えてから次のフェーズへ進む」とフェーズの順番は実装どおり（`InitializationCoordinator.cs` の `InitializeAsync`）。ただし `ISceneInitializationReadiness.Complete` を呼ぶのは Coordinator ではなくエントリポイント（`IngameComposition.cs:163`）で、失敗時にその場で止まる分岐が図に無い。最終確認 2026-09-22 / 73fefeb45
- 整合性: 親 初期化ライフサイクル (3bf7c2c6-cc02-81fc-afe1-dbe0d028cda1) の「⚠️ 失敗時の挙動と進捗報告」と揃えた
- 変更点:
  - シーケンス図: 旧: `Coord ->> Readiness: Complete(sceneName, isSuccess)` → 新: Coordinator は成否を Entry へ返し、Entry が `Complete` を呼ぶ。失敗したフェーズで止まる `alt` を追加した。対象シーンが InGame 以外でも同じであることを冒頭の文に足した
- 織り込んだ反映項目: IF-01（関連部分）
- 出典: `InitializationCoordinator.cs`、`IngameComposition.cs:92-163`、`OutGameSceneInitializer.cs:206`、`PersistentEntryPoint.cs:187`
- 要確認: なし

## 適用する本文

同じフェーズを全モジュールで終えてから、次のフェーズへ進む。図はインゲームの例で、アウトゲーム（`OutGameSceneInitializer`）と常駐（`PersistentEntryPoint`）も同じ流れである。
```Mermaid
sequenceDiagram
    autonumber
    participant Entry as IngameComposition
    participant Coord as InGameInitializationCoordinator
    participant Modules as 各 ~Initializer（Order順）
    participant Readiness as ISceneInitializationReadiness

    Entry ->> Coord: 初期化開始
    Coord ->> Modules: Init()（Order昇順）
    Coord ->> Modules: ResourceLoadAsync()（Order昇順、非同期）
    Coord ->> Modules: Build()（Order昇順。ServiceLocatorへ登録）
    Coord ->> Modules: Ready()（Order昇順。他モジュールを取得して結線）
    alt いずれかのモジュールがfalseを返した
        Coord -->> Entry: false（そのフェーズで以降を止める）
    else 全フェーズ成功
        Coord -->> Entry: true
    end
    Entry ->> Readiness: Complete(sceneName, isSuccess)
    Note over Readiness: ここで初めてロード画面が閉じる
```
