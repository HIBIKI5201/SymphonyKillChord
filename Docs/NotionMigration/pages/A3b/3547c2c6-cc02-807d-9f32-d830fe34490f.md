# Behavior Graphについて

- page_id: 3547c2c6-cc02-807d-9f32-d830fe34490f
- Notion パス: Symphony Kill Chord / システム概要 / Behavior Graphについて
- スナップショットの最終更新: 2026-05-04T07:07:05.545Z
- 書き込み許可: 可（システム概要 配下）
- 処置: 本文置き換え（「作成済みノード一覧」「作成済みのファサード一覧」の 2 表と、グラフ本体の置き場所だけを変更。他の節は原文のまま）
- 確認日 / コミット: 2026-09-22 / 73fefeb45

## 判定

- 信頼性: 一部古い — 概要・開発思想・Blackboard・独自ノードの作り方は現行と一致する（Blackboard にはファサード（View 層の MonoBehaviour）を置く: `Assets/Scripts/Runtime/4.View/InGame/Enemy/AIFacade/`）。ただし 2026-05 以降に追加されたノードとボス用のノード・ファサードが一覧に無い
  - 追加された雑魚敵ノード: `WaitForDiscoveryAction`、`IsDiscoveredCondition`（索敵、20_ EN-07）、`IsBattleAiActivatedCondition`（戦闘 AI の一括切替、PR #1393）
  - ボス用ノード 8 種（`Assets/Scripts/Runtime/4.View/InGame/Enemy/Boss/BehaviorGraphNode/`）とファサード 4 種（`…/Boss/AIFacade/`）
  - `EnemySharedFacade` は初期化されていない（`6.Composition/InGame/Enemy/EnemyLifeCycle.cs:214` の `Initialize` 呼び出しがコメントアウト）。本文の「※まだ未使用」は正しい。ただし本文の「Shared属性がオン」は、`EnemyAIGraph.asset` の Blackboard 変数がすべて `IsShared: 0` である点と食い違う
  - ボス用の `BossSharedFacade` は `6.Composition/InGame/Enemy/Boss/BossLifeCycle.cs:225` で初期化され、`BossGraph.asset` からも参照されている
- 整合性:
  - 「敵」（3957c2c6-cc02-80cd-94c0-f412b11ffcf7）の索敵・硬直の記述（20_ EN-07 / EN-11 / EN-25）と、ノードの役割は一致する。
  - 「Bossシステム」（37a7c2c6-cc02-8022-ae1c-c799fa5e9db8）はボス AI の開発資料で、グラフの原型は `Assets/DevelopProducts/Research/BossSystem/BehaviorGraph/BossGraph.asset` に残っている。本番は `Assets/Level/BehaviorGraphs/BossGraph.asset` を使う。
- 変更点:
  - 「作成済みノード一覧」節: 旧: 8 行 → 新: 雑魚敵用 11 行 + ボス用 8 行。ボス用ノードが複製で、Blackboard 変数の型がボス用ファサードであることを 1 文追記
  - 「作成済みのファサード一覧」節: ボス用 4 行を追加。EnemySharedFacade の行に【要確認】（Shared 属性の食い違い）を追加
  - 「作成済みノード一覧」の前に、グラフ本体の置き場所（`Assets/Level/BehaviorGraphs/EnemyAIGraph.asset` / `BossGraph.asset`）を追記
- 織り込んだ反映項目: なし（10〜60 に本ページを反映先とする項目は無い。20_ EN-07・EN-11・EN-25 の内容をノード表の概要に反映）
- 出典: `Assets/Scripts/Runtime/4.View/InGame/Enemy/BehaviorGraphNode/{Action,Condition}/*.cs` の `NodeDescription` / `Condition` 属性、`Assets/Scripts/Runtime/4.View/InGame/Enemy/Boss/BehaviorGraphNode/*.cs`、`Assets/Scripts/Runtime/4.View/InGame/Enemy/Boss/AIFacade/*.cs`、`Assets/Scripts/Runtime/3.Adaptor/InGame/Enemy/EnemyAIControllerRegistry.cs`、`Assets/Level/BehaviorGraphs/`、PR #1393
- 要確認:
  - `EnemySharedFacade` の「Shared属性がオン」が正しいか（グラフの Blackboard 変数はすべて Shared オフ。敵 AI 担当に）
  - グラフ本体のノード接続（分岐順）はアセットを読んでいないため未確認。20_ の調査も「コードから推定」としている
  - ボス用ノードを雑魚敵用と共通化する予定があるか（クライアントリードに）

## 適用する本文

# 説明 {color="gray_bg"}
Behavior Graphの概要説明と、独自ノードの作成方法の説明を行う。

## 参考資料
公式ドキュメント
（既存のリンクを残す）
個人記事
（既存のリンクを残す）
（既存のリンクを残す）

# 詳細 {color="gray_bg"}

## 概要
　UnityのBehavior Graphは、「Behavior TreeとState Machineの融合体」的なイメージ。一つのノードに「開始」「実行中」「成功」「失敗」の状態がある上、開始時と終了時に呼び出されるメソッドも用意されている。
　なお、Blackboardも存在しているが、これは開発者自身色々設定する必要があり、手数がかかる代わりに自由度が高いことが特徴。

## 開発思想
- 条件と行動は細かく分解してノードを作成する。
「再利用」「組み立て」のイメージでノードを使用する。
- ノード内に複雑な計算処理をしない。
行うのは情報取得、判定、指示、というシンプルなことのみ。
- BlackBoardにはView層にあるファサードを設定する。ノードがファサードから情報を取得したり、対象に指示を出したりする。
（既存のリンクを残す）
- State Machineの要素もあるため、「このノードがどう終了するか」「成功／失敗したらどこに行くのか」を意識する。
- 行動の優先度と、行動実行中に中断される可能性を意識する。

## ノードについて
Unity Behaviorのノードは主に下記種類となる：
| **種類** | **説明・概要** | **使用する場面** |
|---|---|---|
| Action | 具体的な動作を実行する | 移動、攻撃、アニメーション再生などの指示 |
| Condition | 条件を評価しtrue／falseを返す | 射程入り時や、攻撃可否などの判定 |
| Flow/Sequencing | ノードの実行順番、ノード間の遷移を制御する Behavior TreeのSelectorはここに | 一定の順番で行動を行う、状態による行動分岐、状態変わった時に行動を中断など |
| SubGraph | 別のBehavior Graphを呼び出す | 同じロジックを共通化・再利用など |
| Event | 外部イベントを受け取る | 例えばダメージ受ける時に怯むアニメーションを再生するActionを実行、など |
　現状は、主にActionとConditionノードを独自作成して、packageで提供されたFlow系ノードに合わせて敵の行動AIを作成している。

## Blackboardについて
　BlackboardはGraphにある全ノードがアクセスできる、変数を置く空間である。
　このプロジェクトでは、Blackboardに設定するものはファサード（Facade）とする。ファサードは、敵や環境の情報、及び行動を制御するメソッドをシンプルな形にして外に出して、ノードにアクセスさせる役割となる。
　Blackboard変数には、型の制限がある。概して、設定できるのは基本的な値型（int, string, enumなど）と、Unityフレームワークがサポートする型（GameObject, Transform, Animator, MonoBehaviourなど）。
　よって、Blackboardにあるファサードは、View層に存在する、MonoBehaviourを継承したクラスとされている。

### Blackboard変数の設定方法
- Graph画面でBlackboardの右上にある「+」をクリックして、適宜なデータ型を選択する。
画像検索を利用して作成済みファサードを選択している。
（既存の画像を残す）
- Blackboard変数を設定した後、Graphを使用するGameObjectを選択して、そのInspectorのBehavior AgentコンポーネントのBlackboardに設定することができる。
（既存の画像を残す）
- Graph画面にて、Blackboard変数の「Exposed」属性を設定できる。これをオフにした場合、GameObjectのInspectorではこの変数が非表示なる。ドラッグ＆ドロップではなく、コードでBlackboard変数を設定する場合、この属性を調整する。
（既存の画像を残す）
（既存の画像を残す）
- Blackboard変数の「Shared」属性をオンにすると、変数このGraphを使っているすべてのGameObject間の共通変数になる。周囲環境の情報や、プレイヤー情報の参照はSharedに設定することが想定されている。

## 独自ノードの作成

### Actionノード
- Graphの空いている箇所に右クリックして、Create New → [Action]をクリックする
（既存の画像を残す）
- ノード名とカテゴリを設定してNextをクリックする
ノード名は、後に「Action」が付けられて、スクリプトのクラス名になる。
カテゴリは、Scriptable ObjectのmenuNameのように、メニュー階層を作るための情報。
（既存の画像を残す）
- ノードの説明文を設定してCreateをクリックする
ここで、半角スペース区切りでノードの説明、及びパラメータを設定する。
単語ごとに、単純文字か、特定型のパラメータか設定できる。が、型には制限があり、基本的な値型とUnityフレームワークがサポートする型しか設定できない。
※日本語も使えるが、パラメータで日本語を使うと、生成されたコードの変数名も日本語になってしまうので、日本語の使用は単純文字（Regular Text）だけにしましょう。
（既存の画像を残す）
- 生成されたノードに、BlackboardからDrag & Dropして設定する
（既存の画像を残す）
（既存の画像を残す）
- 生成されたコードの「story」、「category」にて、前述の説明文やカテゴリなどを変えることができます。
（既存の画像を残す）

### Conditionノード
　Conditionノードは、あらかじめGraphに「条件が付いているノード」配置してから、追加することができる。
　配置済みのノードを選択して、Inspectorにある「Assign Condition」をクリックして、「Create new Condition」を選択すると、Conditionノードを作成できる。
　後の手順は前述のノードとほぼ同じなので省略とする。
（既存の画像を残す）
（既存の画像を残す）

### ノードのスクリプト
ActionノードとConditionノードは、主役となるメソッドが異なる。

#### Actionノード
ActionノードのOnStart()とOnUpdate()メソッドには戻り値がある。
Status.Successが成功、Status.Failueが失敗、Status.Runningが実行中を表す。
成功と失敗の場合、Graphの作りに従って次に行くべきノードに遷移する。実行中の場合、このノードに止まって、毎フレームOnUpdate()メソッドが呼び出される。
- OnStart()
このノードに遷移した時呼び出されるメソッド。
主に前処理と、事前の状態確認を行う。
- OnUpdate()
OnStart()メソッドがStatus.Runningを返却すると、次にOnUpdate()が毎フレーム呼び出される。
主に毎フレームの処理と判定を担当する。
- OnEnd()
ノード終了時に呼び出されるメソッド。
主に後処理を担当する。

#### Conditionノード
Conditionノードは、IsTrue()メソッドが主役。このメソッドで情報を取得して、判定するのが一般的。メソッド返却値がConditionノードの結果となる。
なお、返却値のないOnStart()とOnEnd()を持っている。これらのメソッドは前処理と後処理を行う。

## 作成済みノード一覧
グラフ本体は `Assets/Level/BehaviorGraphs/` にある。雑魚敵は `EnemyAIGraph.asset`、ボスは `BossGraph.asset` を使う。
ノードのスクリプトは、雑魚敵用が `Assets/Scripts/Runtime/4.View/InGame/Enemy/BehaviorGraphNode/`、ボス用が `Assets/Scripts/Runtime/4.View/InGame/Enemy/Boss/BehaviorGraphNode/` にある。
| **ファイル名** | **種類** | **概要** |
|---|---|---|
| AttackTargetAction | Action | プレイヤーに攻撃する |
| GetStunnedAction | Action | クリティカルヒットされた時の硬直アニメーションを再生する |
| MoveToAttackAction | Action | 攻撃を行える位置になるまで移動する |
| StopMovingAction | Action | 移動を停止する |
| WaitForDiscoveryAction | Action | プレイヤーを発見するまでその場で見回しを続ける。視界の条件を満たすと発見済みにする |
| IsTargetInAttackRangeCondition | Condition | プレイヤーが自分の射程範囲内に居るか |
| IsAimSightClearCondition | Condition | 自分とプレイヤーの間に障害物がないか |
| IsAttackingCondition | Condition | 攻撃を実行中か |
| IsStunnedCondition | Condition | 硬直中か |
| IsDiscoveredCondition | Condition | プレイヤーを発見済みか |
| IsBattleAiActivatedCondition | Condition | 戦闘 AI が有効か（チュートリアルなどで敵の戦闘 AI をまとめて止めるときに使う） |
| BossAttackTargetAction | Action | ボス用。プレイヤーに攻撃する |
| BossGetStunnedAction | Action | ボス用。硬直を開始する |
| BossMoveToAttackAction | Action | ボス用。攻撃を行える位置になるまで移動する |
| BossStopMovingAction | Action | ボス用。移動を停止する |
| BossIsTargetInAttackRangeCondition | Condition | ボス用。プレイヤーが射程範囲内に居るか |
| BossIsAimSightClearCondition | Condition | ボス用。自分とプレイヤーの間に障害物がないか |
| BossIsAttackingCondition | Condition | ボス用。攻撃を実行中か |
| BossIsStunnedCondition | Condition | ボス用。硬直中か |
　ボス用のノードは、雑魚敵用のノードを複製したもので、Blackboard 変数の型がボス用のファサード（BossBattleAIFacade など）になっている。

## 作成済みのファサード一覧
ファサードは、雑魚敵用が `Assets/Scripts/Runtime/4.View/InGame/Enemy/AIFacade/`、ボス用が `Assets/Scripts/Runtime/4.View/InGame/Enemy/Boss/AIFacade/` にある。
| **ファイル名** | **説明** |
|---|---|
| EnemyMovementAIFacade | 敵の移動に関する情報、指示メソッドを持つ |
| EnemyBattleAIFacade | 敵の戦闘に関する情報、指示メソッドを持つ |
| EnemyStateFacade | 敵の状態に関する情報、指示メソッドを持つ |
| EnemySharedFacade | Shared属性がオン。敵全員に共通する情報を持つ ※まだ未使用【要確認: グラフの Blackboard 変数はすべて Shared がオフになっている。どちらが正しいか、敵 AI 担当に】 |
| BossMovementAIFacade | ボス用。EnemyMovementAIFacade の複製 |
| BossBattleAIFacade | ボス用。EnemyBattleAIFacade の複製 |
| BossStateFacade | ボス用。EnemyStateFacade の複製 |
| BossSharedFacade | ボス用。EnemySharedFacade の複製。雑魚敵用と違い、BossLifeCycle で初期化されている |
