# 敵関連　2026-05-08

- page_id: 35a7c2c6-cc02-8036-ba68-d37a2865cc6c
- Notion パス: Symphony Kill Chord / システム概要 / 敵関連　2026-05-08
- スナップショットの最終更新: 2026-05-08T07:48:02.396Z
- 書き込み許可: 可
- 処置: アーカイブ推奨

## 判定

- 信頼性: 陳腐化（記録としては概ね正確） — 「改修内容」は実装に反映済みである。`CanelAttack`・`IEnemySharedFacede`・`IEnemyNavigationAgent` は Runtime に 0 件、`EnemyAttackReservationUsecase` の namespace は `KillChord.Runtime.Application.InGame.Enemy`（`EnemyAttackReservationUsecase.cs:7`）、`EnemyAttackUsecase` に `_musicSyncService` は無い。`EnemyMoveDebugInitializer`・`EnemyArtilleryMoveDebugInitializer` は存在せず、現行は `EnemyLifeCycle` と `IEnemyAttackControllerGenerator`（歩兵・砲兵・三方向の各 Generator）で攻撃 Controller を差し替える。砲弾は `ShellAttackSpecAsset`（5.InfraStructure/InGame/Enemy）と `ShellFactory`・`ShellEntity` でデータ化されている
- 整合性: 現行のモジュール文書 `システム概要 / 敵`（3957c2c6-cc02-80cd-94c0-f412b11ffcf7）と題材が重複する。「各クラス概要」の一覧は 2026-05 時点のもので、ボス・`EnemyLifeCycle`・`ShellLifeCycle` などが無い
- 変更点:
  - ページ冒頭（「各レイヤーの色分け」の前）: アーカイブのコールアウトと、残す設計判断の要点を追加
- 織り込んだ反映項目: EN-33, RL-14
- 出典: 実装（上記ファイル）、`20_敵・ステージ・カメラ・HUD.md` EN-33、`50_基盤・ツール・規約.md` RL-14
- 要確認: アーカイブの置き方（RL-14、要企画確認）。W-23 のキャッシュマップ確認

## 適用する本文

（ページ冒頭、「各レイヤーの色分け」の前に挿入する）

> 📦 **アーカイブ（2026-05 のリファクタリング記録）**
このページは 2026-05 に行った敵モジュールのリファクタリングの記録である。記載の改修は実装済みで、クラス一覧はその時点のものである。現行の構成は「システム概要 / 敵」を参照する。
現行実装に残っている設計判断は次のとおり。
- 歩兵と砲兵の初期化は共通化し、違いである攻撃 Controller だけを `IEnemyAttackControllerGenerator` の実装で差し替える（ボスの三方向攻撃も同じ仕組みで追加された）
- 砲弾の爆発範囲と着地タイミングは砲弾固有のデータとして ScriptableObject（`ShellAttackSpecAsset`）に持たせる
- 砲弾は生成時に敵から攻撃定義（`AttackDefinition`）を受け取ってダメージ計算に使い、敵の AI Controller には依存しない
