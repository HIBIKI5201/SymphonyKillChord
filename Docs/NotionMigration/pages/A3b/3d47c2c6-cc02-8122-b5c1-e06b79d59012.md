# アーキテクチャ

- page_id: 3d47c2c6-cc02-8122-b5c1-e06b79d59012
- Notion パス: Symphony Kill Chord / システム概要 / SinfoniaOperator / アーキテクチャ
- スナップショットの最終更新: 2026-09-07T17:10:20.018Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 一部古い — 確認コミット 73fefeb45。プロジェクト構成・ターゲット（`SinfoniaOperator.Core.csproj:5` の `netstandard2.1;net10.0`、`CopyToUnity` ターゲット :25）と設定の優先順位は実装どおり。CLI に `branches` が追加された（`SinfoniaOperator.cs:37-41,367-401`、#1525）。`serve` は Gemini 要約器（`GeminiSummarizer`）・検索の重み付け表（`SpecPriorityTable`）・`/branches` も注入するようになった（`SinfoniaOperator.cs:305-334`）
- 整合性: 「設定・環境変数リファレンス」(3d47c2c6-cc02-81e6-93d5-fdca6adc65f9) の「引数で渡した JSON ファイルのみを読み込む」は、実装（指定した全ファイルを順に読み、無いキーは環境変数から取る: `SinfoniaOperator.cs:98-124`、`OperatorConfig.cs:99`）と表現がずれている。同ページ側で直した
- 変更点:
  - 「Bot本体（Exe）のCLIサブコマンド」節の表: `branches` 行を追加した。「この分岐の順序」の注意書きに、`branches` が `serve` の後・既定動作の前で判定されることを足した
  - 「常駐（`serve`）とバッチ（デフォルト）の違い」節: `serve` が追加で注入するもの（重み付け表・Gemini 要約器・`/branches`）を追加した
  - 「なぜ`SpecSearch`を別プロジェクトに切り出したか」節: Gemini 要約は Exe 側（`SinfoniaOperator/GeminiSummarizer.cs`）にあり、Core に入れていないことを 1 文追加した
- 織り込んだ反映項目: TL-11, TL-12
- 出典: 実装（上記ファイル）、PR #1502 / #1525
- 要確認: なし

## 適用する本文

SinfoniaOperatorは3つの`.NET`プロジェクトに分かれている。役割の境界を保守時に見失わないよう、まずこの分割の理由から説明する。

## プロジェクト構成
| プロジェクト | ターゲット | 役割 |
|---|---|---|
| `SinfoniaOperator.Core` | `netstandard2.1;net10.0` | Notion API連携、Discord送信、設定管理（`OperatorConfig`）の共通ライブラリ |
| `SinfoniaOperator`（Exe） | `net10.0` | Bot本体。CLIサブコマンドで日次通知・仕様検索インデックス生成・常駐Bot・ブランチ一覧を切り替える |
| `SinfoniaOperator.SpecSearch` | `net10.0` | 仕様検索専用ライブラリ。チャンク分割・埋め込み生成・検索インデックス・検索の重み付け |

### なぜCoreだけ`netstandard2.1`も対象にしているか
`SinfoniaOperator.Core`は、ビルド後に`Assets/Editor/Plugins/SinfoniaOperator/`へ自動コピーされ、Unity Editor拡張（Notionタスク表示・作業ログ送信）から参照される。Unity側から参照可能にするため`netstandard2.1`を維持している。`csproj`の`CopyToUnity`ターゲットがこのコピーを担う。
この制約があるため、Coreには**Unity非互換なAPIを持ち込まない**（`PriorityQueue`など、netstandard2.1に無いものは使用禁止）。

### なぜ`SpecSearch`を別プロジェクトに切り出したか
仕様検索機能はONNX Runtime（`Microsoft.ML.OnnxRuntime`）と`Lokad.Tokenizers`という比較的重量級の依存を持つ。これをCoreに混ぜると、Unity側へコピーされる成果物にも巻き込まれてしまう。Unity Editorには不要な依存であるため、`net10.0`単独ターゲットの別プロジェクトとして切り離した。
Bot本体（Exe）は`SpecSearch`をプロジェクト参照するが、Coreは`SpecSearch`を参照しない。依存の向きはBot → SpecSearch / Bot → Core であり、SpecSearchとCoreの間に依存関係は無い。
検索結果をGemini APIで要約する`GeminiSummarizer`も、Unityには不要なためCoreには入れず、Bot本体（Exe）に置いている。

## Bot本体（Exe）のCLIサブコマンド
`SinfoniaOperator.cs`の`Main`が、第一引数によって挙動を振り分ける。
| 引数 | 挙動 | 実行元 |
|---|---|---|
| （なし） | 今日のタスク・タスクアラート・（月曜のみ）スプリントをDiscordへ通知して終了する | GitHub Actions（毎朝07:00 JST） |
| `send --channel <種別> --message <本文>` | Notionを読まず、指定チャンネルへ単発メッセージを送って終了する | 外部プロセスからの軽量呼び出し |
| `index` | 仕様書からベクトル検索インデックスを生成してファイルへ保存する | 手動、または仕様更新後の再生成 |
| `serve` | Discord Gatewayへ接続したまま常駐し、`/spec`スラッシュコマンド（と、設定があれば`/branches`）に応答する | Oracle Cloud VM上のsystemdサービス |
| `branches [--remote <名前>] [--base <名前>] [--keep <名前>]... [--no-fetch] [--github]` | ベースブランチへマージ済みで削除できるリモートブランチを、最終コミットが古い順に標準出力へ表示して終了する。`--github`を付けるとローカルのクローンではなくGitHub APIを参照する | 手動 |

**この分岐の順序と挙動は変更しないこと。** デフォルト（引数なし）の日次通知は既存のGitHub Actionsワークフロー（`.github/workflows/SinfoniaOperator.yml`）が依存している。新しいサブコマンドを追加する場合は、既存の分岐より前に判定を割り込ませない（`branches`も`serve`の後、既定動作の前で判定している）。

### 常駐（`serve`）とバッチ（デフォルト）の違い
日次通知は「Discordへログイン→送信→即終了」という一過性のプロセスであり、GitHub Actionsのスケジュール実行に向いている。一方`serve`は`DiscordSocketClient`の接続を維持し続け、`InteractionCreated`イベントを待ち受ける。両者は同じ`DiscordBotManager`クラスを使うが、`serve`側だけが`ConfigureSpecSearch`で仕様検索インデックスと埋め込みモデルを注入し、`/spec`コマンドを登録する。
`ConfigureSpecSearch`では、あわせて検索の重み付け表（`SPEC_SEARCH_PRIORITY_PATH`を設定した場合）とGemini要約器（`GEMINI_API_KEY`を設定した場合）も渡す。また`GITHUB_REPOSITORY`を設定した場合に限り、`ConfigureBranchCleanup`で`/branches`コマンドを登録する。
`serve`は`PosixSignalRegistration`でSIGINT/SIGTERMを捕捉し、systemdの`stop`要求に対してグレースフルシャットダウンする。Windows上での動作検証は行っていない（Linux VMでの常駐を前提に実装している）。

## 設定管理の仕組み
`OperatorConfig`（Core）が設定値の解決を一元化している。優先順位は次のとおり。
1. JSON設定ファイル（`sinfonia-operator.env.json`は公開設定、`sinfonia-operator.secrets.json`はGit除外の秘匿設定）
2. 環境変数（JSONに無いキーのフォールバック）

設定キー名の文字列リテラルは`OperatorConfigKeys.cs`（Core）に一元化しており、Bot・Unity両方がこの定数を参照する。**新しい設定キーを追加するときは、必ずここに定数を足してから使う。** 文字列を直接埋め込むと、キー名の変更漏れに気づけなくなる。
詳細な設定キー一覧は「設定・環境変数リファレンス」ページを参照する。
