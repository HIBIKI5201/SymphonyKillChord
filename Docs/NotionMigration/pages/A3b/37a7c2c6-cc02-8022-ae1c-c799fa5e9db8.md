# Bossシステム

- page_id: 37a7c2c6-cc02-8022-ae1c-c799fa5e9db8
- Notion パス: Symphony Kill Chord / システム概要 / Bossシステム
- スナップショットの最終更新: 2026-06-09T06:35:00.737Z
- 書き込み許可: 可
- 処置: 統合（統合先 3957c2c6-cc02-80cd-94c0-f412b11ffcf7）

## 判定

- 信頼性: 一部古い — 研究ブランチ（2026-06-05〜09）の開発記録。その後ボスは本体に入った。`BossLifeCycle`（6.Composition/InGame/Enemy/Boss）、`BossAIController`・`BossAttackPattern`・`EnemyTripleShotAttackController`（3.Adaptor/InGame/Enemy/Boss）、`BossAttackReservationUsecase`・`EnemyTripleShotAttackUsecase`（2.Application）、`BossMoveView`・Behavior Graph ノード（4.View）、`BossAttackKind`（1.Domain）は現存する。一方、`BossTestInitializer`・`BossAttackEntry` は存在せず、現行はテスト用ドライバ `BossInitializer` と、攻撃定義アセット `BossAttackEntryAsset`・`BossAttackEntryRepo`（5.InfraStructure/InGame/Enemy/Boss）である。三方向攻撃の射線判定は `TripleShotRaycastDetectView` で実装済みで、左右は ±45°（`TripleShotRaycastDetectView.cs:116`）。本文の「右30度・左30度」「正面のみで判定」は古い。攻撃パターンは今もランダム選択（`BossAIController.cs:159-161`）
- 整合性: 現行のモジュール文書 `システム概要 / 敵`（3957c2c6-cc02-80cd-94c0-f412b11ffcf7）がボスのクラスを含めて説明しており、題材が重複する。本文の「新規作成（BehaviorGraphNode）」の表は「新規作成（Scripts）」の表の重複コピーで、実際のノード名（`BossMoveToAttackAction` など）は下の「Behavior Graph ノード」節にある。「InGameCompositon.cs に RegisterInstance を追加してほしい」は研究時の依頼で、現行コードでの要否は未確認
- 変更点:
  - Bossシステムのページ冒頭（「ボスシステム 開発ドキュメント」の直後）: アーカイブのコールアウトを追加
  - 統合先「敵」の「🔌 拡張ポイント」表: ボスの攻撃種別を追加する手順の行を追加
  - 統合先「敵」の「🧅レイヤー情報」の後: 「既知の制限」節を新設し、有効な TODO（ランダム選択のみ、スタン演出なし）を移す
- 織り込んだ反映項目: EN-33, RL-14
- 出典: 実装（上記ファイル、`BossLifeCycle.cs:160-164,180`、`BossGetStunnedAction.cs:22`、`BossInitializer.cs:16-21`）、`20_敵・ステージ・カメラ・HUD.md` EN-26・EN-33
- 要確認: 「敵」ページは他領域の担当範囲なので、追記は「敵」の原稿担当と調整する。研究時の「InGameComposition でターゲット登録しないとボスがターゲットされない」問題が現行で解消済みか【要確認: 実機 / 敵担当】。アーカイブの置き方（RL-14 は要企画確認）

## 適用する本文

### A. Bossシステム（このページ）の冒頭に挿入する

（「ボスシステム 開発ドキュメント」の見出しの直後、既存のコールアウトの前に挿入する）

> 📦 **アーカイブ（2026-06 の研究記録）**
このページは研究ブランチ `feature/research/boss/miyamoto` の開発記録である。ボスはその後本体に取り込まれ、現行の構成は「システム概要 / 敵」を参照する。
本文との違いは次のとおり。
- `BossTestInitializer` は `BossInitializer`（テスト用ドライバ）に、`BossAttackEntry` は攻撃定義アセット `BossAttackEntryAsset` / `BossAttackEntryRepo` になった
- 三方向攻撃の射線判定は実装済みで、左右の角度は 45° である（本文の 30°・「正面のみで判定」は古い）
- 研究時の依存コードは `Assets/DevelopProducts/Research/BossSystem/` に残っている

### B. 統合先「システム概要 / 敵」の「🔌 拡張ポイント」表に追加する行

| 拡張したいこと | 実装する場所 | 追加登録の要否 |
|---|---|---|
| ボスの攻撃種別を追加したい | `BossAttackKind` Enumに値を追加し、`IEnemyAttackController`を実装したControllerと、対応する`IEnemyAttackControllerGenerator`を作る。`BossLifeCycle`の`generators`ディクショナリに登録する | 必要（登録が無い種別を攻撃定義に指定すると、`generators[entry.Kind]`で例外になる） |
| ボスの攻撃パターンの選び方を変えたい | `BossAIController.SelectPattern()`を差し替える（HPフェーズ制・順番制など） | 不要 |

### C. 統合先「システム概要 / 敵」の「🧅レイヤー情報」の後に追加する節（Notion の H2 として追加する）

## 既知の制限
- ボスの攻撃パターンはランダムに選ぶ（`BossAIController.SelectPattern()`）。HPフェーズ制・順番制は未実装である
- ボスのスタンは状態を切り替えるだけで、スタン演出は無い（`BossGetStunnedAction`）
- `BossInitializer`はボス単体テスト用のドライバであり、ステージへの本組み込みは未完了である
