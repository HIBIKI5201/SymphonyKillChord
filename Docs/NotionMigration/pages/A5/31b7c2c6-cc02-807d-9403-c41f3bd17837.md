# 開発部門 Unityプロジェクト開発規定

- page_id: 31b7c2c6-cc02-807d-9403-c41f3bd17837
- Notion パス: Symphony Kill Chord / ワークフロー / プログラマー / 開発部門 Unityプロジェクト開発規定
- スナップショットの最終更新: 2026-04-11T09:32:00.030Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 一部古い — ディレクトリ `Assets/DevelopProducts/Research/`・`Design/` と、ブランチ名 `feature/research/[プロダクト名]/[個人名]`（2026-08-15〜09-22 のマージ済み PR に 12 件、`…/master` → develop が 2 件）は実態と一致する。クリエイトメニューの名前が違う。確認したコミット: 73fefeb45
- 整合性:
  - 実装との矛盾: 「便利機能」節の `Create/DevelopProducts/ProductsTemplate` → 実際のメニューは `Assets/Create/DevelopProducts/Products Template (Native)`（`Assets/DevelopProducts/Utility/Editor/Scripts/DevelopProductsTemplateCreator.cs:13`、`DevelopProductsConst.cs:6-7`）
  - 実装との関係: `Assets/DevelopProducts/` には Research・Design のほかに Mocks・TicketSystem・Utility がある
  - 他ページとの関係: GitHub 運用の自動化（AutoCreateMasterBranch など）は クライアント部門 Unityプロジェクト開発規定（34a7c2c6-cc02-8007-a640-cc8a457ddd69）の原稿に書いた。研究ブランチにも同じ自動化が動く
  - 他文書との関係: `Assets/DevelopProducts/Design/Architecture/Scripts/README.md` がある（RL-11。中身の移植は未判断）
- 変更点:
  - 「便利機能」節: 旧 `Create/DevelopProducts/ProductsTemplate` → 新 `Assets/Create/DevelopProducts/Products Template (Native)`
  - 「ディレクトリ」節: Research・Design 以外のフォルダ（Mocks・TicketSystem・Utility）を 1 行追加
  - 「GitHub運用規定」節: 自動化はクライアント部門の規定と同じであることを 1 行追加
- 織り込んだ反映項目: RL-11（README の存在のみ）
- 出典: 実装 `Assets/DevelopProducts/Utility/Editor/Scripts/DevelopProductsTemplateCreator.cs:13`、`Assets/DevelopProducts/Utility/Scripts/Runtime/DevelopProductsConst.cs`、`Assets/DevelopProducts/` / PR 一覧（`feature/research/` 14 件）
- 要確認:
  - `Assets/DevelopProducts/Design/Architecture/Scripts/README.md` を本ページへ移すか（要確認、RL-11）
  - Mocks・TicketSystem・Utility の置き場の規則（要確認）

## 適用する本文

## SymphonyKillChordプロジェクトの場合

### ディレクトリ
開発者はプロダクトごとにディレクトリを作成する。
研究班のディレクトリのパスは
```C#
Assets/DevelopProducts/Research/
```
設計班のディレクトリのパスは
```C#
Assets/DevelopProducts/Design/
```
これらのディレクトリの下にプロダクトのフォルダを作成し、その中で作業する。
`Assets/DevelopProducts/`にはこのほかに`Mocks/`・`TicketSystem/`・`Utility/`がある。

#### 便利機能
クリエイトメニューの
```C#
Assets/Create/DevelopProducts/Products Template (Native)
```
を使えば、自動でフォルダを生成できる。

### スクリプティング
各プロダクトのディレクトリにはasmdef（アセンブリ）を設定する。
アセンブリ名は `DevelopProducts-[プロダクト名]`  とする。
名前空間を必ず設定する。
名前空間名は `DevelopProducts.[プロダクト名].[カテゴリー]`  とする。

### GitHub運用規定
基本的な命名や運用規則は [[GitHub 運用規定]] に従う。
ただし、ブランチ命名の際は `feature/research/[プロダクト名]/[個人名]` とする。
プロダクトの更新時は、 `feature/research/[プロダクト名]/master`  に対してPRを行う。
リードがこれを確認する。承認されれば作業者はmasterブランチにマージする。
最終的に作業が完了すれば、masterをdevelopにマージする。
masterブランチの自動作成と、masterからdevelopへのDraft PRの自動作成は、クライアント部門と同じく動く。
[[クライアント部門 Unityプロジェクト開発規定]]

### 制約
1. 担当プロダクトのディレクトリ以外は編集しない
もしくは編集してもプッシュしない
2. グローバル設定を変えたい際はリードに連絡して、マスターのブランチがまず編集を適用し、次に個人ブランチがプルして適用する
