# パッケージ導入手順

- page_id: 3367c2c6-cc02-8028-86f0-e7b1743c3582
- Notion パス: Symphony Kill Chord / ワークフロー / プログラマー / パッケージ導入手順
- スナップショットの最終更新: 2026-04-02T14:25:01.037Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 一部古い — 申請 → 承認 → develop へ導入 → 各自 Pull の流れはフォーラム「プログラマーワークフロー パッケージ導入について」（3277c2c6-cc02-8076-bc6d-f325365871cd、適用済）と同じ。手順 3 の追記先のリンクが空 `[[]]`。NuGet パッケージの置き場の説明が無い。確認したコミット: 73fefeb45
- 整合性:
  - 他ページとの矛盾: 手順 3 の `[[]]` は、使用素材一覧表（DB 27d7c2c6-cc02-814a-aae1-c011961e1d46）を指すと推定される（RL-13）。同 DB には実際に使っているパッケージの多く（UniTask・ObservableCollections・ZString・NuGetForUnity・uloop など）が載っていない（RL-12）
  - 重複: ルート直下のフォーラム行「プログラマーワークフロー パッケージ導入について」は本ページの元の案で、ステータスは適用済。統合の対象ではなく、記録として残る
  - 実装との関係: UPM のパッケージは `Packages/manifest.json`、NuGet のパッケージは `Assets/Settings/Nuget/packages.config` に宣言され、NuGet の実体は git 管理外の `Assets/AssetStoreTools/` に入る（`Assets/NuGet.config:13`）。2026-06 以降の manifest.json / packages.config の変更は 7 コミット
- 変更点:
  - 「仕様詳細」節の手順 3: 旧「プランナーが[[]] に追記」→ 新「プランナーが[[使用素材一覧表]]に追記」（RL-13）
  - 「仕様詳細」節の後ろに、新節「パッケージの宣言場所」を追加
- 織り込んだ反映項目: RL-13, RL-12（宣言場所の事実のみ）
- 出典: フォーラム（0191、適用済）/ 実装 `Packages/manifest.json`、`Assets/Settings/Nuget/packages.config`、`Assets/NuGet.config:12-14`、`git log -- Packages/manifest.json Assets/Settings/Nuget/packages.config`（2026-06-01 以降 7 件）
- 要確認:
  - 手順 3 のリンク先が使用素材一覧表で正しいか（要確認）
  - このフローで実際に承認されたかは PR から確かめられない（承認は Discord。Discord 未照合）

## 適用する本文

## 概要
外部パッケージはリードによって導入される。
導入を希望する場合は、プログラマーチャンネルにて申請し、ディレクターの承認を経て、developに導入され、Pullする形で導入する。

## 意図
全てのエンジニアが秩序なくパッケージを導入できると、package.jsonの衝突やMITライセンスの不透明性という課題があるため、承認と導入を一元化する必要があるため。

## 仕様詳細
外部パッケージの導入は以下のフローで行われる。
1. 導入を希望するパッケージのリンクをDiscordで共有
2. クライアント部門リードプログラマー及びディレクターが確認して承認
3. プランナーが[[使用素材一覧表]]に追記
4. クライアント部門リードプログラマーがパッケージをUnityに追加してdevelopへマージ
5. 各員がプルして適用

## パッケージの宣言場所
| 種類 | 宣言するファイル | 実体の置き場 |
|---|---|---|
| UPMのパッケージ | `Packages/manifest.json` | Unityが取得する |
| NuGetのパッケージ | `Assets/Settings/Nuget/packages.config` | `Assets/AssetStoreTools/`（git管理外。AssetStoreToolsのunitypackageで配る） |
NuGetのパッケージを追加した場合は、AssetStoreToolsのunitypackageも作り直して共有する必要がある。【要確認: 作り直しの担当をリードプログラマーに】
