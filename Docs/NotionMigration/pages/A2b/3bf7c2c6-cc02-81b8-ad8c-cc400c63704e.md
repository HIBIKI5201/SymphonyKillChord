# レティクル

- page_id: 3bf7c2c6-cc02-81b8-ad8c-cc400c63704e
- Notion パス: Symphony Kill Chord / システム概要 / レティクル
- スナップショットの最終更新: 2026-08-17T18:11:18.696Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 最新（一部補足） — 型 7/7 実在、Order 660 と `TargetSystemModuleContainer` への依存も一致する。コードは 2026-07-21（6f9f7c90e「全敵レティクル表示を実装」）以降変わっていない。直すのは次の 3 点だけである。確認コミット 73fefeb45、確認日 2026-09-22
- 整合性:
  - 実装との矛盾: モジュール結合図の `R_Adaptor --> R_View` は向きが逆。`ReticleHudView` が `ReticleHudPresenter` を保持し、`LateUpdate` で `Tick` を呼ぶ（`Assets/Scripts/Runtime/4.View/InGame/Reticle/ReticleHudView.cs:22,40-47`）
  - 実装との矛盾: Camera への依存は Camera モジュールのクラスではなく、`ReticleHudInitializer` が取得する `Camera.main`（常駐カメラ）である（`Assets/Scripts/Runtime/6.Composition/InGame/Reticle/ReticleHudInitializer.cs`）。メインカメラが無いと初期化に失敗する
  - 他ページとの関係: 「注目中・候補」の意味が本ページで定義されていない。候補は「未ロック時に、今攻撃したらロックオンする敵」（`システム概要 / ターゲットシステム` 39e7c2c6-cc02-80e9-933f-f02210602bf6 の原稿に定義）。ロックオン表示の 3 段階（確定 / 候補 / その他）は `仕様概要 / インゲームHUD / 戦闘UI`（3437c2c6-cc02-806c-b092-e809750fd4f0）に仕様として入っていない（CM-14、仕様概要側の担当）
- 変更点:
  - 「概要」: コールアウトに、注目中＝ロックオン中の敵、候補＝未ロック時に今攻撃したらロックオンする敵、と定義を足し、ロックオン表示の 3 段階のうち「その他の敵」を担当することを追記した。最終更新日 2026-08-17 → 2026-09-22
  - 「🔗 モジュール結合」: 旧: `R_Adaptor --> R_View` → 新: `R_View --> R_Adaptor`。Camera のノードを 旧: 「View カメラ」 → 新: 「Persistent カメラ Camera.main」にし、矢印を Composition からに直した
  - 「📥 依存しているもの」Camera: 旧: 依存箇所「カメラ」 → 新: `Camera.main`（`ReticleHudInitializer` が取得し `CameraScreenProjector` へ渡す）
  - 「④ View」: `LateUpdate` で Presenter を呼ぶこと、マーカーの使い回し、カメラの後ろと画面外の敵は表示しないことを追記した
- 織り込んだ反映項目: CM-14（システム概要 / レティクル側）
- 出典: 実装 `Assets/Scripts/Runtime/3.Adaptor/InGame/Reticle/ReticleHudPresenter.cs`、`4.View/InGame/Reticle/*.cs`（`CameraScreenProjector.cs:30-56`）、`6.Composition/InGame/Reticle/ReticleHudInitializer.cs`、`Assets/Scripts/Runtime/3.Adaptor/InGame/UI/HUDEnemyHealthPresenter.cs:36-63`（確定・候補の表示）
- 決定（2026-09-22 八幡）: R27 ロックオン表示の 3 段階は追認する。本文の「概要」のコールアウト（3 段階のうち「その他の敵」を担当）はそのまま残した
- 要確認: なし

## 適用する本文

- 概要: 画面内の敵へレティクルを表示するモジュール
- カテゴリー: インゲーム

---

## 概要

> 💡 **モジュール概要**
> 画面内の敵にレティクル（照準マーカー）を重ねて表示するモジュールである。注目中（ロックオン中）・候補（未ロック時に、今攻撃したらロックオンする敵）の敵はUIモジュールの体力ウィジェットが強調表示するため、当モジュールはそれ以外の生存敵だけを扱う。ロックオン表示は「確定（ロック中）」「候補」「その他の敵」の3段階であり、当モジュールは「その他の敵」を担当する。

| 項目 | 内容 |
|---|---|
| **モジュール名** | Reticle |
| **カテゴリ** | InGame |
| **ステータス** | 実装済み |
| **最終更新日** | 2026-09-22 |

---

### 🏗️ クラス

| クラス名 | レイヤー | 役割・機能 |
|---|---|---|
| `**IScreenProjector**` | Adaptor | ワールド座標をスクリーン座標へ投影する処理の抽象 |
| `**ReticleHudPresenter**` | Adaptor | 登録済みの敵から、注目・候補を除いた生存敵のレティクル表示情報を算出する |
| `**ReticleMarker**` | Adaptor | 1体分のレティクル表示情報 |
| `**CameraScreenProjector**` | View | `IScreenProjector`実装。Cameraを用いて投影する |
| `**ReticleHudView**` | View | 対象全体のレティクルをScreen Space GUI上に表示する |
| `**ReticleMarkerView**` | View | 1体分のマーカーを表すコンポーネント |
| `**ReticleHudInitializer**` | Composition | レティクル表示まわりの生成と依存解決（Order 660） |

#### 🧩 Composition初期化情報

| 項目 | 内容 |
|---|---|
| **Initializerクラス** | `ReticleHudInitializer` |
| **Order** | 660（Target(100)とEnemy(700)の間。`TargetSystemModuleContainer`を取得するためTargetより後に置く） |
| **公開する ModuleContainer / ServiceLocator登録型** | なし |

---

### 🔗 モジュール結合

```Mermaid
graph TD
    %% 定義 (接続のないレイヤーは省略)
    subgraph ReticleModule [Reticle モジュール]
        R_Adaptor["Adaptor<br>ReticleHudPresenter, IScreenProjector"]
        R_View["View<br>ReticleHudView, CameraScreenProjector"]
        R_Composition["Composition<br>ReticleHudInitializer"]
        R_View --> R_Adaptor
        R_Composition --> R_Adaptor
        R_Composition --> R_View
    end

    subgraph TargetModule [Target モジュール]
        T_Composition["Composition<br>TargetSystemModuleContainer"]
    end

    subgraph UIModule [UI モジュール]
        UI_View["View<br>HUDEnemyHealthView"]
    end

    subgraph CameraModule [Camera モジュール]
        C_Persistent["Persistent<br>カメラ（Camera.main）"]
    end

    %% 依存関係
    R_Composition -->|"登録済みターゲットの取得"| T_Composition
    R_Composition -->|"投影に使うカメラの取得"| C_Persistent
    UI_View -.->|"注目・候補は UI 側が担当（重複回避）"| R_Adaptor
```

#### 📥 依存しているもの

- `**Target**`
  - *依存箇所*: `TargetSystemModuleContainer`, `ITargetSystemViewModel`
  - *詳細*: 登録済みのターゲットから、注目中と候補のIDを除いた生存敵を取り出す
- `**Camera**`
  - *依存箇所*: `Camera.main`
  - *詳細*: `ReticleHudInitializer`がメインカメラを取得して`CameraScreenProjector`へ渡し、ワールド座標をスクリーン座標へ投影する。メインカメラが見つからない場合は初期化に失敗する

#### 📤 依存されているもの

- なし
  - *詳細*: 他モジュールから参照されない表示専用のモジュールである

---

## 詳細

### 🧅レイヤー情報

#### ① Domain

当モジュールでは使用していない。

#### ② Application

当モジュールでは使用していない。

#### ③ Adaptor

`ReticleHudPresenter`が表示対象を決める。注目中と候補の敵は`HUDEnemyHealthView`（UIモジュール）が強調表示するため、両IDを除外して重複を避ける。座標変換は`IScreenProjector`の先へ追い出しており、Presenter自体はUnityのCameraに依存しない。

#### ④ View

`ReticleHudView`がScreen Space GUI上へマーカーを並べ、`ReticleMarkerView`が1体分を表す。`ReticleHudView`は`LateUpdate`で`ReticleHudPresenter.Tick()`を呼んで表示対象を受け取り、対象IDごとにマーカーを割り当てる。表示対象から外れたマーカーは非表示にして使い回す。`CameraScreenProjector`が投影を担当し、カメラの後ろにある敵と画面外の敵は表示対象から外す。

#### ⑤ Infrastructure

当モジュールでは使用していない。

#### ⑥ Composition

`ReticleHudInitializer`（Order 660）が構築する。`TargetSystemModuleContainer`をServiceLocatorから取得するため、Targetモジュールより後に初期化される必要がある。

### 🔌 拡張ポイント

| 拡張したいこと | 実装する場所 | 追加登録の要否 |
|---|---|---|
| 投影方法を差し替えたい | `IScreenProjector`（Adaptor）を実装したクラスを作り、`ReticleHudInitializer`で差し替える | 必要（Initializerでの差し替えが要る） |
| 表示対象の条件を変えたい | `ReticleHudPresenter`の除外条件を変更する。現在は注目中と候補のIDを除外している | 不要 |

### 🔄処理フロー

主要な処理フローは、それぞれ子ページに分けている。

- 📄 [[① レティクル表示フロー（毎フレーム）]] (3bf7c2c6-cc02-81ae-aad1-e1e9477cfd3a)
