# ハードエッジ問題

- page_id: 31e7c2c6-cc02-8097-abfc-c97ecafb660a
- Notion パス: Symphony Kill Chord / システム概要 / リアルトゥーンシェーダー / 背面法を利用したアウトラインの作成 / ハードエッジ問題
- スナップショットの最終更新: 2026-04-05T07:25:50.533Z
- 書き込み許可: 可
- 処置: 本文置き換え（既存の本文はそのまま残し、コード例 1 行の修正と保存先の補足、末尾の節の追加のみ）
- 確認日 / コミット: 2026-09-22 / 73fefeb45

## 判定

- 信頼性: 最新（手法は実装と一致する）。コード例に 1 か所誤りがある — 実装は `TangentBaker.cs:45-49`（`mesh.uv4` へ格納）、`HLSL/OutLine/UVToSmoothNormal.hlsl`、`HLSL/OutLine/OutLine.hlsl`（`_IsSmoothNormal` で切り替え）
- 整合性:
  - 実装との矛盾: 「スムーズノーマルの計算 > 実装例」の `CalcSmoothNormals` が、宣言していない `normals` を使っている（このままではコンパイルできない）。保存先の「UVチャンネル」は、実装では UV4（シェーダーでは TEXCOORD3）
  - 他ページとの矛盾: なし
- 変更点:
  - 「スムーズノーマルの計算 > 実装例」: 旧: `normals` が未宣言 → 新: `var normals = mesh.normals;` を追加
  - 「スムーズノーマルの保存」: 旧: 「UVチャンネルに書き込んで保存」 → 新: 「UVチャンネル（現在の実装では UV4。シェーダー側では TEXCOORD3）に書き込んで保存」
  - ページ末尾に新しい節「現在の実装」を追加: 焼き込みツール（`InspectorTangentBaker` / `MeshAssetTool`）と、SilToon 側の `_IsSmoothNormal`
- 織り込んだ反映項目: AS-16（関連）
- 出典: `Assets/DevelopProducts/Research/ToonShader/Scripts/Editor/TangentBaker.cs`、`InspectorTangentBaker.cs:19-30`、`SmoothNormal/MeshAssetTool.cs:17-60,130`、`Assets/Scripts/Shaders/SilToon/HLSL/OutLine/UVToSmoothNormal.hlsl`、`OutLine.hlsl`
- 要確認: なし

## 適用する本文

### 参考
[https://youtu.be/gPCtS33jx7Q?t=260](https://youtu.be/gPCtS33jx7Q?t=260)

### 詳細
アウトラインの基本的な実装は、
法線方向にメッシュを拡張することで、通常モデルを包むようにしています。
ですが、立方体など角が鋭利なモデルでは頂点ごとに法線方向が大きく異なるため、アウトラインに隙間ができてしまいます。

### 球(角が緩やか)
（既存の画像を残す）

### 立方体(角が鋭利)
（既存の画像を残す）

## 方法
アウトライン専用のスムーズノーマルを用意することで解決できます。
スムーズノーマルとは、隣接するポリゴンの法線を平均化したもので、ハードエッジ部分でも連続したアウトラインが引けるようになります。

### 立方体の理想の法線
（既存の画像を残す）
ただし、シェーダー内では隣接ポリゴンの情報を取得できないため、
C#スクリプト側で事前にスムーズノーマルを計算してモデルに書き込む必要があります。

## スムーズノーマルの計算
同じ座標にある頂点の法線を平均化することでスムーズノーマルを計算します。
`Dictionary<Vector3, Vector3>`を使い、頂点座標をキー、法線の合計をバリューとして集計します。
同じ座標の頂点が見つかるたびに法線を加算し、最後に正規化することでスムーズノーマルが得られます。

### 実装例：
```C#
private static void CalcSmoothNormals(Mesh mesh, out Vector3[] smoothNormals)
{
	//頂点情報を取得
	var vertices = mesh.vertices;
	var normals = mesh.normals;
	var posToNormals = new Dictionary<Vector3, Vector3>();
	smoothNormals = new Vector3[vertices.Length];

	//同じ座標の頂点を探し、法線を加算
	for (int i = 0; i < vertices.Length; i++)
	{
	    var v = vertices[i];
	    if (!posToNormals.ContainsKey(v))
	        posToNormals[v] = Vector3.zero;
	    posToNormals[v] += normals[i];
	}

	//加算した法線の標準化
	for (int i = 0; i < vertices.Length; i++)
	{
	    smoothNormals[i] = posToNormals[vertices[i]].normalized;
	}
}
```

## スムーズノーマルの保存
計算したスムーズノーマルはUVチャンネル（現在の実装では UV4。シェーダー側では TEXCOORD3）に書き込んで保存します。
ただし、そのまま書き込むとボーンアニメーションで法線がずれてしまうため、タンジェント空間に変換してから保存します。
タンジェント空間に変換することで、ボーンの動きに追従した法線として扱えるようになります。
また、スムーズノーマルは正規化されているため、XYの2成分だけ保存すれば、シェーダー側でZを復元できます。（Z = sqrt(1 - X² - Y²)）

### 実装例：
```C#
private static void CalcVectorToTangetToUV(Mesh mesh, in Vector3[] smoothNormals, out Vector2[] result)
{
    var normals = mesh.normals;
    var tangents = mesh.tangents;
    result = new Vector2[normals.Length];

    for (int i = 0; i < normals.Length; i++)
    {
        var N = normals[i];
        var T = (Vector3)tangents[i];
        var B = Vector3.Cross(N, T) * tangents[i].w;

        // タンジェント空間に変換
        var sn = smoothNormals[i];
        var snTS = new Vector3(
            Vector3.Dot(sn, T),
            Vector3.Dot(sn, B),
            Vector3.Dot(sn, N)
        );
        snTS = snTS.normalized;

        // XYだけ格納（Zはシェーダーで復元）
        result[i] = new Vector2(snTS.x, snTS.y);
    }
}
```

## 結果
あとはシェーダー側で、
NormalVectorからUVに格納されているスムーズノーマルに変更するだけで実装完了です。

### 適用前
（既存の画像を残す）

### 適用後
（既存の画像を残す）

## 現在の実装
スムーズノーマルの計算と保存は、エディタ拡張で行います（`Assets/DevelopProducts/Research/ToonShader/Scripts/Editor/`）。
- メッシュの Inspector 上部の「BakeSmoothNormals」ボタンで、選んだメッシュに焼き込みます（`InspectorTangentBaker` → `TangentBaker.BakeMesh`）。
- ルートの GameObject を選んでメニュー「Tools/MeshAssetTool/①Generate Mesh Assets」を実行すると、その子のメッシュを複製したアセットを作って焼き込みます。続けて「②Apply Mesh Assets」で、選択中のオブジェクトのメッシュを複製版に差し替えます（`MeshAssetTool`）。元のメッシュは書き換えません。

シェーダー側は SilToon（`Assets/Scripts/Shaders/SilToon/HLSL/OutLine/UVToSmoothNormal.hlsl`）で、TEXCOORD3 の XY から Z を復元し、タンジェント空間からオブジェクト空間へ戻します。
マテリアルの `_IsSmoothNormal` をオンにしたときだけ、このスムーズノーマルでアウトラインを押し出します。オフのときは通常の法線を使います。

