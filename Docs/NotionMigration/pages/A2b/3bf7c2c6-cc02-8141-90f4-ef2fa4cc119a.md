# ① 通常追従・回転フロー（毎フレーム）

- page_id: 3bf7c2c6-cc02-8141-90f4-ef2fa4cc119a
- Notion パス: Symphony Kill Chord / システム概要 / カメラ / ① 通常追従・回転フロー（毎フレーム）
- スナップショットの最終更新: 2026-08-17T13:19:20.003Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 一部古い — 計算の順番（ボーン回転 → 追従位置 → 距離解決 → 注視点回転 → 反映）は現行と一致する。ただし 2026-08-18 以降に入ったカメラシェイクの後乗せと、非ロックオン時の候補更新、外部制御中の停止が図に無い。確認コミット 73fefeb45、確認日 2026-09-22
- 整合性: 実装との矛盾は無い（欠落のみ）。`Assets/Scripts/Runtime/4.View/InGame/Camera/CameraSystemView.cs:258-289`（更新タイミングと外部制御中の停止）、`:491-546`（Tick）、`:655-671`（BuildFrame での候補更新）
- 変更点:
  - 冒頭の説明: 本番の更新タイミングが LateUpdate であること、外部制御中は更新しないことを追記した
  - シーケンス図: `BuildFrame` の中でロックオン対象の位置取得と、非ロックオン時の候補更新（`UpdateCandidate`）を追加した。`SetPositionAndRotation` の直前にシェイクの揺れ量を足す手順を追加した
- 織り込んだ反映項目: CM-11（システム概要側・フロー部分）
- 出典: 実装 `CameraSystemView.cs`、`Assets/Level/Scenes/Develop/EnemyMoveTest/CameraManager.prefab:93`（`_updateMode: 3` = LateUpdate、`Assets/Scripts/Runtime/0.Utility/InGame/UpdateModeEnum.cs`）
- 要確認: なし

## 適用する本文

`UpdateModeEnum`で選んだタイミング（Update / FixedUpdate / LateUpdate。本番のprefabはLateUpdate）で1フレーム分の状態を組み立て、Transformへ反映する。外部制御中（`BeginExternalControl()`後）は更新しない。

```Mermaid
sequenceDiagram
    autonumber
    participant CSView as CameraSystemView
    participant TargetVM as ITargetSystemViewModel (Target)
    participant FollowCalc as CameraFollowCalculator
    participant BoneCalc as CameraLockOnRotationCalculator / CameraFreeLookRotationCalculator
    participant LookAtCalc as CameraLookAtRotationCalculator
    participant ShakeCalc as CameraShakeCalculator

    CSView ->> CSView: BuildFrame（入力・ロックオン状態を構築）
    alt ロックオン中
        CSView ->> TargetVM: 現在ターゲット位置の取得
        TargetVM -->> CSView: ターゲット位置（無ければロックオン解除）
    else 非ロックオン
        CSView ->> TargetVM: UpdateCandidate（今攻撃したらロックオンする候補を更新）
    end
    CSView ->> BoneCalc: ロックオン状態に応じたボーン回転計算
    BoneCalc -->> CSView: ボーン回転
    CSView ->> FollowCalc: 追従位置の計算
    FollowCalc -->> CSView: 追従位置
    CSView ->> CSView: 障害物を考慮した距離を解決（ResolveDistance）
    CSView ->> LookAtCalc: 注視点回転の計算
    LookAtCalc -->> CSView: カメラ回転
    CSView ->> ShakeCalc: 発生中のシェイクの揺れ量を取得
    ShakeCalc -->> CSView: 揺れ量（計算結果へ後から足す）
    CSView ->> CSView: SetPositionAndRotation
```
