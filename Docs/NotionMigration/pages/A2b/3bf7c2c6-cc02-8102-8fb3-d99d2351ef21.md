# ① クリア → 完了ボタン → OutGame帰還フロー

- page_id: 3bf7c2c6-cc02-8102-8fb3-d99d2351ef21
- Notion パス: Symphony Kill Chord / システム概要 / リザルト / ① クリア → 完了ボタン → OutGame帰還フロー
- スナップショットの最終更新: 2026-08-17T07:36:07.514Z
- 書き込み許可: 可
- 処置: 本文置き換え

## 判定

- 信頼性: 一部古い — 呼び出しの順序は実装と一致する（`InGameSequenceDirector.ClearAsync`、`StageResultController.CompleteAsync`）。ただし `Show()` 内の処理（インゲーム Canvas の無効化・文字表示 Timeline）と、ポリシーによる遷移先の差し替え、遷移失敗時の復帰が図に無い。確認は 73fefeb45
- 整合性: 親ページ リザルト（39e7c2c6-cc02-808d-8c6b-dd69ea4edfa8）の原稿と揃えた。シークエンス ②（3bf7c2c6-cc02-813a-b8d9-e1b983c89a04）と前半が重なるため、ここでは Presenter 以降だけを詳しく書く
- 変更点:
  - 図: `Show()` の中で「インゲーム HUD の Canvas 群を無効化」「完了ボタンを初期選択」「勝利時の文字表示 Timeline → カウントアップ」を追加
  - 図: `CompleteAsync` の遷移先の決め方（ポリシーが返したシーン、無ければ `ReturnSceneName`）を追加
  - 図: 遷移失敗時にボタン操作を戻す分岐を追加
- 織り込んだ反映項目: HD-14、HD-21、HD-22（システム概要側の該当部分）
- 出典: `Runtime/4.View/InGame/Result/StageResultView.cs:41-60,78-104`、`Runtime/3.Adaptor/InGame/Result/StageResultController.cs`、`Runtime/6.Composition/InGame/Sequence/InGameSequenceDirector.cs`（ClearAsync）
- 要確認: なし

## 適用する本文

```Mermaid
sequenceDiagram
    autonumber
    participant Seq as InGameSequenceDirector (Sequenceモジュール)
    participant Presenter as StageResultPresenter
    participant View as StageResultView
    actor Player as プレイヤー
    participant Controller as StageResultController
    participant Policy as IStageResultExitPolicy (任意)
    participant SceneUC as SceneTransitionUsecase

    Seq ->> Presenter: PresentVictory(evaluationResult)
    Presenter ->> Presenter: StageRankCalculator.Calculate / MissionProgressから時間・コンボ取得
    Presenter ->> View: Apply(StageResultDTO)（ViewModel経由）
    Seq ->> View: Show()
    View ->> View: インゲームHUDのCanvas群を無効化 / 完了ボタンを初期選択
    View ->> View: StageResultReveal（Timeline）で文字を上から表示 → 数値をカウントアップ
    Player ->> View: 完了ボタン押下
    View ->> Controller: CompleteAsync()
    opt ポリシーが登録されている
        Controller ->> Policy: TryGetDestinationScene(Complete)
        Policy -->> Controller: 遷移先シーン名（返さなければReturnSceneName）
    end
    Controller ->> SceneUC: UnloadThenChangeSceneAsync(battleScene, inGameScene, 遷移先)
    alt 成功
        Controller ->> Controller: SelectedBattleStageState.Clear() / SelectedMissionState.Clear()
    else 失敗
        Controller -->> View: false
        View ->> View: 完了ボタンの操作を戻す
    end
```
