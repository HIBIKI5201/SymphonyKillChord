# ③ サブミッション進捗のHUD表示フロー（毎フレーム／イベント時）

- page_id: 3bf7c2c6-cc02-81fe-a927-eadd5d8f3456
- Notion パス: Symphony Kill Chord / システム概要 / ミッション / ③ サブミッション進捗のHUD表示フロー（毎フレーム／イベント時）
- スナップショットの最終更新: 2026-08-17T17:31:56.231Z
- 書き込み許可: 可（システム概要 配下）
- 処置: 本文置き換え
- 確認日 / コミット: 2026-09-22 / origin/develop 73fefeb45

## 判定

- 信頼性: 一部古い — 毎フレームの流れは実装どおり（`Assets/Scripts/Runtime/3.Adaptor/InGame/Mission/MissionEventController.cs:29-33`、`MissionHudPresenter.cs:26-28`）。表示状態の名前「達成済み・挑戦中・未達成」は、実装の Failed / Challenging / Succeeded（失敗 / 挑戦中 / 成功、`1.Domain/InGame/Mission/MissionEvaluationDisplaySituation.cs`）と合っていない。タイトルにある「イベント時」の更新（撃破・行動・死亡の通知ごとに `Present` を呼ぶ）が図に無い
- 整合性: 親ページ 39e7c2c6-cc02-80d5-ac4e-cd46b87fd375 の `MissionEvaluationRunner` の説明と揃えた。仕様概要 / ステージルール / ミッション 3257c2c6-cc02-809e-8860-fabeb3648dd8（EN-22、A1 担当）のサブミッションの判定タイミングと矛盾しない
- 変更点:
  - 冒頭の説明文: 旧: 「達成済み・挑戦中・未達成の状態」 → 新: 「成功・挑戦中・失敗」。現行の 4 条件は結果がミッション終了後に確定する（`Cleared`）ため、プレイ中は途中で失敗が確定したときだけ「失敗」になることを追記した
  - 図: 旧: 「未達成/挑戦中/達成済みを判定」 → 新: 「失敗/挑戦中/成功を判定」。通知のたびに `Present` する分岐を追加した
- 織り込んだ反映項目: EN-22（表示状態の名前・判定タイミング）
- 出典: 実装（上記ファイル、`2.Application/InGame/Mission/MissionEvaluationRunner.cs:57-80`、`1.Domain/InGame/Mission/EvaluationCondition/*.cs`、`MissionEvaluationResultTiming.cs`）
- 要確認: なし

## 適用する本文

プレイ中、成功・挑戦中・失敗の状態がリアルタイムでHUDに反映される（保存はされない、表示専用の再計算）。現行の4条件（クリア時間・被ダメージ・コンボ・武器種数）はいずれも結果の確定がミッション終了後（`MissionEvaluationResultTiming.Cleared`）であり、プレイ中は途中で失敗が確定した条件だけが「失敗」になる。

```mermaid
sequenceDiagram
    autonumber
    participant Loop as MissionLoopView
    participant EventCtrl as MissionEventController
    participant Runtime as MissionRuntimeService
    participant EvalRunner as MissionEvaluationRunner
    participant Presenter as MissionHudPresenter
    participant HudVM as IMissionHudViewModel

    alt 毎フレーム Update
        Loop ->> EventCtrl: Tick(deltaTime)
        EventCtrl ->> Runtime: Tick
    else 撃破・行動・プレイヤー死亡の通知時
        EventCtrl ->> Runtime: OnEnemyKilled / OnActionPerformed / OnPlayerDead
    end
    EventCtrl ->> Presenter: Present()
    Presenter ->> Runtime: BuildEvaluationResult()
    Runtime ->> EvalRunner: Run（失敗/挑戦中/成功を判定）
    EvalRunner -->> Runtime: MissionEvaluationResult
    Runtime -->> Presenter: MissionEvaluationResult
    Presenter ->> HudVM: MissionHudDTO を反映
```
