# 18. 命名一貫性と可読性

**最小驚きの原則**（Principle of Least Astonishment）に関わる指摘。
動作には影響しないが、**新規メンバーの学習コストと誤読のリスク**に直結する。

---

## 【確定】ディレクトリ名の綴り誤り

**場所**: `3.Adaptor/InGame/Animaiton/`

正しくは `Animation`。他の3レイヤーは正しく綴られている。

```
3.Adaptor/InGame/Animaiton      ← 誤り
4.View/InGame/Animation         ← 正しい
5.InfraStructure/InGame/Animation
6.Composition/InGame/Animation
```

**修正が容易な理由**: このディレクトリ内3ファイルの `namespace` は
すでに正しく `KillChord.Runtime.Adaptor.InGame.Animation` と書かれている。

```
3.Adaptor/InGame/Animaiton/ICharacterAnimationSignal.cs      → namespace ...InGame.Animation
3.Adaptor/InGame/Animaiton/ICharacterAnimationViewContext.cs → namespace ...InGame.Animation
3.Adaptor/InGame/Animaiton/ICharacterAnimationViewModel.cs   → namespace ...InGame.Animation
```

つまり**フォルダ名を変えるだけでコード修正はゼロ**。
Unityエディタ上でリネームすれば `.meta` のGUIDも保持される。

放置すると、`Animation` で検索したときにこのフォルダが引っかからず、
インターフェース定義を見落とす原因になる。

## 【確定】ファイル名の綴り誤り

**場所**: [AttackPilpelineAsset.cs](../../Assets/Scripts/Runtime/5.InfraStructure/InGame/Battle/AttackPilpelineAsset.cs)

正しくは `AttackPipelineAsset`。`Pipeline` が `Pilpeline` になっている。

関連する型は正しく綴られている。

```
1.Domain/InGame/Battle/IAttackPipeline.cs        ← 正しい
2.Application/InGame/Battle/AttackPipeline.cs    ← 正しい
5.InfraStructure/InGame/Battle/AttackPilpelineAsset.cs  ← 誤り
```

`AttackPipeline` で検索した際にこのアセットクラスだけ漏れるため、
攻撃パイプラインの設定箇所を追う際に見落とす。クラス名のリネームが必要なため
`Animaiton` フォルダより修正コストは高いが、影響範囲は限定的（このファイルと
それを参照する Composition 側のみ）。

---

## 【確定】Usecase と UseCase の混在

| 表記 | 件数 | 例 |
| --- | --- | --- |
| `Usecase`（小文字c） | **14** | `EnemyAttackUsecase`、`ShellAttackUsecase`、`MissionActionPerformedUsecase`、`ScenarioUsecase` |
| `UseCase`（大文字C） | **7** | `SkillUseCase`、`ShowScreenUseCase`、`CloseCurrentScreenUseCase`、`SkillBuildUseCase` |

InGame系が `Usecase`、OutGame系が `UseCase` という傾向はあるが、
`SkillUseCase`（InGame）が例外になっており規則として成立していない。

さらに紛らわしいことに、**同じクラスをフィールド名で別表記している**箇所がある。

```csharp
// 3.Adaptor/InGame/Skill/SkillExecutionController.cs
private readonly SkillUsecase _skillUseCase;   // 型は Usecase、変数は UseCase
```

**修正方針**: 英語として正しいのは `UseCase`（2語）。
ただし14件のリネームはコミット差分が大きくなるため、
**新規追加分から `UseCase` に統一し、既存は機械的リネームを一度に行う**のが現実的。

---

## 【確定】Repo と Repository の混在

| 表記 | 件数 |
| --- | --- |
| `Repository` | **29** |
| `Repo` | **5** |

```
2.Application/OutGame/Scenario/ScenarioHandlerRepo.cs
5.InfraStructure/InGame/Enemy/Boss/BossAttackEntryRepo.cs
5.InfraStructure/OutGame/SkillTree/SkillNodeBindRepo.cs
5.InfraStructure/OutGame/SkillTree/SkillNodeDataRepo.cs
5.InfraStructure/OutGame/SkillTree/SkillNodePhaseBindDataRepo.cs
```

興味深いことに、**この `Repo` 5件のうち3件が
[06. 非効率なデータ構造](06_非効率なデータ構造.md) で指摘した
「線形探索のまま残っているリポジトリ」と一致する**。
命名の逸脱と実装品質の逸脱が同じ場所で起きている。

---

## 【設計指摘】Controller という名前の濫用

`3.Adaptor` のサフィックス分布。

| サフィックス | 件数 |
| --- | --- |
| **Controller** | **41** |
| Presenter | 25 |
| State | 13 |
| Facade | 5 |
| Service | 1 |
| Applicator / Sequencer / Gate | 各1 |

`Controller` が全体の半分近くを占め、実際の役割は多岐にわたる。

| クラス | 実際にしていること | 適した名前 |
| --- | --- | --- |
| `ScreenController` | UseCaseへの委譲 | Controller（適切） |
| `EnemyAIController` | AI状態機械の駆動 | `EnemyAICoordinator` |
| `PlayerAttackController` | 攻撃判定・ダメージ算出 | `AttackPipeline` |
| `SkillExecutionController` | 失敗ポリシーの適用 | `SkillExecutionPolicy` |
| `MissionProgressRecorderController` | 7イベントを購読して記録 | `MissionProgressRecorder` |

`Applicator` / `Sequencer` / `Gate` が各1件しかないのは、
**それらが役割を正確に表す名前として孤立している**ことを示す。
つまり命名の語彙は存在するのに、多くが `Controller` に吸収されてしまっている。

**修正方針**: プロジェクト用語集（`Docs/用語集_追加候補.md` が既に存在）に
サフィックスの定義を追加する。既存クラスの改名は影響範囲が大きいため、
まず新規クラスから適用する。

---

## 【設計指摘】ログメッセージの言語・書式が不統一

同一ファイル内で日本語と英語が混在している。

```csharp
// 4.View/InGame/Enemy/EnemyRaycastDetectView.cs
Debug.LogError("[EnemyRaycastDetectView] Target transform is null.");           // 英語・context無し
Debug.LogWarning("[EnemyRaycastDetectView] Attack warning decal is not assigned. ...", this); // 英語・context有り
Debug.LogError("_beatColor の長さが判定ゾーン数より少ないです。", this);          // 日本語・プレフィックス無し
```

書式のバリエーション:
- プレフィックス: `[ClassName]` / `[{nameof(ClassName)}]` / なし
- 言語: 日本語 / 英語
- 第2引数 `context`: あり / なし

詳細は [12. ログ運用とビルド影響](12_ログ運用とビルド影響.md) 参照。

---

## 【設計指摘】TODOが25件、うち実装欠落を示すものが多数

| 場所 | 内容 |
| --- | --- |
| [EnemyAttackUsecase.cs:49](../../Assets/Scripts/Runtime/2.Application/InGame/Enemy/EnemyAttackUsecase.cs) ほか2件 | 敵の基礎攻撃力がハードコード（[13. DRY](13_DRY原則.md) 参照） |
| [SkillTreeController.cs:105](../../Assets/Scripts/Runtime/3.Adaptor/OutGame/SkillTree/SkillTreeController.cs) | 「実装待ち：スキル効果をプレイヤーに反映する処理」— **スキルツリーの中核機能が未実装** |
| [PlayerStatusPresenter.cs:13](../../Assets/Scripts/Runtime/3.Adaptor/OutGame/SkillTree/PlayerStatusPresenter.cs) | 「現段階実装無し」 |
| [StageDetailPresenter.cs:56](../../Assets/Scripts/Runtime/3.Adaptor/OutGame/StageSelect/StageDetailPresenter.cs) | セーブデータからのミッション達成状況取得が未実装 |
| [StageSelectController.cs:54](../../Assets/Scripts/Runtime/3.Adaptor/OutGame/StageSelect/StageSelectController.cs) | 同上 |
| [StageDetailScreenView.cs:90](../../Assets/Scripts/Runtime/4.View/OutGame/StageSelect/StageDetailScreenView.cs) | 同上 |
| [PlayerAttackController.cs:119](../../Assets/Scripts/Runtime/3.Adaptor/InGame/Battle/PlayerAttackController.cs) | 「射線判定などを追加して、攻撃がヒットするかどうかを判定する必要がある」 |
| [PlayerAttackController.cs:127](../../Assets/Scripts/Runtime/3.Adaptor/InGame/Battle/PlayerAttackController.cs) | 「一時的な手段としてEntityのHashCodeを使う」 |
| [SaveBase.cs:100](../../Assets/Scripts/Runtime/0.Utility/Savedata/SaveBase.cs) | 「セーブデータの格納先が決まったらここを直す」 |
| [EnemyLifeCycle.cs:520,532](../../Assets/Scripts/Runtime/6.Composition/InGame/Enemy/EnemyLifeCycle.cs) | 拡張性・ボイス処理が未対応 |

特に注目すべきは、**「ミッション達成状況をセーブデータから取得する」というTODOが
3ファイルに分散している**こと。1つの機能欠落が複数箇所に痕跡を残しており、
実装時には3箇所を同時に触る必要がある。

`PlayerAttackController.cs:127` の「EntityのHashCodeを攻撃対象特定に使う」は
**設計上の危険信号**。`GetHashCode` は同一性の保証ではなく衝突しうるため、
別の敵に攻撃が当たる可能性がある。優先的に確認すべき。

---

## 【設計指摘】コメントアウトされたコード

| 場所 | 内容 |
| --- | --- |
| [SettingBase.cs:58-70](../../Assets/Scripts/Runtime/4.View/OutGame/Setting/SettingBase.cs) | `Info<T>` / `Warning<T>` / `Error<T>` が丸ごとコメントアウト（13行） |
| [PlayerView.cs:540](../../Assets/Scripts/Runtime/4.View/InGame/Player/PlayerView.cs) | `//_animator.SetFloat(_blendName, ...)` |
| [EnemyLifeCycle.cs:190](../../Assets/Scripts/Runtime/6.Composition/InGame/Enemy/EnemyLifeCycle.cs) | `//_enemySharedFacade.Initialize(target);` |
| [ShellView.cs:92](../../Assets/Scripts/Runtime/4.View/InGame/Enemy/ShellView.cs) | `//Debug.Log(...)` |

Gitで履歴が追える以上、コメントアウトによる保存は不要。
特に `EnemyLifeCycle.cs:190` の `_enemySharedFacade` は
**「無効化されている機能なのか、実装途中なのか」が読み手に判断できない**。

---

## 良い命名として維持すべき点

- **定数の SCREAMING_SNAKE_CASE + XMLコメント** —
  [TargetingSystem.cs:251-258](../../Assets/Scripts/Runtime/4.View/InGame/Target/TargetingSystem.cs) が模範
- **privateフィールドの `_camelCase` 前置** — 全レイヤーで一貫
- **`readonly` の徹底** — 依存フィールドはほぼ全て `readonly` になっている
- **XMLドキュメントコメント** — `<summary>` / `<param>` の記述率が非常に高い
- **値オブジェクトの命名** — `DodgeCooldown` / `AttackRangeMax` など、
  単位や役割が名前から読み取れる
- **レイヤー番号プレフィックス**（`0.Utility` 〜 `6.Composition`）—
  依存方向がディレクトリ順で一目で分かる優れた工夫
