# 疑問点

- id: 3937c2c6-cc02-80a4-b6a7-fb0ab57d29f6
- path: Symphony Kill Chord / システム概要 / 7月β大改修リファクタリング / 疑問点
- last_edited: 2026-07-10T01:16:54.335Z


### シーン

#### オブジェクト混在状態
空オブジェクトをもうちょい使って、見やすくしたい

#### インゲームシーンのStageオブジェクト
NavMeshSurfaceのみがあり、NavMeshDataがミッシングのオブジェクトがある
これは必要なものか？

### スクリプト

#### ParticleControllerの意義について
これは仮で実装したパーティクルシステムなのか、本実装用のシングルトンクラスなのか
本実装にしては雑なシングルトンだが、仮ならRuntime名前空間にあるのはおかしい

#### Cameraのレイヤー構造について
カメラのApplication層でカメラの位置を計算しているが、この位置計算は本質的にはViewの役割なのではないか？

#### Playerのモデル生成をスポナー経由でやるべきなのでは
Playerのモデルをステージシーンに設置でやってるのはおかしい
Playerのプレハブを生成するスポナーを作るべきなのでは？

#### CameraLockOnState
これカメラモジュールでしか使ってないし、カメラモジュールのドメインにしてよいのでは

#### MusicTimingCalculator
コメント・サマリーが何のこと言ってるか分からない
```C#
/// <summary>
///    現在の拍位置を基準に、指定した小節オフセットおよび拍位置に対応する絶対時刻を計算する。
/// </summary>
```

#### RhythmDefinition
このクラスはドメインのデータであって、処理をするのはApplicationにした方がよい
あとCalculateBeatTypeメソッドでイベント発火してるけど、ここでやるんじゃなくて戻り値でIsJustedを返してその結果から発火した方が良くない？
イベント発火は計算と隔離すべきでは

#### RhythmState
実装内容はリズム系の入力バッファなので、RhytmCommandBufferクラスに改名していいのでは

#### PlayerMoveParameter・EnemyMoveSpec・AttackParameterSet
表記揺れおよびクラス・構造体の設計の違い
そもそもパラメータはAdaptor層が永続化を行い、Applicationは引数で逐次受け取るだけにすべきなのでは

#### CameraSystemParameter
直接シリアライズ可能な状態になっている
インフラにAssetクラスを作るべき

#### IBuff
実装と抽象化を1ファイルに書くな！！！！！！

#### BuffTest
テストファイルをRuntimeに置くんじゃねぇ！！！！

#### BuffSystemInit
これテストコードかよクソが！！！！！！！

#### SkillVisualTest・TestSkillEffect・SkillTreeTestInputData
テスト系をScripts/Developの方に移動した方が良さそう

#### CharacterAnimationApplication
キャラクターのアニメーションはViewの処理だから分離しなくていいんじゃね？

#### HealthHudView・HealthHudViewModel
DTOをそのままViewに渡すのは良くない（ref structになってない）
あとEaseのプロパティ化したい

#### ActionParams
使ってない

#### そもそもTargetシステムはカメラから分離した方がいい
Cameraのモジュールとして存在してるけど、プレイヤーやスキルなどのゲームの戦闘システムに大きく関わりすぎてて責任がデカいので、モジュールを分割した方がいい。

#### セーブデータの定義がドメインにある
セーブデータはゲーム外のサービスにデータを保存するために定義される型なのだから、最もレイヤーの外にあるべきなのでは？

#### シーンのモジュールもView層では
シーンのロード自体がUnity依存システムなのだから、これらはView層で解決するべき。

#### [IngameComposition] SelectedBattleStageState が取得できませんでした。
テスト用にステートがない場合はデフォルトを使用するようにしたいね

#### BuffTestなどのテスト用コードがRuntimeアセンブリに混在している
`BuffTest.cs` というテスト用のクラスが `KillChord.Runtime.Application.InGame.Buff` 名前空間（本番コード用ディレクトリ）に配置されている。
また、テスト用アサーション拡張である `UnityEngine.Assertions.Must` の using もRuntimeコードに残ってしまっている。
テスト用のクラスやモックは、EditorアセンブリやTestsアセンブリ（Assembly Definition等で本番用アセンブリから隔離された場所）に移動するか、不要であれば削除すべきである。

#### EventBusの静的イベント（static）によるライフサイクル・メモリリーク問題
`EventBus<T>` が静的（`static`）クラスとして実装され、静的イベント `_onEvent` にリスナーが登録される設計になっている。
Unityにおいては、シーンロードをまたぐ際やプレイモードを終了した際、明示的に `Unregister` が呼ばれなかったリスナー（ゲームオブジェクトやコンポーネント等）への参照が static イベントに残ったままになり、
- メモリリーク（解放されるべきオブジェクトがGCされない）
- すでに破壊されたオブジェクトに対するイベント送信による `MissingReferenceException` / 空参照エラー
といった致命的な不具合を容易に引き起こす。

## 用語

#### リズムパターン（RhythmPattern）
スキルのコマンド

## 知らないこと

#### MulticastDelegate
EventBusで使用

#### IProgress
シーンロード系で使用

#### HashCode.Add
RhythmPatternのGetHashCodeで使用